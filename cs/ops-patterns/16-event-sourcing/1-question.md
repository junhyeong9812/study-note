# ops-patterns/16-event-sourcing — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 TODO/javadoc 을 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 문제 (구현 대상: EventStore TODO 1\~2 · AccountProjection TODO 3\~4 · AccountService TODO 5\~7)

#### 1. EventStore — append / readFrom (TODO 1\~2)

```java
// EventStore.java (src/main) — 채울 것
public synchronized int append(String streamId, int expectedVersion,
                               List<AccountEvent> events) {
    // TODO 1: 버전이 맞으면 **뒤에 붙인다.**
    //
    // 지금 사건 개수와 expectedVersion 이 정확히 같아야 한다. 하나라도 다르면
    // 내가 읽은 상태가 이미 낡은 것이고, 그 위에 붙이면 남의 사건 위에 얹힌다.
    // 사건이 사라지지는 않는데 순서가 뒤엉키고, 접어서 나오는 상태가 틀린다.
    // 그리고 사건 목록만 보면 아무 이상이 없어 보인다.
    //
    // 덮어쓰면 안 된다. 이 저장소에는 붙이기만 있다.
}

public synchronized List<AccountEvent> readFrom(String streamId, int fromVersion) {
    // TODO 2: fromVersion 번째부터 끝까지.
    //
    // 경계를 한 칸 틀리면 사건 하나가 조용히 빠진다. 스냅샷 뒤를 이어 붙일 때
    // 그 하나가 빠지고, 결과가 처음부터 접은 것과 달라진다.
}
```

- append 가 expectedVersion 과 지금 사건 개수를 비교하는 이유는 무엇인가?
- 버전이 안 맞을 때 사건이 사라지는 것이 아니라면 정확히 무엇이 잘못되는가? (출처: README 생각해볼 것 4)
- 왜 그 잘못은 사건 목록만 봐서는 안 보이는가?
- 이 저장소에 고치기·지우기가 없는 이유는 무엇인가?
- readFrom 의 경계를 한 칸 틀리면 어떤 사고가 어디서 드러나는가?
- versionOf 가 "사건 개수"를 버전으로 쓰는 것은 어떤 성질 덕에 성립하는가?

#### 2. AccountProjection — replay / apply (TODO 3\~4, 검증하면 안 되는 자리)

```java
// AccountProjection.java (src/main) — 채울 것
public AccountState replay(List<AccountEvent> events) {
    // TODO 3: 빈 상태부터 하나씩 접는다.
}

public AccountState apply(AccountState state, AccountEvent event) {
    // TODO 4: 사건 하나를 반영한 **새** 상태를 준다.
    //
    // **검증하면 안 된다.** if 로 조건을 다는 순간 어제 저장된 사건이
    // 오늘 규칙에 걸려 터진다. 어제까지 마이너스 통장이 허용됐다면
    // 잔액이 음수가 되는 사건이 실제로 저장돼 있다.
    //
    // Opened 는 상태를 **처음으로 되돌린다.** 앞 상태를 물려받으면
    // 계좌를 다시 열었을 때 옛 주인의 잔액이 새 주인에게 넘어간다.
    // Opened 가 늘 맨 앞에 온다고 믿는 동안에는 안 드러난다.
}
```

- apply 에 if 검증을 달면 안 되는 이유는 무엇인가? (출처: README 생각해볼 것 1)
- `Opened, Deposited(1000), Withdrawn(5000)` 을 재생하면 잔액은 얼마여야 하는가?
- 재생 중에 "잔액이 모자라다"고 거절하는 것이 왜 "과거를 부정하는 것"인가?
- Opened 가 앞 상태를 물려받으면 어떤 사고가 나는가? (출처: README 생각해볼 것 2)
- 그 사고는 왜 "Opened 는 늘 맨 앞"이라고 믿는 동안에는 안 드러나는가?
- 모르는 사건을 만나면 왜 조용히 무시하지 않고 던져야 하는가?
- replayFrom 은 무엇을 위해 있고, replay 와 결과가 같으려면 무엇이 지켜져야 하는가?

#### 3. AccountService — open / deposit / withdraw (TODO 5\~7, 검증이 있는 유일한 자리)

```java
// AccountService.java (src/main) — 채울 것
public void open(String accountId, String owner) {
    // TODO 5: 없는 계좌면 연다.
    // **이미 있으면 거절해라.** 안 막으면 Opened 가 하나 더 붙고,
    // 그 사건이 상태를 처음으로 되돌린다. 잔액이 0 이 되고 주인이 바뀌는데
    // 사건 목록만 보면 이상한 것이 없다.
}

public void deposit(String accountId, long amount) {
    // TODO 6: 입금한다.
    // **자기가 읽은 버전으로 붙여라.** 붙일 때 store.versionOf 를 다시 부르면
    // 낙관적 잠금이 통째로 무의미해진다. 읽은 뒤에 남이 붙였어도 그냥 통과한다.
}

public void withdraw(String accountId, long amount) {
    // TODO 7: 출금한다. **잔액 검증이 여기 있다.**
    // 이 상자에서 검증이 있는 유일한 자리다. 재생할 때는 이 판단을 안 한다.
    // 명령은 거절되고 사건은 안 거절된다. 그 구별이 이 상자의 핵심이다.
}
```

- 명령 처리의 네 단계(읽기→접기→검증→붙이기)는 각각 무엇을 하는가?
- 같은 계좌를 두 번 열면 왜 거절해야 하고, 안 막으면 무슨 일이 생기는가? (출처: README 생각해볼 것 5)
- 붙일 때 versionOf 를 다시 부르면 왜 낙관적 잠금이 통째로 무의미해지는가? (출처: README 생각해볼 것 3)
- 잔액 검증은 왜 withdraw(명령)에만 있고 apply(재생)에는 없는가?
- RejectedException 과 ConcurrentModificationException 은 각각 무엇을 뜻하고, 받은 쪽이 해야 할 일은 어떻게 다른가?
- 서비스가 상태를 캐시로 들고 있지 않고 매번 읽고 접는 이유는 무엇인가?

### B. 개념

#### 4. 명령과 사건 — 이 상자의 핵심 구별

- 명령과 사건의 차이를 한 줄씩으로 말하면?
- 사건 이름이 과거형("출금되었다")인 것이 왜 계약인가?
- 검증은 언제 한 번만 하는가?
- 규칙이 바뀌었을 때(마이너스 통장 폐지) 이 구별이 없으면 무슨 일이 생기는가?

#### 5. 지우지 않고 붙인다

- 실수로 입금을 두 번 눌렀으면 어떻게 고치는가?
- 그 결과 사건 개수는 줄어드는가 늘어나는가?
- 이 방식은 08번 사가의 무엇과 같은 모양인가?
- "실수가 있었다"가 기록에 남는 것은 장점인가 단점인가, 왜인가?

#### 6. 낙관적 잠금 — 락과의 비교

- 낙관적 잠금은 11번 분산 락의 펜싱 토큰과 무엇이 같고 무엇이 다른가?
- 스레드 8개가 50번씩 붙일 때 성공·충돌 건수는 얼마였고, 그 숫자는 무엇을 보여주는가?
- 충돌로 거절당한 쪽은 무엇을 하면 되는가?
- 어떤 조건에서 락보다 낙관적 잠금이 싸고, 반대는 언제인가?

#### 7. 한계와 연결

- 읽을 때마다 전부 접는 비용은 사건 수에 따라 어떻게 자라고, 무엇으로 줄이는가? (출처: README 생각해볼 것 6)
- 스냅샷이 "원래 문제를 조금 되가져온다"는 말은 무슨 뜻이고, 그래서 지켜야 할 원칙은 무엇인가?
- "이 사람 정보를 지워달라"는 요청이 왜 이 패턴에서 어려운가? (출처: README 생각해볼 것 7)
- 변종 10개 중 처음에 살아남은 셋은 무엇이고, 그 셋의 공통점은 무엇인가? (출처: README 생각해볼 것 8)
- 17번 타임시리즈와 이 챕터는 "쌓이는 값"을 각각 어떻게 들고 있는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
