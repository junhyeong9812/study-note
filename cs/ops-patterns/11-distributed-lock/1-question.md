# ops-patterns/11-distributed-lock — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 문제 (NaiveLock · LeaseLock · FencedStore 의 TODO)

#### 1. TODO 1·2 — NaiveLock (기준선: 만료도 소유자 검사도 없다)

```java
// TODO 1: 없으면 잡고 있으면 못 잡는다. **만료는 안 둔다.**
//
// 만료를 넣고 싶어질 텐데, 넣으면 이 기준선이 사라진다.
// 이 클래스는 "만료가 없으면 무슨 일이 벌어지는가" 를 보여주려고 있다.

// TODO 2: 키만 보고 지운다. **소유자를 안 본다.**
//
// 여기도 검사를 넣으면 안 된다. 아무나 남의 락을 푸는 것이 이 구현의 성질이고,
// 그 사고가 얼마나 조용한지가 이 상자의 출발점이다.
```

- 만료가 없으면 잡은 놈이 죽었을 때 무슨 일이 벌어지는가?
- 소유자를 안 보면 어떤 사고가 나고, 그 사고는 왜 조용한가?
- 두 사고 중 어느 쪽이 시끄럽고 어느 쪽이 조용한가, 왜인가?
- putIfAbsent 로 잡는 것 자체는 왜 "여기까지는 맞다"인가?

#### 2. TODO 3 — LeaseLock.tryAcquire (한 연산으로 잡는다)

```java
// TODO 3: 비었거나 만료됐으면 잡는다. **한 연산으로 해라.**
//
// "만료됐나 본다" 와 "내 것으로 표시한다" 를 나누면 그 사이에서 둘 다 잡는다.
// 06번, 09번에서 본 그 자리인데 결과가 제일 나쁘다. 둘이 같이 일한다.
//
// 그리고 **한 번 돌려서는 안 잡힌다.** 창이 너무 좁다.
// 테스트가 500 라운드를 도는 이유가 그것이다.
//
// 토큰은 잡을 때마다 커진다. 같은 소유자가 다시 잡아도 커진다.
// 커지는 것이 계약이고, 그래야 저장소가 낡은 쓰기를 가려낼 수 있다.
```

- 보기와 표시를 나누면 여기서는 왜 "결과가 제일 나쁜가"(06·09번과 비교해)?
- 한 연산은 어떤 API 로 실현하는가?
- 이 결함이 한 번 돌려서는 안 잡히는 이유는, 테스트는 그래서 어떻게 하는가?
- 토큰이 잡을 때마다(같은 소유자여도) 커져야 하는 이유는 무엇인가?

#### 3. TODO 4 — LeaseLock.release (소유자 이름만 보면 안 된다)

```java
// TODO 4: 자기 것일 때만 지운다.
//
// **소유자 이름만 보면 안 된다.** 같은 이름으로 재기동한 프로세스가
// 옛 증서로 남의 점유를 푼다. 토큰까지 봐야 그 경우가 걸린다.
//
// 자기 것이 아니면 아무 일도 안 하고 false 다. 던지지 마라.
// 만료된 뒤에 finally 에서 놓으려는 것은 정상 경로이고,
// 거기서 던지면 부르는 쪽이 finally 안에서 예외를 또 다루게 된다.
```

- 소유자 이름만 검사하면 어떤 시나리오에서 남의 점유가 풀리는가?
- 토큰까지 보면 그 시나리오가 왜 걸리는가?
- 남의 것일 때 던지지 않고 false 를 주는 이유는 무엇인가?
- "만료된 뒤 finally 에서 놓는 것"이 왜 정상 경로인가?

#### 4. TODO 5·6 — LeaseLock.renew / currentHolder

```java
// TODO 5: 자기 것이고 아직 유효하면 만료를 미룬다.
//
// **이미 만료된 것은 되살리지 마라.** 그 사이에 남이 잡았으면 둘이 같이 들게 된다.
// **토큰은 안 올린다.** 갱신은 같은 점유의 연장이다.
// 여기서 올리면 저장소가 보기에 새 점유와 구별이 안 되고,
// 그러면 펜싱이 막아야 할 것을 못 막는다.

// TODO 6: 지금 유효하게 들고 있는 증서. 없거나 만료됐으면 빈 값.
//
// 만료를 안 보면 이 값이 거짓말을 한다. 대시보드와 운영 도구가 그것을 보고,
// 사람이 그걸 믿고 판단한다. 정작 tryAcquire 는 잘 도니까 아무 데서도 안 잡힌다.
```

- 만료된 증서를 되살리면 어떤 상태가 되는가?
- 갱신이 토큰을 올리면 펜싱의 무엇이 깨지는가?
- currentHolder 가 만료를 안 보면 누가 무엇을 잘못 판단하게 되는가?
- 그 거짓말이 테스트·모니터링에서 안 잡히는 이유는 무엇인가?

#### 5. TODO 7 — FencedStore.write (다섯 줄이 락이 못 막는 것을 막는다)

```java
// TODO 7: 지금까지 본 것보다 **작은** 토큰이면 거절한다.
//
// 이 다섯 줄이 락이 못 막는 것을 막는다. A 는 자기가 락을 잃은 줄 끝까지 모르는데,
// 그래도 쓰기가 튕긴다. 확인하는 쪽을 쓰는 놈에서 받는 놈으로 옮긴 것이다.
//
// **같은 토큰은 통과시켜라.** 같은 점유가 갱신하며 여러 번 쓰는 것은 정상이고,
// 그것까지 막으면 오래 걸리는 작업이 아예 못 돈다.
//
// 거절한 것을 기록해라. 그 목록이 비어 있지 않다는 것은
// 실제로 두 놈이 같이 일했다는 증거다.
```

- "락이 못 막는 것"이란 정확히 어떤 시나리오인가(GC 멈춤으로 재구성하면)?
- A가 쓰기 직전에 락을 확인해도 소용없는 이유는 무엇인가?
- 확인하는 쪽을 받는 놈으로 옮기면 왜 창이 사라지는가?
- 같은 토큰을 막으면 어떤 정상 작업이 못 돌게 되는가?
- 거절 목록이 비어 있지 않다는 것은 무엇의 증거인가?

### B. 개념

#### 6. 셋이 반드시 있어야 한다 (원본 README 도입)

- 만료·소유자·토큰은 각각 어떤 사고를 막는가?
- 하나라도 빠지면 "조용히 깨진다"는 말과 "평소에는 잘 돈다"는 말은 어떻게 양립하는가?
- 이 문제는 왜 한 프로세스 안의 락(synchronized)으로는 안 풀리는가?

#### 7. 잡기의 원자성 (원본 README 생각해볼 것 1·9)

- 06번·09번·11번의 "그 자리"는 공통적으로 무엇인가?
- 셋 중 여기가 결과가 제일 나쁜 이유는 무엇인가?
- "잡기를 나눈다" 변종이 한 라운드 테스트에서 살아남은 것은 06번의 어떤 현상과 같은가?

#### 8. 만료와 갱신의 긴장 (원본 README 생각해볼 것 5·8)

- 수명(TTL)이 너무 짧으면/너무 길면 각각 무엇이 나빠지는가?
- expiredTakeovers 가 크다는 것은 무슨 신호인가?
- FencedStore 의 거절 횟수가 0이 아니라는 것은 무슨 신호이고, 무엇을 해야 하는가?

#### 9. 펜싱 토큰의 설계 (원본 README 생각해볼 것 4·6)

- 토큰 비교가 "작으면 거절, 같으면 통과"인 이유를 두 규칙으로 나눠 설명하면?
- 저장소 쪽 검사에는 왜 "검사와 쓰기 사이의 창"이 없는가?
- 펜싱이 있어도 락은 여전히 필요한가, 락과 펜싱의 역할 분담은 무엇인가?

#### 10. 연결

- 이 챕터가 10번 스케줄러와 이어지는 지점은 무엇인가?
- 12번 리더 선출은 이 챕터의 무엇을 어떻게 확장하는가?
- Redis 의 SET NX PX 는 이 구현의 어느 부분에 해당하는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
