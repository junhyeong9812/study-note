# ops-patterns/04-rate-limiter — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 TODO 주석을 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 문제 (구현 대상 TODO — `src/main/java/com/ops/ratelimit/`)

#### 1. AbstractRateLimiter.now — 뒤로 가지 않는 지금 (TODO 1)

```java
/** 뒤로 가지 않는 지금. */
protected final long now() {
    // TODO 1: ticker 의 시각을 주되, 마지막으로 준 값보다 작으면 그 값을 그대로 다시 준다.
    //
    //   - 되돌려준 경우 clockRewinds 를 하나 늘린다
    //   - 앞으로 갔으면 lastNowMillis 를 갱신한다
    //
    // **여기가 이 상자에서 제일 짧고 제일 많은 것을 막는 코드다.**
    // NTP 보정, 컨테이너 이주, 가상화 계층의 시계 재설정. 시각이 뒤로 간다.
    //
    // 방어는 "막고 끝"이 아니다. 셈을 남겨서 밖에서 볼 수 있게 한다.
    // 그리고 **영원히 막으면 그것도 장애다.**
}
```

- 시계 방어를 다섯 구현에 각자 넣지 않고 이 한 곳에 모은 이유는?
- 시계가 뒤로 갔을 때 고정 창·슬라이딩 카운터에서는 무엇이 조용히 두 배가 되는가?
- 같은 일이 토큰 버킷에서는 어떻게 나타나는가(두 가지 경로)?
- 슬라이딩 로그에서는 왜 반대로 한도가 줄어드는가?
- 막기만 하지 않고 `clockRewinds` 를 세는 이유는?
- "영원히 막으면 그것도 장애다"는 무슨 상황을 말하는가?
- `tryAcquire` 를 final 로 막고 하위 구현에 `doTryAcquire` 만 채우게 한 이유는?
- `permits <= 0` 을 조용히 무시하지 않고 거절로 세는 이유는?

#### 2. FixedWindowLimiter — 창마다 계수기 하나 (TODO 1·2)

```java
private void rollTo(long now) {
    // TODO 1: 지금이 몇 번째 창인지 구하고, 들고 있던 번호와 다르면 계수기를 0 으로 되돌린다.
    //
    // 창 번호는 **Math.floorDiv** 로 구한다. 자바의 / 는 0 쪽으로 자른다.
    // 비교는 `!=` 로 쓴다. 다만 **`>` 로 써도 우리 테스트는 다 통과한다**(변종 검증 M05).
}

@Override
protected boolean doTryAcquire(int permits) {
    // TODO 2: now() 를 받아 창을 굴리고, 이번 창의 계수기에 permits 를 더할 수 있는지 본다.
    //
    //   count + permits 가 한도를 **넘으면** 거절, 아니면 더하고 통과.
    //
    // **거절할 때 count 를 건드리면 안 된다.** 먼저 확인하고 나중에 더한다.
}
```

- 창 번호를 `/` 대신 `Math.floorDiv` 로 구하는 이유는(0 을 걸치는 창에 무슨 일이 생기는가)?
- 이 계산이 02번의 어느 코드와 똑같은 자리인가?
- 창 비교를 `!=` 대신 `>` 로 써도 테스트가 통과하는 이유는, 그런데도 `!=` 로 두는 이유는?
- 한도 검사를 `>=` 로 쓰면 몇 개가 어디로 사라지는가?
- 거절할 때 count 를 먼저 더하고 나중에 되돌리면 무엇이 새는가(01번의 무엇과 같은 함정인가)?
- `availableTokens()` 가 읽기인데도 `rollTo` 를 부르는 이유는?
- 저장 항목이 1개뿐이라는 성질이 분산 환경에서 무엇을 가능하게 하는가?

#### 3. SlidingLogLimiter — 시각을 전부 담는다 (TODO 1·2)

```java
private void prune(long now) {
    // TODO 1: 덱이 비지 않았고 맨 앞 시각이 창 밖이면 버린다. 창 안을 만날 때까지 반복한다.
    //
    // 경계는 `>=` 다. 기록이 정확히 1000ms 전이면 **이미 창 밖이다.**
    // 만나면 멈춰야 한다. 전부 훑으면 O(n) 이 매번이다.
    // 오름차순이라는 전제 덕에 앞에서 멈출 수 있고, **그 전제를 지켜주는 것이 now() 다.**
}

@Override
protected boolean doTryAcquire(int permits) {
    // TODO 2: now() 를 받아 창 밖을 버리고, 남은 개수에 permits 를 더할 수 있는지 본다.
    //
    //   통과시키면 **permits 개의 시각을 담는다.** 하나가 아니다.
    //
    // **거절한 요청은 담지 않는다.**
}
```

- 이 구현에 창 경계 문제가 아예 없는 이유는?
- 만료 판정이 `>` 가 아니라 `>=` 인 이유는(02번의 어디서 똑같이 따졌는가)?
- 맨 앞에서 창 안을 만나면 멈춰도 되는 근거는 무엇이고, 그 전제를 누가 지켜주는가?
- 허가 5개를 받아준 요청이 시각 하나가 아니라 다섯 개를 담아야 하는 이유는?
- 거절한 요청을 담으면 동시에 망가지는 두 가지는 무엇인가?
- 이 구현이 맞는 자리와 절대 쓰면 안 되는 자리는 각각 어디인가?
- 덱(Deque)을 고른 이유는 무엇이고, 그 선택의 전제는 무엇인가?

#### 4. SlidingWindowCounterLimiter — 계수기 둘로 근사 (TODO 1·2·3)

```java
private void roll(long now) {
    // TODO 1: 창 번호를 구해(Math.floorDiv) 들고 있던 번호와 비교한다.
    //
    //   같으면        -> 아무것도 안 한다
    //   바로 다음이면 -> 지금 개수를 이전 개수로 옮기고 지금 개수를 0 으로
    //   그보다 멀면   -> **이전 개수도 0 으로.** 지금 개수도 0 으로
}

private long estimate(long now) {
    // TODO 2: 창을 굴린 뒤 가중 추정치를 구한다.
    //
    //   추정치 = previousCount * (windowMillis - 들어선시간) / windowMillis + currentCount
    //
    // **곱하기가 먼저다.**
    // Math.floorMod 를 쓰는 이유는 % 가 음수에 음수를 주기 때문이다.
}

@Override
protected boolean doTryAcquire(int permits) {
    // TODO 3: 추정치에 permits 를 더해 한도를 넘는지 본다. 넘으면 거절, 아니면 지금 창에 더한다.
    //
    // 더하는 것은 **지금 창의 개수**다.
}
```

- "바로 다음 창"과 "더 건너뛴 창"을 구분하지 않으면 어떤 증상이 나타나고, 왜 재현이 안 되는가?
- 추정식에서 괄호를 `previousCount * ((window - elapsed) / window)` 로 묶으면 이 클래스는 무엇이 되는가?
- 그 사고를 04번의 다른 어느 구현에서 한 번 더 만나는가?
- "들어선 시간" 계산에 `Math.floorMod` 를 쓰는 이유는 — `%` 면 가중치가 어떻게 되는가?
- 생성자가 `Math.multiplyExact(limit, windowMillis)` 를 미리 해보는 이유는?
- 이 근사가 깔고 있는 가정은 무엇이고, 그 가정이 틀리면 어느 쪽으로 틀리는가(두 방향)?
- `availableTokens()` 의 `Math.max(0, ...)` 는 지워도 테스트가 통과한다 — 그런데도 남긴 이유는?
- 추정치에 permits 를 더해 들고 있지 않고 지금 창의 개수에만 더하는 이유는?

#### 5. TokenBucketLimiter — 쉬면 모인다 (TODO 1·2)

```java
private void refill(long now) {
    // TODO 1: 마지막 충전 이후 지난 시간만큼 토큰을 넣되 상한을 넘기지 않는다.
    //
    //   4. 남은 자리(room)를 채우는 데 필요한 시간을 먼저 구한다
    //        millisToFull = (room + refillPerSecond - 1) / refillPerSecond   <- 올림
    //      elapsed 가 그보다 크거나 같으면 가득 채우고,
    //      아니면 tokensScaled += elapsed * refillPerSecond
    //
    // **4번의 순서가 이 메서드의 전부다.** 정수 나눗셈과 오버플로 둘을 동시에 막는다.
}

@Override
protected boolean doTryAcquire(int permits) {
    // TODO 2: now() 를 받아 충전하고, permits 만큼의 토큰이 있으면 빼고 통과시킨다.
    //
    //   비용은 (long) permits * SCALE 이다. **long 으로 올린 뒤에 곱한다.**
    //
    // 모자라면 아무것도 빼지 않는다. 부분 소비는 없다.
}
```

- 토큰을 개수가 아니라 1/1000 단위 정수(SCALE)로 세는 이유 두 가지는?
- `(elapsed / 1000) * refillPerSecond` 로 쓰면 초당 10개짜리 리미터가 100ms 주기 클라이언트에게 무엇이 되는가?
- 그 클라이언트가 올리는 신고 문구가 "가끔 되는데 대부분 안 된다"인 이유는?
- 채울 양을 먼저 곱한 뒤 상한과 비교하면 하루 쉰 버킷에 무슨 일이 생기는가?
- `millisToFull` 을 올림으로 구하는 이유는?
- 비용 계산에서 `(long) permits * SCALE` 로 캐스팅을 앞에 두는 이유는?
- 토큰이 모자랄 때 일부만 빼두면 어떤 상태가 만들어지는가?
- 버킷을 빈 채가 아니라 가득 찬 채로 시작하는 이유는, 빈 채로 시작하면 배포 때 무슨 일이 생기는가?
- `elapsed <= 0` 검사는 `now()` 와 겹친 방어인데(M17) 남겨둔 이유는?

#### 6. LeakyBucketLimiter — 큐에 담아 고른 속도로 (TODO 1·2)

```java
private void leak(long now) {
    // TODO 1: 마지막 누출 이후 지난 시간만큼 큐를 줄이되 0 아래로 내려가지 않게 한다.
    //
    //        millisToEmpty = (queuedScaled + leakPerSecond - 1) / leakPerSecond   <- 올림
    //
    // TokenBucketLimiter.refill 을 거울에 비춘 것이다. 막는 사고도 거울상이다.
    // 그냥 빼면 큐가 **음수**가 되고, 음수 큐는 용량 검사를 통과한다.
}

@Override
protected boolean doTryAcquire(int permits) {
    // TODO 2: now() 를 받아 큐를 비우고, permits 만큼 줄에 세울 자리가 있으면 세운다.
    //
    //     lastExitMillis = now + queuedScaled / leakPerSecond
    //
    //   **줄을 세운 뒤의 길이**로 계산해야 방금 들어온 요청이 나가는 시각이 된다.
    //
    // 거절할 때는 lastExitMillis 를 건드리지 않는다.
}
```

- 토큰 버킷은 가득 찬 채로, 누출 버킷은 빈 채로 시작하는 이유는(무엇을 세는 필드인가)?
- 큐가 음수가 되면 다음 몰림 때 무슨 일이 생기는가?
- `lastExitMillis` 를 줄 세우기 **전** 길이로 계산하면 누구의 시각이 나오는가?
- 거절한 요청이 `lastExitMillis` 를 밀면 뒤에 오는 요청들은 어떻게 되는가?
- 이 클래스가 진짜 큐를 들고 있지 않고 길이만 세는 이유는?
- 용량이 곧 최대 지연이라는 말은 용량 100·초당 10에서 몇 초를 뜻하는가?
- 토큰 버킷과 누출 버킷의 차이를 "나가는 시각"으로 한 문장으로 말하면?

### B. 개념

#### 7. 다섯 구현은 각각 다른 것을 판다

- 이 문제 전체가 "셋 중 무엇을 살 것인가"인데, 그 셋은 무엇인가?
- 고정 창을 그래도 쓰는 이유는?
- 슬라이딩 로그를 초당 10만 공용 엔드포인트에 쓰면 안 되는 이유는?
- 슬라이딩 카운터가 계수기 둘로 사는 것과 파는 것은 각각 무엇인가?
- 토큰 버킷과 누출 버킷은 같은 몰림을 각각 무엇으로 바꾸는가?

#### 8. 자로 재야 보인다 (핵심 측정)

- t=999에 100개, t=1001에 100개를 넣었을 때 다섯 구현의 통과량은 각각 얼마인가?
- 고정 창의 200이 "고칠 수 있는 버그가 아니다"인 이유는?
- 그 현상이 01번의 무엇과 같은 구조인가?
- 지속 부하로 재면 순위가 바뀌어 버킷 둘이 199가 되는데, 199는 어디서 나온 숫자인가?
- "한도 100"이 구현마다 다른 뜻이라는 말을 이 숫자들로 설명하면?
- 메모리 측정(1, 1000, 2, 1, 1)에서 1000은 어느 구현이고 무엇에 비례하는가?

#### 9. 정수 나눗셈은 순서 하나로 리미터를 통째로 끈다 (출처: README「특히 생각해볼 것」1)

- 정수 나눗셈 함정이 있는 두 곳은 어디인가? (출처: README 1)
- 이 함정이 01번의 어느 코드와 같은 자리인가? (출처: README 1)
- "통째로 꺼진다"가 구현마다 어떻게 다르게 나타나는가(카운터 / 토큰 버킷)?

#### 10. 곱하기 전에 넘칠지 검사한다 (출처: README「특히 생각해볼 것」2)

- 넘친 long 이 왜 상한 검사를 그냥 통과하는가? (출처: README 2)
- 이 방어가 들어가 있는 자리 세 곳은 어디인가?

#### 11. 시계는 뒤로 간다 (출처: README「특히 생각해볼 것」3)

- 시계가 뒤로 가는 실제 원인 셋은 무엇인가? (출처: README 3)
- "시계 문제는 시계 경계에서 막는다"는 설계 원칙을 이 코드에서 어디가 실현하는가? (출처: README 3)
- 방어가 겹친 자리 셋(M05·M15·M17)을 지워도 테스트가 통과하는데, 왜 지우지 않는가?

#### 12. 조회가 상태를 바꾼다 (출처: README「특히 생각해볼 것」4)

- 이 챕터에서 조회가 상태를 바꾸는 자리는 어디인가? (출처: README 4)
- 굴리지 않고 읽기만 하면 트래픽이 끊긴 뒤 무엇이 거짓말을 하는가?
- 같은 성질을 가진 다른 주제 둘은 무엇인가? (출처: README 4)

#### 13. 막으려고 만든 것이 공격 수단이 된다 (출처: README「특히 생각해볼 것」5)

- 슬라이딩 로그에서 거절한 요청을 기록하면 공격자는 무엇을 할 수 있는가? (출처: README 5)
- 이것이 다른 넷에는 없는 함정인 이유는?

#### 14. 버킷 크기 b 가 정책이다 (출처: README「특히 생각해볼 것」6)

- 실제 트래픽이 고르지 않다는 것이 구체적으로 어떤 모양인가? (출처: README 6)
- b 를 충전 속도 r 과 같게 두면 무엇이 사라지는가?
- b 를 크게 두면 누가 대가를 치르는가?

#### 15. 한계와 다음 챕터로의 다리

- 슬라이딩 카운터가 "양쪽으로 틀린다"는 두 방향은 각각 무엇인가?
- 전부 프로세스 안의 상태라는 것이 서버 20대에서 무엇을 뜻하는가(02번의 어느 한계와 같은가)?
- 02번 `SlidingWindow` 도 "최근"을 셌는데 여기서 새로 설계한 이유는?
- 누출 버킷이 몰림을 지연으로 바꿨는데, 큐가 무제한이면 그것은 무엇을 무엇으로 바꾼 것뿐인가?
- 05-backpressure 가 이어받는 질문은 무엇인가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
