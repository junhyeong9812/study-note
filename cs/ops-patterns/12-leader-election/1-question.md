# ops-patterns/12-leader-election — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 문제 (NaiveElection · LeaseElection 의 TODO)

#### 1. TODO 1 — NaiveElection.campaign (기준선: 번호가 없다)

```java
// TODO 1: 비어 있으면 리더가 된다. **임기 번호는 늘 같은 값을 준다.**
//
// 번호를 세고 싶어질 텐데, 세면 이 기준선이 사라진다.
// 이 클래스는 "번호가 없으면 옛 리더와 새 리더를 구별할 수 없다" 를 보여주려고 있다.
```

- 이 기준선은 무엇을 보여주려고 존재하는가?
- 리더가 죽고 24시간 뒤 NaiveElection 의 리더는 누구인가, 그 사고는 어떻게 나타나는가?
- "리더가 죽었다"는 알림이 안 가는 이유는 무엇인가?

#### 2. TODO 2 — LeaseElection.campaign (넷을 지켜라)

```java
// TODO 2: 살아 있는 리더가 없으면 리더가 된다.
//
// 넷을 지켜야 한다.
//
//   1. 지금 리더가 자기 자신이면 **같은 임기를 그대로** 준다.
//      여기서 올리면 자기 심장 박동이 자기 임기를 무효로 만든다.
//   2. **임기 번호는 선출될 때마다 커진다.** 이것이 이 구조의 유일한 근거다.
//      옛 리더가 깨어나 "나 리더야" 라고 할 때 누가 더 최근인지 가릴 방법이 그것뿐이다.
//      11번의 펜싱 토큰과 정확히 같은 일을 한다.
//   3. 밀려난 옛 리더를 **물러나는 중** 목록에 넣어라. 여기서 안 넣으면
//      옛 리더가 자기 상태를 확인할 방법이 없고, 하던 일을 정리할 자리도 없다.
//   4. 리더가 실제로 바뀐 횟수를 세라. 이 값이 이 설정이 맞는지 판단하는 자다.
//      안 세면 "가끔 배치가 두 번 돈다" 로만 나타나고 원인을 못 찾는다.
```

- 현직 리더가 다시 campaign 을 부를 때 임기를 올리면 무슨 일이 벌어지는가?
- 임기 번호가 "이 구조의 유일한 근거"라는 말은 무슨 뜻인가?
- 밀려난 옛 리더를 물러나는 중 목록에 안 넣으면 무엇이 없어지는가?
- 교체 횟수(leaderChanges)를 안 세면 사고가 어떻게 나타나는가?
- 임기 번호는 11번의 무엇과 정확히 같은 일을 하는가?

#### 3. TODO 3 — LeaseElection.heartbeat (임기까지 본다)

```java
// TODO 3: 살아 있다고 알린다. 내 임기가 맞을 때만.
//
// **노드 이름만 보면 안 된다.** 같은 노드가 재기동해서 다시 뽑혔을 때,
// 재기동 전에 보낸 심장 박동이 뒤늦게 도착할 수 있다. 임기까지 봐야 그것이 걸린다.
//
// 내 임기인데 **이미 끊긴 지 오래면** 거절해야 한다. 아무도 안 가져갔을 뿐이지
// 임기는 이미 끝났다. 여기서 살려주면 "끊기면 잃는다" 는 약속이 거짓이 되고,
// 그 약속을 남들이 믿고 새 리더를 뽑는다.
```

- 노드 이름만 보면 어떤 시나리오가 뚫리는가?
- 옛 리더는 자기가 리더가 아닌 것을 언제 처음 아는가?
- 내 임기인데 끊긴 지 오래된 심장 박동을 살려주면 어떤 약속이 깨지는가?
- 그 약속을 "남들이 믿는다"는 것은 구체적으로 무엇을 뜻하는가?

#### 4. TODO 4·5·6 — resign / currentLeader / roleOf

```java
// TODO 4: 스스로 물러난다. 자기 임기일 때만.
// **임기 번호를 되돌리지 마라.** 되돌리면 옛 리더의 번호와 새 리더의 번호가 겹치고,
// 그러면 둘을 구별할 수 없다. 번호는 커지기만 한다.

// TODO 5: 지금 리더. 심장 박동이 끊긴 지 오래면 빈 값.
// 만료를 안 보면 이 값이 거짓말을 한다. 대시보드가 죽은 리더를 살아 있다고 보여준다.

// TODO 6: 이 노드가 지금 무엇인가. 셋 중 하나.
// 물러나는 중을 팔로워로 뭉뚱그리면 "리더가 아니니 아무것도 안 한다" 가 되는데,
// 그 노드는 이미 뭔가를 하는 중이었다. 정리할 자리가 필요하다.
```

- 물러날 때 임기 번호를 되돌리면 무엇이 겹치는가?
- currentLeader 가 만료를 안 보면 어떤 거짓말을 하게 되는가?
- 역할이 리더/팔로워 둘이 아니라 셋이어야 하는 이유는 무엇인가?
- STEPPING_DOWN 을 팔로워로 뭉뚱그리면 옛 리더는 무엇을 못 하게 되는가?

#### 5. TODO 7 — isStale (부등호 하나)

```java
// TODO 7: 심장 박동이 끊긴 지 임기보다 오래됐나.
//
// 부등호 하나다. 정확히 임기만큼 지난 순간이 "끊긴" 것인지 아닌지를 정한다.
// 한 칸 틀리면 경계에서만 어긋나는데, 그 경계를 실제로 밟는 테스트가 여덟 개다.
```

- 정확히 임기만큼 지난 순간은 "끊긴" 것인가 아닌가(이 구현의 선택은)?
- 부등호를 한 칸 틀리면 어떤 방식으로 어긋나는가, 그것이 위험한 이유는?

### B. 개념

#### 6. 락과 선출의 차이 (원본 README 도입)

- 분산 락과 리더 선출의 차이를 "작업"과 "역할"로 설명하면?
- 그 차이 때문에 락에는 없던 세 가지가 왜 각각 필요해지는가?
- 심장 박동은 무엇을 대신하는가(락의 어떤 개념과 대응되는가)?

#### 7. 옛 리더 문제 (STEPPING_DOWN 이 왜 상태인가)

- GC로 멈췄다 깨어난 옛 리더가 위험한 이유는 무엇인가?
- 옛 리더가 하던 일을 "그냥 계속하는" 사고는 왜 조용한가?
- steppedDown() 은 누가 언제 부르는 것인가?

#### 8. 고칠 수 없는 것 (원본 README "고칠 수 없는 것")

- 죽은 것과 느린 것을 구별할 수 없다는 말은 왜 버그가 아닌가?
- 구별이 안 되면 무엇이 생길 수 있고, 유일한 대책은 무엇인가?
- 임기 500ms/1500ms/5000ms 실험이 보여주는 트레이드오프는 무엇인가?
- 임기를 짧게/길게 잡으면 각각 무엇이 나빠지는가?

#### 9. 조용한 결함들 (원본 README 생각해볼 것 5·6·7·8)

- 변종 11개 중 살아남은 둘은 무엇이고, 어떤 경우를 만들어야 드러났는가?
- 리더 교체 기록(history)이 없으면 무엇을 못 하는가?
- "가끔 배치가 두 번 돈다"는 증상의 원인을 찾으려면 무엇이 필요한가?

#### 10. 연결

- 임기 번호와 11번 펜싱 토큰의 공통점과 차이는 무엇인가?
- 이 챕터의 임기 번호는 한 곳에서 나온다 — 13번은 무엇을 다르게 해야 하는가?
- Raft 의 term, Kubernetes leader election 은 이 구현의 어디에 대응하는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
