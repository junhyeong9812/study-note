# ops-patterns/15-crdt — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 TODO/javadoc 을 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 문제 (구현 대상: NaiveCounter TODO 1 · GCounter TODO 2~4 · OrSet TODO 5~8)

#### 1. NaiveCounter — merge (TODO 1, 기준선. 고치면 안 된다)

```java
// NaiveCounter.java (src/main) — 채울 것
public NaiveCounter merge(NaiveCounter other) {
    // TODO 1: 두 값을 더한다.
    //
    // **고치지 마라.** 이 클래스는 "멱등성이 없으면 무슨 일이 벌어지는가" 를 보여주려고 있다.
    // 교환법칙과 결합법칙은 성립해서 얼핏 보면 문제가 없어 보인다.
}
```

- 나이브 카운터의 merge 는 무엇을 하고, 왜 "고치지 마라"고 하는가?
- 세 성질 중 나이브 카운터에 있는 둘과 없는 하나는 각각 무엇인가?
- A=3, B=5 를 한 번 합치면 8인데 두 번 합치면 13이 되는 이유는?
- 그물 모양 전파에서 A 가 1만 세었는데 B 에서 2가 되는 과정은 어떻게 되는가?
- 이 사고는 왜 예외도 안 나고 조용히 일어나는가?
- 나이브 카운터가 실제로 맞게 동작하는 전제 조건은 무엇이고, 분산에서는 왜 그 전제가 깨지는가?

#### 2. GCounter — increment / value / merge (TODO 2~4)

```java
// GCounter.java (src/main) — 채울 것
public GCounter increment(String node, long amount) {
    // TODO 2: 이 노드의 칸에 amount 를 더한 **새** 상태를 준다.
    // 원본을 고치면 안 된다. **음수는 막아라.**
}

public long value() {
    // TODO 3: 합계. 칸들을 어떻게 하면 되는가.
    // 합치기는 max 인데 읽기는 max 가 아니다. 둘을 헷갈리기 쉽다.
}

public GCounter merge(GCounter other) {
    // TODO 4: 칸마다 합친다.
    // **더하면 안 된다.** 더하면 같은 것을 두 번 받았을 때 두 배가 되고,
    // 그게 나이브 카운터와 똑같아진다.
}
```

- 숫자 하나로 세면 왜 합칠 방법이 없는가(노드마다 칸을 나누는 이유)?
- increment 가 원본을 고치지 않고 새 상태를 주어야 하는 이유는? (출처: README 생각해볼 것 2)
- increment 에서 음수 amount 를 막는 이유는?
- 합치기는 max 인데 읽기(value)는 왜 max 가 아니라 합인가? (출처: README 생각해볼 것 1)
- merge 에서 max 대신 더하기를 쓰면 무엇과 똑같아지는가?
- 같은 칸을 두 번 받아도 값이 안 늘어나는 근거는 무엇인가?
- 14번 벡터 시계와 자료 모양이 같은데, 다른 것은 무엇인가?

#### 3. OrSet — add / remove / elements / merge (TODO 5~8)

```java
// OrSet.java (src/main) — 채울 것
public OrSet<T> add(String node, T element) {
    // TODO 5: 이 원소에 **새 태그**를 붙인다.
    // 태그가 고유해야 한다. 같은 번호를 두 번 쓰면 나중 넣기가 앞의 것과 구별이 안 되고,
    // 그러면 앞의 것을 본 빼기가 나중 것까지 지운다.
}

public OrSet<T> remove(T element) {
    // TODO 6: **지금 보이는 태그들만** 지움 목록에 넣는다.
    // 원소 자체를 지우면 안 된다. 그러면 뒤늦게 합쳐질 새 넣기까지 같이 사라진다.
}

public Set<T> elements() {
    // TODO 7: 지워지지 않은 태그가 **하나라도** 있으면 그 원소는 있는 것이다.
}

public OrSet<T> merge(OrSet<T> other) {
    // TODO 8: 넣은 태그도 지운 태그도 둘 다 합집합으로 합친다.
    // **태그 번호도 큰 쪽을 따라가야 한다.** 안 그러면 합친 뒤에 넣을 때
    // 상대가 이미 쓴 번호를 다시 쓴다.
}
```

- 집합은 왜 카운터처럼 간단히 합칠 수 없는가(합집합·교집합이 각각 무엇을 틀리는가)?
- 태그가 고유하지 않으면 구체적으로 무슨 일이 생기는가? (출처: README 생각해볼 것 3)
- remove 가 원소 자체를 지우면 안 되고 태그만 지움 목록에 넣어야 하는 이유는?
- 없는 원소를 remove 하면 어떻게 동작해야 하는가?
- elements 에서 원소가 "있다"의 판정 기준은 무엇인가?
- merge 에서 지운 태그 목록을 안 합치면 무슨 일이 생기는가?
- merge 에서 태그 번호(nextTag)도 큰 쪽을 따라가야 하는 이유는, 그리고 안 따라가면 어떤 사고가 언제 드러나는가? (출처: README 생각해볼 것 4)
- 동시에 넣고 빼면 누가 이기고, 그것은 버그인가 규칙인가?

### B. 개념

#### 4. 세 성질 — 무엇을 사면 무엇이 공짜인가

- CRDT 에 필요한 세 성질은 각각 무슨 뜻인가?
- 셋이 다 있으면 메시지 전달에 대해 무엇을 견디게 되는가?
- 07번 아웃박스·08번 사가의 어떤 문제가 여기서 공짜로 해결되는가?
- 교환·결합은 있는데 멱등만 없으면 사고가 어떤 모습으로 나타나는가?

#### 5. 한계 — 무엇이 안 되는가

- GCounter 는 왜 줄일 수 없는가? (출처: README 생각해볼 것 5)
- 빼기가 필요하면 어떻게 하는가?
- OrSet 은 왜 지운 태그를 영원히 들고 있어야 하는가? (출처: README 생각해볼 것 6)
- 넣고 빼기를 100번 반복하면 원소와 태그는 각각 몇 개인가?
- 잔액은 왜 CRDT 로 못 만드는가? (출처: README 생각해볼 것 7)
- CRDT 가 되는 자료와 안 되는 자료를 가르는 기준 한 문장은?

#### 6. 연결·검증

- 14번 논리 시계가 멈춘 자리("이 둘은 동시다")를 CRDT 는 어떻게 잇는가?
- 이 상자의 테스트는 왜 "이 입력에 이 출력"이 아니라 "이 성질이 성립한다"로 쓰였는가?
- 변종 10개 중 "태그 번호를 안 맞춘다"가 두 번 살아남은 이유는 무엇인가? (출처: README 생각해볼 것 8)
- 16번 이벤트 소싱과 이 챕터는 순서 문제를 각각 어떻게 다루는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
