# ops-patterns/18-blockchain — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 TODO/javadoc 을 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 문제 (구현 대상: HashChain TODO 1\~4 · ProofOfWorkChain TODO 5\~6)

#### 1. HashChain — append / createBlock (TODO 1\~2)

```java
// HashChain.java (src/main) — 채울 것
public Block append(List<String> transactions) {
    // TODO 1: 맨 뒤 칸의 해시를 앞 해시로 삼아 새 칸을 붙인다.
    //
    // 앞 해시를 안 담으면 그냥 목록이다. 이 한 줄이 사슬을 사슬로 만든다.
}

protected Block createBlock(int index, String previousHash, List<String> transactions) {
    // TODO 2: 해시를 계산해 담은 블록을 만든다. nonce 는 0 이다.
    //
    // 해시는 payload 로 계산한다. 그 안에 안 들어간 값은 보호되지 않는다.
}
```

- "앞 해시를 안 담으면 그냥 목록이다"는 무슨 뜻인가?
- 블록 5의 내용을 고치면 왜 블록 6에서 티가 나는가?
- payload 에 안 들어간 값은 왜 보호되지 않는가? (출처: README 생각해볼 것 1)
- 이 상자의 payload 에는 무엇 무엇이 들어가는가?
- 해시를 자체 제작하지 않고 SHA-256 을 가져다 쓰는 이유는? (출처: README 생각해볼 것 4)
- 05번 해시맵의 해시와 여기 해시는 요구 성질이 어떻게 다른가?

#### 2. HashChain — verify / rebuildFrom (TODO 3\~4, verify 가 본체)

```java
// HashChain.java (src/main) — 채울 것
public TamperReport verify() {
    // TODO 3: 사슬 전체를 검사한다. **처음 깨진 자리**를 알려준다.
    //
    // 넷을 다 봐야 한다. 하나라도 빠지면 그 공격만 통과한다.
    //   1. 번호가 이어지나        칸이 재배치됐나
    //   2. 저장된 해시가 내용과 맞나  내용이 고쳐졌나
    //   3. 앞 해시가 앞 칸과 맞나    칸이 끼워지거나 빠졌나
    //   4. 작업 증명이 되어 있나     그냥 안 하고 붙였나
    //
    // 제네시스의 앞 해시는 "0" 이다. 앞 칸이 없기 때문이다.
}

public int rebuildFrom(int index) {
    // TODO 4: 이 자리부터 끝까지 다시 만든다. 공격자가 하는 일이다.
    //
    // 앞 칸의 해시가 바뀌었으므로 그 다음 칸도 다시 만들어야 하고, 연쇄된다.
}
```

- verify 가 보는 넷은 각각 어떤 공격을 잡는가? (출처: README 생각해볼 것 2)
- 번호 검사를 빼면 어떤 사슬이 통과하는가?
- 난이도 검사를 빼면 무엇이 무의미해지는가?
- "어딘가 이상하다"가 아니라 "어디부터인지"를 알려줘야 하는 이유는?
- 3번 칸을 고쳤을 때와 3번 칸의 해시까지 고쳤을 때 검사 결과는 각각 어떻게 나오는가?
- rebuildFrom 이 한 칸만 다시 만들고 끝날 수 없는 이유는?
- 1000칸 사슬에서 500번부터 위조하는 데 해시 몇 회가 들고, 그것이 보여주는 이 구조의 한계는 무엇인가?
- "고치면 티가 난다"와 "못 고친다"는 어떻게 다른 말인가?

#### 3. ProofOfWorkChain — createBlock / meetsDifficulty (TODO 5\~6)

```java
// ProofOfWorkChain.java (src/main) — 채울 것
public ProofOfWorkChain(int difficulty) {
    // 부모 생성자가 제네시스를 만들 때 이미 이 값이 필요하다.
    // 자바에서는 필드가 부모 생성자 뒤에 초기화되므로 제네시스는 난이도 0 으로 만들어진다.
    // 그래서 생성 뒤에 다시 만든다. 이 순서 문제가 실제로 자주 나온다.
    super();
    ...
    blocks.set(0, createBlock(0, "0", List.of()));
}

protected Block createBlock(int index, String previousHash, List<String> transactions) {
    // TODO 5: 해시 앞이 0 으로 difficulty 개 이어질 때까지 nonce 를 돌린다.
    //
    // 부등호를 조심하라. **이상이지 초과가 아니다.** 초과로 쓰면 난이도 0 이 실은 1 이 되고,
    // 만드는 쪽과 검사하는 쪽이 둘 다 그러면 자기들끼리는 맞아서 안 드러난다.
    // 비용을 세야 보인다.
    // nonce 가 넘치는 경우도 막아라. 난이도가 높으면 실제로 도달할 수 있다.
}

protected boolean meetsDifficulty(Block block) {
    // TODO 6: 이 블록이 난이도를 맞췄나. TODO 5 와 같은 판정이어야 한다.
}
```

- 작업 증명의 절차(nonce 를 돌린다)는 정확히 무엇을 하는 것인가?
- 난이도 판정이 "이상"이어야 하는 이유는, 초과로 쓰면 왜 안 드러나는가? (출처: README 생각해볼 것 3)
- 그 결함은 무엇을 세야 보이는가?
- nonce 넘침을 막아야 하는 이유는?
- 부모 생성자 순서 문제로 제네시스가 어떻게 만들어지고, 어떻게 고치는가? (출처: README 생각해볼 것 5)
- meetsDifficulty 가 TODO 5 와 같은 판정이어야 하는 이유는?

### B. 개념

#### 4. 무엇을 지키고 무엇을 못 지키는가

- 이 구조가 지키는 것을 한 문장으로 말하면?
- 해시 사슬만으로는 왜 1초 만에 위조가 끝나는가?
- 작업 증명의 "만드는 것은 비싸고 검사는 한 번"이라는 비대칭은 왜 이 구조의 전부인가?
- 난이도 0/1/2/3에서 10칸 만드는 해시 횟수는 대략 어떻게 자라는가?
- 작업 증명이 있어도 "못 바꾸는 것은 아니다" — 그럼 실제 방어는 무엇인가?
- 사슬이 한 곳에만 있으면 왜 소용없는가? (출처: README 생각해볼 것 6)

#### 5. 연결 — 앞뒤 챕터와의 다리

- 16번 이벤트 소싱의 "지우지 않고 붙인다"와 이 챕터의 차이를 규칙/구조로 설명하면?
- 27번 머클 트리와 같은 자리라고 한 것은 무엇인가?
- 변종 7개 중 처음에 살아남은 셋의 공통점은 무엇인가? (출처: README 생각해볼 것 7)
- 19번 우아한 종료로 넘어가는 다리는 무엇인가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
