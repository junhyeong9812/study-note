# cs/issue/network/reverse-tunnel-nat-traversal — 정답

> 복습 시 이 파일은 **최후에만** 연다.
> ⚠️ 이 정답은 Claude 초안(2026-09-23) — 이슈 README·코드 기준. 복습 전 읽지 말 것.

## 정답
<!-- 질문 1:1 대응 -->

1. **비대칭의 출처.** 두 호스트가 서로 다른 서브넷에 있고, 홈랩의 라우팅 테이블에는 `default via 게이트웨이` 항목만 있어 **PC 대역으로 가는 경로가 아예 없다**(`ping`이 unreachable). 반대로 PC→홈랩은 경로가 있어(배포 ssh가 매일 그 방향을 쓴다) 뚫려 있다. 핵심은 이것이 "포트가 닫혀서"가 아니라 **패킷이 갈 길(route)이 없어서**라는 것 — 라우팅은 방향마다 독립이므로, 한쪽 경로만 존재하면 통신이 한 방향으로만 성립한다.
   > **라우팅 테이블** — "이 대역 주소로는 어느 게이트웨이/인터페이스로 보내라"의 목록. 해당 항목이 없으면 그 대역은 unreachable.

2. **-L vs -R, 왜 -R.** `ssh -L`(로컬 포워딩)은 "**내가** 상대 포트로 나가는 통로를 내 쪽에 연다", `ssh -R`(원격 포워딩)은 "**상대가** 내 포트로 들어오는 통로를 상대 쪽에 연다". 막힌 방향(홈랩→PC)을 뚫으려면 **연결을 실제로 거는 주체가 나갈 수 있는 쪽(PC)**이어야 한다. PC가 `-R`로 걸면 SSH 세션 자체는 PC→홈랩(뚫린 방향)으로 나가지만, 그 세션 위에 홈랩→PC로 데이터가 흐르는 역방향 통로가 생긴다. 그래서 관건은 "누가 먼저 연결을 거느냐"이고, 답은 **부를 수 있는 쪽이 건다**.
   > **역(reverse) SSH 터널 `-R`** — 부를 수 없는 쪽이 부를 수 있는 쪽으로 나가서, 그 세션 위에 반대 방향 통로를 뚫는 것.

3. **socat이 잇는 것.** `-R`로 포워딩된 포트는 기본적으로 상대(홈랩) 호스트의 **루프백(127.0.0.1)**에만 바인딩된다 — 즉 홈랩 호스트 자신에서만 보인다. 그런데 요청을 보낼 backend는 **컨테이너 안**이라 호스트 루프백을 직접 못 본다(컨테이너의 `127.0.0.1`은 컨테이너 자신이다). 그래서 `socat`을 `0.0.0.0:<LAN포트> → 127.0.0.1:<터널이 연 포트>` 릴레이로 띄워, **호스트 루프백에만 있던 통로를 LAN 주소로 노출**한다. 그러면 backend가 LAN 주소:포트로 닿을 수 있다.
   > **socat** — 한 소켓(TCP 포트 등)을 다른 소켓으로 이어주는 얇은 릴레이 도구.

4. **터널이 끊기면.** 역 SSH 터널은 PC가 건 **하나의 SSH 세션 위**에 있으므로, PC가 껐다 켜지거나 네트워크가 끊기면 세션이 죽고 통로도 함께 사라진다 — 즉 **상시 서비스가 아니라 세션 프로세스**다. 운영상 이는 "PC에서 스크립트로 기동하고 유지해야 한다"는 제약을 만든다(README에 절차 기록). 그래서 호출부(backend)는 브리지 URL이 비었거나 브리지가 오프라인이면 죽지 않고 `available=false` → `escalate_unavailable`로 **우아하게 부재를 안내**한다. 통로의 불안정성을 장애가 아니라 "지금은 로컬 답변만 가능"이라는 정상 분기로 흡수한 것.

5. **원격 실행 표면과 방어선.** 방향을 뒤집어 통로를 열면, 그 relay 포트에 닿는 누구든 PC에서 임의 프롬프트로 `claude -p`를 돌릴 수 있는 **원격 실행 표면**이 생긴다(배포 서버만큼 위험). 방어선 각각: **공유 시크릿 상수시간 검증(`hmac.compare_digest`)** — 인증 없는 실행을 막고, 빈 시크릿이면 전부 거절하며, 상수시간 비교로 타이밍 공격 차단. **도구 전면 차단(`--allowedTools ""`)** — 문서 안에 "파일 읽어라" 같은 지시가 있어도 도구를 못 써 순수 텍스트만 나오게(프롬프트 주입 완화). **동시 1건(세마포어, 초과는 503)** — 느린 요청 하나가 전체를 점유하지 못하게. **크기 상한(프롬프트/출력)** — 폭주·자원 고갈 방지. **stderr 비노출(실패해도 `claude_failed`만)** — 내부 오류 정보 누출 차단. 각각 다른 위험(무인증 실행·주입·자원 점유·폭주·정보 누출)에 대응한다.
   > **상수시간 비교** — 문자열 길이/내용에 따라 걸리는 시간이 달라지지 않게 비교해, 시간차로 시크릿을 추측하는 공격을 막는 것.

6. **왜 socat을 컨테이너로.** 릴레이 하나를 위해서지만 컨테이너로 띄운 이유는 **호스트에 sudo로 손대지 않기 위해서**다. 호스트에서 특권으로 포트 릴레이 데몬을 상시 돌리는 대신, 격리된 컨테이너로 띄우면 권한을 최소화하고(sudo 불필요) 다른 서비스들과 같은 compose 운영 방식에 얹을 수 있다. 최소 권한·격리·운영 일관성을 위해 얇은 릴레이도 컨테이너에 담았다.

7. **연결 — NAT 통과 일반.** 이건 "안에서 밖으로는 나가지만 밖에서 안으로는 못 들어오는"(NAT·불라우팅 서브넷) 상황을 우회하는 전형적 **NAT traversal**이다. 공통 아이디어는 **부를 수 없는 쪽이 먼저 밖으로 나가 연결을 유지**하는 것. 같은 발상의 사례: **웹훅 대신 폴링** — 서버가 클라이언트를 못 부를 때 클라이언트가 주기적으로 나가서 물어봄. **홀펀칭(hole punching)** — 양쪽이 동시에 아웃바운드를 쏴 NAT 매핑을 뚫음(STUN/WebRTC 계열). **아웃바운드 롱폴·웹소켓 기반 에이전트** — 방화벽 뒤 에이전트가 밖으로 상시 연결을 걸어두고 그 위로 명령을 받음(역터널과 정확히 같은 방향 뒤집기). 전부 "다이얼 방향을 가능한 쪽으로 뒤집는다"가 뼈대다.

## 이번 프로젝트 사례
- [ci-cd/issue4](../../../../../project/study-note-deploy-system/ci-cd/issue4/) — 홈랩↔PC 서브넷이 라우팅으로 안 이어져(unreachable), PC가 역 SSH 터널(`-R`)을 걸고 홈랩 host에서 socat 릴레이로 컨테이너에 노출. 인증·도구 차단·동시 1건·크기 상한·stderr 비노출로 원격 실행 표면 축소.
- [backend/issue12](../../../../../project/study-note-deploy-system/backend/issue12/) — backend가 이 브리지로 에스컬레이션 POST, 미설정 시 `available=false`(`escalate_unavailable`)로 우아한 부재.

## 검증 기록
- 2026-09-23: 이슈 README(ci-cd/issue4 §1·§2·§4, backend/issue12 §4) + `tools/claude-bridge/server.py`(루프백 바인딩 `("127.0.0.1", PORT)`·시크릿 `compare_digest`·`--allowedTools ""`·세마포어·크기 상한·`claude_failed`) 대조 작성 (Claude 초안). 내부 IP·시크릿 값은 개념·일반 포워딩 형태로만 기술.
