# cs/issue/testing/performance-measurement-validity — 정답

> 복습 시 이 파일은 **최후에만** 연다.
> ⚠️ 이 정답은 Claude 초안(2026-09-24) — 사건 기록 원문 대조 작성. 복습 전 읽지 말 것.

태그: `test-reliability`

## 정답
<!-- 질문 1:1 대응 -->

1. **생존자 편향.** 실패한 요청은 지연 분포에 들어가지 않는다.\
오류율 71%인 조건의 p95는 전체 요청이 아니라 **실패하지 않고 살아남은 요청**의 지연이므로, 오류율이 낮은 조건(5.4%)의 p95와 같은 모집단이 아니다.\
공정한 비교는 **성공 처리량**(초당 성공 요청 수)을 함께 보는 것이다 — 실측에서 결론은 "지연 8.6배 단축"이 아니라 "처리량 129 → 427 req/s, 3.3배"로 재서술됐다.\
같은 기록에서 세그먼트 수 같은 교란 변수를 정규화하고, 대리 실험 결과를 본 실측으로 쓰지 않으며, "무실패 + 낮은 p95"는 포화 미도달이라는 점도 함께 바로잡았다.
   > **survivorship bias** — 걸러지고 남은 표본만 보고 전체를 판단하는 편향. 실패가 표본에서 빠지면 성과가 과대평가된다.

2. **JIT·분기 예측.** 내림차순 데이터에서 안쪽 비교가 **항상 거짓**이면 분기 예측이 완벽하게 맞고, JIT가 그 루프를 강하게 최적화한다.\
그래서 이론상 O(n·k)인 변종이 3×10⁹회 규모에서도 1초대에 끝났고, O(n²)가 20만 건에서 1.54초로 통과했다.\
교정은 두 가지다: 입력 규모를 상향(100만×k=5만)하고 루프 제거를 막는 체크섬을 넣는 것, 그리고 **타이밍 대신 결정적 계수기**(이동 횟수·트리 높이·회전 수·방문 수)로 한계를 재는 것.\
계수기는 하드웨어·JIT 상태와 무관하게 알고리즘의 일의 양을 직접 센다.
   > **JIT warm-up** — 런타임이 실행 중 뜨거운 코드를 기계어로 최적화하기까지의 구간. 이 전후의 측정값은 같은 코드라도 다르다.

3. **쿠폰 수집가.** 무작위로 키를 뽑을 때 "아직 안 본 키"를 만날 확률은 캐시가 찰수록 줄어서, 전 키를 한 번씩 보는 데는 키 개수보다 훨씬 많은 요청이 필요하다.\
축출 없는 캐시의 히트율은 이 속도로 수렴하므로, 80초 워밍업 뒤 캐시가 6,638/10,000만 차 있었다 — 히트율이 **과소평가**됐다(가설에 불리한 방향의 편향).\
결정적 프리웜은 무작위 요청 대신 **전 키를 정확히 한 번씩** 요청해 채운다(부하 도구의 공유 반복 실행기로 반복 번호를 키 인덱스로 사용).
   > **coupon collector** — n종을 무작위로 모을 때 전부 모으는 데 기대 약 n·ln n 번이 필요한 문제.

4. **생성기도 측정 대상.** 도착률(arrival-rate) 실행기의 사전 할당 VU는 **시나리오마다 곱해지고**, VU마다 런타임 메모리가 있다.\
계단형 부하를 한 런의 여러 시나리오로 잡아 합계 15,000 VU가 되자 초기화가 1,730 근처에서 멈추고 SSH가 끊긴 뒤 호스트 전원 재시작까지 갔다.\
교정은 계단마다 **런을 분리**(런이 끝나면 VU 해제)하고 사전 할당을 상한(≤ 2,000) 아래로 두며, 생성기를 **메모리 캡을 건 컨테이너**로 돌려 최악에도 컨테이너만 죽게 하는 것이다.\
무선 경로의 생성기가 목표 8,000에 4,623(드롭 42%)만 낸 것은 **생성기·네트워크 경로의 한계**가 측정에 섞였다는 뜻이다 — 그 결과는 대상 시스템의 한계가 아니므로 생성기를 실격시키고 유선 생성기를 쓴다.

5. **무작위 기저선.** 두 집합이 서로 무관해도 크기 비율에 따라 **우연히 겹치는 기대값**이 있다.\
실측에서 교집합 0.49는 무작위 기저선 0.5와 거의 같아 "중복"이 아니라 "무관"의 신호였다.\
지표는 기저선을 뺀 **초과분**으로 정의해야 신호가 된다.

6. **데이터 부재.** 입력 배열이 비어 있어도 부하 스크립트는 에러 없이 돌고, 캐시 실험은 ~100% 히트율 같은 "좋은 숫자"를 낼 수 있다.\
측정 파이프라인은 실패를 내지 않으므로, 입력 데이터가 실제로 있는지를 측정 전에 **따로 확인**해야 한다(이 사례의 교정 방법은 원 기록에 남아 있지 않다 — 여기서는 원리만 적는다).\
테스트가 대상이 0개여도 초록을 내는 그린 위장과 같은 구조다: 측정 장치가 **실패할 수 있는 조건**을 갖추지 않으면 출력은 증거가 아니다.

## 문제 구조 (추상화 코드)

### 변형 A — 오류율이 다른 조건의 지연 비교 (생존자 편향)
① 문제 코드
```text
조건      오류율   p95
shard=1   71.2%   높음 (살아남은 요청만)
shard=4   5.4%    낮음
결론: "지연 8.6배 단축"
```
② 고친 코드
```text
조건      오류율   성공 처리량   p95
shard=1   71.2%   129 req/s    (비교 불가 표기)
shard=4   5.4%    427 req/s
결론: "성공 처리량 3.3배" — 교란 변수(세그먼트 수) 정규화, 대리 실험은 상한/하한으로만 표기
```
무엇이 깨졌나: 실패가 빠진 분포끼리 비교했다.

### 변형 B — 벽시계 임계로 복잡도를 판정
① 문제 코드
```kotlin
@Test fun mustBeLinear() {
    val input = descending(300_000); val k = 10_000     // 예측하기 쉬운 데이터
    assertTimeout(ofSeconds(2)) { slidingWindowMax(input, k) }   // O(n·k) 도 통과
}
```
② 고친 코드
```kotlin
@Test fun mustBeLinear() {
    val input = descending(1_000_000); val k = 50_000
    val counter = OpCounter()
    val out = slidingWindowMax(input, k, counter)
    consume(out.oddSum())                               // 홀수합 체크섬: 결과를 써서 루프 제거 방지
    assertThat(counter.moves).isLessThanOrEqualTo(LINEAR_BOUND)   // 일의 양을 직접 센다 (상한은 선형식)
}
```
무엇이 깨졌나: 하드웨어·JIT가 흡수하는 시간을 복잡도의 대리 지표로 썼다.

### 변형 C — 수렴 전 캐시·워밍업 전 JVM 측정
① 문제 코드
```js
// 워밍업: 무작위 키로 80초 → 전체 키의 2/3 만 캐시됨
scenarios: { warm: { executor: "constant-arrival-rate", duration: "80s" } }
```
② 고친 코드
```js
// 결정적 프리웜: 전 키를 정확히 한 번씩
scenarios: { prewarm: { executor: "shared-iterations", iterations: KEYS.length } }
export default function () { hit(KEYS[exec.scenario.iterationInTest]); }
// JVM 비교는 워밍업 뒤 2차 측정을 채택
```
무엇이 깨졌나: 정상 상태에 도달하기 전의 값을 정상 상태의 값으로 읽었다.\
같은 구조: 교차 검증 대상 서비스만 포트를 노출하지 않아 외부 수집이 헬스체크 타임아웃 → 포트 노출 + 헬스체크가 실패 대상을 명시.

### 변형 D — 부하 생성기의 자원 한계
① 문제 코드
```js
STEPS.forEach(rps => {
  scenarios[`s${rps}`] = { executor: "constant-arrival-rate", rate: rps,
                           preAllocatedVUs: Math.min(3000, rps), maxVUs: 6000 };   // 시나리오마다 곱해짐
});
```
② 고친 코드
```sh
for rps in $STEPS; do                                  # 계단마다 별도 런 → 끝나면 VU 해제
  docker run --rm --memory=<cap> loadgen run -e RATE=$rps -e PREALLOC=2000 step.js   # 최악에도 컨테이너만 사망
done
# 생성기는 유선 경로에서 — 목표 대비 드롭률을 먼저 확인해 생성기 실격 여부 판정
```
무엇이 깨졌나: 측정 장비에 상한이 없어 장비가 먼저 죽거나 목표 부하를 내지 못했다.

## 검증 기록
- 2026-09-24: 출처 원문 대조(Claude 초안) — 근거는 작업 log
