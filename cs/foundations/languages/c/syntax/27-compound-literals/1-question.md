# c/syntax/27 — 복합 리터럴: 「**이름 없는 변수를 식 한가운데 세운다**」 — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.
> **환경** — gcc 13.3.0 · clang 18.1.3 · g++ 13.3.0 · x86-64 Linux · 기본 `-std=c17 -Wall -Wextra -pedantic`.
> ★★ **이 주제의 인출 축은 셋**이다 —
> ① **객체인가 값인가**(주소·수정·`sizeof`) ② **언제 죽나**(저장 기간)
> ③ **죽은 뒤에 읽으면 여섯 벌이 어떻게 갈리나**.
> ★★★ **4번과 6번은 한 벌로 답하지 마라** — **컴파일러 2 × 최적화 3 = 여섯 벌**을 다 적어야 한다.
> ★★★ **그중 두 벌은 「정답처럼 보이는 값」을 준다** — 그것이 이 주제에서 가장 위험한 자리다.
> ★★ **「경고 0건」을 근거로 쓰지 마라** — 두 컴파일러 중 한쪽은 이 주제 전체에 **0건**이다.
> ★ **값이 아니라 주소를 찍어야 갈리는 자리**가 하나 있다(6번).
> 선행 — [21번 형제](../21-struct-declaration-initialization-and-designated-initializers/) · [28번 형제](../28-choosing-among-four-storage-durations/).

## 이 파일을 푸는 법

- ★★ **예측형 여섯 문항(1·3·4·6·7·8)은 소스만 보고 출력을 적어 본 뒤** 답을 연다.
- ★★★ **4번은 「UB 다」로 끝내면 답이 아니다** — **여섯 칸을 채워야** 답이다.
- ★★ **6번은 값이 아니라 「같은 객체인가」를 적어야** 답이다.
- ★ 각 문항 끝에서 스스로 물어라 — 「**이 객체는 언제 죽나**」.

## 질문

<!-- 질문 하나 = "?" 하나 = 한 줄. 유형: (왜) / (예측) / (경계) / (연결) -->

### 1. 복합 리터럴로 다섯 가지를 해 보면 (예측) ★★★ 이 주제의 축

```c
/* s27b.c */
#include <stdio.h>

struct P { int x, y; };

int main(void) {
    setvbuf(stdout, NULL, _IONBF, 0);   /* 죽기 전 출력이 사라지지 않게 */
    printf("1) 좌변값이다 — 멤버를 고칠 수 있다\n");
    struct P *p = &(struct P){1, 2};
    p->x = 99;
    printf("   (%d,%d)\n", p->x, p->y);

    printf("2) 배열 복합 리터럴 — sizeof 가 배열 크기다\n");
    printf("   sizeof (int[]){1,2,3} = %zu · 원소 %zu 개\n",
           sizeof (int[]){1, 2, 3}, sizeof (int[]){1, 2, 3} / sizeof(int));

    printf("3) 지정 초기자를 쓰면 나머지는 0 이다\n");
    struct P q = (struct P){.y = 5};
    printf("   (%d,%d)\n", q.x, q.y);

    printf("4) (char[]){\"abc\"} 는 고칠 수 있다\n");
    char *s = (char[]){"abc"};
    s[0] = 'X';
    printf("   %s\n", s);

    printf("5) \"abc\" 는 문자열 리터럴이라 고치면 죽는다 — 다음 줄에서 끝난다\n");
    char *t = (char *)"abc";
    t[0] = 'X';
    printf("   %s   <- 이 줄은 보이지 않는다\n", t);
    return 0;
}
```

- `1)` 의 출력은 무엇인가? ★ **주소를 잡고 멤버를 고치는 것**이 되는가?
- `2)` 의 `sizeof` 는 얼마이고 원소는 몇 개인가? ★ **포인터로 감쇠하지 않는 이유**는?
- `3)` 은 무엇으로 찍히는가?
- `4)` 와 `5)` 는 **어떻게 갈리는가**? ★ `run exit` 는 얼마인가?
- ★★ 소스 첫 줄의 `setvbuf(stdout, NULL, _IONBF, 0)` 가 **없으면** 무슨 일이 나는가?
- ★ 이 다섯 가지는 **어느 층**인가?

### 2. 왜 값이 아니라 객체인가 (왜) ★★

- `3` 이나 `"abc"` 같은 리터럴과 **무엇이 다른가**?
- ★★ **좌변값**이라는 말이 여기서 무엇을 뜻하는가? 두 가지 결과를 대면?
- ★ 그러면 **저장 공간이 실제로 잡히는가**? 언제 잡히나?
- ★★ 이 문법이 **새 저장 기간을 만드는가**? 아니면 기존의 것을 쓰는가?
- ★ 「이름표가 없는 변수」라는 표현이 **어디까지 맞고 어디서부터 틀리나**?

### 3. 파일 스코프와 블록 스코프에 하나씩 놓으면 (예측) ★★★

```c
/* s27a.c */
#include <stdio.h>

struct P { int x, y; };

static struct P *g = &(struct P){10, 20};   /* 파일 스코프 → 정적 저장 기간 */
static struct P *keep;

static void set(void) {
    keep = &(struct P){1, 2};               /* 블록 스코프 → 자동 저장 기간 */
}                                           /* ★ 여기서 그 객체가 죽는다 */

int main(void) {
    printf("파일 스코프 : %d %d   <- 프로그램이 끝날 때까지 산다\n", g->x, g->y);
    set();
    printf("블록 스코프 : %d %d   <- 블록이 끝난 뒤에 읽었다\n", keep->x, keep->y);

    struct P *a[3];
    for (int i = 0; i < 3; i++) a[i] = &(struct P){i, i * i};
    printf("루프 세 번  : (%d,%d) (%d,%d) (%d,%d)\n",
           a[0]->x, a[0]->y, a[1]->x, a[1]->y, a[2]->x, a[2]->y);
    printf("주소가 같나 : a0==a1 %d · a1==a2 %d\n", a[0] == a[1], a[1] == a[2]);
    return 0;
}
```

- 첫 줄(`파일 스코프`)은 무엇으로 찍히는가? 왜인가?
- 둘째 줄(`블록 스코프`)은 **기본 빌드에서** 무엇으로 찍히는가?
- ★★★ 그 값이 **믿을 수 있는 값인가**? 왜 그렇게 판단하는가?
- ★★ 셋째 줄(`루프 세 번`)은 무엇으로 찍히는가?
- ★★ 넷째 줄(`주소가 같나`)은 무엇으로 찍히는가?
- ★ 이 빌드에서 **경고가 몇 건** 나오는가? 플래그 이름은?

### 4. 같은 소스를 여섯 벌로 던지면 (예측) ★★★ 본체

- ★★★ **gcc·clang × `-O0`/`-O1`/`-O2`** 의 여섯 칸에 「블록 스코프」 값을 각각 적으면?
- ★★★ 그중 **「맞아 보이는」 칸은 몇 개**이고 **어느 것**인가?
- ★★ 「루프 세 번」 열은 여섯 칸이 어떻게 갈리는가?
- ★ **`cc exit`** 는 여섯 벌에서 각각 얼마인가? **`run exit`** 는?
- ★★ 이 격자에서 **실행마다 바뀌는 칸**은 어느 것인가? 그러면 **대조할 것은 무엇**인가?
- ★★★ 「`-O0` 에서 잘 되던데」를 근거로 쓰면 **무엇이 틀리는가**?

### 5. 경고를 컴파일러·최적화별로 세면 (경계) ★★★

- ★★★ gcc 의 경고 건수는 `-O0`/`-O1`/`-O2` 에서 각각 **몇 건**인가?
- ★★★ clang 은 **몇 건**인가?
- ★★ 그 비대칭이 실무에서 **무엇을 뜻하는가**?
- ★★ gcc 에서 **최적화를 올릴수록 경고가 느는 이유**는? 어떤 플래그가 추가되는가?
- ★ **`cc exit`** 는 여섯 벌 다 얼마인가? 이것이 규칙 「경고 0건은 종료 코드를 같이 봐야 뜻이 있다」와 **어떻게 맞물리는가**?

### 6. 루프에서 주소를 세 개 모으면 (예측) ★★★

- 3번의 `a[0]`·`a[1]`·`a[2]` 는 각각 무엇을 가리키는가?
- ★★★ `a[0] == a[1]` 과 `a[1] == a[2]` 는 각각 무엇으로 찍히는가?
- ★★★ **값만 보고** 「덮어쓴 것」과 「애초에 하나였던 것」을 **가를 수 있는가**?
- ★★ 그래서 **이 주제의 네 번째 창**은 무엇인가?
- ★★ 이 사실은 **표준이 보장하는 것인가 관찰인가**?
- ★ 객체를 정말 셋 만들려면 어떻게 해야 하는가?

### 7. 죽은 복합 리터럴을 ASan 으로 읽으면 (예측) ★★

```c
/* s27d.c */
#include <stdio.h>

struct P { int x, y; };
static struct P *keep;

static void set(void) { keep = &(struct P){1, 2}; }

int main(void) {
    setvbuf(stdout, NULL, _IONBF, 0);
    set();
    printf("블록이 끝난 복합 리터럴을 읽는다\n");
    printf("%d %d\n", keep->x, keep->y);
    return 0;
}
```

- ASan 빌드로 돌리면 **에러 이름**이 무엇인가? `run exit` 는?
- ★★ **접근 크기**를 얼마라고 적는가? 어디(프레임의 몇 번째 오프셋)라고 적는가?
- ★★★ 리포트가 「**이 프레임에 객체가 몇 개**」라고 적는가? 그 숫자가 6번과 **어떻게 맞물리는가**?
- ★ 리포트에서 **실행마다 바뀌는 칸**과 **안 바뀌는 칸**을 가르면?
- ★★ clang 으로 빌드한 프로젝트에서 이 도구가 **왜 특히 중요한가**?

### 8. 복합 리터럴에 `static` 을 붙이면 (예측) ★★

```c
/* s27e.c */
#include <stdio.h>

struct P { int x, y; };
static struct P *keep;

/* ★ C23 부터 복합 리터럴에 저장 클래스 지정자를 붙일 수 있다 */
static void set(void) { keep = &(static struct P){1, 2}; }

int main(void) {
    set();
    printf("static 복합 리터럴 : %d %d\n", keep->x, keep->y);
    return 0;
}
```

- `gcc -std=c2x -pedantic -O2` 로 돌리면 어떻게 되는가? ★ **`-O2` 인데도** 값이 맞는 이유는?
- ★★ `gcc -std=c17 -pedantic` 으로 돌리면 어떻게 되는가? **`cc exit` 는**?
- ★★★ `clang -std=c2x -pedantic` 으로 돌리면 어떻게 되는가? **`cc exit` 는**?
- ★★★ 「`-std=c17` 로 돌렸으니 C17 코드다」가 여기서 **왜 깨지는가**? 막으려면?
- ★ **어느 버전부터**인가? 지금 이 기능을 써도 되는가?
- ★ 같은 효과를 **이식되는 방법**으로 얻으려면?

### 9. 함수 인자 자리에 쓰면 (경계) ★★

```c
/* s27c.c */
#include <stdio.h>

struct Opt { int retries; int timeout_ms; const char *tag; };

static void connect_to(const char *host, struct Opt o) {
    printf("%-8s retries=%d timeout=%dms tag=%s\n",
           host, o.retries, o.timeout_ms, o.tag ? o.tag : "(없음)");
}

static int sum(const int *a, int n) { int s = 0; for (int i = 0; i < n; i++) s += a[i]; return s; }

int main(void) {
    /* ★ 임시 구조체를 그 자리에서 만들어 넘긴다 — 이름 붙은 인자처럼 읽힌다 */
    connect_to("db",    (struct Opt){.retries = 3, .timeout_ms = 500, .tag = "primary"});
    connect_to("cache", (struct Opt){.timeout_ms = 50});
    connect_to("log",   (struct Opt){0});

    /* 배열도 그 자리에서 만들어 넘긴다 */
    printf("sum = %d\n", sum((int[]){1, 2, 3, 4, 5}, 5));
    return 0;
}
```

- 세 번의 `connect_to` 는 각각 무엇으로 찍히는가?
- ★ `(struct Opt){0}` 하나가 무엇을 뜻하는가?
- `sum((int[]){1,2,3,4,5}, 5)` 는 얼마인가?
- ★★★ 이 용법은 **안전한가**? 4번의 UB 와 **무엇이 다른가**?
- ★ **경고가 몇 건** 나오는가?
- ★★ 이 문법이 **값을 내는 자리**를 한 줄로 요약하면?

### 10. C++ 로 같은 줄을 던지면 (연결) ★★

```c
/* s27f.cpp */
#include <cstdio>

struct P { int x, y; };
static P *keep;

static void set(void) { keep = &(P){1, 2}; }   /* C 에서는 되던 줄 */

int main(void) { set(); std::printf("%d %d\n", keep->x, keep->y); return 0; }
```

- g++ 로 컴파일하면 **무슨 진단**이 나오는가? `cc exit` 는?
- ★★★ C 와 C++ 에서 이 식의 **값 범주**가 어떻게 다른가?
- ★★ 그래서 **4번의 UB 가 C++ 에서는 어떻게 되는가**?
- ★ 복합 리터럴 자체가 **ISO C++ 에 있는가**?
- ★ C 와 C++ 가 같이 쓰는 헤더에서는 어떻게 해야 하는가?

### 11. 다섯 층과 무게중심 (연결) ★★★

- 이 주제에서 **표준 / 조건부 표준 / 구현 정의 / 미명시 / UB** 칸에 각각 무엇이 들어가는가?
- **비어 있는 칸**은 무엇인가?
- ★★ **UB 는 몇 자리**인가? 그런데 왜 이 주제에서 그렇게 큰가?
- ★★★ **미명시 칸에 들어가는 한 자리**는 무엇인가?
- ★★ **도구가 침묵하는 자리**를 층마다 하나씩 대면?
- ★★ 「**종료 코드가 0인데 ill-formed**」인 자리가 **몇 군데**인가? 그중 **막을 플래그가 없는 것**은?

### 12. 경계 — 어디까지가 이 주제인가 (연결) ★

- **`{.x = 1}` 이라는 초기자 문법**은 어느 주제가 정본인가?
- **자동·정적 저장 기간의 수명 규칙**은 어느 주제인가?
- **`"abc"` 를 고치면 왜 죽나**는 어느 주제인가?
- **댕글링 포인터가 왜 최악인가**와 **sanitizer 사용법**은 각각 어느 주제인가?
- ★ 이 주제가 **끝까지 책임지는 것** 세 가지를 대면?
- ★★ 이 문법을 **쓰면 안 되는 자리** 두 가지를 대면?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
