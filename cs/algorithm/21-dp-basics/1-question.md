# algorithm/21-dp-basics — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 TODO 주석을 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 문제 (src/main 의 TODO 1~9)

#### 1. 피보나치 (TODO 1~3)

```java
/** 표 없이 재귀. calls 배열에 호출 횟수를 센다. */
public static long naive(int n, long[] calls) {
    // TODO 1: 정의를 그대로 옮긴다. calls[0]++ 을 잊지 마라.
    //   n=40 이면 호출이 몇 번일까. 재보기 전에 짐작해봐라.
}

/** 위에서 아래로. 필요한 값만 계산한다. */
public static long memoized(int n, CountedTable table) {
    // TODO 2: 표에 있으면 그대로 쓰고 없으면 계산해서 넣는다.
    //   table.isFilled 로 확인하고 table.get 으로 읽어라.
    //   isFilled 대신 get 의 결과를 EMPTY 와 비교해도 되나. 계수기가 어떻게 달라지나.
}

/** 아래에서 위로. 작은 것부터 순서대로 전부 채운다. */
public static long tabulated(int n, CountedTable table) {
    // TODO 3: 바닥값을 먼저 넣고 순서대로 올라간다.
    //   재귀가 없으므로 깊이 걱정이 없다. 대신 무엇을 더 하게 되나.
    //   n 이 0 이나 1 이면 표를 안 만들고 바로 답해도 되나.
}
```

- naive 는 n 이 1 늘 때마다 호출 수가 대략 몇 배로 느는가, 왜 그런가?
- 표 하나를 두는 것만으로 지수가 선형이 되는 이유는 무엇인가?
- memoized 에서 `isFilled` 대신 `get` 결과를 EMPTY 와 비교해도 답은 맞는데, 계수기는 어떻게 달라지는가?
- tabulated 는 재귀 깊이 걱정이 없는 대신 무엇을 더 하게 되는가?
- tabulated 에서 n 이 0 이나 1 이면 표를 안 채우고 바로 답해도 되는가?
- 적중(hit)이 0 이라면 그것은 무엇을 뜻하는가?

#### 2. 0-1 배낭 (TODO 4~6)

```java
/** 표 없이 전부 시도. calls 배열에 호출 횟수를 센다. */
public static long naive(int[] weights, int[] values, int capacity, long[] calls) {
    // TODO 4: 물건마다 넣는 경우와 안 넣는 경우를 둘 다 시도한다.
    //   무게가 남은 것보다 크면 넣는 경우를 시도할 수 있나.
    //   물건을 다 봤으면 무엇을 돌려주나.
}

/** 메모이제이션. */
public static long memoized(int[] weights, int[] values, int capacity, CountedTable table) {
    // TODO 5: 상태를 무엇으로 잡나.
    //   "어떤 순서로 여기까지 왔나" 가 앞으로의 답에 영향을 주나.
    //   안 준다면 무엇만 기억하면 되나.
    //   두 값을 표의 한 자리로 어떻게 누르나. 2 차원 배열 대신 1 차원을 쓰려면.
    //   눌러 담는 식에서 남은 무게의 범위가 0 부터 capacity 까지라는 것을 잊지 마라.
}

/** 타뷸레이션. 모든 상태를 채운다. */
public static long tabulated(int[] weights, int[] values, int capacity, CountedTable table) {
    // TODO 6: 어느 방향으로 채워야 하나.
    //   메모이제이션은 index 가 큰 쪽(물건을 다 본 상태)을 먼저 알아낸다.
    //   그러면 표는 어느 쪽부터 채워야 하나.
    //   순서를 거꾸로 하면 아직 안 채운 칸을 읽게 된다. 그러면 무슨 값이 나오나.
}
```

- naive 에서 물건의 무게가 남은 무게보다 크면 "넣는 경우"를 시도할 수 있는가?
- naive 에서 물건을 다 봤으면 무엇을 돌려주는가?
- 상태를 무엇으로 잡는가, 그리고 "어떤 순서로 여기까지 왔나"는 왜 상태에 안 들어가는가?
- 그 성질(최적 부분 구조)이 없으면 왜 DP 를 못 쓰는가?
- 2차원 상태 `(index, remaining)` 을 1차원 표 한 자리로 누르는 식은 무엇인가?
- 그 식에서 capacity 가 아니라 `capacity + 1` 을 곱해야 하는 이유는?
- 타뷸레이션은 index 를 어느 방향으로 채워야 하는가, 거꾸로 채우면 무슨 값이 나오는가?
- 같은 문제에서 메모이제이션이 표의 56% 만 채우는 이유는 무엇인가?
- 피보나치에서는 왜 그 차이(메모 56% vs 타뷸 100%)가 안 보였는가?

#### 3. 최장 증가 부분수열 (TODO 7~9)

```java
/** O(n^2). table 에 "i 로 끝나는 최장 길이" 를 담는다. */
public static int quadratic(int[] a, CountedTable table) {
    // TODO 7: i 로 끝나는 가장 긴 증가 수열의 길이를 앞의 모든 j 를 보고 정한다.
    //   최솟값이 얼마인가. 자기 혼자면 길이가 몇인가.
    //   답은 표의 마지막 칸인가, 표 전체의 최댓값인가. 왜 그런가.
    //   빈 배열이면 무엇을 돌려주나.
}

/**
 * O(n log n). tails[k] 는 길이 k+1 인 증가 수열의 마지막 값 중 가장 작은 것.
 * comparisons 배열에 이진 탐색의 비교 횟수를 센다.
 */
public static int logarithmic(int[] a, long[] comparisons) {
    // TODO 8: 값마다 tails 에서 자리를 찾아 덮어쓴다.
    //   어떤 자리를 찾나. 그 값 이상인 첫 자리다. 06 번의 lowerBound 와 같다.
    //   tails 가 항상 증가한다는 것을 왜 믿을 수 있나.
    //   자리를 찾았을 때 길이가 늘어나는 경우는 언제인가.
    //   tails 자체가 답인 부분수열인가. 그렇게 생각하면 어디서 틀리나.
}

/** 실제 부분수열 하나를 복원한다. O(n^2) 판의 표가 있어야 한다. */
public static int[] reconstruct(int[] a, CountedTable table) {
    // TODO 9: 길이가 가장 큰 자리에서 시작해 거꾸로 이어 붙인다.
    //   길이가 하나씩 줄어드는 자리를 찾으면 되나. 그것만으로 충분한가.
    //   값이 증가하는지도 봐야 할 것 같다. 정말 그런가.
    //   (정답 구현에는 그 검사가 없다. 넣었다가 필요 없는 것을 증명하고 지웠다)
}
```

- quadratic 에서 표의 i 번 칸은 정확히 무엇을 담는가?
- 그 칸의 최솟값은 얼마이고 왜 그런가?
- 답은 표의 마지막 칸인가 전체의 최댓값인가, 왜 그런가?
- 빈 배열이면 무엇을 돌려주는가?
- logarithmic 의 tails[k] 는 무엇을 담는가?
- 값마다 tails 에서 찾는 자리는 어떤 자리인가?
- tails 가 항상 증가 상태라는 것을 왜 믿을 수 있는가?
- 자리를 찾았을 때 전체 길이가 늘어나는 경우는 언제인가?
- tails 자체가 답인 부분수열인가, 그렇게 생각하면 어떤 입력에서 틀리는가?
- reconstruct 는 어디서 시작해 어떤 자리를 이어 붙이는가?
- "값도 증가하는지" 검사는 왜 필요 없는가(증명의 뼈대만이라도)?

### B. 개념

<!-- 앞 5개: 원본 README 「생각해볼 것」 이관 -->

- (원본 생각해볼 것) 배낭의 표를 1차원으로 줄일 수 있다 — 무게 루프를 어느 방향으로 돌아야 하는가?
- (원본 생각해볼 것) 무게가 아니라 가치를 상태로 잡으면 점화식이 어떻게 바뀌는가?
- (원본 생각해볼 것) 가치를 상태로 잡는 쪽이 유리한 것은 언제인가?
- (원본 생각해볼 것) LIS 를 O(n log n) 으로 풀면서 복원까지 하려면 무엇을 더 기억해야 하는가?
- (원본 생각해볼 것) 메모이제이션은 재귀라 깊이가 쌓인다 — 언제 위험한가?

<!-- 아래: 서머리 기반 파생 질문 -->

- DP 를 쓸 수 있는 조건(최적 부분 구조)을 한 문장으로 말하면 무엇인가?
- "같은 것을 두 번 계산하지 않는다"는 주장은 왜 결과만 봐서는 확인이 안 되는가?
- 메모이제이션과 타뷸레이션 중 무엇을 언제 고르는가(채우는 칸·재귀 깊이·채우는 순서 기준으로)?
- LIS 의 O(n²) 판과 O(n log n) 판은 "관리하는 정보"가 어떻게 다른가?
- n=2,000 실측에서 표 읽기 1,013,183 인데 계측기는 왜 "훑기의 절반만" 세고 있다고 하는가?
- 이 챕터의 이진 탐색(tails 자리 찾기)은 06-binary-search 의 무엇과 같은 것인가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
