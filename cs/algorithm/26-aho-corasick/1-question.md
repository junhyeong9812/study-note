# algorithm/26-aho-corasick — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 TODO 골격을 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 과제 (AhoCorasick — TODO 1~7)

#### 1. 트라이 만들기 (`build` — TODO 1)

```java
/** 패턴들을 넣은 오토마톤을 만든다. */
public static CountedAutomaton build(List<String> patterns, CountedAutomaton automaton) {
    // TODO 1: 트라이를 만든다.
    //   글자를 0~25 로 바꿔 간선을 따라간다. 없으면 노드를 새로 만든다.
    //   패턴이 끝나는 노드에 그 패턴의 번호를 표시한다.
    //   같은 패턴이 두 번 들어오면 어떻게 되나.
    //   한 노드에 여러 패턴이 끝날 수 있나. "he" 와 "he" 가 아니라 다른 경우로.

    // TODO 2: 실패 링크를 건다. 아래 메서드를 만들어라.
    return automaton;
}
```

- 글자를 0~25 로 바꾼다는 것은 입력에 대해 무엇을 전제하는가?
- 간선이 있을 때와 없을 때 각각 무엇을 하는가?
- 패턴이 끝나는 노드에는 무엇을 기록하고, 그것이 나중에 어디에 쓰이는가?
- 같은 패턴이 두 번 들어오면 어떻게 되는가? (원본 TODO 1)
- 서로 다른 패턴 여러 개가 한 노드에서 끝나는 입력은 어떤 모양인가? (원본 TODO 1)
- `he` 와 `hers` 는 트라이에서 무엇을 공유하는가?
- 트라이 만들기의 비용은 무엇에 비례하는가?

#### 2. 실패 링크 걸기 (`linkFailures` — TODO 3~6)

```java
/** 실패 링크를 건다. */
private static void linkFailures(CountedAutomaton automaton) {
    // TODO 3: 어떤 순서로 채워야 하나.
    //   어떤 노드의 실패 링크를 구하려면 무엇이 먼저 있어야 하나.
    //   DFS 로 하면 그 조건이 지켜지나. BFS 는?
    //   15 번 플로이드와 22 번 구간 DP 에서도 같은 질문이 나왔다.

    // TODO 4: 깊이 1 의 실패 링크는 어디로 가나. 왜 그런가.

    // TODO 5: 없는 간선을 어떻게 다룰지 두 가지 방법이 있다.
    //   (가) 찾을 때 실패 링크를 따라 올라가며 있는 간선을 찾는다
    //   (나) 만들 때 없는 간선을 실패 링크 쪽으로 미리 이어둔다
    //
    //   (나)로 하면 찾을 때 루프가 사라진다. 대신 무엇을 더 쓰나.
    //   MeasurementTest 가 (나)를 전제로 이동 횟수를 박아 두었다.

    // TODO 6: 출력 링크.
    //   "she" 를 찾았을 때 "he" 도 같이 찾아야 한다. 어떻게 알아채나.
    //   실패 링크가 가리키는 곳의 출력을 물려받으면 된다.
    //   그것을 언제 해야 하나. 그 노드의 실패 링크가 이미 정해진 뒤여야 한다.
    //   계약 테스트가 "she" 와 "he" 를 같이 주는 입력을 쓴다.
}
```

- 실패 링크는 어디로 가는 링크인가(한 문장으로)?
- 어떤 노드의 실패 링크를 구하려면 무엇이 먼저 있어야 하는가? (원본 TODO 3)
- DFS 로 채우면 그 조건이 왜 깨지고, BFS 는 왜 지켜지는가? (원본 TODO 3)
- 깊이 1 노드의 실패 링크는 어디로 가는가, 왜 그런가? (원본 TODO 4)
- 없는 간선을 찾을 때 따라 올라가는 방법(가)과 만들 때 미리 잇는 방법(나)은 각각 무엇을 내주고 무엇을 얻는가? (원본 TODO 5)
- (나)를 골랐을 때 트라이는 무엇으로 바뀌는가?
- 출력을 물려받지 않으면 어떤 입력에서 무엇을 놓치는가? (원본 TODO 6)
- 출력 물려받기는 왜 그 노드의 실패 링크가 정해진 뒤에 해야 하는가? (원본 TODO 6)
- `she` 의 `e` 노드의 실패 링크는 어디로 가는가, 그 이유는?

#### 3. 본문 훑기 (`findAll` — TODO 7)

```java
/**
 * 본문에서 모든 패턴의 모든 출현을 찾는다. 결과는 (끝 위치, 패턴 번호) 다.
 */
public static List<Match> findAll(CountedAutomaton automaton, String text) {
    // TODO 7: 상태 하나를 들고 본문을 한 번 훑는다.
    //   글자마다 automaton.readChar() 를 부르고 automaton.move 로 옮겨간다.
    //   되돌아가는 루프가 있나. TODO 5 에서 (나)를 골랐다면 없어야 한다.
    //
    //   결과를 왜 시작 위치가 아니라 끝 위치로 주나.
    //   패턴마다 길이가 다르다는 것을 생각해봐라.
    return List.of();
}
```

- 본문 한 글자당 상태 이동은 몇 번인가?
- 되돌아가는 루프가 없는 이유는 무엇인가? (원본 TODO 7)
- 아무 패턴도 못 여는 글자(예: `ushers` 의 `u`)를 읽으면 상태는 어디에 있는가?
- 결과를 시작 위치가 아니라 끝 위치로 주는 이유는? (원본 TODO 7)
- 시작 위치가 필요하면 어떻게 구하는가?
- 전체 비용 O(n + m + z) 에서 z 는 왜 따로 붙는가?

### B. 개념

#### 4. 생각해볼 것 (원본 README 이관)

- 출력 링크를 물려받는 대신 찾을 때 실패 링크를 따라 올라가면 무엇이 달라지나, 어느 쪽이 나은가? (원본 README)
- 알파벳이 유니코드면 노드마다 배열 26 개로 안 된다 — 무엇으로 바꿔야 하나? (원본 README)
- 패턴을 나중에 하나 더 넣으려면 오토마톤을 통째로 다시 만들어야 하나? (원본 README)
- 겹치지 않는 매칭만 원하면(가장 긴 것 우선) 무엇을 바꿔야 하나? (원본 README)

#### 5. 파생 — 왜 그렇게 생겼는가

- 아호 코라식은 25 번 KMP 의 무엇을 어디로 올린 것인가?
- 패턴별로 KMP 를 k 번 돌리는 것과 비교해 시간에서 사라진 k 는 어디로 갔는가?
- 노드 수를 정하는 것은 패턴 개수인가, 아니면 무엇인가?
- 본문 10 만 글자에 패턴이 10 개든 1,000 개든 본문 읽기가 100,000 번 그대로인 이유는?
- "본문 한 번 훑기 O(n)" 이라는 말이 빼고 하는 비용은 무엇인가?
- 실패 링크의 BFS 순서 조건은 15 번 플로이드·22 번 구간 DP 의 어떤 공통 질문과 같은가?
- 없는 간선을 미리 채우는 거래는 19 번 네트워크 플로우의 무엇과 같은 성격인가?
- URL 필터·금칙어 목록 같은 데이터에서 아호 코라식이 특히 유리한 이유는?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
