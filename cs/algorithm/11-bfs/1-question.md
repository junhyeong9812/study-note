# algorithm/11-bfs — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.
>
> ⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제.

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 javadoc/TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 과제 (구현 대상 — src TODO 1~8)

#### 1. `Bfs.distances` / `Bfs.multiSource` (TODO 1~3)

```java
/** start 에서 각 정점까지의 최단 거리. 못 가면 UNREACHABLE. */
public static int[] distances(CountedGraph g, int start, CountedDeque queue)
    // TODO 1: multiSource 로 넘겨라. 시작점이 하나인 것은 여러 개인 것의 특수한 경우다.

/** 여러 시작점에서 동시에 출발했을 때 각 정점까지의 최단 거리. */
public static int[] multiSource(CountedGraph g, int[] starts, CountedDeque queue)
    // TODO 2: 시작점들을 전부 큐에 넣고 거리를 0 으로 둔다.
    //   시작점마다 BFS 를 따로 돌려 최솟값을 취해도 답은 같다. 그러면 비용이 어떻게 되나.
    //   한 번에 넣어도 되는 이유는 무엇인가. 꺼내는 순서가 여전히 거리 순인가.
    //   같은 시작점이 두 번 들어오면 어떻게 하나. 답은 같은데 무엇이 달라지나.
    //   시작점이 범위 밖이면. ... 전부 하거나 아무것도 안 하거나로 만들려면 무엇을 먼저 해야 하나.
    // TODO 3: 큐가 빌 때까지 꺼내면서 이웃을 본다.
    //   방문 표시를 언제 하나. 큐에 넣을 때인가 뺄 때인가.
```

- BFS가 최단 거리를 주는 이유는 무엇인가?
- 그 성질은 어떤 조건에서만 성립하는가?
- `distances`가 한 줄로 `multiSource`에 넘기고 끝나는 이유는 무엇인가?
- 시작점 k개마다 BFS를 따로 돌려 최솟값을 취하는 풀이의 비용은 얼마인가?
- 시작점을 전부 큐에 넣고 한 번만 돌려도 답이 같은 이유는 무엇인가?
- 같은 시작점이 두 번 들어오면 어떻게 처리하고, 안 걸러내면 무엇이 달라지는가?
- 시작점 범위 검사와 큐에 넣기를 한 루프에 섞으면 무슨 일이 생기는가?
- 방문 표시는 큐에 넣을 때 하는가 뺄 때 하는가, 뺄 때 하면 무엇이 달라지는가?
- 그 실수를 결과 테스트로는 왜 못 잡고, 무엇으로 잡는가?
- 정점 200개 완전 그래프에서 뺄 때 표시가 큐를 얼마나 키우는가(실측)?
- 직선 그래프에서는 두 방식의 차이가 왜 아예 없는가?
- `if (dist[to] == UNREACHABLE)`에 `|| dist[v] + 1 < dist[to]`를 붙이면 무엇이 달라지는가?

#### 2. `Bfs.levels` (TODO 4)

```java
/** 거리별로 묶은 정점 목록. levels.get(d) 는 거리가 d 인 정점들이다. */
public static List<List<Integer>> levels(CountedGraph g, int start, CountedDeque queue)
    // TODO 4: 큐에 지금 들어 있는 것이 정확히 한 층이다.
    //   그 크기를 언제 재야 하나. 안쪽 루프를 돌면서 재면 무슨 일이 생기나.
    //   층을 나누지 않고 거리 배열로도 같은 것을 만들 수 있다. 어느 쪽이 나은가.
```

- "지금 큐에 들어 있는 것이 정확히 한 층"이라는 말은 어떤 불변식에서 나오는가?
- 층 크기는 언제 재야 하고, 안쪽 루프를 돌면서 재면 무슨 일이 생기는가?
- 거리 배열(`distances`)로도 층을 만들 수 있는데 두 방법의 차이는 무엇인가?

#### 3. `ZeroOneBfs.distances` (TODO 5~8)

```java
/**
 * 0-1 BFS. 간선 가중치가 0 또는 1 뿐일 때의 최단 거리.
 * 보통의 BFS 는 모든 간선 비용이 같아야 성립한다. 0 이 섞이면 왜 무너지는지 생각해봐라.
 */
public static int[] distances(CountedGraph g, int start, CountedDeque deque)
    // TODO 5: 거리 배열의 초기값을 무엇으로 두나.
    //   -1 로 두면 "아직 안 감" 과 "거리 -1" 이 구분되나.
    // TODO 6: 꺼낸 정점의 이웃마다, 지금 가는 길이 더 짧은지 본다.
    //   Bfs 에서는 "처음 만났나" 를 봤다. 여기서는 왜 그걸로 안 되나.
    // TODO 7: 더 짧아졌으면 덱에 넣는다. 앞인가 뒤인가.
    // TODO 8: 못 간 정점의 값을 UNREACHABLE 로 바꾼다.
```

- 가중치 0인 간선이 섞이면 보통 BFS는 왜 무너지는가?
- 그것을 고치는 한 줄은 무엇인가?
- "처음 만났나"가 아니라 "더 짧아졌나"를 봐야 하는 이유는 무엇인가?
- 거리 초기값을 -1로 못 두는 이유는 무엇이고, 대신 무엇으로 두는가?
- 못 간 정점은 마지막에 어떻게 처리하는가?
- 0-1 BFS는 간선을 정확히 한 번씩만 보는가(실측 기준으로)?
- 다익스트라로도 답이 나오는데 왜 0-1 BFS를 쓰는가?

### B. 개념

#### 4. 원본 README "생각해볼 것" (이관)

- 최단 거리뿐 아니라 경로 자체가 필요하면 무엇을 더 기억해야 하는가?
- 최단 경로가 몇 개인지 세려면 거리 배열 하나로 되는가?
- 양쪽에서 동시에 BFS 하면(양방향 탐색) 왜 빨라지고, 얼마나 빨라지는가?
- 0-1 BFS의 가중치가 0, 1, 2 세 종류면 어떻게 되고, 어디까지 이 방법이 통하는가?

#### 5. 파생 질문 (서머리 기반)

- BFS의 핵심 불변식을 한 문장으로 말하면 무엇인가?
- BFS의 비용이 O(V+E)인 이유를 정점과 간선 각각으로 설명하면?
- 같은 그래프 탐색인데 BFS는 최단 거리를 보장하고 DFS(12번)는 못 하는 이유는?
- 가중치가 제각각이면 BFS의 어떤 성질이 무너지고, 다익스트라(14번)는 무엇으로 그것을 복구하는가?
- `CountedGraph`가 이웃 목록을 통째로 주지 않고 `degree`/`edge`로 나눈 이유는 무엇인가?
- 개발용 작은 예제에서 안 보이던 "뺄 때 표시" 실수가 배포 후에 터지는 이유는(그래프 모양 기준)?
- `levels`에서 층 경계를 표시하는 다른 방법(예: 큐에 구분자 넣기)과 크기 재두기 중 무엇이 나은가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
