# algorithm/23-greedy — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 TODO 주석을 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 문제 (src/main 의 TODO 1~10)

#### 1. 활동 선택 (TODO 1~3)

```java
/** 시작과 끝. 끝나는 시각에 다음 것이 시작해도 된다고 본다. */
public record Activity(int start, int end) {
}

/** 겹치지 않게 고를 수 있는 최대 개수. */
public static List<Activity> select(List<Activity> activities, CountedChoices choices) {
    // TODO 1: 무엇으로 정렬하나.
    //   짧은 것부터? 일찍 시작하는 것부터? 일찍 끝나는 것부터?
    //   셋 다 그럴듯하다. 그런데 하나만 맞는다.
    //   고른 기준이 왜 최적인지 교환 논증으로 설명해봐라.
    //   최적해의 첫 선택을 우리 것으로 바꿔도 되나. 왜 되나.
    // TODO 2: 순서대로 보면서 안 겹치는 것만 고른다.
    //   "안 겹친다" 를 어떻게 판정하나. 마지막으로 고른 것의 무엇을 기억하면 되나.
    //   시작 시각이 이전 끝 시각과 같으면 겹치나 안 겹치나. 계약을 먼저 정해라.
}

/** 시작이 이른 것부터 고르는 판. 틀린다. */
public static List<Activity> selectByStart(List<Activity> activities, CountedChoices choices) {
    // TODO 3: 위와 같은데 정렬 기준만 시작 시각으로 바꾼다.
    //   어떤 입력에서 틀리나. 아주 긴 활동 하나가 앞에 있으면 어떻게 되나.
}
```

- 그럴듯한 세 기준(짧은 것·일찍 시작·일찍 끝남) 중 맞는 것은 무엇인가?
- "끝나는 시각이 이른 것부터"가 최적임을 교환 논증으로 어떻게 보이는가?
- "안 겹친다"는 마지막으로 고른 활동의 무엇 하나만 기억하면 판정되는가?
- 시작 시각이 이전 끝 시각과 같으면 겹치는가 — 그것은 누가(알고리즘/문제) 정하는 것인가?
- 시작이 이른 것부터 고르는 판은 어떤 입력에서 틀리는가?
- "짧은 것부터" 기준이 틀리는 입력은 어떤 모양인가?
- 정렬을 빼면 select 의 비용은 얼마이고, 실측에서 실제 비용은 어디에 있었는가?

#### 2. 동전 거스름돈 (TODO 4~6)

```java
/** 큰 동전부터 쓰는 그리디. coins 는 내림차순이어야 한다. */
public static int greedy(int[] coins, int amount, CountedChoices choices) {
    // TODO 4: 큰 것부터 최대한 쓴다.
    //   다 쓰고도 남으면 어떻게 하나. 못 만든다는 것을 어떻게 알리나.
    //   금액이 0 이면 몇 개인가. 음수면?
}

/** DP 로 구한 진짜 최소 개수. 그리디의 답과 대조하는 용도다. */
public static int optimal(int[] coins, int amount) {
    // TODO 5: 21 번의 배낭과 같은 모양이다. 상태를 무엇으로 잡나.
    //   못 만드는 금액을 어떻게 표시하나. 큰 값으로 두면 거기에 1 을 더할 때 넘치나.
}

/** 이 화폐에서 그리디가 최적이 아닌 가장 작은 금액. 없으면 -1. */
public static int smallestCounterexample(int[] coins, int limit) {
    // TODO 6: 1 부터 limit 까지 둘을 비교한다.
    //   limit 까지 안 나왔다고 반례가 없는 것인가.
    //   "반례를 못 찾았다" 와 "반례가 없다" 는 다르다. 메서드 이름과 문서에 그것을 담아라.
}
```

- greedy 에서 다 쓰고도 금액이 남으면 무엇을 반환하는가?
- 금액이 0 이면 답이 몇 개이고, 음수면 어떻게 하는가?
- optimal 의 상태는 무엇이고, 21번 배낭과 어떤 점에서 같은 모양인가?
- 못 만드는 금액을 Integer.MAX_VALUE 로 표시할 때 무엇을 조심해야 하는가?
- "못 만드는 금액"과 "그리디가 실패하는 금액"은 어떻게 다른가 — (5, 3) 화폐에서 7 과 6 의 차이는?
- (1, 3, 4) 화폐에서 6 원이 반례인 이유는(그리디 답과 최적 답)?
- smallestCounterexample 이 -1 을 돌려줬을 때 그것이 증명이 아닌 이유는?
- 한국·미국 화폐에서 그리디가 되는 것은 일반적인 경우인가 특수한 경우인가?

#### 3. 허프만 코딩 (TODO 7~10)

```java
/** 빈도가 주어졌을 때 부호화한 전체 비트 수의 최솟값. */
public static long encodedBits(long[] frequencies, CountedChoices choices) {
    // TODO 7: 트리를 만들지 않고 합계만 구할 수 있다.
    //   두 덩어리를 합칠 때 그 합이 무엇을 뜻하나.
    //   합친 덩어리에 속한 모든 글자가 한 단계씩 깊어진다. 그 비용이 얼마인가.
    //   그러면 합친 값들을 전부 더하면 무엇이 되나.
    // TODO 8: 경계.
    //   글자가 0 종류면 몇 비트인가.
    //   글자가 1 종류면? 코드 길이가 0 일 수 있나. 그러면 무엇을 못 하게 되나.
    //   이 경우를 빼먹으면 답이 0 으로 나온다. 계약 테스트가 잡는다.
    // TODO 9: 왜 가장 적은 둘을 합치는 것이 최적인가.
    //   가장 적게 나오는 두 글자가 최적 트리에서 어디 있을 수 있나.
    //   거기 없다고 가정하면 무엇과 바꿔치기할 수 있나. 그러면 무엇이 줄어드나.
}

/** 고정 길이 부호로 했을 때의 비트 수. 비교 대상이다. */
public static long fixedLengthBits(long[] frequencies) {
    // TODO 10: 글자가 k 종류면 한 글자에 몇 비트가 필요한가.
    //   k 가 2 의 거듭제곱이 아니면 올림해야 한다. 어떻게 구하나.
}
```

- 트리를 안 만들고 "합칠 때마다 합을 누적"하면 왜 전체 비트 수가 되는가?
- 글자가 0 종류면 몇 비트이고, 1 종류면 왜 0 이 아닌가?
- 글자 1 종류에서 코드 길이 0 을 허용하면 무엇을 못 하게 되는가?
- 왜 "가장 적게 나오는 둘"을 합치는 것이 최적인가 — 교환 논증의 뼈대는?
- 합치기는 정확히 몇 번 일어나는가, 그 수는 트리의 무엇과 같은가?
- 고정 길이 부호에서 글자 k 종류면 한 글자에 몇 비트가 필요한가?
- 코드는 그 올림을 어떤 연산으로 구하는가?
- 허프만의 이득이 1.0배(전부 같은 빈도)에서 2.5배(2^i 치우침)까지 흔들리는 이유는?

### B. 개념

<!-- 앞 4개: 원본 README 「생각해볼 것」 이관 -->

- (원본 생각해볼 것) 활동에 가치가 있으면(가중 활동 선택) 그리디가 되는가, 안 되면 무엇으로 풀어야 하는가?
- (원본 생각해볼 것) 쪼갤 수 있는 배낭(분할 배낭)에서는 왜 그리디가 맞는가 — 21번의 0-1 배낭과 무엇이 다른가?
- (원본 생각해볼 것) 어떤 화폐가 정준인지 판정하려면 어디까지 확인하면 충분한가?
- (원본 생각해볼 것) 허프만 트리가 여러 개일 수 있는데 그래도 비트 수는 같은가?

<!-- 아래: 서머리 기반 파생 질문 -->

- 그리디와 13번 백트래킹의 결정적 차이는 무엇이고, 그것이 왜 지수와 선형을 가르는가?
- 그리디와 DP(동전 실측 1,260배)는 무엇을 맞바꾸는 관계인가?
- "그럴듯한 기준이 여러 개"일 때 코드를 짜기 전에 무엇을 해야 하는가?
- 허프만의 압축률이 알고리즘이 아니라 데이터에서 온다는 실측은 실무에서 무엇을 시사하는가?
- 활동 선택의 교환 논증과 허프만의 교환 논증은 어떤 공통 형태를 갖는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
