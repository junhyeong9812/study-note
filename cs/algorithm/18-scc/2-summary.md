# algorithm/18-scc — 정리 (힌트)

> 복습 시 이 파일은 **질문에 막혔을 때만** 연다. 먼저 읽고 답하면 인출이 아니라 받아쓰기다.
> 작성 방식: 내가 먼저 기억으로 흐름을 서술하고, Claude는 빠지거나 틀린 곳을 짚는다. 대신 써주지 않는다.
> 이미 따라 치며 만든 정리본이 따로 있으면(organize류) 이 파일은 핵심 문장 압축 + 링크만 담는다.
> 2026-09-14: 쉽게 풀어쓴 서머리(Claude 작성) — 원본 myway/algorithm 코드 기준.

## 한눈에 — 쉽게 말하면

**강연결 요소(SCC) = 일방통행 도시에서 "서로 왕복 가능한 동네" 묶기.**

- 도로가 전부 일방통행(방향 그래프)이다. A에서 B로 갈 수 있어도 B에서 A로 못 돌아올 수 있다.
- **서로 오갈 수 있는** 정점들의 최대 묶음이 강연결 요소다. 같은 동네 안에서는 어디든 왕복이 된다.
- 동네를 점 하나로 축약해 보면, 동네 사이 지도는 반드시 **DAG**(사이클 없는 방향 그래프)가 된다 — 동네 사이에 고리가 있었다면 애초에 한 동네였을 테니까.
- 실무 예: 웹페이지 링크 덩어리 분석, 2-SAT 문제 풀이, 순환 참조(서로 물고 있는 모듈) 찾기, 데드락 탐지.
- 찾는 알고리즘이 둘: **타잔**(DFS 한 번 + low-link), **코사라주**(DFS 두 번 + 역방향 그래프). 둘 다 O(V+E)인데 실제 일의 양은 3배 다르다.

```text
일방통행 지도 (화살표 방향으로만 이동 가능)

  0 ---> 1 ---> 3          0->1->2->0 : 셋이 고리 -> 서로 왕복 가능
  ^     /       ^|         3->4, 4->3 : 둘이 고리 -> 서로 왕복 가능
  |    /        |v         1->3 : 가긴 가는데 못 돌아옴
  +-- 2         4

동네(SCC)로 묶으면

  +-------+          +-----+
  | 0 1 2 |  ------> | 3 4 |     동네 사이는 한 방향뿐 -> DAG
  +-------+          +-----+
```

## 문제 — 이 챕터가 시키는 것

원본 README가 내는 문제: **타잔과 코사라주를 둘 다 구현하고, "둘 다 O(V+E)"라는 표기가 버린 상수(간선 훑는 횟수 E vs 3E)를 계측기로 확인하라.** "차수가 같다"와 "비용이 같다"는 다른 말이라는 것이 챕터의 핵심 주장이다. 스택 소속 표시(onStack), 끝난 순서 쌓기, 역그래프 비용까지가 과제 범위다.

과제(채울 것) — `src/main/java/com/algo/scc/`의 TODO 1~8:

- `Tarjan.components()` — TODO 1: order/low 배열과 미방문 표시, 모든 정점에서 시작
- `Tarjan.visit()` — TODO 2~4: 들어가며 시각 찍고 스택에 → 이웃 세 경우 나누기(미방문/스택 위/잘려나감) → 나오며 뿌리 판정과 잘라내기
- `Tarjan.groups()` — TODO 5: 번호별로 묶기
- `Kosaraju.components()` — TODO 6~8: 1패스 끝난 순서 쌓기 → 역그래프에서 늦게 끝난 것부터 2패스 → 역그래프 생성 비용도 센다

응용 문제 파일(*Problems.java)은 이 챕터에 없다.

아래 서머리는 이 문제(README)를 분석·정리한 것이다.

## 전체 흐름

```text
방향 그래프
   |
   +--> [타잔] DFS 1번으로 끝
   |     - 정점마다 order(몇 번째 방문)와 low(닿을 수 있는 가장 이른 order) 기록
   |     - 스택에 쌓아두다가, 나올 때 low == order 인 정점(뿌리)을 만나면
   |       스택에서 자기까지 잘라낸 것이 한 덩어리
   |
   +--> [코사라주] DFS 2번 + 역그래프
         - 1패스: 원래 그래프 DFS, "끝난 순서"를 스택에 쌓음
         - 역방향 그래프를 만듦
         - 2패스: 늦게 끝난 정점부터 역그래프 DFS — 한 번에 닿는 것들이 한 덩어리
   |
   v
결과: comp[v] = 정점 v가 속한 덩어리 번호
      (번호 붙는 순서는 두 알고리즘이 다르다 — 비교할 것은 "분할"이지 배열이 아니다)
```

- 방향 그래프는 한 정점에서 전부에 못 닿을 수 있으므로, 두 알고리즘 모두 **모든 정점을 시작 후보로** 돈다.

## 동작 — 타잔

**언제 쓰나**: 그래프를 한 번만 훑고 싶을 때, 메모리를 아끼고 싶을 때(배열 몇 개, O(V)).

예시 그래프: 0→1, 1→2, 2→0, 1→3, 3→4, 4→3. 덩어리는 {0,1,2}와 {3,4}.

```text
전:  0 --> 1 --> 3          order: 방문한 순서표 (0,1,2,3,4번째)
     ^     |     ^|         low:   "여기서 출발해 닿을 수 있는
     |     v     |v                 가장 이른 order"
     +---- 2     4          스택: [] (아직 아무도 없음)
```

```text
중: DFS 0 -> 1 -> 2, 2에서 0으로 가는 간선 발견
    order: 0:0  1:1  2:2         스택: [0,1,2] (전부 아직 스택 위)
    2->0: 0이 스택에 있다 -> low[2] = min(2, order[0]) = 0   "위로 가는 길 있음"
    2에서 나옴: low[2]=0 != order[2]=2 -> 뿌리 아님, 자르지 않음
    1로 돌아옴: low[1] = min(1, low[2]) = 0

    1 -> 3 -> 4, 4->3: 3이 스택에 있다 -> low[4] = min(4, order[3]) = 3
    4에서 나옴: low[4]=3 != 4 -> 자르지 않음
    3에서 나옴: low[3]=3 == order[3]=3 -> 뿌리! 스택에서 3까지 자름: {4,3} = 덩어리 0번
```

```text
후: 1로, 0으로 돌아 나옴
    low[1]=0 != 1 -> 통과.  low[0]=0 == order[0]=0 -> 뿌리! {2,1,0} = 덩어리 1번

    comp = [1, 1, 1, 0, 0]   먼저 "끝난" 덩어리 {3,4}가 0번을 받았다
```

단계 (그림 해설: 한 단계 한 문장):

- [1] 정점에 들어갈 때 order(방문 시각)를 찍고 low를 같은 값으로 시작하며, 스택에 쌓고 "스택에 있음" 표시를 한다.
- [2] 이웃이 미방문이면 재귀로 들어갔다 나오면서 `low[v] = min(low[v], low[to])` — 자식이 위로 가는 길을 찾았으면 나도 그 길이 있는 셈이다.
- [3] 이웃이 **스택에 아직 있으면** `low[v] = min(low[v], order[to])` — 위로 올라가는 길 발견. **"이미 방문했다"만 보면 안 된다**: 이미 다른 덩어리로 잘려나간(스택에 없는) 정점을 low에 반영하면 서로 다른 덩어리가 하나로 붙는다. 12번 DFS의 3색 구분과 같은 종류다.
- [4] 정점에서 나올 때 `low == order`면 그 정점 위로 올라갈 길이 없다는 뜻 — 스택에서 자기까지 잘라낸 묶음이 강연결 요소 하나다.

**비용**: O(V+E), 간선을 **정확히 E번** 본다(측정으로 확인). 메모리는 배열 몇 개 O(V).

**덤**: 타잔의 덩어리 번호는 위상 순서의 역순이다 — 먼저 끝난 덩어리부터 번호를 받아서, 간선은 항상 `comp[u] >= comp[v]` 방향으로만 있다(위 예시에서 1→3은 comp 1 → comp 0). 위상 정렬을 따로 안 해도 된다.
  - *위상 순서(topological order)*: DAG의 정점을 "화살표가 항상 앞→뒤로만 가게" 줄 세운 순서.

**변종이 가르쳐준 것**: 스택에 있는 이웃을 만났을 때 `order[to]` 대신 `low[to]`를 써도 SCC는 똑같이 나온다(그 값도 결국 같은 덩어리 안의 order라 뿌리 판정이 안 바뀜). 그런데 무방향 그래프의 **다리·단절점** 찾기에서는 `order`를 써야 한다 — 거기서 `low[to]`를 쓰면 진짜 다리를 놓친다. "타잔이면 이렇게 쓴다"가 아니라 **"무엇을 판정하려는가"가 정한다.**

## 동작 — 코사라주

**언제 쓰나**: low-link 없이 "DFS 두 번"으로 이해가 쉬운 쪽을 원할 때. 대신 일을 3배 하고 역그래프 메모리 O(V+E)를 더 쓴다.

같은 그래프: 0→1, 1→2, 2→0, 1→3, 3→4, 4→3.

```text
전(1패스): 원래 그래프를 DFS 하며 "끝난 순서"를 쌓는다
    0 들어감 -> 1 -> 2 (2->0은 이미 방문) -> 2 끝남(쌓기)
    -> 1의 다음 이웃 3 -> 4 (4->3 이미 방문) -> 4 끝남 -> 3 끝남 -> 1 끝남 -> 0 끝남
    끝난 순서 스택 (위가 마지막):  [0, 1, 3, 4, 2]
    * 반드시 "나올 때" 쌓는다. 들어갈 때 쌓으면 위상 순서가 안 나온다.
```

```text
중(역그래프): 모든 화살표를 뒤집은 그래프를 새로 만든다
    1->0, 2->1, 0->2, 3->1, 4->3, 3->4
    (만드는 것도 그래프를 한 번 다 훑는 일이다 — 공짜가 아니다)
```

```text
후(2패스): 늦게 끝난 것부터 역그래프에서 DFS
    0에서: 0 -> 2 -> 1  (역그래프에서 닿는 것)  => {0,1,2} = 덩어리 0번
    1, 3? 1은 이미 배정. 3에서: 3 -> 4           => {3,4}   = 덩어리 1번

    comp = [0, 0, 0, 1, 1]   <- 타잔과 번호는 다르지만 "묶음"은 같다
```

단계 (그림 해설: 한 단계 한 문장):

- [1] 1패스: 모든 정점에서 DFS를 돌리며 각 정점이 **끝날 때** 스택에 쌓는다 — 스택 위쪽이 위상 순서의 앞쪽이 된다.
- [2] 역그래프를 만든다 — 화살표 방향만 전부 뒤집은 복사본.
- [3] 2패스: 스택에서 꺼내는 순서(늦게 끝난 것부터)대로, 아직 배정 안 된 정점에서 역그래프 DFS — 한 번의 DFS로 닿는 것들이 한 덩어리다.

**왜 되나**: 축약 그래프는 DAG고, 끝난 순서의 역순은 그 DAG의 위상 순서다. 뒤집은 그래프에서 그 순서대로 출발하면 자기 덩어리를 벗어날 수 없다 — 벗어나려면 원래 그래프에서 뒤로 가는 간선이 있어야 하는데 DAG라 없다.

**비용**: O(V+E)지만 실제로는 **3E** — 1패스 E + 역그래프 만들기 E + 2패스 E. 역그래프를 "공짜로 주면" 실제보다 싸 보인다.

## 비교 — 타잔 vs 코사라주

| | 방법 | 간선 훑기 | 메모리 |
|---|---|---|---|
| 타잔 | DFS 1번 + low-link | E | 배열 몇 개 O(V) |
| 코사라주 | DFS 2번 + 역그래프 | 3E | + 그래프 통째 O(V+E) |

- **둘 다 "O(V+E)"라고 부른다. 표기가 버린 상수가 3배다.** "차수가 같다"와 "비용이 같다"는 다른 말.
- 대신 코사라주는 low-link가 없어 사람이 읽기 쉽다 — "이해하기 쉬운 쪽"과 "적게 일하는 쪽"이 다를 때 무엇을 고를지는 상황이 정한다.
- 측정 하나 더: 정점 2,000개에서 정점당 나가는 간선을 1→2→3→5개로 늘리면 덩어리 수가 1,971→375→102→**15**로 급락한다. 무작위 방향 그래프에서 거대 SCC가 생기는 문턱이 정점당 간선 1개 근처다.

## 핵심 문장

- 강연결 요소 = 서로 왕복 가능한 정점의 최대 묶음이고, 덩어리로 축약하면 반드시 DAG다.
- 타잔의 핵심은 low-link: "나올 때 low == order면 그 정점이 덩어리의 뿌리" — 단, 위로 가는 길 판정에는 "방문했나"가 아니라 **"스택에 아직 있나"**를 써야 한다.
- 코사라주의 핵심은 순서: 끝난 순서의 역순(위상 순서)대로 **역그래프**를 DFS하면 덩어리 밖으로 못 나간다.
- 두 알고리즘의 출력에서 비교할 것은 번호 배열이 아니라 **분할**(누가 누구와 같은 묶음인가)이다.
- 같은 O(V+E)여도 일의 양은 E vs 3E — 역그래프를 만드는 비용도 알고리즘의 비용이다.
- 같은 코드 모양(low 갱신 자리)도 문제가 바뀌면(SCC vs 다리 찾기) 정답이 달라진다.

## 관련 자료

- 챕터 안내: `/home/jun/project/myway/algorithm/18-scc/README.md`
- 내 구현(스켈레톤): `/home/jun/project/myway/algorithm/18-scc/src/main/java/com/algo/scc/Tarjan.java`, `Kosaraju.java`
- 계측 그래프(다 주어짐): `/home/jun/project/myway/algorithm/18-scc/src/main/java/com/algo/scc/CountedGraph.java` (`reversed()` 포함)
- 정답 기준 소스: `/home/jun/project/myway/algorithm/18-scc/impl/com/algo/scc/Tarjan.java`, `impl/com/algo/scc/Kosaraju.java`
- 계약 테스트: `/home/jun/project/myway/algorithm/18-scc/src/test/java/com/algo/scc/SccTest.java`, `MeasurementTest.java`

## 용어 풀이

- **방향 그래프(directed graph)**: 간선에 방향(화살표)이 있는 그래프. 일방통행 도로망.
- **강연결(strongly connected)**: 두 정점이 서로 **양방향으로** 도달 가능한 것.
- **강연결 요소(SCC, strongly connected component)**: 서로 오갈 수 있는 정점들의 최대 묶음. 더 넣으면 왕복이 깨지는 한계까지 키운 것.
- **DAG(directed acyclic graph)**: 사이클(고리)이 없는 방향 그래프. SCC들을 점으로 축약한 그래프는 항상 DAG다.
- **축약 그래프(condensation)**: 각 SCC를 점 하나로 접은 그래프.
- **DFS(깊이 우선 탐색)**: 갈 수 있는 데까지 들어갔다가 막히면 되돌아 나오는 탐색.
- **order(방문 시각)**: DFS에서 정점에 몇 번째로 들어왔는지 적은 숫자.
- **low(최소 도달, low-link)**: 그 정점에서 출발해 닿을 수 있는 것 중 가장 이른 order. 자기 order와 같으면 "위로 갈 길 없음" = 덩어리의 뿌리.
- **스택(stack)**: 나중에 넣은 것을 먼저 꺼내는 통. 타잔은 "아직 덩어리 배정 안 된 정점"을 여기 쌓아두고, 뿌리를 만나면 자기까지 잘라낸다.
- **역그래프(reversed/transpose graph)**: 모든 간선의 방향을 뒤집은 그래프. 코사라주 2패스의 재료.
- **위상 순서(topological order)**: DAG의 정점을 화살표가 항상 앞→뒤로만 가도록 줄 세운 순서. DFS의 "끝난 순서 역순"이 이것이다.
- **다리(bridge) / 단절점(articulation point)**: 무방향 그래프에서 지우면 그래프가 두 동강 나는 간선/정점. 타잔류 low-link로 찾지만 갱신 규칙이 SCC와 다르다.
- **2-SAT**: "x이거나 y" 형태의 조건들을 모두 만족시키는 참/거짓 배정 찾기. SCC로 푼다 — x와 not x가 같은 덩어리면 모순.
- **O(V+E)** (빅오 표기): 일의 양이 정점 수 + 간선 수에 비례한다는 뜻. 같은 O(V+E)라도 상수(몇 번 훑나)는 다를 수 있다.
