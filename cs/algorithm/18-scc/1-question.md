# algorithm/18-scc — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.
>
> ⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제.

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 javadoc/TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 과제 (구현 대상 — src TODO 1~8)

#### 1. `Tarjan.components` / `Tarjan.visit` / `Tarjan.groups` (TODO 1~5)

```java
/**
 * 타잔. DFS 한 번으로 강연결 요소를 찾는다.
 * 각 정점에 두 숫자를 붙인다. 방문 시각과 "여기서 갈 수 있는 가장 이른 방문 시각".
 */
public static int[] components(CountedGraph g)
    // TODO 1: 정점마다 방문 시각(order)과 최소 도달(low)을 둔다.
    //   안 간 정점의 표시를 무엇으로 하나. 0 으로 두면 "0 번에 방문" 과 구분되나.
    //   방향 그래프는 한 정점에서 전부에 못 닿는다. 어디서부터 시작해야 하나.

private static void visit(CountedGraph g, int v)
    // TODO 2: 들어가면서 order 와 low 를 지금 시각으로 두고 스택에 넣는다.
    //   스택에 들어 있는지를 따로 표시해야 한다. 왜 그런지는 TODO 3 에서 나온다.
    // TODO 3: 이웃마다 세 경우를 나눈다.
    //   아직 안 간 정점    : 내려간 뒤 그 정점의 low 로 내 low 를 갱신한다.
    //   스택에 있는 정점    : 위로 올라갈 길이 있다는 뜻이다. 무엇으로 갱신하나.
    //                       그 정점의 low 인가 order 인가. 하나는 아직 확정 전이다.
    //   스택에 없는 정점    : 이미 다른 덩어리로 잘려나갔다. 어떻게 해야 하나.
    //                       여기를 빠뜨리면 서로 다른 덩어리가 하나로 붙는다.
    // TODO 4: 나오면서 뿌리인지 판정한다.
    //   low 와 order 가 같다는 것이 무슨 뜻인가. 뿌리라면 스택에서 어디까지 잘라내야 하나.

/** 번호별로 묶은 정점 목록. */
public static List<List<Integer>> groups(int[] comp)
    // TODO 5: 번호가 0 부터 연속이라는 것을 이용하면 된다.
```

- order와 low는 각각 무엇을 뜻하는가?
- 미방문 표시를 0으로 두면 무엇과 구분이 안 되고, impl은 무엇으로 표시하는가?
- 방향 그래프에서 왜 모든 정점을 시작 후보로 돌아야 하는가?
- 정점을 스택에 넣는 것 외에 `onStack` 표시를 따로 두는 이유는 무엇인가?
- 이웃이 "아직 안 간 정점"일 때 내 low를 무엇으로 갱신하는가?
- 이웃이 "스택에 있는 정점"일 때 order로 갱신하는가 low로 갱신하는가, 그 근거는?
- 이웃이 "스택에 없는 정점"일 때는 어떻게 해야 하고, 빠뜨리면 무슨 일이 생기는가?
- 그 구분이 12번 DFS의 3색 구분과 같은 종류라는 말은 무슨 뜻인가?
- 나올 때 low == order 라는 것은 무슨 뜻이고, 그때 스택에서 어디까지 잘라내는가?
- `groups`가 "번호가 0부터 연속"이라는 성질을 어떻게 이용하는가?

#### 2. `Kosaraju.components` (TODO 6~8)

```java
/**
 * 코사라주. DFS 를 두 번 돌린다.
 * 타잔보다 이해하기 쉽다는 말을 많이 한다. 그 대신 무엇을 더 내는지 재봐라.
 */
public static int[] components(CountedGraph g)
    // TODO 6: 1 패스. 원래 그래프에서 DFS 를 돌리며 순서를 쌓는다.
    //   들어갈 때 쌓나 나올 때 쌓나. 하나만 맞는다.
    //   무엇을 얻으려는 것인가. 축약 그래프의 위상 순서다.
    // TODO 7: 2 패스. 간선을 뒤집은 그래프에서 어떤 순서로 출발하나.
    //   한 번의 DFS 로 닿는 것들이 하나의 강연결 요소다. 왜 그런가.
    //   축약 그래프가 DAG 라는 것과 위상 순서를 어떻게 엮으면 그 결론이 나오나.
    // TODO 8: 역방향 그래프를 만드는 비용도 이 알고리즘의 비용이다.
```

- 1패스에서 정점을 "나올 때" 쌓아야 하는 이유는 무엇이고, 들어갈 때 쌓으면 무엇이 깨지는가?
- 1패스가 얻으려는 것은 무엇인가?
- 2패스는 어느 그래프에서 어떤 순서로 출발하는가?
- 역그래프에서 한 번의 DFS로 닿는 것들이 정확히 하나의 강연결 요소인 이유는 무엇인가?
- 축약 그래프가 반드시 DAG인 이유는 무엇인가?
- 역방향 그래프를 만드는 비용을 왜 알고리즘 비용에 넣어야 하는가?
- 코사라주가 간선을 총 몇 번 훑는지 그 3E의 내역은 무엇인가?
- 메모리 사용은 타잔과 어떻게 다른가?

### B. 개념

#### 3. 원본 README "생각해볼 것" (이관)

- 타잔을 재귀로 썼는데 정점 100만 개면 어떻게 되는가(12번의 깊이 측정 기준)?
- 2-SAT을 강연결 요소로 풀 때 `x`와 `not x`가 같은 덩어리면 무슨 뜻인가?
- 축약 그래프를 실제로 만들면 무엇에 쓸 수 있는가?
- 무방향 그래프에는 강연결 요소가 없는 이유는 무엇이고, 무엇이 그 자리를 대신하는가?

#### 4. 파생 질문 (서머리 기반)

- 강연결 요소의 정의를 한 문장으로 말하면 무엇인가?
- 두 알고리즘의 결과 배열을 그대로 비교하면 안 되는 이유는 무엇이고, 무엇을 비교해야 하는가?
- "둘 다 O(V+E)인데 상수가 3배"라는 측정은 알고리즘 선택에 무엇을 시사하는가?
- 타잔의 덩어리 번호가 위상 순서의 역순이 되는 이유는 무엇이고, 그것이 왜 공짜 선물인가?
- 역간선 갱신에 order 대신 low를 쓴 변종이 SCC 테스트에서 안 잡힌 이유는 무엇인가?
- 같은 자리가 다리·단절점 문제에서는 왜 order여야만 하는가?
- 무작위 방향 그래프에서 정점당 간선 수를 늘리면 덩어리 수는 어떻게 변했는가(실측)?
- SCC가 실무에서 순환 참조·데드락 탐지에 쓰이는 이유를 축약 DAG로 설명하면?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
