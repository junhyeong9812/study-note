# algorithm/17-mst — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.
>
> ⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제.

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 javadoc/TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 과제 (구현 대상 — src TODO 1~9)

#### 1. `Kruskal.mst` / `Kruskal.weight` (TODO 1~4)

```java
/**
 * 크루스칼. 간선을 가벼운 것부터 보면서, 사이클을 만들지 않는 것만 고른다.
 * 왜 되는지 먼저 설명할 수 있어야 한다.
 * 가장 가벼운 간선이 반드시 어떤 최소 신장 트리에 들어간다는 것을 어떻게 보이나.
 * 안 들어간다고 가정하고, 그 간선을 넣으면 생기는 사이클을 생각해봐라.
 */
public static List<CountedGraph.Edge> mst(CountedGraph g, CountedUnionFind uf)
    // TODO 1: 간선을 가중치 오름차순으로 정렬한다.
    //   g.edgeList() 가 사본을 준다. 원본을 정렬하면 안 되는 이유가 있나.
    // TODO 2: 가벼운 것부터 보면서 사이클을 안 만드는 간선만 고른다.
    //   "사이클을 만드나" 를 어떻게 판정하나. 두 끝이 이미 같은 덩어리인지 보면 된다.
    //   uf.union 이 그 판정과 합치기를 한 번에 해준다. 돌려주는 값이 무엇을 뜻하나.
    // TODO 3: 언제 멈추나. 간선을 몇 개 고르면 신장 트리가 완성되나.
    //   안 멈추고 끝까지 봐도 답은 같다. 무엇이 달라지나.

/** 고른 간선들의 가중치 합. */
public static long weight(List<CountedGraph.Edge> edges)
    // TODO 4: 더하면 된다. 합이 int 를 넘길 수 있나.
```

- 가장 가벼운 간선이 반드시 어떤 MST에 들어간다는 것을 어떻게 증명하는가?
- 그 논증이 두 번째, 세 번째 간선에도 계속 적용되는 이유는 무엇인가?
- "사이클을 만드나"를 유니온 파인드는 어떻게 한 번의 호출로 판정하는가?
- `uf.union(a, b)`가 돌려주는 boolean은 각각 무엇을 뜻하는가?
- 간선을 몇 개 고르면 멈춰도 되고, 그 수는 어디서 나오는가?
- 안 멈추고 끝까지 봐도 답이 같은 이유는 무엇이고, 무엇이 달라지는가?
- 조기 종료의 실익이 작다는 것을 측정은 어떤 숫자로 보여줬는가?
- `weight`의 합을 int가 아니라 long으로 받아야 하는 이유는 무엇인가?
- 크루스칼의 비용은 어디가 지배하고, 유니온 파인드는 측정에서 얼마나 나왔는가?
- 정렬 비교 횟수가 이론값 E log E보다 적게 나온 이유는 무엇인가?
- 그래프가 안 이어져 있을 때 크루스칼의 결과는 무엇이 되는가?

#### 2. `Prim.mst` / `Prim.weight` (TODO 5~9)

```java
/**
 * 프림. 한 정점에서 시작해 트리를 키운다. 트리 밖으로 나가는 간선 중 가장 가벼운 것을 고른다.
 * 크루스칼이 전부 정렬해두고 고르는 것과 반대다. 여기서는 무엇만 후보로 두나.
 */
public static List<CountedGraph.Edge> mst(CountedGraph g)
    // TODO 5: 시작 정점을 트리에 넣고 그 정점의 간선을 전부 힙에 넣는다.
    // TODO 6: 힙에서 가장 가벼운 간선을 꺼낸다.
    //   반대쪽이 이미 트리 안이면 어떻게 하나. 넣으면 무엇이 되나.
    //   14 번 다익스트라의 "낡은 항목" 과 같은 모양이다.
    // TODO 7: 그래프가 안 이어져 있으면 어떻게 되나.
    //   한 정점에서 시작하면 그 덩어리만 훑고 끝난다. 나머지 덩어리는?
    //   크루스칼은 이 문제가 없다. 왜 없나.
    // TODO 8: 이미 트리 안인 정점으로 가는 간선을 힙에 넣어야 하나.
    //   넣어도 답은 같다. 안 넣으면 무엇이 달라지나.
```

- 크루스칼이 간선 전부를 정렬하는 것과 달리, 프림은 무엇만 후보(힙)에 두는가?
- 프림이 정답인 근거(절단 논증)는 무엇이고, 크루스칼의 논증과 어떤 관계인가?
- 힙에서 꺼낸 간선의 반대쪽이 이미 트리 안이면 어떻게 하고, 그냥 넣으면 무엇이 되는가?
- 그것이 14번 다익스트라의 "낡은 항목"과 같은 모양이라는 말은 무슨 뜻인가?
- 씨앗 하나에서만 시작한 프림은 안 이어진 그래프에서 무엇을 놓치는가?
- impl은 안 이어진 그래프를 어떻게 처리하는가?
- 크루스칼에는 왜 그 문제가 없는가?
- 이미 트리 안인 정점으로 가는 간선을 힙에 넣을 때 거르면 무엇이 달라지고, 답은 왜 그대로인가?

### B. 개념

#### 3. 원본 README "생각해볼 것" (이관)

- 배열로 짠 프림(O(V²))을 만들면 어느 밀도에서 크루스칼을 이기는가, 계산으로 먼저 예측하면?
- 간선을 전부 정렬하지 않고 힙에서 하나씩 꺼내면 크루스칼이 빨라지는가, 어떤 그래프에서?
- 최대 신장 트리는 어떻게 구하고, 코드를 얼마나 고쳐야 하는가?
- 간선 하나의 가중치가 바뀌면 MST를 처음부터 다시 만들어야 하는가?

#### 4. 파생 질문 (서머리 기반)

- 신장 트리의 간선이 정확히 V-1개인 이유는 무엇인가?
- 가중치가 같은 간선이 여럿이면 왜 두 알고리즘의 간선 집합을 비교하면 안 되고, 무엇을 비교해야 하는가?
- "성긴 그래프는 크루스칼, 촘촘한 그래프는 프림"이라는 통설은 측정에서 왜 안 맞았는가?
- "촘촘하면 프림"이라는 말이 원래 가리키던 프림은 어떤 구현인가?
- 무거운 간선도 MST에 반드시 들어가는 경우는 언제인가?
- 크루스칼과 프림이 같은 그래프를 "다른 모양으로 본다"는 말은 코드에서 어떻게 드러나는가?
- 경로 압축이 `find` 한 번의 평균 걸음을 어디까지 내렸는가(실측)?
- 이 챕터의 측정이 14번(다익스트라)에서 재확인해준 교훈은 무엇인가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
