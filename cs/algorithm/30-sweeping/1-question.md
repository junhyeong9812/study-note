# algorithm/30-sweeping — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 골격(javadoc + TODO)을 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 문제 (src/main 의 TODO 1~4)

#### 1. 좌표 압축 (`Compress` — TODO 1~2)

```java
/**
 * 좌표 압축.
 *
 * <p>값이 무엇인지는 중요하지 않고 순서만 중요한 문제가 있다.
 * 그럴 때 값을 등수로 바꾸면 배열 크기가 값의 범위가 아니라 값의 개수가 된다.
 *
 *   원래 값  10  1,000,000,000  10  50
 *   정렬 유일 10  50  1,000,000,000
 *   등수      0   1   2
 *
 *   배열 크기가 10 억에서 3 으로 준다.
 *
 * <p>02 번 이분 탐색이 여기 쓰인다. 등수를 찾는 것이 곧 하한 탐색이다.
 */

/** 정렬하고 중복을 없앤 값들. 이것이 등수 표다. */
public static int[] distinctSorted(int[] values, CountedSweep sweep) {
    throw new UnsupportedOperationException("TODO 1");
}

/**
 * 값의 등수. 표에 없으면 -1.
 *
 * <p>이분 탐색이다. 표가 정렬돼 있어야 한다.
 */
public static int rankOf(int[] table, int value, CountedSweep sweep) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 좌표 압축이 하는 일을 한 문장으로 말하면 무엇인가?
- 압축이 가능한 조건은 무엇인가 — 어떤 문제에서는 쓰면 안 되는가?
- 입력 배열을 `clone()` 해서 정렬하는 이유는?
- 중복 제거를 새 배열이 아니라 같은 배열의 앞쪽에 눌러 담는 방식은 무엇을 아끼는가?
- 등수를 이분 탐색으로 찾을 수 있는 전제는 무엇인가?
- 표에 없는 값에 대해 `rankOf` 가 -1 을 주는 것과, 들어갈 자리(하한)를 주는 것은 어떻게 다른 계약인가?
- 압축한 값으로 차이나 합을 계산하면 왜 안 되는가? (원본 함정)
- 10 과 50 의 "거리 40"은 압축 후 어디로 갔는가?

#### 2. 최대 겹침 (`Sweep.maxOverlap` — TODO 3)

```java
/**
 * 동시에 겹치는 최대 구간 수.
 *
 * <p>closedEnd 가 true 면 [start, end] 로 본다. 끝점에서도 겹친 것으로 센다.
 * false 면 [start, end) 다. 끝나는 자리에서 다음 것이 시작하면 안 겹친 것이다.
 *
 *   [1,3] 과 [3,5]
 *
 *     닫힌 구간 : 3 에서 둘 다 있다  -> 2
 *     반열린    : 3 에서 앞의 것은 끝났다 -> 1
 *
 * <p>정렬 순서 하나로 이 둘이 갈린다. 같은 좌표에서 시작을 먼저 처리하면 닫힌 구간,
 * 끝을 먼저 처리하면 반열린 구간이다. 그래서 이 정렬 비교자가 계약이다.
 */
public static int maxOverlap(List<Interval> intervals, boolean closedEnd, CountedSweep sweep) {
    throw new UnsupportedOperationException("TODO 3");
}

// 대조군은 완성 제공 — 좌표를 한 칸씩 훑는다. 값의 범위에 비례한다.
public static int maxOverlapByScan(List<Interval> intervals, boolean closedEnd, CountedSweep sweep)
```

- 구간 하나가 이벤트 몇 개가 되고, 각 이벤트의 변화량은 얼마인가?
- "구간 안쪽에서는 아무 일도 안 일어난다"가 왜 볼 곳을 `2n` 개로 줄이는가?
- 같은 좌표에서 시작과 끝 중 무엇을 먼저 처리하느냐가 무엇을 가르는가? (원본 함정)
- 그 규칙이 코드의 어디 한 줄에 들어 있는가 — 닫힌 구간이면 비교자가 어떻게 되는가?
- `[1,3]` 과 `[3,5]` 의 답이 닫힌/반열린에서 각각 몇인가?
- 끝점 규칙이 "구석 사례가 아니다"라는 실측은 어떤 수치였는가? (원본 측정 셋)
- 정하지 않고 짜면 "3분의 2 확률로 틀린다"는 말은 무슨 뜻인가?
- 대조군(`maxOverlapByScan`)의 진짜 비용은 칸 수인가 무엇인가? (원본 측정 둘)
- 칸만 세면 30배, 안쪽 루프까지 세면 6,000배 — 이 차이가 주는 교훈은?
- 구간 1,000개·범위 10억에서 50만 배 차이가 난다는 것은 무엇과 무엇의 비교인가? (원본 측정 하나)
- 계측기가 기록한 최대치를 답의 교차 확인에 쓸 수 있는 이유와, 그것이 완전한 독립 검증이 아닌 이유는? (원본 측정 다섯)
- 빈 입력에서 답이 0 이어야 하는 것은 알고리즘이 정하는가 계약이 정하는가?

#### 3. 구간 병합 (`Sweep.merge` — TODO 4)

```java
/**
 * 겹치거나 닿는 구간을 합친다.
 *
 * <p>시작 순으로 정렬하고 한 번 훑는다.
 * 다음 구간의 시작이 지금 끝보다 크면 새 구간이고, 아니면 끝을 늘린다.
 *
 *   [1,3] [2,6] [8,10]
 *
 *     [1,3] 에 [2,6] 을 붙인다 -> [1,6]     2 <= 3 이므로
 *     [8,10] 은 따로              8 > 6 이므로
 *
 * <p>touching 이 true 면 [1,3] 과 [4,6] 도 붙인다. 정수 구간에서 3 과 4 는 붙어 있다.
 */
public static List<Interval> merge(List<Interval> intervals, boolean touching, CountedSweep sweep) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 병합에서 정렬을 먼저 하지 않으면 무엇을 놓치는가? (원본 함정)
- 끝을 `max` 로 늘리지 않고 덮어쓰면 어떤 입력에서 무엇이 틀리는가? (원본 함정)
- `touching` 이 true 와 false 일 때 이어붙임 조건이 각각 어떻게 달라지는가?
- 틈을 계산할 때 `long` 으로 올리는 이유는?
- 마지막 구간을 루프 밖에서 한 번 더 `add` 해야 하는 이유는?
- 시작이 같을 때 끝으로 한 번 더 정렬하는 것은 필요한가, 없으면 무엇이 달라지는가?
- 병합 비용의 89퍼센트가 정렬이라는 실측은 실무에서 어떤 질문으로 바뀌는가? (원본 측정 넷)
- 병합과 최대 겹침은 같은 "정렬 후 한 번 훑기" 틀인데 무엇이 다른가?

### B. 개념

<!-- 앞 4개: 원본 README 「생각해볼 것」 이관 -->

- (원본 생각해볼 것) 겹침이 가장 큰 **구간**(자리 하나가 아니라)을 알려면 무엇을 더 들고 있어야 하는가?
- (원본 생각해볼 것) 구간이 계속 추가되고 질의도 계속 들어와 정렬 한 번으로 안 되면 무엇을 쓰는가?
- (원본 생각해볼 것) 2차원 직사각형의 합집합 넓이는 어떻게 구하는가 — 스위핑을 한 축에만 쓰면 다른 축은 무엇으로 다루는가?
- (원본 생각해볼 것) 좌표 압축을 하면 "빈 칸" 정보가 사라지는데, 그게 문제가 되는 질문은 어떤 것인가?

<!-- 아래: 서머리 기반 파생 질문 -->

- 등수 찾기가 06번(원본 표기 02번) 이분 탐색 그대로인 이유는?
- 압축 후 배열에 +1/−1 을 누적하면 10번 누적 합이 되는데, 그때 얻는 것은 무엇인가?
- 구간을 시작 순으로 정렬해 하나씩 처리하는 모양이 17번 그리디와 같다는 말은 무슨 뜻인가?
- "무엇을 세는지가 결론을 정한다"는 교훈이 28·29번에서도 나왔는데, 공통된 실수의 모양은 무엇인가?
- 스위핑을 실무에 적용할 때(동시 접속 피크, 일정 합치기, IP 대역 정리) 가장 먼저 확정해야 할 계약은 무엇인가?
- 이 챕터의 비용이 사실상 정렬 비용이라면, 성능을 더 끌어올릴 여지는 어디에 있는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
