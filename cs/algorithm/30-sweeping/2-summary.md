# algorithm/30-sweeping — 정리 (힌트)

> 복습 시 이 파일은 **질문에 막혔을 때만** 연다. 먼저 읽고 답하면 인출이 아니라 받아쓰기다.
> 작성 방식: 내가 먼저 기억으로 흐름을 서술하고, Claude는 빠지거나 틀린 곳을 짚는다. 대신 써주지 않는다.
> 이미 따라 치며 만든 정리본이 따로 있으면(organize류) 이 파일은 핵심 문장 압축 + 링크만 담는다.
> 2026-09-14: 쉽게 풀어쓴 서머리(Claude 작성) — 원본 myway/algorithm 코드 기준.

## 한눈에 — 쉽게 말하면

**스위핑 = 회의실 예약표를 1분 단위로 검사하지 않고, "들어온 시각·나간 시각"만 보는 것.**

- "동시에 최대 몇 팀이 회의실에 있었나?"를 알고 싶다. 하루를 1분 단위로 다 검사할 수도 있지만, 사람 수는 **누가 들어오거나 나갈 때만** 바뀐다.
- 그러니 볼 곳은 시간 전체가 아니라 **경계(이벤트) 2n개**뿐이다. 구간 안쪽에서는 아무 일도 안 일어난다 — 그게 스위핑의 전부다.
- **좌표 압축**은 "값을 훑기"를 "이벤트를 훑기"로 바꾸는 준비 작업이다. 값이 10억까지 퍼져 있어도, 실제 등장하는 값에 등수만 매기면 배열 크기가 값의 **개수**가 된다. 값이 무엇인지는 중요하지 않고 순서만 중요할 때 쓸 수 있다.

```text
  좌표를 훑는다   : 값의 범위에 비례. 10 억이면 10 억 번
  이벤트를 훑는다 : 구간 개수에 비례. 1,000 개면 2,000 번
```

이 방식이 **똑같은 구조로** 회의실/서버 동시 접속 피크 계산, 달력의 겹치는 일정 합치기, IP 대역 병합에 쓰인다.

## 전체 흐름

```text
구간 n 개
   |
   v
이벤트로 쪼개기        구간 [s, e]  ->  (s, +1 열림) 과 (e, -1 닫힘) 두 이벤트
   |
   v
정렬                  좌표 순. 같은 좌표에서 시작/끝 중 무엇을 먼저 볼지가 "구간의 정의"
   |
   v
한 번 훑기            active += 변화량 을 누적하며 최대치 기록  (또는 병합·넓이 등)
                      비용 O(n log n) — 거의 전부가 정렬

(값이 클 때) 좌표 압축: 정렬 -> 중복 제거 -> 값의 등수(이분 탐색)로 바꿔치기
```

## 동작 — 좌표 압축 (`Compress.distinctSorted`, `rankOf`)

**언제 쓰나**: 값의 범위는 거대한데(10억) 실제 등장하는 값은 몇 개 안 될 때, 값을 배열 인덱스로 쓰고 싶을 때.

  - *등수(rank)*: 정렬한 유일값 표에서 그 값이 몇 번째인지(0부터). 값 자체 대신 이 번호를 쓰는 것이 압축이다.

```text
  전:  원래 값   [10,  1000000000,  10,  50]     범위 10 억 -> 이대로는 배열을 못 만든다

  [1] 정렬 + 중복 제거    [10, 50, 1000000000]    <- 등수 표
  [2] 각 값을 등수로       10 -> 0,  50 -> 1,  1000000000 -> 2

  후:  압축된 값  [0, 2, 0, 1]     배열 크기가 10 억에서 3 으로
```

그림 해설 (한 단계씩):

- 값들을 복사해 정렬하고, 앞 원소와 같으면 건너뛰며 앞쪽에 눌러 담는다 — 남은 것이 "등수 표"다.
- 어떤 값의 등수는 표에서 이분 탐색으로 찾는다(`rankOf`, 02번 이분 탐색 그대로). 표에 없으면 -1.
- 함정: **압축한 값으로 차이나 합을 계산하면 안 된다.** 등수는 순서만 보존한다 — 10과 50의 "거리 40"은 등수 0과 1 사이에서 사라졌다.
- 비용: 정렬 O(n log n) + 값 하나당 등수 찾기 O(log n).

## 동작 — 최대 겹침 (`Sweep.maxOverlap`)

**언제 쓰나**: "동시에 최대 몇 개가 겹치나" — 동시 접속 피크, 필요한 회의실 개수.

  - *이벤트(event)*: 상태가 바뀌는 지점. 구간 [s, e] 하나가 (s, +1 열림)과 (e, -1 닫힘) 두 이벤트가 된다.

구간 [1,3], [2,6], [5,8], [7,9] (닫힌 구간):

```text
  수직선     1  2  3  4  5  6  7  8  9
  [1,3]     +--+--+
  [2,6]        +--+--+--+--+
  [5,8]                 +--+--+--+
  [7,9]                       +--+--+

  이벤트(정렬 후):  (1,+1) (2,+1) (3,-1) (5,+1) (6,-1) (7,+1) (8,-1) (9,-1)
  active:            1      2      1      2      1      2      1      0
                            ^             ^             ^
  답: 최대 2                 어느 순간에도 3 개가 같이 있지는 않았다
```

그림 해설 (한 단계씩):

- 구간마다 이벤트 두 개를 만든다: 시작에 +1, 끝에 -1.
- 좌표 순으로 정렬한 뒤 왼쪽부터 훑으며 `active`(지금 열린 구간 수)를 누적하고, 최대치를 기억한다.
- **같은 좌표에서 시작과 끝 중 무엇을 먼저 처리하나 — 이 한 줄이 구간의 정의다.** [1,3]과 [3,5]에서: 시작을 먼저 보면 3에서 둘 다 열려 있어 겹침 2(닫힌 구간 [a,b]), 끝을 먼저 보면 앞 것이 이미 닫혀 겹침 1(반열린 [a,b)). 원본 구현에서 이 규칙은 **정렬 비교자 한 줄**에 들어 있다.
- 대조군 `maxOverlapByScan`은 좌표를 한 칸씩 훑는다 — 값의 범위에 비례하고, 칸마다 모든 구간을 다시 보므로 진짜 비용은 칸×구간이다.
- 비용: O(n log n) — 이벤트 2n개 정렬이 지배하고, 훑기는 O(n).

## 동작 — 구간 병합 (`Sweep.merge`)

**언제 쓰나**: 겹치거나 닿는 구간들을 하나로 합칠 때 — 일정 합치기, IP 대역 정리.

구간 [8,10], [1,3], [2,6]:

```text
  전:  [1,3] [2,6] [8,10]        (정렬 먼저! 시작 순으로)

  [1]  [1,3] 을 잡는다                        start=1, end=3
  [2]  [2,6]: 2 <= 3 (겹침)  -> 끝을 늘린다    end = max(3, 6) = 6
  [3]  [8,10]: 8 > 6 (안 겹침) -> [1,6] 확정, 새로 시작

  후:  [1,6] [8,10]
```

그림 해설 (한 단계씩):

- **정렬 먼저다.** 시작 순으로 안 세워두면 뒤에 오는 겹침을 놓친다.
- 다음 구간의 시작이 지금 끝 이하이면 이어진 것 — 끝을 `max`로 늘린다. **덮어쓰면 안 된다**: [1,10] 안에 [2,3]이 통째로 들어온 경우, 덮어쓰면 끝이 10에서 3으로 **줄어든다**(함정).
- `touching` 옵션이 true면 틈이 1인 것([1,3]과 [4,6])도 붙인다 — 정수 구간에서 3과 4는 붙어 있다고 보는 계약.
- 비용: O(n log n) — 실측에서 병합 비용의 89%가 정렬이다(비교 8,693 vs 훑기 999). 그래서 "이미 정렬돼 들어오나"(DB의 ORDER BY)가 실제로는 가장 큰 질문이다.

## 측정이 알려준 것 (원본 README 요약)

- **값의 범위가 아니라 값의 개수가 비용이 된다.** 구간 1,000개·범위 10억: 좌표 훑기 10억 칸 vs 이벤트 2,000개 — 50만 배.
- **칸만 세면 30배, 안쪽 루프까지 세면 6,000배.** 훑기의 진짜 비용은 칸×구간(칸마다 전 구간 검사)이다. 무엇을 세는지가 결론을 정한다 — 28·29번과 같은 교훈.
- **끝점 규칙은 구석 사례가 아니다.** 무작위 구간 20개짜리 시험 1,000번 중 687번에서 닫힌/반열린의 답이 달랐다. "끝점을 포함하나"는 계약의 본체이고, 정하지 않고 짜면 3분의 2 확률로 틀린다.
- **계측기를 답의 교차 확인에 쓸 수 있다.** 계측기가 이벤트마다 열린 개수의 최대치를 따로 기록하므로, 함수의 답과 어긋나면 둘 중 하나가 틀린 것이다.

## 이 챕터가 앞의 것들과 만나는 곳 (원본 README)

- 등수 찾기 = 이분 탐색 그대로 (원본 README 표기는 "02번" — 이 시리즈에서는 `06-binary-search`).
- 압축 후 배열에 +1/-1을 누적하면 **10번** 누적 합이 된다.
- 구간을 시작 순으로 정렬해 하나씩 처리하는 모양은 **17번** 그리디와 같다.

## 핵심 문장

- 구간 안쪽에서는 아무 일도 안 일어난다 — 상태는 경계에서만 바뀌니, 볼 곳은 값의 범위가 아니라 이벤트 2n개다. 그게 스위핑의 전부다.
- 좌표 압축은 "좌표 훑기"를 "이벤트 훑기"로 바꾸는 일이다. 등수는 순서만 보존하므로, 압축한 값으로 차이나 합을 계산하면 안 된다.
- 같은 좌표에서 시작과 끝 중 무엇을 먼저 처리하느냐(정렬 비교자 한 줄)가 닫힌 구간과 반열린 구간을 가른다 — 끝점 규칙은 구석 사례가 아니라 계약의 본체다.
- 병합은 정렬 먼저, 끝은 max로 늘리기다 — 안에 포함된 구간이 끝을 줄이는 함정과, 정렬 없이 뒤늦은 겹침을 놓치는 함정이 각각 걸려 있다.
- 스위핑 비용의 대부분은 정렬이다(실측 89%). "이미 정렬돼 들어오나"가 실전에서 가장 큰 질문이다.

## 관련 자료

- 챕터 안내: `/home/jun/project/myway/algorithm/30-sweeping/README.md`
- 정답 기준 소스: `/home/jun/project/myway/algorithm/30-sweeping/impl/com/algo/sweep/Sweep.java`, `impl/com/algo/sweep/Compress.java`
- 채울 골격(TODO 1~4): `/home/jun/project/myway/algorithm/30-sweeping/src/main/java/com/algo/sweep/` (Sweep·Compress)
- 계측기(전부 제공): `/home/jun/project/myway/algorithm/30-sweeping/src/main/java/com/algo/sweep/CountedSweep.java`
- 계약(테스트): `.../src/test/java/com/algo/sweep/SweepTest.java`, `MeasurementTest.java`
- 앞 챕터와의 다리: `06-binary-search`(등수 찾기 — 원본 README는 "02번"으로 표기하는데 이 시리즈에서 이분 탐색은 06번이다), `10-prefix-sum`(+1/-1 누적), `17-mst`(정렬 후 순서대로 처리하는 그리디 모양)

## 용어 풀이

- **스위핑(sweeping, 훑기)**: 수직선(또는 평면)을 한 방향으로 쓸고 지나가며, 이벤트가 있는 곳에서만 상태를 갱신하는 기법.
- **구간(interval)**: 시작과 끝으로 표현되는 범위 [s, e]. 원본의 `Interval` record는 끝점 포함 여부를 정하지 않는다 — 부르는 쪽이 정한다.
- **이벤트(event)**: 상태가 바뀌는 지점. 구간 하나가 (시작, +1)과 (끝, -1) 두 이벤트가 된다.
- **active(열린 구간 수)**: 지금 훑는 위치에서 열려 있는 구간의 개수. +1/-1을 누적한 값.
- **닫힌 구간 [a,b] / 반열린 구간 [a,b)**: 끝점 b를 포함하는/포함하지 않는 구간. [1,3]과 [3,5]가 겹치는지를 가르는 정의.
- **비교자(comparator)**: 정렬 순서를 정하는 함수. 같은 좌표에서 +1과 -1 중 무엇을 앞세울지 — 여기 계약이 들어 있다.
- **좌표 압축(coordinate compression)**: 실제 등장하는 값만 모아 정렬·중복 제거한 뒤, 각 값을 그 등수로 바꾸는 것. 배열 크기가 값의 범위에서 값의 개수로 준다.
- **등수(rank)**: 정렬된 유일값 표에서의 위치(0부터). 순서만 보존하고 값 사이의 거리는 버린다.
- **이분 탐색(binary search)**: 정렬된 표에서 가운데를 보고 절반을 버리기를 반복해 위치를 찾는 방법. O(log n). 등수 찾기가 이것이다.
- **병합(merge)**: 겹치거나 닿는 구간들을 하나로 합치는 것. 시작 순 정렬 + 한 번 훑기.
- **touching(닿음)**: [1,3]과 [4,6]처럼 틈 없이 이어진 정수 구간을 붙일지 정하는 옵션.
- **O(n log n)**: n개를 정렬하는 비용 급. 스위핑의 전체 비용은 사실상 정렬 비용이다.
