# algorithm/13-backtracking — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.
>
> ⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제.

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 javadoc/TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 과제 (구현 대상 — src TODO 1~6)

#### 1. `NQueens.countWithPruning` / `countByFiltering` (TODO 1~2)

```java
/** 가지치기를 하는 판. 놓을 수 있는 자리만 내려간다. */
public static long countWithPruning(int n, CountedBoard board)
    // TODO 1: 행을 하나씩 내려가며 열을 고른다.
    //   끝까지 내려갔다는 것을 어떻게 아나. 그때 무엇을 돌려주나.
    //   놓기 전에 검사하나 놓고 나서 검사하나. 그 차이가 이 박스의 전부다.
    //   되돌리기(remove)를 빠뜨리면 어떤 증상이 나오나. 답이 커지나 작아지나.

/** 걸러내는 판. 열 순열을 전부 만들고 다 놓은 뒤에 확인한다. */
public static long countByFiltering(int n, CountedBoard board)
    // TODO 2: 열을 중복 없이 고르되, 대각선은 마지막에 board.isFullySafe 로 확인한다.
    //   가지치기 판과 답이 같아야 한다. 계약 테스트가 둘을 대조한다.
    //   그런데 비용은 몇 배나 차이 날까. 재보기 전에 짐작해봐라.
    //   n=8 에서 순열은 40,320 개다. 그 중 답은 92 개다.
```

- 끝까지 내려갔다는 것을 어떻게 알고, 그때 무엇을 돌려주는가?
- 가지치기 판과 걸러내기 판은 각각 언제 검사하는가?
- 걸러내기 판도 열이 겹치는 배치는 애초에 안 만드는데(n^n이 아니라 n!), 왜 그런데도 훨씬 비싼가?
- n=8에서 두 판의 놓기 횟수는 각각 얼마였는가(실측)?
- 그 격차는 n이 커지면 어떻게 변하는가(n=6/8/9 실측)?
- 검사 횟수 기준으로는 작은 n에서 어느 판이 이기고, n이 커지면 왜 뒤집히는가?
- 걸러내기 판의 검사 횟수가 정확히 n!인 이유는 무엇인가?
- 되돌리기(remove)를 빠뜨리면 어떤 증상이 나오는가 — 답이 커지는가 작아지는가?
- 계약 테스트는 되돌리기 누락을 어떤 두 조건으로 잡는가?
- n=0의 답이 1인 이유는 무엇이고, 0이라고 답하면 무엇을 잘못 잡은 것인가?

#### 2. `NQueens.solutions` (TODO 3)

```java
/** 가지치기 판으로 실제 배치를 전부 모은다. 각 배치는 행마다의 열 번호다. */
public static List<int[]> solutions(int n, CountedBoard board)
    // TODO 3: countWithPruning 과 같은 모양인데 세는 대신 모은다.
    //   board.snapshot() 을 그대로 넣으면 되나. 사본을 안 뜨면 무슨 일이 생기나.
```

- `countWithPruning`과 `solutions`의 차이는 어디 한 곳뿐인가?
- 결과에 사본을 뜨지 않고 작업용 배열을 그대로 넣으면 무슨 일이 생기는가?
- `snapshot()`이 계수기(placements 등)를 올리지 않는 이유는 무엇인가?

#### 3. `Generate.permutations` (TODO 4)

```java
/** [0, n) 의 모든 순열. 사전순으로 나온다. */
public static List<int[]> permutations(int n)
    // TODO 4: 자리마다 아직 안 쓴 값을 고른다.
    //   "안 썼다" 를 어떻게 기억하나.
    //   되돌리기를 어디에 넣어야 하나. 재귀 앞인가 뒤인가.
    //   결과에 current 를 그대로 넣으면 무슨 일이 생기나.
    //   n 이 0 이면 순열이 몇 개인가. 0 개인가 1 개인가.
```

- "안 썼다"를 무엇으로 기억하는가?
- 되돌리기(`used[v] = false`)는 재귀 호출 앞인가 뒤인가, 왜 그 자리여야 하는가?
- 결과에 `current`를 그대로 넣으면 무슨 일이 생기는가?
- n=0이면 순열이 0개인가 1개인가, 왜 그런가?
- 순열이 사전순으로 나오는 것은 코드의 무엇이 보장하는가?

#### 4. `Generate.combinations` (TODO 5~6)

```java
/** [0, n) 에서 k 개를 고르는 모든 조합. 오름차순으로, 사전순으로 나온다. */
public static List<int[]> combinations(int n, int k)
    // TODO 5: 순열과 뼈대가 같다. 고를 수 있는 후보만 다르다.
    //   조합은 순서를 안 따진다. [0,1] 과 [1,0] 을 하나로 보려면 무엇을 제한해야 하나.
    //   "안 쓴 것 전부" 대신 무엇으로 바꾸면 되나. boolean 배열이 아직 필요한가.
    // TODO 6: 남은 자리보다 후보가 적으면 어차피 못 채운다. 그 가지를 미리 자를 수 있나.
    //   자르지 않아도 답은 같다. 무엇이 달라지나.
    //   k 가 n 보다 크면 조합이 몇 개인가.
    //   그 경우를 위한 검사를 따로 두어야 하나. 위 상한이 그때 어떤 값이 되나.
```

- [0,1]과 [1,0]을 하나로 보려면 무엇을 제한해야 하는가?
- 그 한 줄이 n!을 C(n,k)로 만든다는 말은 무슨 뜻인가?
- 순열의 `used` boolean 배열이 조합에서는 왜 필요 없는가?
- 가지치기 상한 `v <= n - (k - depth)`는 어떤 가지를 자르고, 안 잘라도 답이 같은 이유는 무엇인가?
- k > n이면 조합이 몇 개이고, `if (k > n)` 가드가 왜 죽은 코드였는가?
- 20개의 순열과 20개에서 3개 조합의 규모는 각각 얼마나 다른가?

### B. 개념

#### 5. 원본 README "생각해볼 것" (이관)

- 열과 대각선을 `boolean[]` 세 개로 기억하면 충돌 검사가 O(1)이 되는데(지금은 O(n)), 왜 안 했을까?
- N퀸의 해는 좌우 대칭이 짝을 이루는데, 절반만 찾고 두 배 하면 되는가, 언제 안 되는가?
- 순열을 재귀 없이 만들 수 있는가(다음 순열 알고리즘), 그러면 무엇이 좋아지는가?
- 가지치기를 더 세게 하면(예: 남은 행에 놓을 자리가 없으면 즉시 중단) 얼마나 더 줄까?

#### 6. 파생 질문 (서머리 기반)

- 백트래킹의 비용은 왜 자료구조에 안 남고, 그래서 계측기는 무엇을 세는가?
- 백트래킹과 12번 DFS는 무슨 관계인가?
- "가지치기가 항상 낫다"가 왜 틀린 말인가?
- 가지치기의 본질이 "검사를 늘려 놓기를 줄이는 맞바꿈"이라면, 그게 남는 장사인지는 무엇으로 판단하는가?
- 순열과 조합이 "같은 뼈대"라는 말에서, 정확히 무엇 하나가 다른가?
- 테스트가 안 깨지는 방어 코드가 07, 12번에 이어 여기서도 나왔다 — 공통 교훈은 무엇인가?
- N퀸 두 판 모두 열이 겹치는 배치는 애초에 안 만드는데, 각각 무엇이 그것을 보장하는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
