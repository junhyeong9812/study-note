# algorithm/24-divide-conquer — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 TODO 주석을 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 문제 (src/main 의 TODO 1~10)

#### 1. 빠른 거듭제곱 (`FastPower` — TODO 1~3)

```java
/** a^n mod m. */
public static long modPow(long base, long exponent, long mod, CountedOps ops) {
    // TODO 1: 잘못된 입력을 먼저 막는다. 음수 지수는. 법이 0 이하면.
    //   법이 1 이면 답이 무엇인가. 1 % 1 이 얼마인가.

    // TODO 2: 지수를 반으로 접는다.
    //   a^n 을 a^(n/2) 로 어떻게 표현하나. 홀수면 무엇이 하나 더 필요한가.
    //
    //   재귀를 두 번 부르면 안 된다. pow(a, n/2) * pow(a, n/2) 라고 쓰면
    //   같은 값을 두 번 계산해서 다시 O(n) 이 된다.
    //   한 번 부르고 그 결과를 변수에 담아 제곱해라.
    //   MeasurementTest 가 그 차이를 곱셈 횟수로 박아 두었다.
    //
    //   ops.enter() 와 ops.leave() 를 짝 맞춰 불러라. 깊이를 잰다.
    //   ops.multiply() 도 곱할 때마다 불러라.
}

/** 한 번씩 곱하는 판. 비교 대상이다. */
public static long naivePow(long base, long exponent, long mod, CountedOps ops) {
    // TODO 3: 지수만큼 반복한다. 곱할 때마다 ops.multiply() 를 불러라.
}
```

- `a^n` 을 `a^(n/2)` 로 표현하는 식은 무엇이고, 지수가 홀수면 무엇이 하나 더 필요한가?
- `pow(a, n/2) * pow(a, n/2)` 라고 쓰면 왜 O(log n) 이 사라지고 다시 O(n) 이 되는가?
- 그 실수를 막는 장치가 코드에서 무엇 하나인가 — 21번은 같은 실수를 무엇으로 막았는가?
- 법이 1 이면 답이 항상 무엇이고, 0 제곱에서 `1` 을 그대로 돌려주면 왜 틀리는가? (원본 함정)
- 지수가 음수이거나 법이 0 이하면 impl 은 무엇을 하는가 — 값으로 알리는가 예외인가?
- `ops.enter()`/`ops.leave()` 를 `try/finally` 로 감싸는 이유는?
- 지수 10억에서 곱셈이 43번이라는 수는 어떻게 나오는 수인가? (원본 측정 하나)
- 호출 수와 깊이가 둘 다 31로 같다는 것이 무엇을 증명하는가?

#### 2. 행렬 거듭제곱 (`MatrixPower` — TODO 4~7)

```java
/** 2x2 행렬 곱. mod 로 나눈 나머지. */
static long[][] multiply(long[][] a, long[][] b, long mod, CountedOps ops) {
    // TODO 4: 정의대로 곱한다. 2x2 면 곱셈이 몇 번인가.
    //   곱한 뒤 바로 mod 를 취하지 않으면 무엇이 넘치나.
    //   mod 가 10^9 근처면 두 수의 곱이 10^18 이다. long 에 들어가나.
}

/** 행렬의 n 제곱. */
public static long[][] power(long[][] base, long exponent, long mod, CountedOps ops) {
    // TODO 5: 0 제곱은 무엇인가. 행렬에서 1 에 해당하는 것이 무엇인가.

    // TODO 6: FastPower 와 같은 모양이다. 다른 것은 곱셈이 행렬 곱이라는 것뿐이다.
    //   여기서도 재귀를 한 번만 불러야 한다.
}

/** n 번째 피보나치 수. F(0)=0, F(1)=1. */
public static long fibonacci(long n, long mod, CountedOps ops) {
    // TODO 7: 어떤 행렬을 몇 제곱해야 하나.
    //   [F(n+1), F(n)] 을 [F(n), F(n-1)] 로 만드는 행렬을 써봐라.
    //   그 행렬의 k 제곱에서 F(n) 이 어느 칸에 있나.
    //   n 이 0 이면 그 식이 성립하나. 따로 다뤄야 하나.
}
```

- 2x2 행렬 곱 한 번에 곱셈은 몇 번 일어나는가?
- 곱한 뒤 바로 `% mod` 를 취하지 않으면 무엇이 넘치는가?
- 그 `% mod` 를 빼도 지금 테스트가 전부 통과하는 이유는 무엇인가? (원본 변종)
- 그런데도 `% mod` 를 남겨야 하는 이유는 — 어떤 조건이 바뀌면 터지는가?
- 행렬에서 "숫자 1"에 해당하는 것은 무엇이고, 그것도 왜 `1 % mod` 여야 하는가?
- 피보나치 점화식을 어떤 2x2 행렬로 쓰는가, `F(n)` 은 그 행렬의 몇 제곱 어느 칸에 있는가?
- `n = 0` 을 따로 다뤄야 하는 이유는?
- `F(10^18)` 을 곱셈 808번으로 구한다는 수는 어떻게 쪼개지는가? (원본 측정 둘)
- 21번의 표 방식으로는 왜 `n = 10^18` 에 갈 수 없는가?
- "상태 공간을 만들지 않고 푸는 방법이 있다"는 말은 피보나치 말고 무엇까지 적용되는가?

#### 3. 카라추바 곱셈 (`Karatsuba` — TODO 8~10)

```java
/** 자릿수 배열로 표현한 큰 수의 곱. digits[0] 이 1 의 자리다. */
public static int[] multiply(int[] x, int[] y, CountedOps ops) {
    // TODO 8: 두 수를 반으로 자르면
    //     x = a B + b,  y = c B + d
    //     x y = ac B^2 + (ad + bc) B + bd
    //   곱셈이 네 번이다. 그런데 ad + bc 를 어떻게 하면 곱셈 한 번으로 얻나.
    //   ac 와 bd 는 어차피 계산했다는 것을 이용해라.
    //
    //   자릿수가 홀수면 반으로 어떻게 자르나. 양쪽 길이가 다르면 어떻게 맞추나.

    // TODO 9: 자리올림.
    //   계산 중에는 자릿값이 10 을 넘어도 된다. 마지막에 한 번만 정리하면 된다.
    //   그런데 중간에 뺄셈이 있어서 음수 자릿값이 나올 수 있다.
    //   그때 그냥 / 10 과 % 10 을 쓰면 무엇이 잘못되나.
    //   자바에서 -3 / 10 은 얼마이고 -3 % 10 은 얼마인가.
}

/** 곱셈을 네 번 하는 판. 비교 대상이다. */
public static int[] multiplyNaive(int[] x, int[] y, CountedOps ops) {
    // TODO 10: 네 조각을 그대로 곱한다.
    //   마스터 정리로 두 판의 차수를 각각 구해봐라.
    //     T(n) = 4 T(n/2) + O(n)
    //     T(n) = 3 T(n/2) + O(n)
    //   답이 각각 무엇인가. 무엇이 지수 자리에 들어가나.
}
```

- `ad + bc` 를 곱셈 한 번으로 얻는 항등식은 무엇인가?
- 그 항등식이 "공짜"인 이유는 — 이미 계산해 둔 것이 무엇이기 때문인가?
- 12 × 34 를 카라추바로 손으로 풀면 세 곱과 가운데 항이 각각 얼마인가?
- 자릿수가 홀수이거나 양쪽 길이가 다르면 impl 은 어떻게 맞추는가?
- 계산 중에 자릿값이 10 을 넘어도 되는 이유는, 정리는 언제 한 번 하는가?
- 자바에서 `-3 / 10` 과 `-3 % 10` 은 각각 얼마이고, 그것이 자리올림에서 왜 문제인가?
- `/`·`%` 로 바꿔도 지금 테스트가 통과하는 대수적 이유는 무엇인가? (원본 변종)
- 그래도 `floorDiv`·`floorMod` 를 쓰는 이유는?
- `T(n) = 4T(n/2) + O(n)` 과 `T(n) = 3T(n/2) + O(n)` 의 답은 각각 무엇인가?
- 자릿수 64에서 729 대 4,096 이라는 수가 `3^6`·`4^6` 인 이유는? (원본 측정 셋)
- 호출 수 1,093 과 5,461 은 각각 어떤 트리의 무엇인가?

### B. 개념

<!-- 앞 3개: 원본 README 「생각해볼 것」 이관 -->

- (원본 생각해볼 것) 슈트라센 행렬 곱은 8번을 7번으로 줄인다 — 차수가 얼마가 되는가?
- (원본 생각해볼 것) 카라추바는 자릿수가 작을 때 손해인데, 실제 라이브러리는 어디서 갈아타는가?
- (원본 생각해볼 것) 마스터 정리가 안 통하는 점화식은 어떤 모양이고, 행렬로 풀 다른 점화식을 하나 만들면?

<!-- 아래: 서머리 기반 파생 질문 -->

- 마스터 정리 `T(n) = a·T(n/b) + f(n)` 에서 a·b·f 는 각각 무엇이고, 왜 a 가 지수 자리에 들어가는가?
- "상수 배가 아니라 차수 차이"라는 말은 자릿수가 늘 때 배수 표에서 어떻게 보이는가?
- 이 챕터의 세 가지(지수·점화식의 지수·자릿수)는 공통으로 무엇을 반으로 접는가?
- 24번의 빠른 거듭제곱과 28번 정수론의 이진 거듭제곱은 무엇이 같고 무엇이 다른가?
- "지금 안 터진다"와 "안전하다"가 다르다는 것을 이 챕터는 어떤 두 사례로 보여주는가?
- 변종 검증(mutation testing)이 "테스트의 구멍"과 "불필요한 코드" 중 어느 쪽을 가리키는지 어떻게 판단하는가?
- 카라추바가 `10 × 10 = 10` 을 냈던 사건의 진짜 원인은 무엇이었고, 거기서 얻는 교훈은?
- 02번 병합 정렬과 이 챕터는 분할 정복의 어떤 다른 면을 보여주는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
