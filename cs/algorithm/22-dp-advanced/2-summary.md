# algorithm/22-dp-advanced — 정리 (힌트)

> 복습 시 이 파일은 **질문에 막혔을 때만** 연다. 먼저 읽고 답하면 인출이 아니라 받아쓰기다.
> 작성 방식: 내가 먼저 기억으로 흐름을 서술하고, Claude는 빠지거나 틀린 곳을 짚는다. 대신 써주지 않는다.
> 이미 따라 치며 만든 정리본이 따로 있으면(organize류) 이 파일은 핵심 문장 압축 + 링크만 담는다.
> 2026-09-14: 쉽게 풀어쓴 서머리(Claude 작성) — 원본 myway/algorithm 코드 기준.

## 한눈에 — 쉽게 말하면

**DP 심화 = "표를 쓰면 빨라진다"(21번) 다음 단계: 표의 칸, 즉 상태를 어떻게 잡느냐가 승부를 가른다.**

- 지도를 그릴 때 무엇을 좌표로 삼느냐에 따라 지도가 쓸모 있기도, 쓸모없기도 하다. DP도 같다 — **상태를 잘못 잡으면 표를 써도 지수를 못 벗어난다.**
- 이 챕터는 상태의 세 가지 새로운 모양을 다룬다:
  - **구간 DP**: 상태 = 구간 `[i, j]`. "마지막에 어디를 자를지"가 문제를 둘로 쪼갠다. (행렬 곱 순서)
  - **비트마스크 DP**: 상태 = (방문한 도시 **집합**, 현재 위치). 집합을 정수 하나의 비트로 눌러 담는다. (외판원 순회)
  - **트리 DP**: 상태 = (정점, 골랐나/안 골랐나). 사이클이 없어서 자식들의 답을 독립적으로 합친다. (최대 독립 집합)
- 실측: 외판원 12개 도시에서 순서 전부 시도 **108,505,112번** vs 상태로 눌러 담기 **11,254번** — 9,641배.

```text
순서로 보면: 0->1->2->4, 0->2->1->4, ...  전부 다른 길 (11! = 약 4 천만 가지)
상태로 보면: "[0,1,2,4] 를 방문했고 지금 4 에 있음"  <- 위의 순서들이 전부 이 하나
                                                       (2^12 x 12 = 49,152 가지)
어떤 순서로 왔는지가 앞으로의 비용에 영향을 안 주니까 합칠 수 있다.
```

## 전체 흐름

```text
문제를 받는다
  |
  v
"무엇이 같으면 앞으로의 답이 같은가"를 찾는다  <- 상태 설계. 이 챕터의 전부
  |
  |-- 구간을 합치는 문제인가?      -> 상태 = [i, j] 구간          (구간 DP)
  |-- "어떤 집합을 거쳤나"가 핵심? -> 집합을 비트로 눌러 담는다    (비트마스크 DP)
  |-- 그래프가 트리인가?           -> 정점마다 (골랐다/안 골랐다)  (트리 DP)
  v
채우는 순서를 정한다  <- 작은 답이 먼저 준비되도록 (구간이면 짧은 것부터)
  |
  v
답의 위치를 확인한다  <- 뿌리의 두 칸 중 큰 쪽, 전체 구간 칸, ... 문제마다 다르다
```

## 동작 — 구간 DP: 행렬 곱 순서 (`MatrixChain.java`)

**언제 쓰나**: 행렬 여러 개를 곱할 때, 어떤 순서로 곱해도 결과는 같지만 비용은 크게 다르다. 최소 곱셈 횟수를 찾는다. "구간을 하나로 합치는 최소 비용" 유형의 대표다.

  - *행렬 곱 비용*: A(p×q)와 B(q×r)를 곱하면 곱셈이 p×q×r번 든다.

전 상태 — 순서가 비용을 바꾼다:

```text
A(10x30)  B(30x5)  C(5x60) 세 개를 곱한다. dims = [10, 30, 5, 60]

(AB)C : 10x30x5 + 10x5x60  = 1,500 + 3,000  =  4,500 번
A(BC) : 30x5x60 + 10x30x60 = 9,000 + 18,000 = 27,000 번   <- 6 배 차이
```

단계 (그림 해설: 한 단계 한 문장):

- 상태를 "구간 [i, j]의 행렬들을 하나로 합치는 최소 비용"으로 잡는다.
- 그 구간에서 **마지막에 자를 자리 k**를 전부 시도한다: `비용 = [i,k]의 답 + [k+1,j]의 답 + 두 덩어리를 곱하는 비용(dims[i] × dims[k+1] × dims[j+1])`. 최소를 고른다.
- 배낭과 다른 점: 배낭은 "앞에서부터" 하나씩 결정했지만, 여기는 마지막 자르기가 문제를 **양쪽으로** 쪼갠다. 그래서 구간 DP다.
- **채우는 순서는 짧은 구간부터** — 길이 1(비용 0)을 먼저 깔고, 길이 2, 3, …으로 늘린다. 긴 구간이 짧은 구간의 답을 읽기 때문이다. `i`를 바깥 루프에 두면 순서가 안 맞는다(15번 플로이드에서 `k`를 바깥에 둬야 했던 것과 같은 종류).

후 상태 — 표가 채워지는 순서:

```text
       j=0    j=1     j=2
i=0  [  0 ] [1500 ] [4500]   <- 길이 1 (대각선) 먼저, 그 다음 길이 2, 길이 3
i=1         [  0  ] [9000]      [0][2] 칸이 답 = 4,500
i=2                 [  0 ]
     (i > j 인 왼쪽 아래 절반은 "끝이 시작보다 앞" 이라 의미 없음 -> 안 씀)
```

**비용**: 구간 O(n²)개 × 자를 자리 O(n)개 = O(n³). 실측: 행렬 60개에서 표 3,600칸 중 쓰기 1,830번 — 정확히 `n(n+1)/2`, **표의 절반만 쓴다**. 최적 순서를 찾는 데 표 읽기 71,981번을 쓰고 곱셈 10,382,856 → 134,863번(76배)을 아꼈다 — 찾는 비용이 아끼는 비용의 0.7%다.

## 동작 — 비트마스크 DP: 외판원 순회 (`TravelingSalesman.java`)

**언제 쓰나**: 모든 도시를 한 번씩 들르고 출발지로 돌아오는 최소 비용(TSP). "어떤 집합을 이미 거쳤나"가 상태의 일부일 때 집합을 정수의 비트로 표현한다.

  - *비트마스크(bitmask)*: 집합을 2진수 한 개로 표현하는 것. i번 도시를 방문했으면 i번째 비트를 1로 켠다. 예: {0, 2, 3} = 2진수 1101 = 13.

전 상태 — 집합을 정수로:

```text
도시 4 개. mask 는 4 비트.
mask = 0101  ->  {0, 2} 방문함        비트 켜기: mask | (1 << next)
        ^^        2 0                 비트 확인: mask & (1 << next)
상태 = (mask, 현재 도시)  ->  전부 2^4 x 4 = 64 칸
```

단계 (그림 해설: 한 단계 한 문장):

- 시작: 0번 도시에서 출발하므로 `mask = 0001`(0번만 방문), 현재 위치 0.
- 각 상태에서 아직 안 간 도시(비트가 꺼진 도시)를 하나씩 시도한다: `이동 비용 + 그 다음 상태의 답` 중 최소.
- 모든 비트가 켜지면(`mask == 1111`) 출발지 0으로 돌아가는 비용을 반환한다.
- **원래 `mask`를 고치면 안 된다** — `mask | (1 << next)`로 새 값을 만들어 넘긴다(고치면 형제 갈래가 오염된다).
- 상태는 `key = mask * n + at`로 눌러 1차원 표에 담고, 메모이제이션으로 채운다.

후 상태 — 실측 숫자:

```text
도시 12 개:
  순서 전부 시도 : 108,505,112 번 호출     (11! = 39,916,800 가지 순서)
  비트마스크 DP  :      11,254 칸 채움     (상태는 2^12 x 12 = 49,152 개)
                                            -> 9,641 배

채운 칸이 49,152 중 11,254 (23%) 뿐인 이유:
  "집합에 없는 도시에 서 있다", "0 번을 안 거친 집합" 같은 상태는 도달 불가능.
  21 번 배낭의 56% 보다 낮다 — 상태를 넓게 잡을수록 빈 칸 비율이 커진다.
```

**비용**: 상태 2^n × n개, 상태마다 다음 도시 n개 시도 = O(2^n × n²). 지수이긴 하지만 (n-1)!보다 훨씬 작다. **n이 20을 넘으면 이 방법도 못 쓴다** — 2^n이 백만을 넘기 때문(메모리가 먼저 터진다).

## 동작 — 트리 DP: 최대 독립 집합 (`TreeDp.java`)

**언제 쓰나**: 이웃한 두 정점을 같이 못 고를 때, 고른 정점들의 가중치 합을 최대로. 일반 그래프에서는 NP 난해하지만 **트리에서는 선형**이다.

  - *트리(tree)*: 사이클(빙 돌아 제자리로 오는 길)이 없는 연결 그래프.
  - *독립 집합(independent set)*: 서로 이웃(간선으로 직접 연결)인 정점이 하나도 없는 정점 모음.
  - *NP 난해(NP-hard)*: 모든 경우를 다 보는 것보다 본질적으로 나은 방법이 알려지지 않은 어려움의 등급.

전 상태 — 예시 트리:

```text
        0(3)          괄호는 가중치
       /    \         0 을 고르면 1, 2 는 못 고른다
    1(5)    2(4)      1 을 고르면 0, 3, 4 는 못 고른다
    /  \
  3(2) 4(6)
```

단계 (그림 해설: 한 단계 한 문장):

- 상태를 정점마다 **두 칸**으로 잡는다: `표[v][0]` = v를 안 고른 경우의 최대합, `표[v][1]` = v를 고른 경우의 최대합.
- 잎에서 뿌리로 올라가며(재귀의 되돌아오는 길에) 채운다: **고르면** 자식들은 못 고르니 `가중치(v) + Σ 자식[0]`, **안 고르면** 자식은 자유니 `Σ max(자식[0], 자식[1])`.
- 무방향 간선이라 이웃 목록에 부모도 들어 있다 — `child == parent`를 걸러야 한다(안 거르면 무한 재귀).
- **답은 뿌리의 두 칸 중 큰 쪽** — `max(표[0][0], 표[0][1])`. 하나만 보면 틀린다.

후 상태 — 표가 채워진 모습:

```text
채우는 순서(잎부터):  3      4      1               2      0
표[v][0] (안 고름):   0      0      8 (=2+6)        0      12 (=max(8,5)+max(0,4))
표[v][1] (고름)  :    2      6      5 (=5+0+0)      4      11 (=3+표[1][0]+표[2][0]=3+8+0)

답 = max(표[0][0], 표[0][1]) = max(12, 11) = 12   (고른 것: 3, 4, 2 -> 2+6+4)
```

**비용**: 정점마다 두 칸, 간선마다 한 번씩 오르내림 = O(n). 실측: 정점 20개에서 전부 시도는 재귀 28,655번, DP는 표 쓰기 40번(= 20×2) — 716배. 전부 시도는 정점이 늘 때마다 약 1.6배씩 는다 — **피보나치 수열과 같은 증가율**("고른다/안 고른다" 두 갈래가 겹치며 그 수열이 나온다).

**왜 트리에서만 쉬운가**: 사이클이 없으므로 어떤 정점의 결정이 자기에게 돌아오지 않는다. 그래서 자식들의 답을 독립적으로 합칠 수 있다 — 부분 문제가 안 겹친다.

## 비교 — 세 가지 상태 모양

```text
              구간 DP           비트마스크 DP        트리 DP
상태          [i, j] 구간       (집합, 현재 위치)     (정점, 골랐나)
쪼개는 방식   마지막 자르기 k    다음 도시 하나        자식들로
상태 수       O(n^2)            O(2^n x n)           O(n) x 2
채우는 순서   짧은 구간부터      메모이제이션          잎에서 뿌리로
표 활용률     50% (i<=j 만)     23% (도달 가능만)     100%
```

## 핵심 문장

- 21번은 "표를 쓰면 빨라진다"를 쟀고, 여기서는 "상태를 어떻게 잡느냐"를 잰다 — 상태를 잘못 잡으면 표를 써도 지수를 못 벗어난다.
- 순서가 달라도 "지나온 집합과 현재 위치"가 같으면 미래가 같다 — 그 관찰 하나가 (n-1)!을 2^n×n으로 줄인다.
- 구간 DP는 "마지막에 어디를 자르나"가 문제를 양쪽으로 쪼갠다 — 그래서 짧은 구간부터 채워야 한다.
- 트리는 사이클이 없어 결정이 자기에게 돌아오지 않는다 — NP 난해한 문제가 트리 위에서는 선형이 되는 이유.
- 상태를 넓게 잡을수록 도달 불가능한 칸의 비율이 커진다(배낭 56% → TSP 23%) — 타뷸레이션이면 그 빈 칸까지 전부 채운다.

## 관련 자료

- 챕터 안내: `/home/jun/project/myway/algorithm/22-dp-advanced/README.md`
- 정답 기준 소스: `/home/jun/project/myway/algorithm/22-dp-advanced/impl/com/algo/dpadv/MatrixChain.java`, `impl/com/algo/dpadv/TravelingSalesman.java`, `impl/com/algo/dpadv/TreeDp.java`
- 내 구현(TODO 채우기): `/home/jun/project/myway/algorithm/22-dp-advanced/src/main/java/com/algo/dpadv/` (MatrixChain, TravelingSalesman, TreeDp, CountedTable)
- 계약(테스트): `/home/jun/project/myway/algorithm/22-dp-advanced/src/test/java/com/algo/dpadv/DpAdvancedTest.java`, `MeasurementTest.java`
- 앞 챕터: `21-dp-basics` (메모/타뷸의 기본), `15-bellman-floyd` (채우는 순서가 중요한 첫 사례)

## 용어 풀이

- **상태(state)**: "지금 상황"을 요약한 값 묶음. 상태가 같으면 앞으로의 답도 같아야 한다.
- **구간 DP(interval DP)**: 상태를 구간 [i, j]로 잡고, 마지막에 자를 자리를 전부 시도해 양쪽 답을 합치는 DP.
- **비트마스크(bitmask)**: 집합을 정수 하나의 2진 비트로 표현하는 기법. i번 원소가 있으면 i번째 비트가 1.
- **비트 연산**: `mask | (1 << i)` = i를 집합에 넣은 새 값, `mask & (1 << i)` = i가 집합에 있는지 확인.
- **외판원 순회(TSP, traveling salesman problem)**: 모든 도시를 한 번씩 들르고 출발지로 돌아오는 최소 비용 경로 문제.
- **트리(tree)**: 사이클이 없는 연결 그래프. 정점 n개면 간선 n-1개.
- **트리 DP(tree DP)**: 잎에서 뿌리로 올라가며 자식들의 답을 합쳐 정점의 답을 만드는 DP.
- **독립 집합(independent set)**: 서로 이웃인 정점이 없는 정점들의 모음. "최대"는 가중치 합 기준.
- **NP 난해(NP-hard)**: 사실상 모든 경우를 보는 것 이상의 방법이 알려지지 않은 문제 등급. 트리 같은 특수 구조에서는 쉬워질 수 있다.
- **메모이제이션 / 타뷸레이션**: 위에서 아래로 필요한 칸만 / 아래에서 위로 전부 채우기 (21번 참조).
- **도달 불가능한 상태**: 문제 규칙상 생길 수 없는 (상태) 조합. 메모이제이션은 안 채우고 타뷸레이션은 모르고 채운다.
- **n!(팩토리얼)**: 1×2×…×n. 순서(순열)의 개수. 12개 도시의 방문 순서가 11! = 약 4천만.
- **2^n**: 원소 n개의 부분집합 개수. 비트마스크 상태 수의 뼈대. n=20이면 약 백만.
