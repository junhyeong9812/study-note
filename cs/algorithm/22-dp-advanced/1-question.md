# algorithm/22-dp-advanced — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 TODO 주석을 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 문제 (src/main 의 TODO 1~12)

#### 1. 구간 DP — 행렬 곱 순서 (TODO 1~4)

```java
/**
 * dims 는 행렬 n 개의 크기를 이어 붙인 것이다. 길이가 n+1 이다.
 * i 번째 행렬은 dims[i] x dims[i+1] 이다.
 */
public static long minCost(int[] dims, CountedTable table) {
    // TODO 1: 상태를 무엇으로 잡나.
    //   21 번 배낭은 "앞에서부터 하나씩" 결정했다. 여기서도 그렇게 되나.
    //   마지막에 어디를 자르는지가 문제를 어떻게 쪼개나.
    //   그 쪼갠 두 덩어리가 각각 같은 모양의 문제인가.
    // TODO 2: 표를 어떤 순서로 채우나.
    //   구간 [i, j] 의 답이 무엇의 답을 쓰나. 그것들이 먼저 채워져 있어야 한다.
    //   i 를 바깥에 두고 j 를 안쪽에 두면 그 순서가 지켜지나.
    //   무엇을 바깥에 둬야 하나. 15 번 플로이드에서도 같은 질문이 나왔다.
    // TODO 3: 자르는 자리 k 를 전부 시도한다.
    //   k 의 범위가 어디부터 어디까지인가. i 와 j 를 포함하나.
    //   비용 계산에서 dims 의 인덱스가 어긋나기 쉽다.
    //   구간 [i, k] 의 결과 행렬은 몇 x 몇인가.
    //   길이 1 짜리 구간의 비용은 얼마인가. 그걸 안 넣으면 무슨 값을 읽게 되나.
}

/** 순서를 안 정하고 왼쪽부터 차례로 곱했을 때의 비용. 비교 대상이다. */
public static long leftToRightCost(int[] dims) {
    // TODO 4: 왼쪽부터 하나씩 곱해 나간다. 중간 결과의 크기가 어떻게 바뀌나.
}
```

- 상태를 무엇으로 잡는가, 그리고 배낭처럼 "앞에서부터 하나씩" 결정하는 방식은 왜 여기서 안 통하는가?
- "마지막에 어디를 자르는가"(k)는 문제를 어떻게 두 개의 같은 모양 문제로 쪼개는가?
- 표는 어떤 순서로 채워야 하는가 — i 를 바깥 루프에 두면 왜 안 되는가?
- 이 채우는 순서 문제는 15번 플로이드의 무엇과 같은 종류인가?
- k 의 범위는 어디부터 어디까지인가, j 를 포함하면 무슨 일이 생기는가?
- 구간 [i, k] 의 결과 행렬은 몇 x 몇이고, 합치는 비용 식은 dims 로 어떻게 쓰는가?
- 길이 1 짜리 구간의 비용은 얼마이고, 그것을 안 넣으면 무슨 값을 읽게 되는가?
- leftToRightCost 에서 왼쪽부터 곱해 나갈 때 중간 결과의 크기는 어떻게 변하는가?

#### 2. 비트마스크 DP — 외판원 순회 (TODO 5~8)

```java
/** 순서를 전부 시도. calls 배열에 호출 횟수를 센다. */
public static long naive(long[][] cost, long[] calls) {
    // TODO 5: 방문 표시를 하고 안 간 도시마다 내려간다.
    //   다 방문했으면 무엇을 더해야 하나. 출발점으로 돌아와야 한다.
    //   갈 수 없는 길(IMPOSSIBLE)을 어떻게 거르나.
}

/** 비트마스크 DP. */
public static long minTour(long[][] cost, CountedTable table) {
    // TODO 6: 상태를 무엇으로 잡나.
    //   "어떤 순서로 이 도시들을 방문했나" 가 앞으로의 비용에 영향을 주나.
    //   안 준다면 무엇만 기억하면 되나.
    //   그 집합을 정수 하나로 어떻게 표현하나.
    // TODO 7: 표의 크기가 얼마인가.
    //   집합이 2^n 가지, 현재 도시가 n 가지. 둘을 하나의 번호로 어떻게 누르나.
    //   n=12 면 칸이 몇 개인가. (n-1)! 과 비교해봐라.
    // TODO 8: 방문한 도시를 어떻게 판정하나.
    //   비트가 서 있는지 보면 된다. mask 와 무엇을 어떻게 연산하나.
    //   방문 표시는 어떻게 하나. 원래 mask 를 고치면 안 된다. 왜 안 되나.
    //   갈 수 없는 길에서 IMPOSSIBLE 을 그대로 더하면 무슨 일이 생기나.
}
```

- naive 에서 모든 도시를 다 방문했으면 무엇을 더 더해야 하는가?
- naive 는 갈 수 없는 길(IMPOSSIBLE)을 어떻게 거르는가?
- 상태를 무엇으로 잡는가, "어떤 순서로 방문했나"는 왜 버려도 되는가?
- 방문한 도시 집합을 정수 하나로 어떻게 표현하는가?
- 표의 크기는 얼마이고, (mask, at) 을 하나의 key 로 누르는 식은 무엇인가?
- n=12 에서 상태 수와 (n-1)! 은 각각 얼마인가?
- next 도시를 방문했는지는 mask 와 어떤 연산으로 판정하는가?
- 방문 표시는 어떻게 하는가 — 원래 mask 를 고치면 왜 안 되는가?
- 갈 수 없는 길에서 IMPOSSIBLE 을 그대로 더하면 무슨 일이 생기고, 코드는 어떻게 막는가?
- IMPOSSIBLE 이 Long.MAX_VALUE 가 아니라 `Long.MAX_VALUE / 4` 인 이유는?

#### 3. 트리 DP — 최대 독립 집합 (TODO 9~12)

```java
/** 최대 독립 집합의 가중치 합. 표는 정점마다 두 칸이다. */
public static long maxIndependentSet(Tree tree, CountedTable table) {
    // TODO 9: 상태를 무엇으로 잡나.
    //   한 정점에 대해 알아야 할 것이 몇 가지인가.
    //   "고른 경우" 와 "안 고른 경우" 의 답이 서로 다른가.
    //   답은 뿌리의 어느 칸인가. 하나만 보면 되나.
    // TODO 10: 아래에서 위로 올라가며 채운다.
    //   v 를 고르면 자식들은 어떻게 되나. 안 고르면?
    //   무방향 간선이라 부모도 이웃 목록에 있다. 그걸 어떻게 거르나.
    //   부모를 정점 번호로 기억하면 되나. 트리에 중복 간선이 없다면.
    // TODO 11: 왜 트리에서만 이게 되나.
    //   사이클이 있으면 무엇이 깨지나. 어떤 정점의 결정이 돌아와서 자기에게 영향을 주나.
}

/** 전부 시도. calls 배열에 호출 횟수를 센다. */
public static long naive(Tree tree, long[] calls) {
    // TODO 12: 부모를 골랐는지를 인자로 넘기며 내려간다.
    //   같은 정점을 여러 번 계산하게 되나. 그게 표를 쓰는 이유다.
}
```

- 상태는 정점마다 몇 칸이고 각 칸은 무엇을 담는가?
- v 를 고르면 자식들은 어떻게 되고, 안 고르면 어떻게 되는가?
- 답은 뿌리의 어느 칸인가 — 한 칸만 보면 왜 틀리는가?
- 무방향 간선이라 이웃 목록에 부모도 있는데 어떻게 거르는가?
- 부모를 정점 번호 하나로 기억하는 것으로 충분한 조건은 무엇인가?
- 왜 이 문제가 일반 그래프에서는 NP 난해한데 트리에서는 선형인가?
- naive 는 무엇을 인자로 넘기며 내려가고, 왜 같은 정점을 여러 번 계산하게 되는가?
- 트리 naive 의 호출 수가 피보나치 수열의 증가율(약 1.6배)과 같은 모양인 이유는?

### B. 개념

<!-- 앞 4개: 원본 README 「생각해볼 것」 이관 -->

- (원본 생각해볼 것) 구간 DP 로 풀 수 있는 다른 문제(팰린드롬 분할, 돌 합치기)의 공통점은 무엇인가?
- (원본 생각해볼 것) 비트마스크는 n 이 몇까지 쓸 수 있고, 어디서 메모리가 터지는가?
- (원본 생각해볼 것) 트리 DP 로 트리의 지름을 구하려면 상태를 무엇으로 잡는가?
- (원본 생각해볼 것) 사이클이 하나만 있는 그래프(선인장)에서는 어떻게 하는가?

<!-- 아래: 서머리 기반 파생 질문 -->

- 21번 배낭의 상태와 이 챕터 세 문제의 상태는 "무엇이 같으면 합칠 수 있나"라는 관점에서 어떻게 이어지는가?
- 배낭 56% → 외판원 23% — 채워지는 칸의 비율이 왜 더 떨어졌는가, 무엇을 시사하는가?
- 구간 DP 표 3,600칸 중 쓰기가 정확히 1,830(= n(n+1)/2)인 이유는?
- "순서를 찾는 비용이 아끼는 비용의 0.7%"라는 실측은 무슨 의사결정을 정당화하는가?
- 외판원 상태 (mask, at) 에서 at 를 빼고 mask 만 상태로 잡으면 왜 틀리는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
