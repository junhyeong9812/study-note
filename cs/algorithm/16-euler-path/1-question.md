# algorithm/16-euler-path — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.
>
> ⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제.

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 javadoc/TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 과제 (구현 대상 — src TODO 1~6)

#### 1. `EulerPath.exists` (TODO 1)

```java
/** 오일러 경로가 있나. 회로도 경로에 포함한다. */
public static boolean exists(CountedGraph g)
    // TODO 1: 조건이 둘이다. 하나만 보면 틀린다.
    //   차수 조건: 무방향이면 홀수 차수 정점이 몇 개여야 하나. 0 개? 2 개? 둘 다?
    //             방향이면 무엇을 봐야 하나. 나가는 것과 들어오는 것의 차이를 세어봐라.
    //             회로일 때와 경로일 때 그 차이가 각각 어떤 모양인가.
    //   또 하나: 떨어진 삼각형 두 개를 생각해봐라. 차수는 전부 짝수다.
    //           그런데 한 붓 그리기가 되나. 무엇을 더 봐야 하나.
    //           고립된 정점(차수 0)은 그 검사에 포함해야 하나.
    //   간선이 하나도 없으면 무엇을 답해야 하나.
```

- 무방향 그래프에서 홀수 차수 정점이 0개 또는 2개여야만 하는 이유는 무엇인가?
- 홀수 차수 정점이 정확히 1개인 무방향 그래프가 존재할 수 있는가?
- 방향 그래프에서는 정점마다 무엇을 세어 판정하는가?
- 회로일 때와 경로일 때 `나간 수 - 들어온 수`의 전체 모양은 각각 어떤가?
- 차수 조건만 보면 어떤 그래프에 속는가?
- 연결 검사에서 고립된 정점(차수 0)을 빼야 하는 이유는 무엇인가?
- 간선이 하나도 없는 그래프에는 무엇을 답해야 하는가?
- `inDegree(v)`를 판정 루프 안에서 정점마다 부르면 비용이 어떻게 되고, 측정은 그것을 어떤 숫자로 보여줬는가?
- "될까?"만 판정하는 데도 O(E)가 드는 이유는 무엇인가?

#### 2. `EulerPath.startVertex` (TODO 2)

```java
/** 시작할 정점. */
static int startVertex(CountedGraph g)
    // TODO 2: 경로면 어디서 시작해야 하나. 회로면 어디든 되나.
    //   회로에서 "아무 데나" 로 0 번을 골랐는데 0 번에 간선이 없으면 무슨 일이 생기나.
```

- 경로일 때는 왜 반드시 홀수 차수 정점(방향이면 나간 쪽이 하나 많은 정점)에서 시작해야 하는가?
- 회로에서 "아무 데나"로 0번을 골랐는데 0번이 고립되어 있으면 무슨 일이 생기는가?
- 이 함수를 망가뜨린 변종이 계약 테스트에서 처음에 안 잡힌 이유는 무엇인가?
- 그것을 잡으려고 추가한 테스트 그래프 두 개는 각각 어떤 모양인가?

#### 3. `EulerPath.findTrail` — 히어홀저 (TODO 3~6)

```java
/**
 * 오일러 경로 하나. 정점 목록으로 돌려준다. 없으면 빈 목록.
 * 간선이 E 개면 정점이 E+1 개 나온다.
 */
public static List<Integer> findTrail(CountedGraph g)
    // TODO 3: 스택에 시작 정점을 넣고 안 쓴 간선을 따라 계속 내려간다.
    //   무엇을 "썼다" 고 표시하나. 정점인가 간선인가.
    //   무방향 간선은 양쪽에 저장되어 있다. 한쪽만 표시하면 어떻게 되나.
    //   정점 번호로 짝을 찾으면 중복 간선에서 무엇이 잘못되나.
    //   (그래서 CountedGraph.Arc 가 id 를 들고 있다)
    // TODO 4: 막혔을 때가 이 알고리즘의 핵심이다.
    //   보통의 백트래킹이라면 무엇을 하나. 히어홀저는 무엇을 하나.
    //   막힌 정점을 답에 넣고 물러나면, 그 위 정점에 안 쓴 간선이 남아 있을 수 있다.
    //   그러면 거기서 다시 내려간다. 그 조각이 답의 어디에 끼워지나.
    // TODO 5: 이미 지나간 호를 매번 처음부터 다시 훑으면 비용이 어떻게 되나.
    //   정점마다 "다음에 볼 호의 위치" 를 기억해두면 무엇이 달라지나.
    // TODO 6: 답을 그대로 돌려주면 되나. 무엇부터 쌓였나.
```

- "썼다" 표시의 대상은 정점인가 간선인가, 왜 그쪽인가?
- 간선을 정점 쌍으로 기억하면 중복 간선(다중 그래프)에서 무엇이 잘못되는가?
- 무방향 간선은 양쪽 인접 리스트에 저장되는데, 한쪽만 표시하는 실수를 `Arc`의 id는 어떻게 막는가?
- 막혔을 때 보통의 백트래킹과 히어홀저는 각각 무엇을 하는가?
- 막힌 정점을 답에 넣고 물러난 뒤 다시 내려가서 생긴 조각은 답의 어디에 끼워지는가?
- 히어홀저가 선택을 물리지 않아도 정답이 되는 근거는 무엇인가?
- 시작점·끝점이 아닌 중간 정점에서는 막히는 일이 없는 이유는 무엇인가?
- 이미 지나간 호를 매번 처음부터 훑으면 전체 비용이 어떻게 되는가?
- 정점별 커서의 유무를 측정은 어떤 숫자로 보여줬는가?
- 끝난 뒤 답을 왜 뒤집는가?
- 결과 정점 수가 간선 수 + 1인 이유는 무엇인가?
- `useEdge()` 호출 수와 결과 길이를 따로 세는 이유는 무엇인가?

### B. 개념

#### 4. 원본 README "생각해볼 것" (이관)

- 홀수 차수 정점이 4개면 간선을 몇 개 더해야 한 붓 그리기가 되는가(중국인 우편배달부 문제)?
- 방향 그래프의 연결성 검사를 왜 방향을 무시하고 하는가, 강연결이어야 하는 것 아닌가?
- 히어홀저를 재귀로 쓰면 어디가 위험한가(12번의 깊이 측정을 떠올려서)?
- 해밀턴 경로는 왜 이렇게 못 푸는가, 오일러와 무엇이 다른가?

#### 5. 파생 질문 (서머리 기반)

- 오일러 경로와 오일러 회로의 차이는 무엇이고, 판정 조건은 어디서 갈리는가?
- 홀수 차수 정점은 왜 시작점 또는 끝점만 될 수 있는가?
- 같은 DFS 기반인데 12번 DFS의 방문 표시와 히어홀저의 사용 표시는 대상이 어떻게 다른가?
- 겉보기에 비슷한 두 문제(오일러 vs 해밀턴)의 난이도가 갈리는 데서 무엇을 배울 수 있는가?
- 판정(`exists`)을 생략하고 `findTrail`만 돌리면 조건이 안 되는 그래프에서 어떤 잘못된 결과가 나오는가?
- 커서 최적화가 "계측기 없이는 안 보인다"는 말은 무슨 뜻인가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
