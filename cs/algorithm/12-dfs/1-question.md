# algorithm/12-dfs — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.
>
> ⚠️ 질문 목록은 Claude 초안(2026-09-14) — 원본 문제·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제.

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 javadoc/TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     유형: 왜(why) / 예측(what if) / 경계(어느 계층·모듈의 책임인가) / 연결(다른 주제와의 다리) -->

### A. 과제 (구현 대상 — src TODO 1~9)

#### 1. `Dfs.recursiveOrder` / `Dfs.iterativeOrder` (TODO 1~3)

```java
/** 재귀로 방문한 순서. 이웃은 인접 리스트에 넣은 순서대로 본다. */
public static List<Integer> recursiveOrder(CountedGraph g, int start)
    // TODO 1: 방문 표시를 하고 순서에 넣은 뒤 안 본 이웃마다 내려간다.
    //   여기는 BFS 보다 짧다. 대신 깊이가 자바 호출 스택에 쌓인다.
    //   정점이 10 만 개인 직선 그래프를 주면 무슨 일이 생기나.

/** 명시적 스택으로 방문한 순서. recursiveOrder 와 정확히 같아야 한다. */
public static List<Integer> iterativeOrder(CountedGraph g, int start, CountedStack stack)
    // TODO 2: 이웃을 어느 순서로 스택에 넣어야 재귀 판과 같은 순서가 나오나.
    //   스택은 마지막에 넣은 것을 먼저 꺼낸다. 그러면 0 번 이웃을 먼저 보려면?
    // TODO 3: 방문 표시를 넣을 때 하나 꺼낼 때 하나.
    //   11 번 BFS 에서는 넣을 때 해야 했다. 여기서는 반대다. 왜 그런가.
    //   꺼낼 때 표시하면 같은 정점이 스택에 여러 번 들어간다. 그건 어떻게 거르나.
```

- 재귀 판에서 "되돌아올 곳"은 누가 기억하는가?
- 정점 10만 개 직선 그래프를 재귀 판에 주면 무슨 일이 생기는가(실측 기준)?
- 스택 판이 재귀 판과 정확히 같은 순서를 내야 하는 이유는 무엇인가?
- 이웃을 어느 순서로 push해야 같은 순서가 나오고, 왜 그런가?
- 방문 표시는 넣을 때 하는가 꺼낼 때 하는가 — 11번 BFS와 왜 반대인가?
- 넣을 때 표시하면 어떤 경우에 순서가 달라지는가?
- 꺼낼 때 표시하면 같은 정점이 스택에 여러 번 들어가는데 어떻게 거르는가?
- 두 판의 간선 조회 횟수는 다른가(실측)?
- 직선 10만 개에서 반복 판의 스택 최대 크기는 얼마였고 왜 그런가?
- 정점 300개 완전 그래프에서는 어느 판이 이기고 몇 배 차이인가?
- 재귀 깊이는 O(V)인데 순진한 명시적 스택은 왜 O(E)인가?
- "어느 쪽이 빠른가"가 잘못된 질문인 이유는 무엇인가?

#### 2. `Dfs.times` (TODO 4)

```java
/** 방문 시각과 종료 시각. times[v][0] 이 진입, times[v][1] 이 종료다. */
public static int[][] times(CountedGraph g, int start)
    // TODO 4: 시계를 하나 두고 들어갈 때와 나올 때 각각 찍는다.
    //   시계를 어떻게 재귀에 넘기나. int 는 값으로 복사된다.
    //   안 간 정점의 시각은 무엇으로 두나. 0 으로 두면 "0 번에 방문" 과 구분되나.
```

- 진입 시각과 종료 시각은 각각 언제 찍는가?
- 시계를 재귀에 어떻게 넘기는가 — int로 그냥 넘기면 왜 안 되는가?
- 안 간 정점의 시각을 0으로 두면 왜 안 되고, 대신 무엇으로 두는가?
- 진입·종료 시각이 "괄호 구조"를 이룬다는 말은 무슨 뜻인가?

#### 3. `CycleDetector` (TODO 5~7)

```java
/** 방향 그래프에 사이클이 있나. 모든 정점에서 시작해 본다. */
public static boolean hasDirectedCycle(CountedGraph g)
    // TODO 5: "이미 방문한 정점을 또 만났다" 를 사이클로 보면 왜 안 되나.
    //   0 에서 1 과 2 로 갈라졌다가 둘 다 3 으로 가는 그래프를 그려봐라.
    //   필요한 것은 "지금 내려가고 있는 경로 위에서 다시 만났나" 다.
    //   그걸 구분하려면 상태가 몇 개 필요한가. boolean 하나로 되나.
    // TODO 6: 시작점 하나로 충분한가.

/** 무방향 그래프에 사이클이 있나. */
public static boolean hasUndirectedCycle(CountedGraph g)
    // TODO 7: 여기서는 상태가 둘이면 된다. 대신 다른 문제가 생긴다.
    //   무방향 간선은 양쪽에 저장된다. 그러면 자식이 부모를 이웃으로 본다.
    //   부모를 빼야 한다. 그런데 0 과 1 사이에 간선이 두 개 있으면 그건 사이클이다.
    //   부모로 가는 간선을 전부 빼버리면 자식 쪽에서는 그 사이클을 못 본다. 그래도 답이 맞을까.
    //   자기 자신으로 가는 간선은 따로 처리해야 하나. 그 시점에 seen[v] 는 이미 어떤 값인가.
```

- 방향 그래프에서 "이미 방문 = 사이클"이 오탐하는 반례 그래프는 무엇인가?
- 상태가 왜 셋 필요하고, 각각 무슨 뜻인가?
- DONE을 IN_PROGRESS와 뭉뚱그리면 무슨 일이 생기는가?
- 방향 그래프는 왜 모든 정점에서 시작해 봐야 하는가?
- 이미 다 본(UNSEEN이 아닌) 정점에서 다시 시작할 필요가 있는가?
- 무방향 그래프는 왜 색 둘로 충분하고, 대신 무엇을 빼야 하는가?
- 부모로 가는 간선을 전부 빼면 중복 간선(0-1이 두 개) 사이클을 자식이 못 보는데 답이 왜 맞는가?
- 자기 자신으로 가는 간선 검사(`if (to == v)`)는 왜 필요 없었는가?
- 사이클을 찾으면 그 자리에서 멈춘다는 것이 실측에서 어떻게 나타났는가?

#### 4. `Components` (TODO 8~9)

```java
/** 각 정점이 속한 덩어리 번호. 번호는 0 부터 발견 순서대로다. */
public static int[] labels(CountedGraph g)
    // TODO 8: 정점마다 DFS 를 시도하되 이미 본 것은 건너뛴다.
    //   정점마다 새로 도는데 왜 전체가 O(V+E) 로 유지되나.
    //   번호는 언제 올리나. 매 정점마다 올리면 무엇이 잘못되나.

/** 덩어리 개수. */
public static int count(CountedGraph g)
    // TODO 9: labels 결과로 셀 수 있다. 정점이 하나도 없으면 몇 개인가.
```

- 정점마다 DFS를 시도하는데 전체 비용이 O(V+E)로 유지되는 이유는 무엇인가?
- 덩어리 번호는 언제 올리는가, 루프의 매 정점마다 올리면 무엇이 잘못되는가?
- 정점이 하나도 없는 그래프에서 count는 몇이어야 하고, 구현은 그것을 어떻게 보장하는가?

### B. 개념

#### 5. 원본 README "생각해볼 것" (이관)

- 반복 판에서 "정점과 이웃 번호"를 스택에 넣으면 메모리가 어떻게 되고, 코드는 얼마나 복잡해지는가?
- 진입/종료 시각으로 무엇을 알 수 있고, 두 정점이 조상 관계인지 O(1)로 판정할 수 있는가?
- 방향 그래프의 연결 요소는 무엇이고, 무방향과 왜 다른가(18번 강연결 요소)?
- 사이클을 찾는 것과 사이클 자체를 돌려주는 것은 무엇이 다른가?

#### 6. 파생 질문 (서머리 기반)

- DFS의 시간 비용이 BFS와 같은 O(V+E)인 이유는 무엇인가?
- 같은 그래프에서 DFS와 BFS의 방문 순서가 달라지는 근본 원인은 무엇인가(자료구조 기준)?
- 순서를 안 맞춘 스택 DFS도 "DFS이긴 하다"는데, 계약 테스트는 왜 두 판을 대조하는가?
- 3색의 IN_PROGRESS는 재귀 실행 중 무엇과 정확히 대응하는가?
- "테스트가 하나도 안 깨진 방어 코드"(자기 간선 검사)를 지운 판단에서 무엇을 배우는가?
- 사이클 탐지의 "최악 O(V+E)"와 "평균은 훨씬 빠를 수 있다"는 왜 다른 이야기인가?
- 폴더 트리 훑기, 패키지 순환 의존 검사, 친구 무리 나누기는 각각 이 박스의 어느 함수에 대응하는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
