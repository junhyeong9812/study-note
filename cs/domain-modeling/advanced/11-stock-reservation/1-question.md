# domain-modeling-advanced/11-stock-reservation — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 요구사항 한 줄 (원본 README)

```text
  "결제 전까지 10 분 잡아둔다"

    잡아둔 것을 빼나        빼면 덜 팔리고, 안 빼면 초과 판매가 난다
    만료를 언제 반영하나    조회할 때인가 배치가 돌 때인가
    만료 시각 그 순간은     살아 있나 죽었나
```

- 이 한 줄이 **안 말해주는** 결정 셋은 각각 무엇인가?
- "몇 개 남았어요?"에 답할 때 잡아둔 것을 빼는 판과 안 빼는 판은 각각 무엇을 잃는가?
- 만료를 조회할 때 반영하는 것과 배치가 돌 때 반영하는 것은 무엇이 다른가?
- 만료 시각 정각(10:10:00)은 살아 있는가 죽었는가?
- 이 셋을 enum 으로 올리지 않고 그냥 하나씩 골라 구현하면 무엇을 못 하게 되는가?
- 확정(CONFIRMED)된 수량은 왜 세 스위치와 무관하게 항상 빠지는가?

#### 1. TODO 1 — expired (그 시각에 만료됐나)

```java
/**
 * 그 시각에 이 예약이 만료됐나.
 *
 * <p>확정되거나 풀린 예약은 만료를 따질 것이 없다.
 */
private boolean expired(Reservation reservation, Instant at, Boundary boundary) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- HELD 가 아닌 예약에 대해 이 함수는 무엇을 돌려줘야 하는가?
- 확정된 예약에 대해 true 를 돌려주면 `sweep` 에서 무슨 일이 벌어지는가?
- `EXPIRED_AT_DEADLINE` 과 `ALIVE_AT_DEADLINE` 은 각각 어떤 부등호가 되는가?
- `!at.isBefore(deadline)` 와 `at.isAfter(deadline)` 는 정확히 어떤 시각 하나에서만 갈리는가?
- 만료 시각은 어디서 오는가 — 필드로 저장돼 있는가 계산되는가?

#### 2. TODO 2 — holding (그 시각에 재고를 잡고 있나)

```java
/**
 * 그 시각에 이 예약이 재고를 잡고 있나.
 *
 * <p>배치 방식이면 만료됐어도 아직 잡고 있는 것으로 센다.
 * 배치가 돌아야 풀리기 때문이다.
 */
private boolean holding(
        Reservation reservation, Instant at, ExpiryCheck check, Boundary boundary) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- SWEEP 방식에서 만료된 예약이 여전히 "잡고 있는" 것으로 세지는 이유는 무엇인가?
- 이 함수에서 `check == SWEEP` 를 먼저 보면 `expired` 는 언제 호출되는가?
- `expired` 를 `holding` 안에서 부르는 구조는 두 결정(만료 경계·만료 시점)을 어떻게 분리하는가?
- SWEEP 인데 `sweep()` 을 한 번도 안 돌리면 이 함수는 만료된 예약을 언제까지 잡힌 것으로 세는가?

#### 3. TODO 3 — available (그 시각의 가용 재고)

```java
/**
 * 그 시각의 가용 재고.
 *
 * <p>물리 재고에서 확정된 수량을 뺀 것이 남은 실물이고,
 * 거기서 잡고 있는 것을 더 뺄지가 규칙이다.
 */
public int available(
        String sku, Instant at, AvailabilityRule rule, ExpiryCheck check, Boundary boundary) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- 확정을 빼는 분기와 잡은 것을 빼는 분기는 왜 `else if` 로 갈라야 하는가?
- 품목 필터를 빼먹으면 어떤 값이 어떻게 틀리는가?
- 품목이 하나뿐인 테스트로 품목 필터 누락이 절대 안 잡히는 이유는 무엇인가?
- 이 함수가 음수를 돌려줄 수 있는가, 그 음수는 무엇을 뜻하는가?
- 재고에 없는 품목을 물으면 예외인가 0인가, 왜 그렇게 정했는가?

#### 4. TODO 4 — confirm (확정)

```java
/**
 * 예약을 확정한다.
 *
 * <p>만료된 예약을 확정하면 안 된다. 그런데 결제는 이미 끝났다.
 * 그래서 확정을 막는 것만으로는 문제가 안 끝난다.
 *
 * @return 확정됐으면 true. 만료됐거나 없으면 false
 */
public boolean confirm(String id, Instant at, Boundary boundary) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 확정 순서 네 단계는 무엇인가?
- 같은 예약을 두 번 확정하려 하면 어디서 막히는가?
- 초과 판매를 예외로 던지지 않고 카운터에 기록하는 이유는 무엇인가?
- 초과 판매를 셀 때 품목을 안 나누면 A 8개·B 5개가 왜 초과 판매로 잡히는가?
- "만료된 예약의 확정을 막았다"로 문제가 안 끝나는 이유는 무엇인가?
- `confirm` 이 `rule`(가용 재고 규칙)을 인자로 안 받는 것은 무슨 뜻인가?

#### 5. TODO 5 — sweep (배치)

```java
/**
 * 만료된 예약을 실제로 푼다. 배치가 하는 일이다.
 *
 * @return 푼 예약 수
 */
public int sweep(Instant at, Boundary boundary) {
    throw new UnsupportedOperationException("TODO 5");
}
```

- 이 함수가 건드려야 하는 것과 절대 건드리면 안 되는 것은 각각 무엇인가?
- 만료 안 된 것까지 푸는 배치는 어떤 사고가 되는가?
- 같은 시각에 `sweep` 을 두 번 부르면 두 번째 반환값은 얼마인가, 왜 그런가?
- 반복 중에 map 을 고치는데 왜 `new ArrayList<>(entrySet())` 로 복사하는가?

### B. 개념

#### 6. 측정 — 배치를 안 돌리면 (원본 README "측정이 알려준 것" 하나)

재고 100, 손님 300, 결제율 30 %:

| 규칙 | 확정 | 거절 | 초과 판매 |
|---|---|---|---|
| 잡은 것 뺌 / 조회 시 만료 | ? | ? | ? |
| 잡은 것 뺌 / 배치 만료 | ? | ? | ? |
| 물리 재고만 / 조회 시 만료 | ? | ? | ? |
| 물리 재고만 / 배치 만료 | ? | ? | ? |

- 네 칸의 숫자는 각각 얼마였는가?
- 배치를 안 돌리면 몇 명을 돌려보내는가?
- "초과 판매의 반대"가 왜 더 알아채기 어려운가?
- 물리 재고만 보는 두 줄의 값이 똑같은 이유는 무엇인가?

#### 7. 측정 — 결제율 (원본 README "측정이 알려준 것" 둘·셋)

- 결제율 10·30·60·100 %에서 물리 재고만 보는 판의 확정/거절/초과 판매는 각각 얼마였는가?
- 결제율 30 %에서 물리 재고만 봐도 초과 판매가 0인 것이 왜 위험한가?
- 결제율 100 %면 100개짜리가 몇 개 확정되는가?
- 잡은 것을 빼는 판이 결제율 10 %에서 잃는 것은 몇 개인가?
- "안 팔린 5개"와 "초과 판매 50개" 중 무엇이 나쁜지는 누가 정하는가?

#### 8. 측정 — 배치 주기 (원본 README "측정이 알려준 것" 넷)

| 배치 주기 | 확정 | 거절 |
|---|---|---|
| 안 돎 | ? | ? |
| 60 초 | ? | ? |
| 300 초 | ? | ? |
| 600 초 | ? | ? |

- 배치 주기를 짧게 하면 조회 시 만료(확정 61·거절 103)에 수렴하는가?
- 60초 배치가 조회 시 만료보다 더 많이 파는 이유는 무엇인가?
- "두 방식은 자리가 나는 모양이 다르다"는 말은 무슨 뜻인가?

#### 9. 측정 — 만료 경계와 TTL (원본 README "측정이 알려준 것" 다섯·여섯)

- 만료 경계 두 규칙은 11초 중 몇 초에서 갈렸는가?
- 시각을 초 단위로 흩뿌린 자료로 이 갈림이 거의 안 잡히는 이유는 무엇인가?
- TTL 을 넉넉히 주면 두 만료 방식이 같아지는 이유는 무엇인가?
- 개발 환경에서 이 함정의 절반이 사라지는 까닭은 무엇인가?

#### 10. 함정 (원본 README "함정")

- README 가 꼽은 함정 넷은 각각 무엇인가?
- 확정된 것을 물리 재고에서 안 빼면 어떤 값이 영원히 안 변하는가?
- 만료된 예약을 확정해주면 무엇이 깨지는가?

#### 11. 변종 검증 (원본 README "변종 검증에서 고친 것")

- `available` 의 품목 필터를 지웠는데 테스트가 전부 통과한 이유는 무엇인가?
- 품목 둘을 넣는 테스트를 넣고도 여전히 안 잡힌 자리는 어디였는가?
- 그 자리를 잡으려고 만든 사례(A 8개·B 5개)는 왜 갈림을 드러내는가?
- 측정 시뮬레이션에서 결제를 전부 뒤로 몰아 처리하면 무엇이 거짓말이 되는가?

#### 12. 생각해볼 것 (원본 README 이관)

- 만료된 예약으로 결제가 이미 끝났으면 그 돈을 어떻게 하는가?
- 서버가 여럿이면 "조회할 때 만료"를 어디서 판정하는가?
- TTL 연장 요청은 어떻게 처리하고, 무한정 연장할 수 있는가?
- 잡아둔 재고가 다른 손님에게 "품절"로 보이는 것을 어떻게 설명하는가?

#### 13. 연결

- 이 박스의 `Boundary` 문제는 01-parking-fee 의 어느 문제와 같은 자리인가?
- `oversold` 카운터처럼 "틀린 것을 예외로 안 던지고 세어두는" 방식은 어떤 상황에서 쓰는가?
- LAZY / SWEEP 의 대립은 실무의 어떤 설계 선택과 같은 구조인가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
