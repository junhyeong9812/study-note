# domain-modeling-advanced/23-warehouse-pick — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 한 줄 규칙 (원본 Picker javadoc · README)

```text
  "재고 있는 창고에서 뽑아 보낸다"

  어느 창고에서 뽑나   가까운 곳인가 재고 많은 곳인가
  여러 창고에 걸치면   나눠 보내나 한 곳에 다 있을 때만 보내나
  어느 주문부터        먼저 온 것부터인가 지금 채울 수 있는 것부터인가
```

```text
  서울 창고   앨범 3   포토카드 0
  부산 창고   앨범 0   포토카드 5

  앨범 1 + 포토카드 1 짜리 주문

    나눠 보내기   두 창고에서 하나씩. 상자 둘, 거리 410 km
    한 곳에만     보낼 수 있는 창고가 없다. 아무것도 안 나간다
```

- 이 한 줄이 **안 말해주는** 결정은 무엇무엇인가?
- 재고가 0인 창고도 후보에 남기나 빼나?
- 순위가 같은 창고 둘이면 어느 쪽을 고르나?
- 못 보내는 주문에서 있는 것만 먼저 빼두나, 아무것도 안 빼나?
- "채울 수 있다"는 판정이 합계 기준인가 한 창고 기준인가?
- 지금 채울 수 있는 주문이 하나도 없으면 멈추나, 먼저 온 것을 그냥 처리하나?
- 넘겨받은 주문 목록의 담긴 순서를 믿나, 들어온 순서로 다시 세우나?
- "합계는 충분한데 한 창고에는 모자란다"가 왜 구석 사례가 아니라 늘 생기는 일인가?
- 세 갈래(창고·분할·순서)가 "서로 간섭한다"는 말은 구체적으로 무슨 뜻인가?
- 이 세 갈래를 코드 안에 숨기지 않고 enum 세 개로 올리면 무엇을 할 수 있게 되는가?

#### 1. TODO 1 — sitesFor (그 상품을 뽑을 창고 목록)

```java
/**
 * 그 상품을 뽑을 창고를 우선순위 순으로.
 *
 * <p>재고가 0 인 창고는 뺀다. 순위가 같으면 창고 번호로 가른다.
 * 안 가르면 같은 자료에서 매번 다른 창고가 뽑힌다. 그러면 어제 왜 그
 * 창고에서 나갔는지를 설명할 수 없다.
 */
public List<Site> sitesFor(String sku, SiteChoice siteChoice) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- 후보를 추리는 조건은 무엇인가?
- 재고가 0인 창고를 후보에 남겨두면 현장에서 무슨 일이 벌어지는가?
- `NEAREST` 와 `MOST_STOCK` 의 정렬 기준은 각각 무엇인가?
- `MOST_STOCK` 은 왜 `reversed()` 가 붙는가?
- 순위가 같을 때 창고 번호로 가르지 않으면 무엇을 설명할 수 없게 되는가?
- 같은 거리·같은 재고인 "나·가·다" 세 창고를 넣으면 두 규칙이 각각 어떤 순서를 내놓는가?
- 이 함수가 반환하는 것이 "창고 하나"가 아니라 "목록"인 이유는 무엇인가?
- `MOST_STOCK` 의 재고 비교는 어느 시점의 재고를 보는가 — 뽑기 전인가 뽑는 중인가?

#### 2. TODO 2 — canFillWhole (한 창고로 다 채울 수 있나)

```java
/** 그 창고 하나로 그 주문을 다 채울 수 있나. */
public boolean canFillWhole(PickOrder order, String siteId) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 이 판정은 주문의 몇 개 줄을 보는가 — 하나라도 모자라면 어떻게 되는가?
- "그 창고에 있다"를 `available(siteId, sku) > 0` 으로 쓰면 무엇이 틀리는가?
- 이 함수를 `totalAvailable` 로 짜면 어떤 주문이 잘못 통과하는가?
- 이 판정이 왜 `pick` 안에 숨지 않고 public 으로 나와 있는가 — 누가 또 쓰는가?

#### 3. TODO 3 — pick (주문 하나를 뽑는다)

```java
/**
 * 주문 하나를 뽑는다. 재고를 실제로 뺀다.
 *
 * <p>한 곳에 다 있을 때만 보내는 규칙이면 다 있는 창고를 찾아 거기서만
 * 뽑는다. 없으면 <b>아무것도 안 뽑는다.</b> 반쯤 뽑아두면 그 재고가
 * 잠긴 채로 아무 주문에도 안 쓰인다.
 *
 * <p>나눠 보내는 규칙이면 줄마다 우선순위 창고를 훑어 채운다.
 */
public Pick pick(PickOrder order, SiteChoice siteChoice, Splitting splitting) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- `WHOLE_ONLY` 에서 다 있는 창고를 못 찾으면 무엇을 반환하는가?
- 그때 `unfilled` 에는 무엇이 들어가는가?
- "반쯤 뽑아두면 재고가 잠긴다"에서 잠긴 재고를 다시 선반에 올리는 것은 누구인가?
- `WHOLE_ONLY` 가 창고를 훑는 순서는 `sitesFor` 와 왜 다른가?
- `orderedSites` 는 왜 재고 0인 창고를 안 빼는가?
- `stockScore` 는 무엇을 더한 값인가 — 왜 상품 하나가 아니라 주문의 모든 상품인가?
- `ALLOW_SPLIT` 에서 한 줄을 채우는 루프는 언제 멈추는가?
- `taken == 0` 이면 왜 `continue` 인가 — `break` 로 쓰면 무엇이 깨지는가?
- 여러 줄이 같은 창고에서 나오면 상자가 몇 개가 되는가?
- 서울 앨범 2 + 대전 앨범 3 에 앨범 5개 주문이면 `NEAREST` 의 결과 창고 목록과 거리는 얼마인가?
- 서울 앨범 3 + 부산 포카 5 에 앨범1+포카1 주문이면 두 규칙의 상자·거리·남은 재고는 각각 어떻게 되는가?
- 재고를 "실제로 뺀다"는 것이 이 함수를 어떤 함수로 만드는가 — 두 번 부르면 같은 답이 나오는가?

#### 4. TODO 4 — run (하루치를 처리한다)

```java
/**
 * 하루치를 처리한다.
 *
 * <p>먼저 온 것부터 처리하는 규칙은 들어온 순서 그대로다.
 *
 * <p>지금 채울 수 있는 것부터 처리하는 규칙은 매번 남은 주문을 훑어
 * 다 채울 수 있는 첫 주문을 고른다. 하나도 없으면 제일 먼저 온 것을
 * 그냥 처리한다. 안 그러면 아무것도 안 나간다.
 */
public Run run(
        List<PickOrder> orders, SiteChoice siteChoice, Splitting splitting,
        OrderSequence orderSequence) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 받은 목록을 왜 먼저 정렬하는가 — 무엇으로 정렬하는가?
- 정렬 키가 `placedAt` 하나가 아니라 `placedAt` 다음 `id` 인 이유는 무엇인가?
- 정렬을 안 하면 우선순위가 무엇을 따라 바뀌는가?
- `FILLABLE_FIRST` 는 왜 한 번 정렬해두고 끝내지 못하고 매번 남은 주문을 다시 훑는가?
- 채울 수 있는 주문이 하나도 없으면 `index` 는 무엇이 되는가?
- 그때 멈추지 않고 그냥 처리하는 이유는 무엇인가?
- `unfilledOrders` 에는 어떤 주문이 들어가는가 — 하나도 못 받은 주문만인가?
- 서울 앨범 3에 o1(앨범5)·o2(앨범1)·o3(앨범1) 이면 두 순서의 충족 건수와 처리 순서는 각각 무엇인가?
- 그때 밀린 주문은 누구인가?
- `picks` 가 "처리한 순서"라는 것이 왜 계약인가?

#### 5. 주어진 것 — fillableNow · take · Warehouse

```java
/** 지금 재고로 그 주문을 다 채울 수 있나. */
public boolean fillableNow(PickOrder order, Splitting splitting) {
    if (splitting == Splitting.WHOLE_ONLY) { ... canFillWhole ... }
    ... warehouse.totalAvailable(line.getKey()) < line.getValue() ...
}

/** 그 창고에서 뺀다. 있는 만큼만 빠지고 실제로 뺀 수를 낸다. */
public int take(String siteId, String sku, int quantity) {
    int have = available(siteId, sku);
    int taken = Math.min(have, Math.max(0, quantity));
    ...
}
```

- `fillableNow` 가 `Splitting` 을 인자로 받는 이유는 무엇인가?
- 같은 이름의 판정이 규칙마다 다른 뜻이어야 한다는 말은 무슨 뜻인가?
- `fillableNow` 를 합계로만 판정하면 `WHOLE_ONLY` 에서 무슨 일이 일어나는가?
- `take` 가 `Math.min(have, ...)` 로 있는 만큼만 빼는 이유는 무엇인가?
- 재고가 음수가 되면 그 뒤의 무엇이 어긋나는가?
- `Warehouse.copy()` 는 왜 있는가 — 무엇을 하려면 필요한가?
- `PickOrder` 가 생성자에서 막는 잘못된 입력 셋은 무엇인가?
- `Site` 가 생성자에서 막는 것은 무엇인가?

### B. 개념

#### 6. 함정 (원본 README "함정")

- README가 꼽은 함정 넷은 각각 무엇인가?
- "재고가 0인 창고를 후보로 남기는 것"의 대가는 코드 안이 아니라 어디에서 치러지는가?
- "못 보내는 주문에서 있는 것만 먼저 빼두는 것"이 왜 그냥 손해가 아니라 **잠김**인가?
- "목록 순서로 처리하는 것"이 왜 조회 쿼리와 엮이는가?
- "채울 수 있다를 합계로 판정하는 것"이 왜 규칙마다 틀린 뜻이 되는가?

#### 7. 측정이 알려준 것 (원본 README "측정이 알려준 것")

- 측정의 전제는 무엇인가 — 창고 몇 개·상품 몇 종·주문 몇 건·몇 회인가?
- 하나. `WHOLE_ONLY` 의 충족·상자·거리·남은 재고는 `ALLOW_SPLIT` 대비 각각 얼마인가?
- 배송비만 보면 이길 수 없다는데, 그 대가는 어디에 적혀 있는가?
- 남은 재고가 2.7배라는 것이 왜 "그냥 재고가 많다"가 아닌가?
- 둘. `WHOLE_ONLY` 에서 두 처리 순서의 충족·상자·거리는 각각 얼마인가?
- 세 값이 완전히 같은 이유는 무엇인가?
- 같은 설정이 `ALLOW_SPLIT` 에서는 충족을 몇 건 바꾸는가?
- "한쪽에서는 일하고 한쪽에서는 안 한다"가 설정 화면을 만드는 사람에게 무엇을 뜻하는가?
- 셋. `NEAREST` 와 `MOST_STOCK` 의 충족은 각각 얼마이고 왜 그런가?
- 그때 갈리는 것은 무엇 몇 개, 무엇 몇 km인가?
- 상자와 거리가 서로 반대로 움직인다는 것은 무슨 뜻인가?
- "충족률 화면만 보면 이 설정이 아무 일도 안 하는 것처럼 보인다"가 왜 위험한가?
- 넷. 흩어짐 0 %·30 %·70 %·100 % 에서 `WHOLE_ONLY` 의 충족은 각각 얼마인가?
- "재고를 몰아두면 한 곳에서만 보내기가 쉬워진다"가 왜 틀렸는가?
- 여기서 "몰아둔다"가 정확히 무슨 뜻인가?
- 흩어져 있으면 오히려 한 창고가 세 상품을 다 갖고 있을 확률이 왜 오르는가?
- `ALLOW_SPLIT` 은 흩어짐을 타는가 안 타는가?
- 다섯. `FILLABLE_FIRST` 는 충족을 몇 건 올리고 큰 주문 평균 순번을 얼마에서 얼마로 미는가?
- 순번이 반 칸밖에 안 밀린 이유는 무엇인가?
- "밀린다"가 "영영 안 나간다"가 아니라면 무엇인가?
- 못 채운 큰 주문 수는 얼마에서 얼마로 바뀌는가?
- 여섯. 한 줄짜리 주문에 재고가 넉넉하면 몇 번 비교에서 몇 건이 갈리는가?
- 그때 여덟 조합이 통째로 같아지는 이유 셋은 무엇인가?
- "개발할 때 만드는 자료가 대개 이렇다"가 테스트 설계에 무엇을 시사하는가?

#### 8. 변종 검증에서 고친 것 (원본 README)

- 변종 검증이란 무엇을 하는 것인가?
- 하나. 어떤 변종이 통과했고, 왜 통과했는가?
- 그 변종은 무엇을 바꾸니 잡혔는가?
- 둘. `fillableNow` 의 어떤 변종이 통과했는가?
- 그 변종이 실제로 돌면 무슨 일이 벌어지는가?
- 그 변종은 무엇을 확인하는 테스트로 잡았는가?
- 두 경우 모두 "약한 테스트"였다는데, 약함의 공통 무늬는 무엇인가?

#### 9. 생각해볼 것 (원본 README 이관)

- 나눠 보낸 주문의 배송비를 누가 내나?
- 가까운 곳부터 뽑으면 그 창고가 먼저 마른다 — 재보충은 언제 하나?
- 못 보낸 주문을 다음 입고 때까지 잡아두나 취소하나?
- 큰 주문이 계속 밀리면 언제 강제로 앞으로 올리나?

#### 10. 연결

- 이 트랙이 세는 것(충족·상자·거리·남은 재고)은 알고리즘 트랙이 세던 것과 무엇이 다른가?
- "설정이 죽어 있다"(순서 설정이 `WHOLE_ONLY` 에서 값을 안 바꿈)는 무늬는 앞의 어느 박스와 같은 자리인가?
- 이 박스의 "전부 아니면 전무"는 데이터베이스의 어느 개념과 같은가?
- `FILLABLE_FIRST` 의 기아(밀림)는 운영체제·스케줄러의 어느 문제와 같은가?
- 조회 쿼리 순서를 믿지 않고 다시 세우는 습관은 실무의 어떤 버그를 막는가?
- 한 줄짜리 주문·넉넉한 재고로 만든 테스트 자료가 여덟 조합을 전부 같게 만든 것은, 테스트 설계 원칙 중 무엇의 사례인가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
