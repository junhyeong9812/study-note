# domain-modeling-advanced/22-parcel-split — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 규칙 한 줄 (원본 README)

```text
  상한   5 kg, 30 L

  머그컵 다섯 개 (하나 1,200 g / 800 cm3)

    무게로만   6,000 g 이라 두 상자
    부피로만   4,000 cm3 라 한 상자   <- 6 kg 짜리 상자 하나
```

```text
  "한 상자에 5 킬로그램까지" 라는 말에 정해지지 않은 것이 셋이다.   (Packer 클래스 주석)

    무엇으로 나누나       무게인가 부피인가 둘 다인가
    상한을 넘는 물건은    혼자 보내나 못 보낸다고 하나
    어떤 순서로 담나      들어온 순서인가 큰 것부터인가 작은 것부터인가
```

- 이 한 줄이 **안 말해주는** 결정은 무엇무엇인가?
- "5 킬로그램까지"에서 딱 5,000g 은 들어가는가 안 들어가는가?
- 무게와 부피 중 무엇으로 나누는지를 요구사항이 안 정해주면 무슨 일이 생기는가?
- 혼자서도 상한을 넘는 물건을 요구사항은 뭐라고 하는가?
- 물건을 담는 순서가 결과를 바꾼다는 것을 요구사항 문장에서 알 수 있는가?
- 세 결정이 "서로 간섭한다"는 것은 무슨 뜻인가?
- 요금이 상자 수에 붙는다는 사실이 왜 이 세 결정을 전부 돈 문제로 만드는가?
- 이 챕터가 `Limit` · `Oversize` · `FillOrder` 세 enum 으로 답을 고정하지 않고 **인자로 받는** 이유는 무엇인가?

#### 1. TODO 1 — exceeds (상한을 넘나)

```java
/**
 * 그 상자에 그 물건을 더 넣으면 상한을 넘나.
 *
 * <p>딱 맞는 것은 안 넘는 것이다. 5,000 그램 상한에 5,000 그램은 들어간다.
 * 여기를 뒤집으면 정확히 상한인 주문마다 상자가 하나씩 더 생긴다.
 */
public boolean exceeds(long grams, long volume, Item item, Limit limit) {
    throw new UnsupportedOperationException("TODO 1");
}
```

```java
/** 무엇으로 나누나. */
public enum Limit {
    /** 무게만 본다. 큰 것이 안 들어간다. */
    WEIGHT,
    /** 부피만 본다. 무거운 것이 상자를 찢는다. */
    VOLUME,
    /** 둘 다 본다. 어느 하나만 넘어도 새 상자다. */
    BOTH
}
```

- 이 함수의 부등호는 `>` 인가 `>=` 인가?
- 부등호를 뒤집으면 요금이 어느 방향으로 얼마나 움직이는가?
- `BOTH` 는 두 조건을 `&&` 로 묶는가 `||` 로 묶는가?
- `BOTH` 를 `&&` 로 쓰면 어떤 주문에서 무엇이 잘못되는가?
- 왜 `exceeds` 는 현재 담긴 무게·부피를 `Item` 이 아니라 `long` 두 개로 받는가?
- 앨범 11장(장당 300g / 3,000cm³)은 세 `Limit` 에서 각각 몇 상자가 되는가?
- 머그컵 5개(개당 1,200g / 800cm³)는 세 `Limit` 에서 각각 몇 상자가 되는가?
- 부피로만 나눈 머그컵 상자는 몇 그램이고, 그게 왜 문제인가?

#### 2. TODO 2 — sorted (담을 순서)

```java
/**
 * 담을 순서로 세운다.
 *
 * <p>크기는 무게와 부피를 상한으로 나눈 비율의 합으로 본다. 그램과
 * 세제곱센티미터를 그냥 더하면 <b>단위가 큰 쪽이 순서를 독차지한다.</b>
 *
 * <p>같은 크기면 번호로 가른다. 안 가르면 같은 주문이 매번 다른 상자 수가 된다.
 */
public List<Item> sorted(List<Item> items, FillOrder fillOrder) {
    throw new UnsupportedOperationException("TODO 2");
}

/** 상한 대비 크기. 만분율로 낸다. */
public long score(Item item) {
    return item.grams() * 10_000 / maxGrams + item.volume() * 10_000 / maxVolume;
}
```

- `score` 가 그램과 세제곱센티미터를 그냥 더하지 않고 각자의 상한으로 나누는 이유는 무엇인가?
- 그냥 더하면 머그컵과 앨범 중 어느 쪽이 "더 큰 것"이 되는가?
- 상한 대비로 재면 어느 쪽이 더 큰 것이 되는가?
- 그 둘 중 **상자를 더 잡아먹는 것**은 실제로 어느 쪽인가?
- 왜 하필 만분율(×10,000)인가 — 백분율이면 무엇이 깨지는가?
- 동점을 번호로 안 가르면 무엇이 흔들리는가?
- 상자 수는 같은데 송장이 달라지는 게 왜 문제인가?
- `AS_IS` 가 원본 리스트를 그대로 돌려주지 않고 복사본을 만드는 이유는 무엇인가?
- `LARGEST_FIRST` 는 `SMALLEST_FIRST` 의 비교자를 뒤집은 것인데, 동점 처리까지 같이 뒤집히면 무엇이 문제인가?

#### 3. TODO 3 — pack (나눠 담기)

```java
/**
 * 나눠 담는다.
 *
 * <p>앞 상자부터 훑어 처음 들어가는 상자에 넣는다. 아무 상자에도 안 들어가면
 * 새 상자를 연다.
 *
 * <p>혼자서도 상한을 넘는 물건은 규칙이 정한다. 그 물건은 다른 물건과
 * 안 섞는다. 섞으면 상한을 넘는 상자가 더 커진다.
 */
public Packing pack(List<Item> items, Limit limit, Oversize oversizeRule,
        FillOrder fillOrder) {
    throw new UnsupportedOperationException("TODO 3");
}

/** 혼자서도 상한을 넘는 물건인가. */
public boolean oversize(Item item, Limit limit) {
    return exceeds(0, 0, item, limit);
}

/** 혼자서도 상한을 넘는 물건은. */
public enum Oversize {
    /** 상한을 무시하고 혼자 한 상자에 보낸다. */
    OWN_BOX,
    /** 못 보낸다고 한다. */
    REJECT
}
```

- "앞 상자부터 훑어 처음 들어가는 상자에 넣는다"는 전략의 이름은 무엇인가?
- 그 전략은 상자 수를 가장 적게 만드는가?
- 그런데도 그 전략을 고른 이유는 무엇인가?
- `oversize(item, limit)` 를 `exceeds(0, 0, item, limit)` 로 **파생**시킨 것이 왜 중요한가?
- "넘는 물건"의 정의가 `Limit` 에 달려 있다는 것은 무슨 뜻인가?
- 40,000cm³짜리 가벼운 물건은 `WEIGHT` 와 `VOLUME` 에서 각각 어떻게 되는가?
- 상한을 넘는 물건을 다른 물건과 섞으면 정확히 무엇이 나빠지는가?
- `REJECT` 로 거부된 물건은 결과의 어디에 들어가는가?
- 거부된 물건이 상자에 안 들어가면 그 주문의 요금은 어떻게 되는가?
- oversize 상자는 일반 상자보다 결과 목록의 앞에 오는가 뒤에 오는가?
- 그 순서가 계약인가 구현 세부인가?
- 담기 도중 `exceeds` 를 다시 부르는 것과, 상자마다 현재 무게·부피를 들고 다니는 것 중 왜 후자인가?

#### 4. TODO 4 — fee (배송비)

```java
/**
 * 배송비.
 *
 * <p>상자마다 기본 3,000 원. 그 상자가 2 킬로그램을 넘으면 넘는 만큼
 * 킬로그램당 500 원을 더 받는다. 남는 그램은 올림이다.
 *
 * <p>상한을 넘겨 혼자 보내는 상자에는 5,000 원을 더 받는다.
 */
public long fee(Packing packing) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- "남는 그램은 올림"을 정수 나눗셈으로 어떻게 쓰는가?
- 2,001g 짜리 상자의 요금은 얼마인가?
- 3,000g 짜리 상자의 요금은 얼마인가?
- 초과분이 음수일 때 올림 식을 그냥 태우면 무슨 값이 나오는가?
- 요금이 거부된 물건에는 안 붙는다는 것이 무슨 뜻인가?
- 앨범 10장을 한 상자로 보낼 때와 두 상자로 나눌 때 요금 차이는 얼마인가?
- 상자를 하나 더 만들면 요금은 최소 얼마가 오르는가?
- 8kg 짜리 하나를 혼자 보내면 요금이 얼마이고, 그 세 조각은 각각 얼마인가?
- 요금 함수가 `Packing` 전체를 받고 상자 하나를 안 받는 것은 어떤 결정을 반영하는가?

### B. 개념

#### 5. 함정 (원본 README "함정")

```text
- 상한에 딱 맞는 것을 넘는 것으로 보는 것.
- 크기를 그램 + 세제곱센티미터로 재는 것.
- 상한을 넘는 물건을 다른 것과 섞는 것.
- 같은 크기의 순서를 안 가르는 것.
```

- 넷은 각각 어느 TODO 에서 터지는가?
- "상한에 딱 맞는 것을 넘는 것으로 본다"가 만드는 비용은 어떻게 세는가?
- "단위가 큰 쪽이 순서를 독차지한다"를 머그컵과 앨범으로 보일 수 있는가?
- 넷 중 **테스트를 안 짜면 영영 안 보이는** 것은 무엇인가?

#### 6. 측정이 알려준 것 — 하나: 부피로만 나누기 (원본 README)

```text
12 건짜리 주문 500 개. 가볍고 큰 것과 무겁고 작은 것이 반반이다.

| 기준 | 상자 | 요금 | 5 kg 넘는 상자 |
|---|---|---|---|
| 무게로만 | 1,392 | 5,820,000 | 0 |
| 부피로만 |   861 | 4,816,500 | 483 |
| 둘 다    | 1,408 | 5,871,500 | 0 |
```

- 부피로만 나누면 상자가 몇 퍼센트 줄고 요금은 몇 퍼센트 싸지는가?
- 그 대가로 무엇이 몇 개 생기는가?
- 그 대가가 요금표에 안 나오는 이유는 무엇인가?
- "요금을 최소화하라"는 지시만 주면 최적화가 왜 이 답을 고르는가?
- 지표에 안 잡히는 비용을 어떻게 지표로 끌어올리는가?

#### 7. 측정이 알려준 것 — 둘: 한 종류만 팔면 안 보인다

```text
| 가볍고 큰 것 비율 | 무게로만 | 부피로만 | 둘 다 |
|---|---|---|---|
|   0 % | 2,174 | 500 (전부 무게 초과) | 2,174 |
|  50 % | 1,392 |   861               | 1,408 |
| 100 % |   528 (500 개 부피 초과) | 1,291 | 1,291 |
```

- 무겁고 작은 것만 팔면 어느 두 규칙이 같아지는가?
- 가볍고 큰 것만 팔면 어느 두 규칙이 같아지는가?
- 상한 하나를 빼먹은 버그는 언제 나타나는가?
- 코드를 안 고쳤는데 버그가 나타난다는 게 무슨 뜻인가?
- 이런 버그를 배포 전에 잡으려면 테스트 데이터를 어떻게 만들어야 하는가?

#### 8. 측정이 알려준 것 — 셋: 주문이 작으면 순서가 안 보인다

```text
| 주문 크기 | 들어온 순서 | 큰 것부터 | 작은 것부터 |
|---|---|---|---|
|  4 건 |   596 |   596 |   596 |
|  8 건 | 1,054 | 1,048 | 1,085 |
| 12 건 | 1,408 | 1,397 | 1,470 |
| 20 건 | 2,175 | 2,149 | 2,319 |
```

- 4건짜리 주문에서 세 순서가 완전히 같은 이유는 무엇인가?
- 20건에서 작은 것부터가 큰 것부터보다 상자를 몇 개 더 만드는가?
- 그 차이는 요금으로 얼마인가?
- "개발할 때 만드는 주문이 대개 두세 건이다"라는 문장이 왜 경고인가?
- 손으로 만든 주문으로 테스트하면 무엇을 못 보는가?

#### 9. 측정이 알려준 것 — 넷: 거부가 요금을 네 배로 가른다

```text
| 초과 비율 | 혼자 보내기 | 못 보내기 |
|---|---|---|
|  0 % |  5,871,500 / 거부 0 | 5,871,500 / 거부     0 |
|  5 % |  8,882,500 / 거부 0 | 5,557,000 / 거부   297 |
| 20 % | 18,271,500 / 거부 0 | 4,783,000 / 거부 1,200 |
```

- 초과 물건이 0%면 두 규칙이 왜 완전히 같은가?
- 20%에서 두 요금의 차이는 몇 배인가?
- 싼 쪽이 싼 진짜 이유는 무엇인가?
- "배송비를 줄이는 제일 확실한 방법은 안 파는 것이다"가 꼬집는 것은 무엇인가?
- 이 표를 경영진에게 보여줄 때 반드시 같이 보여야 하는 열은 무엇인가?

#### 10. 측정이 알려준 것 — 다섯: 한 상자면 전부 같다

```text
한 상자에 다 들어가면 열여덟 조합이 통째로 같다. 8,500 번 비교에서 0.
```

- 열여덟 조합은 무엇 × 무엇 × 무엇인가?
- 두 건짜리 주문에서 어긋남이 0인 이유는 무엇인가?
- 8,500 이라는 비교 횟수는 어떻게 나온 수인가?
- 이 결과가 "테스트가 초록색이다"를 왜 못 믿게 만드는가?

#### 11. 변종 검증에서 고친 것 (원본 README)

```text
고칠 것이 없었다. 변종 17 개가 전부 잡혔다.

상한 경계(딱 맞는 것), 크기 재는 방식, 앞 상자부터 훑는 규칙,
요금의 올림, 초과 상자 추가요금이 각각 다른 테스트에 걸렸다.
```

- "변종"을 심어서 검증한다는 게 무슨 방법인가?
- 변종 17개가 전부 잡혔다는 것은 테스트에 대해 무엇을 말해주는가?
- 다섯 자리(경계·크기·훑기·올림·추가요금)가 **각각 다른** 테스트에 걸린 게 왜 좋은 신호인가?
- 변종이 안 잡히면 무엇을 고쳐야 하는가 — 코드인가 테스트인가?

#### 12. 생각해볼 것 (원본 README 이관)

- 상자를 적게 만드는 것과 상한을 지키는 것 중 무엇이 계약인가?
- 송장 순서와 상자 수 중 무엇을 포기하나?
- 상한을 넘는 물건을 거부하면 그 고객에게 무엇을 보여주나?
- 상자를 나눈 뒤 한 상자만 반품되면 배송비는 어떻게 나누나?

#### 13. 연결

- 이 챕터의 "상한에 딱 맞으면 들어간다"는 01-parking-fee 의 어느 결정과 같은 자리인가?
- 여기서 재는 것(상자 수·요금·상한 넘는 상자 수)은 알고리즘 트랙이 재던 것과 무엇이 다른가?
- 담는 순서를 바꾸면 답이 달라지는 성질은 알고리즘 트랙의 어떤 개념과 이어지는가?
- 동점을 id 로 가르는 것은 정렬·페이지네이션의 어떤 관행과 같은가?
- 요금표에 안 나오는 비용이 최적화에 밟히는 현상은 실무의 어떤 사고와 같은 모양인가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
