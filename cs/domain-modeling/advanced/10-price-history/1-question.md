# domain-modeling-advanced/10-price-history — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 기록 세 줄 (원본 README)

```text
  유효일   가격      기록일    성격
  1/1     10,000    1/1      처음
  3/1     12,000    3/1      정시 인상
  2/1     11,000    3/15     소급 정정

  2 월 10 일의 가격은
    그때 알던 값   10,000    (3/15 정정은 아직 없었다)
    지금 아는 값   11,000    (정정을 반영하면)
```

- "2월 10일의 가격이 얼마였나"에 답이 둘인 이유는 무엇인가?
- 둘 중 어느 쪽이 맞는가?
- 영수증을 다시 뽑을 때는 어느 값인가?
- 정산을 다시 할 때는 어느 값인가?
- 기록 한 줄에 시간이 둘 붙는데 각각 무엇인가?
- 시간을 하나만 들고 있으면 무엇을 못 하게 되는가?
- 대개 어느 쪽만 만드는가?
- 왜 그쪽만 만드는가?

#### 1. TODO 1 — 생성자 (기록 정렬)

```java
/**
 * 기록들을 받는다. 유효일 순, 같으면 기록 시각 순으로 정렬해 둔다.
 *
 * <p>정렬해두지 않으면 "가장 늦은 유효일" 을 찾는 코드가 목록 순서를 탄다.
 */
public PriceHistory(List<PriceRecord> records) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- 정렬 기준은 무엇 다음에 무엇인가?
- 왜 기록 시각까지 두 번째 기준으로 넣는가?
- 정렬을 아예 지워도 `priceOn` 의 답이 같은 이유는 무엇인가?
- 그렇다면 정렬이 실제로 필요한 곳은 어디인가?
- 정렬을 안 하면 무엇이 결과를 정하게 되는가?
- 빈 목록을 받으면 어떻게 되는가?
- `null` 을 받으면 어떻게 되는가?

#### 2. TODO 2 — priceOn (그 시점에 알던 그날의 가격)

```java
/**
 * 그 시점에 알고 있던, 그날의 가격.
 *
 * <pre>
 *   유효일이 knownAt 이하이고
 *   기록 시각이 asOf 이하인 것 중에서
 *   유효일이 가장 늦은 것
 * </pre>
 *
 * <p>유효일이 같은 것이 둘이면 정정 규칙이 정한다.
 *
 * @param asOf 이 시각까지 알려진 것만 본다. null 이면 지금 아는 전부
 * @return 가격. 그날에 유효한 기록이 없으면 -1
 */
public long priceOn(String sku, LocalDate on, LocalDateTime asOf, Tie tie) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 후보를 거르는 조건 셋은 무엇인가?
- 그중에서 어느 것을 고르는가?
- `asOf` 가 `null` 이면 무슨 뜻인가?
- "그때 알던 값"을 구하려면 `asOf` 에 무엇을 넣는가?
- 가격이 없을 때 0이 아니라 -1을 돌려주는 이유는 무엇인가?
- 0으로 두면 어디로 흘러 들어가는가?
- 품목 필터를 빼먹으면 무슨 일이 생기는가?
- 그 실수가 왜 유효일 순 정렬에서 특히 나기 쉬운가?

#### 3. TODO 3 — changedDays (소급 정정이 바꾼 날들)

```java
/**
 * 소급 정정이 바꾼 날들.
 *
 * <p>그 시점에 알던 값과 지금 아는 값이 다른 날을 센다.
 * 이미 발행한 영수증 중 몇 장이 지금 다시 뽑으면 달라지는지와 같다.
 */
public List<LocalDate> changedDays(String sku, LocalDate from, LocalDate toInclusive, Tie tie) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- 이 함수는 하루마다 무엇 둘을 비교하는가?
- "그때 알던 값"을 구할 때 `asOf` 에 정확히 무엇을 넣는가?
- 세 줄짜리 예제에서 바뀌는 날은 정확히 며칠인가?
- 1월과 3월이 안 바뀌는 이유는 무엇인가?
- 구간의 끝을 포함하는가 제외하는가?
- 끝을 제외로 바꿔도 통과하는 구간은 어떤 구간인가?
- 그 실수를 잡으려면 어떤 구간으로 시험해야 하는가?
- 이 함수가 세는 것은 실무에서 무엇을 뜻하는가?

#### 4. Tie — 같은 유효일에 기록이 둘이면

```java
/** 같은 유효일에 기록이 둘이면. */
public enum Tie {
    /** 나중에 기록한 것을 쓴다. 정정이 이긴다. */
    LATEST_RECORD,
    /** 먼저 기록한 것을 쓴다. 정정을 무시한다. */
    FIRST_RECORD
}
```

- "정정"이라는 말은 어느 쪽을 뜻하는가?
- 그런데 왜 둘 다 값으로 올렸는가?
- 이 규칙을 안 정하면 무엇이 정하는가?
- 그 순서는 다시 무엇이 정하는가?
- 3/1 12,000(기록 3/1)과 3/1 13,000(기록 3/20)에서 두 규칙의 답은 각각 얼마인가?

### B. 개념

#### 5. 함정 (원본 README "함정")

- README가 꼽은 함정 넷은 각각 무엇인가?
- "없다는 것과 0원인 것은 다르다"를 한 문장으로 설명하면?
- 시간을 하나만 들고 있는 실수가 왜 제일 앞에 있는가?

#### 6. 측정 — 답이 갈리는 건수 (원본 README "측정이 알려준 것")

- 소급 비율 0·10·30·50%에서 값이 바뀐 날 평균은 각각 며칠인가?
- 최대는 각각 며칠인가?
- 소급 기록 평균은 각각 몇 건인가?
- 소급 정정이 열에 하나만 있어도 1년의 몇 퍼센트가 바뀌는가?
- 기록 하나가 며칠씩 뒤집는 셈인가?
- 소급 정정이 없으면 200개 이력에서 몇 개가 바뀌는가?
- 이 박스는 정확히 언제 시작되는가?
- 과거 매출을 두 방식으로 계산하면 총액이 각각 얼마인가?
- 차이는 몇 퍼센트인가?
- 총액이 다른 이력은 200개 중 몇 개인가?
- "작년 매출이 얼마였나"에 대한 답이 왜 둘 다 맞는가?
- 같은 유효일이 겹치는 이력은 200개 중 몇 개인가?
- 갈린 날은 평균 며칠인가?

#### 7. 변종 검증에서 고친 것 (원본 README 이관)

- `changedDays` 의 구간 끝을 제외로 바꿔도 통과한 이유는 무엇인가?
- 어떤 구간을 넣으니 잡혔는가?
- 정렬하는 줄을 지워도 통과한 이유는 무엇인가?
- 그래서 어떤 테스트를 넣었는가?
- 정렬의 기록 시각 기준을 빼도 여전히 통과한 이유는 무엇인가?
- "자바 정렬이 안정적"이라는 말은 무슨 뜻인가?
- 그것을 어떻게 갈랐는가?

#### 8. 생각해볼 것 (원본 README 이관)

- 소급 정정을 하면 이미 발행한 세금계산서는 어떻게 하나?
- "그때 알던 값"으로 뽑은 보고서를 나중에 또 뽑으면 같아야 하나?
- 정정의 정정이 들어오면 어떻게 되나?
- 가격을 지우는 것은 어떻게 표현하나?
- 그것을 0원과 어떻게 구별하나?

#### 9. 연결

- 09-policy-version의 "어느 날짜로 정책을 고르나"와 이 박스의 두 시간은 무엇이 같고 무엇이 다른가?
- "목록 순서가 정하고 그 순서는 조회 쿼리가 정한다"는 앞의 어느 박스에서 똑같이 나왔는가?
- 이 박스의 바이템포럴 모델은 실무의 어떤 기술과 같은 자리인가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
