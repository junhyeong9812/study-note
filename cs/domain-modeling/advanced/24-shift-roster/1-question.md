# domain-modeling-advanced/24-shift-roster — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 규칙 한 줄 (원본 `Roster` javadoc + README)

```text
  "주 40 시간, 연속 6 일까지"
```

```text
  3/29(일) 22:00 ~ 3/30(월) 06:00   여덟 시간짜리 야간 근무 하나

    시작일로 세면   3/29 에 8 시간   3/23 주로 들어간다
    종료일로 세면   3/30 에 8 시간   3/30 주로 들어간다
    나눠서 세면     3/29 에 2, 3/30 에 6

  월~금 45 시간을 일한 사람의 초과근무가 13, 5, 7 시간이 된다.

  세 답이 다 맞는다. 그리고 월급이 다르다.
```

- 이 한 줄이 **안 말해주는** 결정은 무엇무엇인가?
- "주"는 월요일부터인가 일요일부터인가?
- 밤을 넘긴 근무는 시작한 날 것인가 끝난 날 것인가 나눠서인가?
- "연속 6일"의 연속은 달력 날짜로 세나 실제로 쉰 시간으로 세나?
- 자정 정각(00:00)에 끝난 근무는 그날 것인가 다음 날 것인가?
- 딱 24시간을 쉰 사람은 연속이 끊긴 쪽인가 안 끊긴 쪽인가?
- 이 세 결정은 서로 독립인가, 아니면 서로 간섭하는가?
- 안 정해진 자리를 코드 안에 숨기지 않고 enum 세 개로 올리면 무엇을 할 수 있게 되는가?
- "세 답이 다 맞는다"면 어느 하나를 골라 구현하는 것의 무엇이 위험한가?

#### 1. TODO 1 — weekOf (그 날짜가 속한 주의 첫날)

```java
/**
 * 그 날짜가 속한 주의 첫날.
 *
 * <p>일요일 시작에서 일요일 자신은 <b>그 주의 첫날</b>이다. 앞주로 보내면
 * 일요일 근무가 매번 앞주 초과근무가 된다.
 */
public LocalDate weekOf(LocalDate date, WeekStart weekStart) {
    throw new UnsupportedOperationException("TODO 1");
}
```

```java
/** 주를 어디서 끊나. */
public enum WeekStart {
    /** 월요일부터. 근로기준법의 주다. */
    MONDAY,
    /** 일요일부터. 달력의 주다. */
    SUNDAY
}
```

- 자바의 `DayOfWeek.getValue()` 는 월요일부터 일요일까지 각각 몇인가?
- 월요일 시작에서 "그 주의 첫날"은 어떤 식으로 구하는가?
- 일요일 시작에서 `% 7` 이 하는 일은 무엇인가?
- 일요일을 앞주로 보내면 어떤 증상이 **매주** 반복되는가?
- 2026-03-29(일)의 주 첫날은 월요일 기준과 일요일 기준으로 각각 어느 날인가?
- 2026-03-30(월)의 주 첫날은 월요일 기준과 일요일 기준으로 각각 어느 날인가?
- 같은 하루가 기준에 따라 다른 주에 속한다는 사실이 초과근무 계산에 왜 치명적인가?
- 이 함수가 `dayOfWeek(date)` 와 다른 점은 무엇인가 — 왜 요일 자체는 주 시작 규칙과 무관한가?

#### 2. TODO 2 — minutesByDate (근무를 날짜별 분으로)

```java
/**
 * 그 근무를 날짜별로 나눈다.
 *
 * <p>나눠 넣는 규칙만 두 칸이 나올 수 있다. 나머지 둘은 한 칸이다.
 */
public Map<LocalDate, Long> minutesByDate(Shift shift, NightAttribution attribution) {
    throw new UnsupportedOperationException("TODO 2");
}
```

```java
/** 두 날에 걸친 근무를 어느 날로 세나. */
public enum NightAttribution {
    /** 시작한 날. 근무표에 적힌 날이다. */
    START_DAY,
    /** 끝난 날. 퇴근한 날이다. */
    END_DAY,
    /** 자정에서 잘라 두 날에 나눠 넣는다. */
    SPLIT
}
```

```java
/**
 * 끝난 날.
 *
 * <p>자정 정각에 끝나면 그 앞날로 본다. 00:00 에 끝난 근무를 다음 날
 * 근무로 세면 <b>0 분짜리 근무일</b>이 하나 생긴다.
 */
public LocalDate endDate() { ... }
```

- 왜 "두 칸이 나올 수 있는 것은 SPLIT 뿐"인가?
- 밤을 안 넘긴 근무(09:00~18:00)에서 세 규칙은 어떻게 되는가?
- 밤을 안 넘긴 경우를 먼저 걸러내지 않으면 SPLIT 에서 무슨 일이 생기는가?
- SPLIT 에서 자르는 자정은 정확히 어느 시각인가 — 시작일 기준인가 종료일 기준인가?
- 3/29(일) 22:00 ~ 3/30(월) 06:00 의 세 규칙 결과를 분으로 각각 적을 수 있는가?
- 18:00~24:00 근무의 `endDate()` 는 왜 다음 날이 아닌가?
- 자정 정각 종료를 다음 날로 세면 "0분짜리 근무일"이 무엇을 망가뜨리는가?
- 세 규칙 중 근무 시간의 **총합**이 달라지는 규칙이 있는가?

#### 3. TODO 3 — weeks (주별 근무 시간과 초과근무)

```java
/**
 * 주마다의 근무 시간과 초과근무.
 *
 * <p>근무가 없는 주는 안 낸다. 0 짜리 주가 끼면 "이번 달에 몇 주를
 * 일했나" 가 안 맞는다.
 */
public List<Week> weeks(
        List<Shift> shifts, WeekStart weekStart, NightAttribution attribution) {
    throw new UnsupportedOperationException("TODO 3");
}
```

```java
/**
 * 한 주의 집계.
 *
 * @param weekStart 그 주의 첫날
 * @param minutes 그 주에 잡힌 근무 시간
 * @param overtimeMinutes 상한을 넘은 시간
 */
public record Week(LocalDate weekStart, long minutes, long overtimeMinutes) {}
```

- 이 함수는 TODO 1·TODO 2 를 어떤 순서로 부르는가?
- 초과근무는 어떤 식으로 구하는가 — 음수가 안 나오게 하려면?
- 왜 주별 합계를 정렬된 맵(`TreeMap`)에 담는가 — 무엇을 보장하려는 것인가?
- "근무가 없는 주는 안 낸다"를 지키려면 빠뜨린 주를 채우지 **않는** 것으로 충분한가?
- 3월 첫 주와 셋째 주에만 하루씩 일하면 `weeks` 의 크기는 몇이어야 하는가?
- 빈 주를 0으로 채우면 어떤 지표가 정확히 어떻게 틀어지는가?
- 월~일 9시간씩 7일(63시간) 일한 사람의 초과근무는 월요일 시작과 일요일 시작에서 각각 몇 시간인가?
- 월~금 45시간 + 3/29(일) 밤 근무 8시간의 초과근무는 세 귀속 규칙에서 각각 몇 시간인가?
- 초과근무 합이 갈리는데 총 근무 시간은 안 갈리는 이유는 무엇인가?
- 근무가 하나도 없으면 `weeks` 는 무엇을 돌려주는가?

#### 4. TODO 4 — longestRun (제일 길게 이어 일한 날 수)

```java
/**
 * 제일 길게 이어 일한 날 수.
 *
 * <p>달력으로 세면 근무가 걸친 날을 다 채운 뒤 이어진 구간을 잰다.
 * 밤을 넘긴 근무 하나가 <b>이틀을 채운다.</b>
 *
 * <p>쉰 시간으로 세면 근무 사이가 24 시간 이상이면 끊는다. 그리고
 * 끊기지 않은 묶음이 걸친 날 수를 잰다.
 */
public int longestRun(List<Shift> shifts, ConsecutiveRule rule) {
    throw new UnsupportedOperationException("TODO 4");
}
```

```java
/** 연속 근무를 어떻게 세나. */
public enum ConsecutiveRule {
    /** 근무가 걸친 달력 날짜로. 밤을 넘긴 근무는 두 날을 채운다. */
    BY_DATE,
    /** 실제로 쉰 시간으로. 24 시간 이상 쉬면 끊는다. */
    BY_GAP
}
```

- BY_DATE 는 무엇을 모아서 무엇을 재는가?
- BY_GAP 은 무엇을 기준으로 끊고, 끊기지 않은 묶음의 **무엇**을 재는가?
- 근무를 왜 정렬해야 하는가 — 정렬 기준이 시작 시각 하나면 왜 모자라는가?
- BY_GAP 에서 "직전 근무의 끝"이 아니라 "지금까지 본 근무 중 제일 늦은 끝"을 들고 다녀야 하는 이유는 무엇인가?
- 월 09:00~18:00 + 화 20:00~수 04:00 은 두 규칙에서 각각 며칠인가?
- 그 사람이 실제로 쉰 시간은 몇 시간인가?
- 밤 근무 3연속(월·화·수 22:00~06:00)에서 두 규칙이 같아지는 이유는 무엇인가?
- 하루를 통째로 쉬면 두 규칙이 같아지는 이유는 무엇인가?
- 두 규칙이 갈리는 경우는 결국 어떤 모양의 근무표뿐인가?
- 딱 24시간을 쉬면 BY_GAP 과 BY_DATE 는 각각 며칠인가?
- 근무가 하나도 없으면 왜 1이 아니라 0을 내야 하는가?

### B. 개념

#### 5. 함정 (원본 README "함정")

- README 가 꼽은 함정 넷은 각각 무엇인가?
- 자정 정각에 끝난 근무를 다음 날 것으로 보면 연속 근무 일수가 어떻게 되는가?
- 일요일 시작에서 일요일을 앞주로 보내면 어떤 증상이 생기는가?
- 근무가 없는 주를 0으로 채우면 무엇이 안 맞는가?
- "딱 24시간 쉰 사람을 어느 쪽으로 놓을지 안 정하는 것"이 왜 특히 위험한가?
- 네 함정 중 **예외 없이 조용히 틀린 값이 나오는** 것은 어느 것들인가?

#### 6. 측정 하나 — 주 4일인데 달력으로 세면 6일 연속을 넘긴다

```text
주 상한 40 시간, 12 주, 300 명. 주간 9 시간, 야간 8 시간. 야간 30 %.

| 주당 근무일 | 달력으로 | 쉰 시간으로 |
|---|---|---|
| 4 일 | 182 / 300 | 6 / 300 |
| 5 일 | 293 / 300 | 49 / 300 |
| 6 일 | 300 / 300 | 215 / 300 |
| 7 일 | 300 / 300 | 299 / 300 |
```

- 주 4일 근무에서 달력으로 세면 300명 중 몇 명이 6일 연속을 넘기는가?
- 같은 사람들을 쉰 시간으로 세면 몇 명인가?
- 왜 이렇게 벌어지는가 — 야간 근무 하나가 무엇을 하는가?
- 화 20:00~수 04:00 근무자가 실제로 쉰 시간은 몇 시간일 수 있는가?
- 주당 근무일이 늘수록 두 규칙이 가까워지는 이유는 무엇인가?
- "주 4일만 일하는 사람의 3분의 2가 6일 연속 초과로 잡힌다"는 결과를 그대로 두면 운영에 무슨 일이 생기는가?

#### 7. 측정 둘 — 야간 근무가 없으면 세 귀속 규칙이 완전히 같다

```text
12 주, 주 6 일, 300 명. 월요일 시작.

| 야간 비율 | 시작일로 | 종료일로 | 나눠서 |
|---|---|---|---|
| 0 % | 3,024,000 | 3,024,000 | 3,024,000 |
| 10 % | 2,896,260 | 2,886,180 | 2,888,700 |
| 30 % | 2,638,380 | 2,602,860 | 2,611,740 |
| 100 % | 1,728,000 | 1,602,720 | 1,634,040 |
```

- 야간 0%에서 세 규칙의 초과근무가 완전히 같은 이유는 무엇인가?
- 개발할 때 만드는 근무표가 대개 어떤 모양이길래 이 사실이 문제가 되는가?
- 야간 비율이 오를수록 초과근무 총량이 **줄어드는** 이유는 무엇인가 — 이것이 착시인가 실제인가?
- 야간 100%에서 세 규칙의 값은 각각 얼마인가?
- 이 표는 테스트 데이터를 어떻게 만들어야 한다고 말하는가?

#### 8. 측정 셋 — 야간이 없어도 주 시작 갈래는 그대로 남는다

```text
야간 0 %, 주 5 일, 300 명.

- 여섯 조합을 다 견주면 어긋난 것   900 / 1,500
- 야간 규칙 셋만 견주면 어긋난 것     0 /   600
```

- 야간 근무가 한 건도 없는데 900건이 어긋나는 이유는 무엇인가?
- 같은 데이터에서 야간 규칙 셋만 비교하면 왜 0건인가?
- 두 갈래가 "다른 조건에서 나타난다"는 말은 무슨 뜻인가?
- 야간 귀속 갈래가 보이려면 무엇이 필요한가?
- 주 시작 갈래가 보이려면 무엇이 필요한가?
- "야간 근무 넣어서 테스트해봤다"로 왜 절반만 덮이는가?
- 측정에서 근무 요일을 무작위로 뽑는 이유는 무엇인가 — 요일을 고정하면 무엇이 안 보이는가?

#### 9. 측정 넷 — 여섯 조합의 초과근무가 2,284시간 벌어진다

```text
12 주, 주 6 일, 야간 30 %, 300 명.

| | 초과근무(분) | 초과한 주 |
|---|---|---|
| 월요일 + 시작일로 | 2,638,380 | 3,600 |
| 월요일 + 종료일로 | 2,602,860 | 3,599 |
| 일요일 + 종료일로 | 2,509,980 | 3,438 |
| 일요일 + 나눠서 | 2,501,340 | 3,442 |
```

- 여섯 조합은 어떻게 만들어지는가?
- 제일 많은 조합과 제일 적은 조합은 각각 무엇이고 차이는 몇 분인가?
- 그 차이는 300명 기준 한 사람당 몇 시간인가?
- "초과한 주"의 수는 최대 몇 주 차이가 나는가?
- 초과근무 분(分)의 차이와 "초과한 주" 수의 차이가 왜 따로 봐야 하는 값인가?
- 이 표를 근로계약서 없이 개발자가 골라도 되는가 — 왜 그런가?

#### 10. 변종 검증에서 고친 것 (원본 README)

```text
구현에서 하나를 고쳤다. crossesMidnight 이 원시 종료 날짜를 봤다.
18:00~24:00 근무가 "두 날에 걸친 것" 으로 잡혀 나눠 넣기에서 뒷날에 0 분이
들어가고 그 0 분이 앞날 값을 덮어썼다. 보정한 종료일을 보도록 고쳤다.

테스트 하나를 더 넣었다. 딱 24 시간을 쉰 경우를 안 만들어서
>= 24 를 > 24 로 바꿔도 통과했다. 그 경계가 이 규칙의 전부인데도.
```

- `crossesMidnight` 이 원시 종료 날짜를 보면 18:00~24:00 근무에서 정확히 어떤 일이 일어나는가?
- "뒷날의 0분이 앞날 값을 덮어썼다"는 것은 자료구조 관점에서 무슨 일인가?
- 고친 방법은 무엇인가 — 어느 값을 보게 했는가?
- 딱 24시간 경계 테스트가 없으면 `>= 24` 를 `> 24` 로 바꿔도 통과하는 이유는 무엇인가?
- "그 경계가 이 규칙의 전부"라는 말은 무슨 뜻인가?
- 이 두 사건은 테스트를 어떻게 설계하라고 말하는가?

#### 11. 생각해볼 것 (원본 README 이관)

- 야간 근무를 어느 날로 세는지가 근로계약서에 적혀 있나?
- 주 시작 요일을 바꾸면 이미 지급한 수당은 어떻게 하나?
- 연속 근무를 달력으로 세는 화면과 쉰 시간으로 세는 화면이 같이 있으면 무슨 일이 생기나?
- 시차가 있는 지역의 근무는 어느 자정으로 자르나?

#### 12. 연결

- 이 박스가 세는 것("여섯 조합의 초과근무 차이")은 01-parking-fee 가 세던 것과 무엇이 같고 무엇이 다른가?
- "하루의 기준"(01-parking-fee)과 "주의 기준"(여기)은 같은 종류의 문제인가?
- 결정을 enum 으로 올리는 방식과 record 정책값으로 올리는 방식은 어떤 차이가 있는가?
- 이 박스의 "자정 정각 경계"는 다른 어느 박스와 같은 자리인가?
- 여기서 얻은 "갈래마다 보이는 조건이 다르다"는 교훈은 실무의 어떤 테스트 관행에 대응하는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
