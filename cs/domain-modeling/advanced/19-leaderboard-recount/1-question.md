# domain-modeling-advanced/19-leaderboard-recount — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 상황 다섯 줄 (원본 README)

```text
  3/31 23:00  a 가 10,000 참여
  3/31 22:00  b 가  8,000 참여
  4/01 00:00  이벤트 종료, 순위 발표.  1 등 a
  4/03 10:00  a 의 참여가 취소됨

  4/05 에 다시 계산하면  1 등 b
```

- "두 답 중 어느 것도 틀리지 않았다"는 말은 무슨 뜻인가?
- 그럼 무엇이 다른 것인가?
- "참여 금액 순으로 상위 3 명"이 **안 말해주는 결정** 셋은 무엇무엇인가?
- 이 셋이 서로 간섭한다는 말은 무엇을 뜻하는가?
- 셋을 코드 안에 숨기지 않고 enum 세 개로 올리면 무엇을 할 수 있게 되는가?
- 이 문제가 주차 요금(01번)의 "하루의 기준" 문제와 같은 자리인 이유는 무엇인가?

#### 1. TODO 1 — counts (그 참여를 세나)

```java
/**
 * 그 참여를 세나.
 *
 * <p>종료 시각 뒤의 참여는 어느 규칙에서도 안 센다. 이벤트 밖이다.
 *
 * <p>종료 시점 기준이면 종료 전에 들어온 취소만 본다. 종료 뒤에 들어온
 * 취소는 그 시점에는 아직 없던 일이다.
 */
public boolean counts(Contribution contribution, LocalDateTime endsAt, Snapshot snapshot) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- 판정 순서 세 단계는 무엇인가?
- 참여 시각이 종료 시각과 **정확히 같으면** 세는가 안 세는가?
- 그 판정을 어떤 한 줄로 쓰는가?
- "4 월 1 일까지"와 "4 월 1 일 0 시까지"가 왜 다른 말인가?
- 종료 정각을 포함으로 잡으면 실무에서 왜 하필 크게 터지는가?
- 안 취소된 참여는 두 기준에서 답이 같은가?
- `AS_OF_NOW` 에서 취소된 참여를 세는 경우가 있는가?
- `AS_OF_END` 는 취소된 참여를 언제 세는가?
- 취소 시각이 종료 시각과 **정확히 같으면** `AS_OF_END` 는 세는가?
- 두 기준이 갈리는 구간은 정확히 어디인가?
- 그 구간이 개발할 때 만드는 자료에 잘 안 생기는 이유는 무엇인가?
- 이 함수가 `Contribution` 안이 아니라 `Leaderboard` 안에 있는 이유는 무엇인가?

#### 2. TODO 2 — standings (사람마다의 집계)

```java
/**
 * 사람마다의 집계.
 *
 * <p>참여를 시각 순으로 훑는다. 안 훑으면 도달 시각이 입력 순서를 탄다.
 * 같은 시각끼리는 안 가른다. 어느 쪽을 나중으로 놓아도 도달 시각이 같다.
 *
 * <p>합이 0 인 사람은 안 낸다. 전부 취소한 사람이 순위표에 남으면 안 된다.
 */
public List<Standing> standings(
        List<Contribution> contributions, LocalDateTime endsAt, Snapshot snapshot) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- `Standing` 의 세 칸은 각각 무엇인가?
- `reachedAt`(도달 시각)의 정의는 무엇인가 — 첫 참여인가 마지막 참여인가?
- 시각 순으로 안 훑으면 정확히 무엇이 어떻게 틀어지는가?
- 입력이 같은데 결과가 달라지는 것이 왜 버그인가?
- 같은 시각의 두 참여를 굳이 안 가르는 이유는 무엇인가?
- 합계와 도달 시각을 왜 `Map` 두 개로 따로 모으는가?
- `totals.merge(userId, amount, Long::sum)` 는 무슨 일을 하는가?
- 전부 취소한 사람을 순위표에 남기면 무슨 일이 생기는가?
- 그런데 impl 코드에는 `total == 0` 을 걸러내는 `if` 가 없다 — 그래도 되는 이유는 무엇인가?
- `LinkedHashMap` 을 쓴 이유는 무엇인가?
- `standings` 가 돌려주는 목록은 금액 순으로 정렬돼 있는가?

#### 3. TODO 3 — rank (등수 매기기)

```java
/**
 * 등수를 매긴다.
 *
 * <p>금액이 큰 순으로 세우고 동점은 규칙대로 가른다. 안 가르는 규칙에서도
 * 목록 순서는 정해야 하므로 사용자 번호로 세운다. 그래야 같은 자료가
 * 같은 순서를 낸다.
 *
 * <p>가른 동점은 더 이상 동점이 아니다. 그래서 가르는 규칙에서는 등수가
 * 1 2 3 4 로 쭉 간다. 등수 방식은 <b>안 가를 때만</b> 일을 한다.
 */
public List<Row> rank(List<Standing> standings, TieBreak tieBreak, RankStyle rankStyle) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- 정렬 키는 순서대로 무엇무엇인가?
- 금액 비교에 `.reversed()` 를 붙이는 이유는 무엇인가?
- `EARLIEST` 와 `LATEST` 는 comparator 를 각각 어떻게 만드는가?
- `NONE` 인데도 마지막에 사용자 번호로 한 번 더 세우는 이유는 무엇인가?
- 그 사용자 번호 정렬을 빼면 화면에서 무엇이 보이는가?
- `tied` 판정의 조건 세 개는 무엇인가?
- `tieBreak == NONE` 을 `tied` 조건에 넣지 않으면 무슨 일이 생기는가?
- `COMPETITION` 은 등수를 어떤 식으로 계산하는가?
- `DENSE` 는 어떤 식으로 계산하는가?
- a 10,000 · b 10,000 · c 5,000 · d 3,000 에서 네 조합의 등수는 각각 무엇인가?
- 동점을 가르면 왜 두 등수 방식이 통째로 같아지는가?
- 그것이 설정 화면 관점에서 무슨 뜻인가?
- `List.copyOf(rows)` 로 돌려주는 이유는 무엇인가?

#### 4. TODO 4 — winners (상위 몇 명)

```java
/**
 * 상위 몇 명.
 *
 * <p>동점을 안 가르면 상위 3 명이 네 명이 될 수 있다. 등수가 3 이하인
 * 사람을 다 넣기 때문이다. 그리고 경품은 세 개뿐이다.
 */
public List<Row> winners(List<Row> ranked, int topN) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- `winners` 의 선별 규칙 한 줄은 무엇인가?
- 왜 "앞에서 topN 개를 자른다"로 쓰지 않는가?
- `topN < 1` 을 거부하는 이유는 무엇인가 — `topN == 0` 을 빈 목록으로 돌려주면 무엇이 조용해지는가?
- a·b·c 가 셋 다 10,000 일 때 `NONE` 으로 상위 2 명을 뽑으면 몇 명이 나오는가?
- a b 동점, c d 동점, e 인 다섯 명에서 "3 등까지"는 표준 경쟁과 조밀이 각각 몇 명인가?
- 정원이 넘쳤을 때의 처리(잘라낼지 경품을 더 줄지)는 왜 이 함수 밖의 일인가?

#### 5. Contribution — 주어진 계약

```java
public Contribution {
    if (id == null || id.isBlank()) { ... }
    if (userId == null || userId.isBlank()) { ... }
    if (at == null) { ... }
    if (amount <= 0) { throw new IllegalArgumentException("금액이 0 이하다: " + amount); }
    if (cancelledAt != null && cancelledAt.isBefore(at)) {
        throw new IllegalArgumentException("참여하기 전에 취소될 수 없다: " + id);
    }
}
```

- 취소를 `boolean cancelled` 가 아니라 `LocalDateTime cancelledAt` 으로 둔 이유는 무엇인가?
- `boolean` 으로 뒀다면 이 챕터의 문제가 어떻게 되는가?
- `amount <= 0` 을 생성자에서 막아두면 `standings` 의 어떤 코드가 필요 없어지는가?
- `cancelledAt < at` 을 막는 것은 어떤 종류의 불변식인가?
- `cancelled()` 메서드의 javadoc 이 "**지금 기준으로** 취소됐나"인 것은 무슨 뜻인가?

### B. 개념

#### 6. 측정 하나 — 취소 비율 (원본 README "측정이 알려준 것")

```text
  사용자 50, 참여 300, 100 회. 종료 시점 기준과 지금 기준을 견줬다.
```

- 취소가 0 % 일 때 상위 3 명이 바뀐 회차와 1 등이 바뀐 회차는 각각 몇인가?
- 취소가 5 %(절반이 늦은 것)일 때는 각각 몇인가?
- 취소가 20 %(전부 늦은 것)일 때는 각각 몇인가?
- 취소가 0 % 면 두 기준이 항상 같은 답을 내는 이유는 무엇인가?
- "개발 환경이 그렇다"는 말이 왜 무서운 말인가?

#### 7. 측정 둘 — 금액 단위 (원본 README)

```text
  금액 단위    넘친 회차   초과 인원   동점 쌍
```

- 1 원 단위일 때 넘친 회차·초과 인원·동점 쌍은 각각 몇인가?
- 500,000 원 단위일 때는 각각 몇인가?
- 측정 코드는 금액을 어떻게 굵게 만드는가 — 곱하나 깎나?
- 곱하는 방식이 안 되는 이유는 무엇인가?
- 500,000 원 단위에서 실제로 나올 수 있는 참여 금액은 몇 가지인가?
- 그렇다면 그때 두 사람의 합계가 같아지는 조건은 무엇인가?
- "1 원 단위면 동점 규칙을 안 정해도 된다"가 왜 함정인가?
- 어떤 날 이 함정이 터지는가?

#### 8. 측정 셋 — 시점 vs 동점 규칙 (원본 README)

```text
  종료 시점 + 먼저 도달 + 표준 경쟁을 기준으로 놓고 열두 조합을 견줬다.
```

- 열두 조합은 무엇을 곱해서 열둘인가?
- 같은 시점 안에서 동점 규칙만 바꾼 조합은 최대 몇 회차나 갈렸는가?
- 시점을 바꾼 조합은 최소 몇 회차나 갈렸는가?
- "열여섯 배"는 어느 수를 어느 수로 나눈 것인가?
- 이 측정이 주는 교훈 한 문장은 무엇인가?
- 회의에서 오래 얘기하는 것과 실제로 답을 바꾸는 것이 어긋나는 이유는 무엇인가?

#### 9. 측정 넷 — 수상자 305 명 (원본 README)

- 100 회에 상위 3 명씩이면 정원은 몇 명인가?
- 가르면 몇 명, 안 가름 + 표준 경쟁은 몇 명, 안 가름 + 조밀은 몇 명인가?
- 갈래 두 개가 "곱해진다"는 말은 무슨 뜻인가?
- 5 명 초과가 경품 예산에서 왜 무시 못 할 수인가?

#### 10. 측정 다섯 — 열두 조합이 통째로 같아지는 자료 (원본 README)

- 어떤 조건에서 열두 조합이 전부 같은 답을 내는가?
- 그때 비교 횟수와 어긋난 횟수는 각각 몇인가?
- 1,100 이라는 수는 어떻게 나온 것인가?
- 개발할 때 만드는 자료가 왜 하필 그 모양인가?
- "갈래가 보이려면 취소와 동점을 일부러 넣어야 한다"를 테스트 자료 만들기 규칙으로 바꾸면 어떤 문장이 되는가?
- 측정 테스트가 `new Random(31)` 로 씨앗을 고정한 이유는 무엇인가?

#### 11. 함정 (원본 README "함정")

- README 가 꼽은 함정 넷은 각각 무엇인가?
- 넷 중 "말없이 바뀐다"는 표현이 붙은 것은 어느 것인가?
- 0 원짜리 줄이 등수를 차지한다는 것은 구체적으로 어떤 화면인가?
- "상위 3 명이 세 명이 아니게 된다"가 왜 버그로 안 보이고 넘어가는가?

#### 12. 변종 검증에서 고친 것 (원본 README)

- 고친 것은 무엇이었는가?
- 그 타이브레이커가 죽은 코드였던 이유를 한 문장으로 말하면?
- 그런데 **시각 정렬 자체는** 왜 지우면 안 되는가?
- 죽은 코드를 지우고 주석을 남기는 것이 지우기만 하는 것보다 나은 이유는 무엇인가?
- 이 챕터에 "죽은 것"이 두 개 나온다 — 나머지 하나는 무엇인가?

#### 13. 생각해볼 것 (원본 README 이관)

- 발표한 순위를 나중에 고칠 때 누구에게 무엇을 알리나?
- 종료 뒤 취소를 막으면 이 문제가 없어지나?
- 그럼 청약철회는 어떻게 하나?
- 동점자에게 경품을 하나씩 더 주는 것과 추첨하는 것 중 어느 쪽이 싼가?
- 도달 시각을 마지막 참여로 잡는 것과 처음 참여로 잡는 것은 무엇이 다른가?

#### 14. 연결

- 이 챕터가 세는 것("답이 갈리는 회차 수")은 알고리즘 트랙이 세던 것과 무엇이 다른가?
- `Snapshot` 2 택은 데이터베이스의 어떤 개념과 같은 자리인가?
- "규칙을 켜면 다른 규칙이 죽는다"는 패턴을 이 노트의 다른 챕터에서 본 적이 있는가?
- 종료 정각 경계 문제는 앞의 어느 챕터와 같은 자리인가?
- 이 챕터의 교훈을 실무 코드 리뷰 체크리스트 한 줄로 만들면 무엇인가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
