# domain-modeling-advanced/26-photocard-set — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 말 한 줄 (원본 javadoc)

```text
  "N 종 랜덤, 여러 장 사면 세트 보장"

  이 말에 정해지지 않은 것이 셋이다.

    세트를 어떻게 보장하나   먼저 한 장씩 깔고 나머지 랜덤인가 랜덤 뒤에 바꾸나
    한 사람이 같은 종류를    두 장 받아도 되나
    나머지는 어떻게 고르나   무작위인가 재고 많은 것부터인가
```

- 이 한 줄이 **안 말해주는** 결정 셋은 각각 무엇인가?
- 셋이 "서로 간섭한다"는 말은 구체적으로 무슨 뜻인가?
- 세 축을 각각 enum 으로 올리면 몇 가지 조합이 나오는가?
- 축을 값으로 올리지 않고 그냥 하나 골라 구현하면 무엇을 잃는가?
- "보장은 앞사람 것이다"는 재고가 어떤 상태일 때 드러나는가?
- 5종 1장씩(총 5장)인데 5장씩 사는 사람이 둘이면 둘째 사람은 몇 장을 받는가?

#### 1. TODO 1 — choices (줄 수 있는 종류들)

```java
/**
 * 그 사람에게 줄 수 있는 종류들.
 *
 * <p>중복을 막는 규칙이면 이미 받은 종류를 뺀다. 뺀 뒤에 비면 못 준다.
 */
public List<String> choices(Stock stock, List<String> already, WithinBuyer within) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- "줄 수 있는 종류"는 어떤 두 조건을 다 만족하는 종류인가?
- `Stock.available()` 이 이미 걸러주는 조건은 무엇인가?
- 중복 금지에서 빼는 기준은 "재고에 있나"가 아니라 무엇인가?
- 이 목록이 비었는데도 한 장을 주면 어떤 두 가지가 깨지는가?
- 반환 목록의 **순서**가 왜 중요한가?

#### 2. TODO 2 — giveOne (한 장 주기)

```java
/**
 * 한 장 준다. 못 주면 null 이다.
 *
 * <p>재고 많은 것부터 고르는 규칙에서도 같은 장수끼리는 종류 이름으로 가른다.
 * 안 가르면 같은 seed 로도 다른 배분이 나온다.
 */
public String giveOne(
        Stock stock, List<String> already, WithinBuyer within, FillRule fillRule,
        Random random) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 못 줄 때 예외가 아니라 `null` 을 주는 이유는 무엇인가?
- `RANDOM` 과 `MOST_STOCK` 은 각각 후보 중 무엇을 고르는가?
- `MOST_STOCK` 에서 재고가 같은 종류가 여럿이면 무엇으로 가르는가?
- 타이브레이크를 안 두면 정확히 무엇이 깨지는가 — "같은 seed 인데 다른 결과"가 왜 문제인가?
- 재고 A=10, B=1, C=1 에서 세 장을 `MOST_STOCK` 으로 주면 무엇을 주는가?
- 카드를 고른 뒤 `stock.take` 를 빼먹으면 무슨 일이 나는가?
- `MOST_STOCK` 일 때도 `Random` 을 인자로 받는 이유는 무엇인가?

#### 3. TODO 3 — distribute (주문 순서대로 배분)

```java
/**
 * 주문 순서대로 배분한다.
 *
 * <p>먼저 까는 규칙은 <b>살 수 있는 만큼</b> 종류를 한 장씩 준다. 산 장수가
 * 종류 수보다 적으면 세트가 애초에 불가능하다.
 *
 * <p>랜덤 뒤에 바꾸는 규칙은 다 준 뒤에 중복된 장을 재고에 돌려놓고
 * 빠진 종류를 가져온다. 돌려놓을 중복도 없고 가져올 종류도 없으면 못 바꾼다.
 */
public Distribution distribute(
        List<Order> orders, Stock stock, SetGuarantee guarantee, WithinBuyer within,
        FillRule fillRule, long seed) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- `Random` 은 사람마다 새로 만드는가, 전체에서 하나만 만드는가?
- `SET_FIRST` 에서 "살 수 있는 만큼"을 안 지키면(3장 산 사람에게 5장을 깔면) 무엇이 깨지는가?
- `SET_FIRST` 의 먼저 깔기 루프는 재고 없는 종류를 만나면 어떻게 하는가?
- `SWAP_AFTER` 의 교체는 배분 **전**인가 **후**인가, 왜 그런가?
- 교체가 성립하려면 필요한 두 가지는 무엇인가?
- `shortage` 는 어떤 식으로 세는가?
- 재고가 A만 5장인 상태에서 `SWAP_AFTER` 로 5장을 주면 결과가 무엇인가?
- `given` 을 `List.copyOf` 로 굳혀서 넣는 이유는 무엇인가?

#### 4. TODO 4 — completeSets (세트를 채운 사람 수)

```java
/**
 * 세트를 채운 사람 수.
 *
 * <p>산 장수가 종류 수보다 적은 사람은 애초에 세트가 불가능하다.
 * 그런 사람까지 분모에 넣으면 보장률이 낮게 나온다.
 */
public int completeSets(Distribution distribution, List<Order> orders, int kindCount) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 세트를 채웠다는 판정은 무엇을 무엇과 비교하는가?
- 받은 **장수**가 아니라 받은 **종류 수**를 보는 이유는 무엇인가?
- 여기에 "종류 수보다 적게 산 사람 거르기" 필터를 넣으면 왜 죽은 코드가 되는가?
- 같은 필터가 `eligibleForSet` 에서는 왜 본체인가?
- 보장률의 분자와 분모는 각각 무엇인가?

### B. 개념

#### 5. 측정 — 발견 하나: 이름이 효과를 안 말해준다 (원본 README)

```text
  8 종 x 60 장, 구매자 100 명, 100 회. 세트 가능자 2,945 명.
```

- "세트 보장"이라는 이름의 조합(먼저 깔기 + 무작위)은 세트를 몇 건 만들었는가?
- 보장 없이 나머지 고르기만 `MOST_STOCK` 으로 바꾸면 세트가 몇에서 몇으로 뛰는가?
- 열두 조합 중 세트가 제일 많은 조합은 무엇이고 몇 건인가?
- 중복을 막으면 세트는 늘고 무엇이 3배가 되는가?
- "설정 이름이 효과를 안 말해준다"가 실무에서 왜 위험한가?

#### 6. 측정 — 발견 둘: 뒤 50명 (원본 README)

```text
  8 종 x 30 장에서
                 앞 50 명        뒤 50 명
   먼저 깔기      795 / 1,488      0 / 1,457
   랜덤뒤교체   1,130 / 1,488      0 / 1,457
   보장없음       102 / 1,488      0 / 1,457
```

- 뒤 50명의 세트 수는 세 규칙에서 각각 얼마인가?
- 합계로 보면 보장률이 몇 퍼센트로 보이고, 앞뒤를 나눠 보면 각각 몇 퍼센트인가?
- "평균이 분포를 가린다"를 이 표로 한 줄로 설명하면?
- 앞뒤를 나눠 재지 않았다면 어떤 잘못된 결론에 도달하는가?

#### 7. 측정 — 발견 셋: 못 준 장수 (원본 README)

```text
   종류당 장수   못 준 장수 (세 규칙 전부)
    60 장         3,992
    40 장        19,566
    30 장        27,566
    25 장        31,566
```

- 못 준 장수가 세 보장 규칙에서 "글자 그대로 같다"는 것은 무엇을 뜻하는가?
- 보장 규칙이 바꾸는 것과 안 바꾸는 것은 각각 무엇인가?
- "보장을 켜서 재고가 모자란다"는 말이 왜 틀렸는가?
- 중복 금지는 왜 못 준 장수를 바꾸는데 보장 규칙은 안 바꾸는가?

#### 8. 측정 — 발견 넷: 큰손이 없는 자료 (원본 README)

```text
  전부 1~4 장만 사는 자료. 8 종이니 아무도 세트를 못 만든다.
    세트 가능자   열두 조합 전부 0
    중복 장수     보장없음 + 허용 + 무작위에서만 2,925, 나머지 열한 조합은 0
```

- 세트가 아무도 불가능한 자료에서 보장 규칙이 여전히 바꾸는 것은 무엇인가?
- 네 장을 산 사람이 `SET_FIRST` 에서 받는 카드는 무엇인가?
- 중복 장수가 0이 아닌 조합이 딱 하나뿐인 이유는 무엇인가?
- "이름만 봐서는 알 수 없다"는 것이 여기서 뜻하는 바는 무엇인가?

#### 9. 함정 (원본 README)

- README가 꼽은 함정 넷은 각각 무엇인가?
- 재고가 같을 때 순서를 안 가르면 고객 문의에 무엇을 못 하게 되는가?
- 줄 수 있는 종류가 없는데 주면 어떤 두 불변식이 깨지는가?
- 종류보다 적게 산 사람을 분모에 넣으면 숫자가 어느 쪽으로 틀리는가?

#### 10. 변종 검증에서 고친 것 (원본 README)

- 죽은 코드로 드러난 것은 무엇이었고, 왜 지워도 통과했는가?
- 약한 테스트로 드러난 것은 무엇이었고, 무엇 때문에 우연히 통과했는가?
- `Map.of` 대신 무엇으로 바꾸니 잡혔는가?
- "지워도 테스트가 통과한다"가 알려주는 두 가지 가능성은 무엇인가?

#### 11. 생각해볼 것 (원본 README 이관)

- "세트 보장"을 몇 번째 구매자까지 보장하나, 그 수를 공개하나?
- 재고 편성(종류마다 몇 장)을 누가 정하나?
- 중복을 막아서 못 준 장수가 생기면 환불인가 다음 회차인가?
- 뒤에 산 사람에게 앞사람과 같은 확률을 주려면 무엇을 바꿔야 하나?

#### 12. 연결

- `seed` 로 재현 가능성을 확보하는 방식은 실무의 어떤 관행과 같은가?
- 이 박스의 "분모를 어떻게 정의하나"는 다른 어느 주제와 같은 자리인가?
- "정책을 값으로 올려 조합을 전부 돌려본다"는 방식은 앞의 어느 박스와 같은 방식인가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
