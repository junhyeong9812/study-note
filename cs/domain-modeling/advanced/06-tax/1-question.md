# domain-modeling-advanced/06-tax — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 주문 한 건 (원본 README)

```text
  노트북 1,100,000 과세 / 책 30,000 면세 / 수출품 50,000 영세율, 할인 100,000

  할인을 어디서 빼나
    금액 비례   과세 93,220   면세 2,542    영세율 4,238
    과세 먼저   과세 100,000  면세 0        영세율 0
    면세 먼저   과세 70,000   면세 30,000   영세율 0
```

- "부가세 10 퍼센트"라는 한 줄이 **안 말해주는** 결정 셋은 무엇인가?
- 세 배분 방식 모두에서 고객이 낼 돈이 1,083,000원으로 같은 이유는 무엇인가?
- 그렇다면 무엇이 갈리는가?
- 고객이 낼 돈이 안 갈리는 결정이 왜 위험한가?
- 이 결정의 오류는 어느 단계에서 드러나는가?
- "면세 먼저"가 면세 상품이 작을 때 사실상 무엇과 같아지는가?
- 세 배분을 enum 으로 올린 것과 하나 골라 `for` 문에 박아 넣는 것은 무엇이 다른가?

#### 1. TODO 1 — share (비례배분 한 조각)

```java
private long share(long amount, long part, long whole) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- 곱하기를 먼저 하는가 나누기를 먼저 하는가?
- 나누기를 먼저 하면 할인 1원을 999,999 대 1로 나눌 때 무슨 일이 생기는가?
- `whole == 0` 을 따로 막지 않으면 어떤 주문에서 터지는가?
- 그 예외가 하필 어느 자리에서 나길래 문제인가?
- 곱하기를 먼저 하면 중간 자릿수(소수 10자리)를 왜 안 남겨도 되는가?
- 반올림을 두 번 하는 것과 한 번 하는 것은 무엇이 다른가?

#### 2. TODO 2 — allocate (할인 배분)

```java
/**
 * 할인을 구분별로 나눈다.
 *
 * <p>어느 구분에서 빼든 그 구분의 금액을 넘을 수 없다.
 * 넘기면 남는 만큼을 다음 구분으로 흘려보낸다.
 *
 * @return 과세, 면세, 영세율 순서의 할인액
 */
private long[] allocate(TaxOrder order, DiscountAllocation allocation) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 비례배분에서 세 몫을 각각 반올림하면 합이 왜 어긋나는가?
- 마지막 몫을 빼기로 구하면 무엇을 얻고 무엇을 잃는가?
- 세 몫 중 어느 것을 "마지막"으로 삼았고 그 선택이 무엇을 바꾸는가?
- 흘려보내기(cascade)를 안 하면 고객이 낼 돈이 왜 늘어나는가?
- 할인 1,150,000원을 "과세 먼저"와 "면세 먼저"로 빼면 결과가 왜 똑같아지는가?
- 할인이 0원인 주문으로 시험하면 이 함수의 무엇이 안 보이는가?
- `TaxOrder` 생성자가 `discount > total` 을 막는데도 흘려보내기가 필요한 이유는 무엇인가?

#### 3. TODO 3 — taxableShipping (배송비의 과세분)

```java
/** 배송비 중 과세인 부분. */
private long taxableShipping(TaxOrder order, ShippingTax shippingTax) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- 세 규칙(전액 과세·비례·있으면 전액)은 각각 무엇을 계산하는가?
- 면세 상품만 산 주문에서 세 규칙의 배송비 부가세는 각각 얼마인가?
- `hasTaxable()` 은 영세율을 과세로 세는가 안 세는가?
- 그 선택의 근거는 무엇이고 무엇을 바꾸는가?
- 배송비가 0원인 주문으로 시험하면 이 함수의 무엇이 안 보이는가?
- 과세 아닌 배송비는 세금계산서의 어느 줄로 가는가?

#### 4. TODO 4 — invoice (세금계산서 한 장)

```java
/**
 * 세금계산서를 만든다.
 *
 * <p>포함가면 과세분에서 공급가액을 역산한다. 11 로 나누는 것이 아니라
 * 10 을 곱하고 11 로 나눈다. 그래야 나머지가 안 사라진다.
 *
 * <p>면세와 영세율은 세금이 0 이므로 역산할 것이 없다.
 * 포함가든 별도든 금액이 그대로 공급가액이다.
 */
public Invoice invoice(
        TaxOrder order,
        DiscountAllocation allocation,
        PriceBasis basis,
        ShippingTax shippingTax) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 포함가 역산에서 11로 먼저 나누면 무엇이 사라지는가?
- 부가세를 `공급가액 × 0.1` 로 구하면 왜 안 되는가?
- 빼기로 구하면 세금계산서의 어느 등식이 언제나 맞게 되는가?
- 면세와 영세율은 왜 역산할 것이 없는가?
- 별도가에서는 부가세를 어떻게 구하고, 그때는 왜 빼기를 못 쓰는가?
- 과세 총액(taxableGross)은 어떤 세 조각으로 만들어지는가?
- 영세율과 면세를 한 필드로 합치면 무엇을 못 만들게 되는가?

### B. 개념

#### 5. 함정 (원본 README "함정")

- README가 꼽은 함정 다섯은 각각 무엇인가?
- "나눗셈을 먼저 하는 것"이 왜 제일 앞에 있는가?
- 상품 금액이 0원인 주문은 실제로 어떤 주문인가?

#### 6. 측정 — 답이 갈리는 건수 (원본 README "측정이 알려준 것")

- 주문 5,000개 중 부가세가 갈린 것은 몇 건이고 총액이 갈린 것은 몇 건인가?
- 부가세 폭의 평균과 최대는 각각 얼마인가?
- 세 배분 방식의 부가세 총합은 각각 얼마이고 "과세 먼저"는 몇 퍼센트 줄어드는가?
- 그 차이는 금액으로 얼마인가?
- 배송비 규칙의 영향은 할인 배분의 몇 분의 1인가?
- 거기서 얻을 교훈은 무엇인가?
- 별도가로 바꾸면 총액이 갈리는 주문이 0건에서 몇 건이 되는가?
- 왜 포함/별도를 제일 먼저 정해야 하는가?
- 과세 상품만 담긴 주문 1,955개 중 부가세가 갈린 것은 몇 개인가?
- 개발할 때 만드는 주문이 대개 과세만 담는 것이 왜 위험한가?

#### 7. 변종 검증에서 고친 것 (원본 README 이관)

- 분모 0 보호를 지워도 테스트가 통과한 이유는 무엇인가?
- 중간 자릿수 10자리를 0으로 바꿔도 안 깨진 이유는 무엇인가?
- "순서가 load-bearing 이고 자릿수는 아니었다"는 말은 무슨 뜻인가?
- 지워도 안 깨지는 코드를 발견했을 때 할 수 있는 선택 둘은 무엇인가?

#### 8. 생각해볼 것 (원본 README 이관)

- 부분 취소하면 할인 배분을 다시 계산하나 처음 배분을 저장해두나?
- 세금계산서를 발행한 뒤에 배분 규칙을 바꾸면 이미 낸 신고는 어떻게 하나?
- 면세 상품과 과세 상품을 묶어 파는 세트 상품의 구분은 무엇인가?
- 영세율을 "과세 상품이 있다"로 셀 것인가?
- 그 판단이 배송비 세금을 어떻게 바꾸는가?

#### 9. 연결

- "고객 화면에 안 보이는 결정"은 앞의 어느 박스와 같은 자리인가?
- "곱하기 먼저, 마지막 몫은 빼기"는 어느 박스에서 또 나오는가?
- 이 박스의 enum 세 개는 01-parking-fee의 `FeePolicy` 와 무엇이 같고 무엇이 다른가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
