# domain-modeling-advanced/25-lottery-draw — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 안내문 한 줄 (원본 `Draw` javadoc · README)

```text
  "응모권 많을수록 당첨 확률이 오르고 중복 당첨은 안 됩니다"

  ── 정해지지 않은 것이 셋이다 ──
    무엇으로 합산하나   회원 번호인가 이름과 전화인가 주문인가
    중복 당첨은         이벤트 전체인가 상품별인가 안 막나
    어떻게 뽑나         응모권을 뽑나 사람을 뽑나
```

```text
  같은 이름과 전화로 계정 둘을 만들어 각각 두 번 주문했다. 건마다 응모권 1 장.

    회원 번호로   두 사람, 각각 2 장
    이름+전화로   한 사람, 4 장
    주문으로      네 사람, 각각 1 장

  응모권 총합은 언제나 4 장이다.
```

- 이 한 줄이 **안 말해주는** 결정은 무엇무엇인가?
- "1 인당"의 1인은 누구인가 — 회원인가 사람인가 주문인가?
- 합산 키를 바꾸면 응모권 총합이 늘거나 주는가?
- 합산 키를 바꾸면 무엇이 바뀌는가?
- "중복 당첨은 안 됩니다"에서 "안 됩니다"의 범위는 어디까지인가?
- "확률이 오릅니다"라고 써 놓고 사람을 뽑으면 무엇이 거짓이 되는가?
- 상품을 1등부터 뽑을지 3등부터 뽑을지는 이 안내문 어디에 있는가?
- 응모자가 뽑을 인원보다 적으면 어떻게 하라고 안내문이 말하는가?
- 안 정해진 자리를 코드 안에 숨기지 않고 `SumKey`·`DuplicateRule`·`Pick` 세 enum 값으로 올리면 무엇을 할 수 있게 되는가?
- 세 갈래가 "서로 간섭한다"는 것은 무슨 뜻인가?

#### 1. TODO 1 — key (그 응모가 어느 그룹으로 묶이나)

```java
/**
 * 그 응모가 어느 그룹으로 묶이나.
 *
 * <p>이어 붙일 때는 구분자를 넣는다. 이름 "김하" + 전화 "1234" 와
 * 이름 "김" + 전화 "하1234" 가 같은 사람이 되면 안 된다.
 */
public String key(Entry entry, SumKey sumKey) {
    throw new UnsupportedOperationException("TODO 1");
}
```

```java
/**
 * 응모 한 건.
 *
 * <p>같은 사람을 가리키는 값이 셋이다. 그리고 <b>셋이 서로 다른 그룹</b>을 만든다.
 *
 * <pre>
 *   회원 번호       계정을 여러 개 만들면 여러 사람이 된다
 *   이름 + 전화     계정이 달라도 한 사람이다. 개명하면 두 사람이 된다
 *   주문 번호       한 사람이 열 번 주문하면 열 사람이다
 *
 *   "1 인당 최대 5 회" 의 1 인이 누구인지가 여기서 정해진다.
 * </pre>
 */
```

- 세 키(MEMBER·IDENTITY·ORDER)는 각각 어떤 문자열이 되는가?
- 구분자 없이 이름과 전화를 그냥 붙이면 "김하"+"1234"와 "김"+"하1234"는 각각 무엇이 되는가?
- 두 사람이 한 사람이 되면 당첨은 어떻게 되는가?
- 구분자만 넣으면 충분한가, 접두사(`m:`·`n:`·`o:`)는 왜 붙이는가?
- 전화번호에 `|` 가 들어 있는 자료가 오면 이 키는 여전히 안전한가?
- MEMBER 키는 어떤 부정을 못 막는가?
- IDENTITY 키는 어떤 정상 사용자를 두 사람으로 만드는가?
- ORDER 키를 쓰면 큰손의 응모권은 어떻게 되는가?

#### 2. TODO 2 — aggregate (같은 키끼리 응모권을 더한다)

```java
/**
 * 같은 키끼리 응모권을 더한다.
 *
 * <p>키 순으로 세운다. 안 세우면 같은 자료에서 <b>같은 seed 로도</b>
 * 다른 당첨자가 나온다. 추첨은 다시 돌려서 같은 답이 나와야 한다.
 */
public List<Applicant> aggregate(List<Entry> entries, SumKey sumKey) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 같은 키를 여러 번 만났을 때 응모권을 합치는 관용구는 무엇인가?
- "키 순으로 세운다"를 안 지키면 무엇이 입력 순서를 타게 되는가?
- 입력 순서는 실무에서 무엇이 정하는가?
- 같은 seed 인데 당첨자가 달라지면 무슨 일이 생기는가?
- `seed` 만 고정하면 재현이 되는가, 왜 정렬까지 필요한가?
- 정렬 기준이 키 하나로 충분한 이유는 무엇인가(키가 같은 응모자가 둘 생길 수 있는가)?
- 결과를 `List.copyOf` 로 돌려주는 것은 무엇을 막는가?
- 테스트는 이 성질을 어떻게 확인하는가?

#### 3. TODO 3 — pickOne (후보 중 하나를 뽑는다)

```java
/**
 * 후보 중 하나를 뽑는다. 못 뽑으면 null 이다.
 *
 * <p>응모권을 뽑는 규칙은 응모권 총합 중 하나를 골라 그 응모권의 주인을 낸다.
 * 사람을 뽑는 규칙은 후보 중 하나를 균등하게 고른다.
 */
public Applicant pickOne(List<Applicant> candidates, Pick pick, Random random) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- 후보가 비었으면 무엇을 돌려주는가?
- 빈 목록에서 아무나 하나 내면 실제로 언제 그 잘못을 알게 되는가?
- BY_PERSON 은 어떤 한 줄로 끝나는가?
- BY_TICKET 은 어떤 절차로 "응모권 하나의 주인"을 찾는가?
- 응모권 총합을 `long` 으로 세는 이유는 무엇인가?
- `random.nextLong()` 은 음수도 내는데 왜 `Math.floorMod` 를 쓰는가?
- `%` 만 쓰면 무엇이 깨지는가?
- 누적 차감에서 판정을 `drawn < 0` 으로 하는 이유는 무엇인가 — `<= 0` 으로 쓰면 무엇이 어긋나는가?
- 루프가 끝까지 돌았는데 아무도 못 골랐다면 그건 어떤 경우인가?
- a 90장·b 10장으로 1,000번 뽑으면 두 방식은 각각 a 를 몇 번쯤 내는가?
- `Random` 을 인자로 받는 것과 함수 안에서 새로 만드는 것은 무엇이 다른가?

#### 4. TODO 4 — draw (상품을 차례로 뽑는다)

```java
/**
 * 상품을 차례로 뽑는다.
 *
 * <p>이벤트 전체 중복을 막으면 한 번 뽑힌 사람은 <b>남은 상품 전부에서</b>
 * 빠진다. 상품별로 막으면 그 상품에서만 빠진다.
 *
 * <p>후보가 모자라면 그 상품의 인원을 다 못 채운다. 채운 척하면 없는
 * 당첨자가 생긴다.
 */
public List<Winner> draw(
        List<Applicant> applicants, List<Prize> prizes, DuplicateRule duplicateRule,
        Pick pick, long seed) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 이 함수의 루프는 몇 겹인가?
- `Random` 은 어디서 몇 번 만들어지는가 — 상품마다 새로 만들면 무엇이 깨지는가?
- 세 중복 규칙은 각각 "당첨된 사람"을 어느 집합에서 찾는가?
- `wonAnywhere` 와 `wonHere` 는 각각 언제 비워지는가?
- NONE 은 왜 아무도 안 막는데도 코드가 필요한가?
- 후보 목록은 한 명 뽑을 때마다 새로 만드는가, 한 번 만들어 재사용하는가?
- `pickOne` 이 null 을 주면 무엇을 하는가?
- 거기서 `break` 대신 `continue` 를 써도 결과가 같은 이유는 무엇인가?
- 그것을 보장하는 불변식을 한 문장으로 말하면 무엇인가?
- 응모자 2명에게 1등 3명을 뽑으면 당첨은 몇 건인가?
- 응모자가 a 하나뿐인데 1등 1명·2등 1명이면 세 규칙은 각각 당첨 몇 건을 내는가?
- 응모자가 a 하나뿐인데 1등 3명이면 PER_PRIZE 와 NONE 은 각각 몇 건인가?
- 상품을 1등부터 뽑는 것과 3등부터 뽑는 것은 이 코드에서 어디에 나타나는가?

### B. 개념

#### 5. 함정 (원본 README "함정")

```text
  - 키를 그냥 이어 붙이는 것
  - 합산 결과를 안 세우는 것
  - 후보가 없는데 뽑는 것
  - 인원을 못 채웠는데 채운 척하는 것
```

- 이 넷은 각각 무엇을 조용히 망가뜨리는가?
- "키를 그냥 이어 붙이면 그 둘 중 하나만 당첨된다"는 왜 그런가?
- "합산 결과를 안 세우면 이의가 들어와도 다시 못 돌린다"는 무슨 뜻인가?
- "없는 당첨자가 생긴다"를 실무 사고로 옮기면 어떤 장면인가?
- "인원을 못 채웠는데 채운 척하는 것"은 어떤 이벤트에서 늘 생기는가?
- 넷 중 셋은 예외가 안 나고 틀린 값이 나온다 — 그것을 뭐라고 부르는가?

#### 6. 측정 — 갈래가 얼마나 갈리나 (원본 README "측정이 알려준 것")

```text
  사람 1,000 명, 100 회. 상품은 1 등 1 명, 2 등 5 명, 3 등 50 명.
```

- 합산 키 셋의 응모자 수는 각각 몇인가?
- 세 키의 응모권 총합은 각각 몇인가?
- 총합이 안 바뀌는데 최다 보유는 왜 바뀌는가?
- 주문 기준에서 최다 보유가 절반(4,997)이 되는 이유는 무엇인가?
- 회원 기준과 이름+전화 기준의 응모자 수 차이 2,000 은 누구인가?
- 응모권을 뽑을 때와 사람을 뽑을 때 당첨 건수는 각각 몇인가?
- 응모권 20장 이상인 사람의 당첨은 두 방식에서 각각 몇 건인가?
- "당첨자 수는 둘 다 같은데 누가 되느냐만 바뀐다"가 왜 무서운 말인가?
- 세 중복 규칙의 "당첨자 응모권 합"과 "버려진 응모권"은 각각 몇인가?
- 버려진 응모권이 당첨자 응모권의 몇 퍼센트인가?
- 50장을 산 사람이 3등에 당첨되면 1등 추첨에서 몇 장이 빠지는가?
- 그래서 상품을 뽑는 순서가 왜 "돈이 되는가"?
- 세 중복 규칙의 서로 다른 당첨자 수는 각각 몇인가?
- 당첨 건수가 늘 같은데 서로 다른 당첨자만 줄면 명단은 어떻게 보이는가?
- 상품이 1등 1명뿐일 때 여섯 조합을 다 견주면 어긋난 것이 몇인가?
- 그때 중복 규칙 셋만 견주면 어긋난 것이 몇인가?
- 상품이 하나면 중복 규칙 갈래가 왜 안 보이는가?
- 대부분의 이벤트가 상품 하나라는 사실은 이 규칙을 언제 처음 필요하게 만드는가?
- 그날 규칙을 새로 정하면 무엇이 어긋나는가?

#### 7. 변종 검증에서 고친 것 (원본 README)

```text
  고칠 것이 없었다. 변종 15 개 중 14 개가 잡혔다.
  살아남은 하나는 동작이 같은 변종이었다.
```

- 살아남은 변종 하나는 정확히 무엇이었는가?
- 그 변종이 안 잡히는 것은 테스트의 결함인가 아닌가?
- 그 자리에서 무엇을 코드에 남겼는가?
- "동작이 같은 변종"을 잡으려고 테스트를 더 쓰면 무엇이 나빠지는가?

#### 8. 생각해볼 것 (원본 README 이관)

- 계정을 여러 개 만든 사람을 어떻게 하나 — 막을 수 있나?
- 당첨자를 뽑는 순서(1등부터냐 3등부터냐)를 약관에 적어두었나?
- 응모권이 죽는 것을 응모자가 아나 — 알면 몇 장을 살까?
- 이의가 들어오면 무엇을 보여주나 — seed 를 공개하나?

#### 9. 연결

- 이 트랙이 세는 것("갈래가 갈리는 건수")은 알고리즘 트랙이 세던 것과 무엇이 다른가?
- 키를 이어 붙일 때 구분자를 넣는 문제는 앞의 어느 박스와 같은 무늬인가?
- "같은 seed 면 같은 결과"라는 계약은 실무의 어떤 관행과 같은가?
- 세 enum 을 인자로 받는 설계는 01-parking-fee 의 `FeePolicy` 와 무엇이 같고 무엇이 다른가?
- "안내문과 코드가 어긋나도 아무도 모른다"는 성질을 막으려면 무엇을 테스트해야 하는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
