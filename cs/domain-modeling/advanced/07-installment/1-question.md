# domain-modeling-advanced/07-installment — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 나눗셈 한 줄 (원본 README)

```text
  1,000,000 원을 12 개월로 나누면 한 달에 83,333.33... 원
  원 단위로 끊으면 12 번 합해도 999,996 원이라 4 원이 남는다

    마지막에 몰기   83,333 x 11,  83,337
    처음에 몰기     83,337,  83,333 x 11
    나눠서          83,334 x 4,   83,333 x 8
```

- "1,000,000원을 12개월 할부"라는 한 줄이 **안 말해주는** 결정은 무엇무엇인가?
- 세 나머지 규칙의 합이 전부 정확히 1,000,000원인 이유는 무엇인가?
- 그렇다면 무엇이 갈리는가?
- 그 4원이 "어느 달에 몇 원 더 내나"로 안 끝나고 **총 이자까지** 바꾸는 이유는 무엇인가?
- 나머지 4원을 마지막 달에 몰면 고객 안내문에서 무엇이 문제가 되는가?
- "연 12 퍼센트"라는 말에 안 들어 있는 두 번째 결정은 무엇인가?

#### 1. TODO 1 — split (총액을 나눠서 합을 정확히 맞추기)

```java
/**
 * 총액을 가중치대로 나누되 합이 정확히 총액이 되게 한다.
 *
 * <p>각 몫을 내림으로 구하면 합이 총액보다 작거나 같다.
 * 그 차이를 나머지 규칙대로 얹는다. 반올림으로 구하면 합이 넘칠 수 있고,
 * 그러면 나머지가 음수가 되어 어느 회차에서 돈을 빼야 한다.
 */
private long[] split(long total, BigDecimal[] weights, Residue residue) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- 몫을 내림(FLOOR)으로 구하는가 반올림(HALF_UP)으로 구하는가?
- 내림으로 구하면 나머지의 부호가 왜 언제나 0 이상인가?
- 반올림으로 구하면 어느 회차에서 무엇을 해야 하는가?
- 나머지가 음수가 되면 상환표의 어떤 불변식이 깨지는가?
- SPREAD 규칙은 남은 금액이 회차 수보다 많으면 어떻게 도는가?
- 이 함수가 원금과 이자 양쪽에 다 쓰이는데, 왜 같은 함수로 되는가?

#### 2. TODO 2 — equalPaymentWeights (원리금 균등의 회차별 원금 비율)

```java
/**
 * 원리금 균등에서 회차마다 갚는 원금의 비율.
 *
 * <pre>
 *   매달 내는 돈 = P x r x (1+r)^n / ((1+r)^n - 1)
 *
 *   그 안에서 이자는 남은 원금에 비례하므로 점점 줄고,
 *   원금은 그만큼 점점 는다.
 * </pre>
 *
 * <p>이자율이 0 이면 이 식이 0 으로 나누기가 된다. 그때는 원금 균등과 같다.
 */
private BigDecimal[] equalPaymentWeights(Loan loan) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 이자율이 0이면 이 식의 분모가 왜 0이 되는가?
- 무이자 할부는 얼마나 흔한가?
- 무이자일 때 왜 "원금 균등과 같다"로 갈아타도 되는가?
- 매달 내는 돈이 같은데 왜 원금이 점점 느는가?
- 이 함수가 돌려주는 것은 금액인가 비율(가중치)인가?
- 금액이 아니라 가중치를 돌려주면 무엇이 편해지는가?
- 1회차 원금이 78,848원이라는 사실이 중도 상환에서 무엇을 뜻하는가?

#### 3. TODO 3 — schedule (상환표 만들기)

```java
/**
 * 상환표를 만든다.
 *
 * <p>원금을 먼저 나누고, 이자는 그 원금이 만든 잔액에서 나온다.
 * 애드온만 이자를 따로 나눈다. 애드온은 잔액을 안 보기 때문이다.
 */
public Schedule schedule(Loan loan, Method method, Residue residue) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- 왜 원금을 먼저 나누고 이자를 나중에 구하는가?
- 애드온만 이자를 따로 나누는 이유는 무엇인가?
- 애드온의 총 이자는 어떤 식으로 구하는가?
- 잔액을 안 줄이고 이자를 매기면 무슨 일이 생기는가?
- 이자를 매기는 루프에서 잔액을 빼는 시점이 언제인지가 왜 중요한가?
- 상환표가 지켜야 하는 불변식 셋은 무엇인가?
- 그 불변식을 몇 개 조합에서 확인해야 하는가?

### B. 개념

#### 4. 함정 (원본 README "함정")

- README가 꼽은 함정 넷은 각각 무엇인가?
- "애드온을 이자율이 같으니 비슷하겠지로 보는 것"이 왜 함정인가?
- 마지막 잔액이 0이 아닌 상환표는 왜 조용한 결함인가?

#### 5. 측정 — 답이 갈리는 건수 (원본 README "측정이 알려준 것")

- 할부 5,000건 중 첫 회차 금액이 갈린 건은 몇 건인가?
- 안 갈린 건은 몇 건이고 그 이유는 무엇인가?
- 첫 회차 폭의 평균과 최대는 각각 얼마인가?
- 사람이 예제를 만들 때 왜 이 갈림이 안 보이는가?
- 나머지 규칙 셋의 총 이자 합계는 각각 얼마인가?
- 그 차이는 얼마이고 11억 중 비율로는 얼마인가?
- 비율이 없는 셈인데도 왜 중요한가?
- 세 방식의 총 이자 합계는 각각 얼마이고 애드온은 몇 배인가?
- 방식이 갈린 건이 4,836/5,000인데 안 갈린 164건은 어떤 경우인가?
- 3·6·12·24·36개월에서 애드온의 배수는 각각 얼마인가?
- 기간이 길수록 배수가 커지는 이유는 무엇인가?
- 짧은 할부로 시험하면 무엇이 안 보이는가?
- 원리금 균등의 폭 8원과 원금 균등의 폭 9,163원은 성격이 어떻게 다른가?
- 무이자면 방식 셋이 통째로 같은데, 그것이 왜 위험한가?

#### 6. 변종 검증 (원본 README 이관)

- 변종 15개가 전부 잡힌 비결은 무엇인가?
- "회차별 원금 목록으로 통째로 못 박는다"는 무슨 뜻인가?
- 잔액을 안 줄이는 변종이 세 자리에 있었는데 왜 셋 다 잡혔는가?

#### 7. 생각해볼 것 (원본 README 이관)

- 중도 상환하면 남은 이자를 어떻게 계산하나?
- 애드온이면 이미 다 낸 것 아닌가?
- 연체가 생기면 그달 이자는 잔액에 붙나 연체 원금에 붙나?
- 나머지를 마지막에 몰면 마지막 회차만 금액이 다른데 그것을 안내문에 뭐라고 쓰나?
- 실질 연이율을 표시하려면 어느 방식으로 계산해야 하나?

#### 8. 연결

- "나누어떨어지지 않는 금액을 여럿에 나누기"는 앞의 어느 박스와 같은 자리인가?
- 06-tax의 "마지막 몫은 빼기로"와 여기의 "나머지를 규칙대로 얹기"는 무엇이 같고 무엇이 다른가?
- `Residue` enum 과 `Method` enum 은 성격이 어떻게 다른가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
