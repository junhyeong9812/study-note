# domain-modeling-advanced/20-approval-chain — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 요구사항 한 줄 (원본 README + 클래스 javadoc)

```text
  "팀장, 본부장, 대표 순으로 결재받는다"

  결재선   1 팀장   2 본부장 A B C   3 대표

  대표 -> 본부장A -> 팀장 순으로 눌렀다

    아무나 먼저 눌러도 됨   셋 다 유효  ->  통과
    순서를 지켜야 함        대표와 본부장 클릭이 버려짐  ->  팀장만 유효
```

- 이 한 문장이 **안 말해주는** 결정 셋은 무엇무엇인가?
- 그 셋을 고르면 조합이 몇 가지가 되는가?
- "순으로 결재받는다"에서 "순으로"는 순서를 **강제**한다는 뜻인가 그냥 **적어놓은 목록**이라는 뜻인가?
- 두 번째 판(순서를 지키는 판)에서 대표와 본부장의 화면은 실제와 어떻게 어긋나 있었는가?
- 셋이 "서로 간섭한다"는 말은 무슨 뜻인가?
- 안 정해진 자리를 코드 안에 숨기지 않고 `Order`·`Quorum`·`AmendRule` 로 올리면 무엇을 할 수 있게 되는가?

#### 1. TODO 1 — required (그 단계에 몇 명이)

```java
/**
 * 그 단계에 몇 명이 눌러야 하나.
 *
 * <p>과반은 절반보다 <b>큰</b> 수다. 둘이면 둘, 셋이면 둘, 넷이면 셋이다.
 * 절반으로 잡으면 둘짜리 단계에서 한 명이 통과시킨다.
 */
public int required(Step step, Quorum quorum) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- 과반을 식 하나로 쓰면 무엇인가?
- 승인자가 1·2·3·4명일 때 전원/과반/한 명은 각각 몇인가?
- 과반을 `size() / 2` 로 잡으면 둘짜리 단계에서 무슨 일이 생기는가?
- 왜 하필 둘짜리 단계가 제일 위험한가?
- `Step` 이 정족수를 직접 안 갖고 밖에서 `Quorum` 으로 받는 이유는 무엇인가?
- 승인자가 4명일 때 과반이 2가 아니라 3인 것을 테스트가 어떻게 못 박았는가?

#### 2. TODO 2 — accepts (클릭 하나를 받아들이나)

```java
/**
 * 그 승인이나 반려를 받아들이나.
 *
 * <p>넷을 본다. 그 단계의 승인자인가, 이미 누른 사람인가, 상신자 자신인가,
 * 그리고 순서 규칙에 맞나.
 *
 * <p>상신자는 자기 건을 못 누른다. 순서를 지키든 안 지키든 그렇다.
 */
public boolean accepts(
        Map<Integer, List<String>> approvals, ApprovalEvent event, String requesterId,
        Order order, Quorum quorum) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 네 가지 검사는 각각 무엇이고 어떤 순서로 보는가?
- "그 단계의 승인자인가"를 안 보면 결재선은 무엇이 되는가?
- 아무도 없는 9단계를 누르면 어디서 걸리는가?
- "이미 누른 사람인가"를 안 보면 과반 정족수가 어떻게 채워지는가?
- 상신자 검사는 왜 순서 규칙보다 **앞**에 있어야 하는가?
- 상신자 검사가 `Order` 와 무관하다는 말은 무슨 뜻인가?
- 순차(SEQUENTIAL)일 때 "내 아래 단계가 다 찼나"를 어떻게 확인하는가?
- 그 확인 루프가 `earlier.level() >= event.level()` 에서 `break` 하는 이유는 무엇인가?
- 병렬(PARALLEL)이면 왜 순서 검사 없이 바로 true 인가?
- `accepts` 가 `quorum` 을 인자로 받는 이유는 무엇인가 — 정족수는 이미 다른 함수 일이 아닌가?
- 이 함수가 public 인 것이 계약 표면에 무엇을 더하는가?

#### 3. TODO 3 — run (사건을 차례로 흘린다)

```java
/**
 * 결재함의 사건들을 차례로 흘려보낸다.
 *
 * <p>반려가 하나 들어오면 거기서 끝난다. 그 뒤 사건은 다 버린다.
 *
 * <p>금액 변경은 규칙대로 승인을 지운다. 지우면 되돌린 횟수를 센다.
 * 그 횟수가 "결재가 몇 번 뒤집혔나" 다.
 */
public Progress run(
        List<ApprovalEvent> events, Order order, Quorum quorum, AmendRule amendRule) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- `run` 이 루프를 돌며 들고 다니는 상태 여섯 가지는 무엇인가?
- 상태가 PENDING 이 아니게 되면 그 뒤 사건은 어떻게 처리하는가?
- 두 번째 SUBMIT 은 왜 받지 않는가?
- 받아주면 무엇이 "말없이" 바뀌는가?
- AMEND 를 상신자만 할 수 있게 막지 않으면 결재가 무엇으로 바뀌는가?
- 상신 전에 들어온 APPROVE 는 어떻게 되는가?
- "올라갔다"를 `>` 로 볼지 `>=` 로 볼지가 왜 큰 차이를 만드는가?
- 금액을 바꾸는 줄과 승인을 지우는 줄의 순서는 왜 그렇게 되어 있는가?
- REJECT 가 들어오면 그 사건 자체는 `ignored` 에 들어가는가 안 들어가는가?
- 승인이 하나 붙을 때마다 무엇을 다시 검사하는가?
- 마지막에 `approvals` 를 `List.copyOf` 로 얼려서 돌려주는 이유는 무엇인가?
- `Progress` 가 `ignored` 와 `resets` 를 **결과의 일급 필드**로 갖는 이유는 무엇인가?
- 이 설계가 "지금 상태만 저장하는" 설계와 무엇이 다른가?

#### 4. TODO 4 — waitingOn (누구를 기다리나)

```java
/** 아직 정족수를 못 채운 사람들. 누구를 기다리는지 화면에 띄울 값이다. */
public Set<String> waitingOn(Progress progress, Quorum quorum) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 이 함수가 제일 먼저 확인하는 것은 무엇인가?
- 이미 통과했거나 반려된 결재함의 대기 목록은 무엇이어야 하는가?
- 정족수를 채운 단계의 안 누른 사람은 목록에 넣는가 빼는가?
- "안 누른 사람"과 "기다리는 사람"은 어떻게 다른가?
- 순차 결재에서 팀장만 눌렀을 때 이 함수는 누구누구를 돌려주는가?
- 그 목록을 그대로 알림에 쓰면 무슨 일이 생기는가?
- 반환 타입이 `Set` 인데 `LinkedHashSet` 을 쓰는 이유는 무엇인가?
- 이 함수가 순서를 안 보는 것은 버그인가 아니면 정해진 계약인가?

#### 5. 계약(다 주어진 것) — Step · ApprovalEvent

```java
public record Step(int level, List<String> approvers) { ... }
public record ApprovalEvent(long seq, Kind kind, String actorId, int level, long amount) { ... }
```

- `ApprovalChain` 생성자가 결재선에 대해 확인하는 것 둘은 무엇인가?
- 단계가 2부터 시작하면 왜 거부하는가?
- 한 단계에 같은 승인자가 두 번 있으면 정족수 계산이 어떻게 망가지는가?
- 승인·반려는 `level >= 1` 이어야 하고 상신·금액 변경은 `level == 0` 이어야 하는 이유는 무엇인가?
- 상신·금액 변경의 금액이 0 이하면 왜 막는가?
- `seq` 는 왜 필요한가 — 어차피 리스트 순서대로 처리하지 않는가?

### B. 개념

#### 6. 측정 — 아무 일도 안 일어나면 (원본 README "측정이 알려준 것" 하나)

- 결재선 순서대로 누르고 금액도 안 고치면 열여덟 조합 중 몇 개가 통과하는가?
- 그때 버린 클릭과 되돌림은 각각 몇인가?
- 몇 번의 비교에서 어긋난 것이 몇 건이었는가?
- "개발할 때 만드는 결재함이 이렇다"는 말이 왜 무서운가?
- 그러면 여섯 갈래(2×3) 중 아무거나 골라도 테스트가 통과한다 — 이게 무슨 사고로 이어지는가?

#### 7. 측정 — 순서를 섞으면 (둘)

- 섞임 0·10·30·100 %에서 순차 결재의 통과 건수는 각각 몇인가?
- 같은 조건에서 병렬 결재의 통과 건수는 몇인가?
- 섞임 10 %일 때 순차가 버린 클릭은 몇 개인가?
- 섞임 100 %일 때 버린 클릭은 몇 개까지 가는가?
- 병렬 결재가 "한 건도 안 버린다"는 것은 정족수가 전원일 때의 이야기인가 항상인가?
- 클릭을 10 %만 섞었는데 통과가 87 건이나 줄어든다 — 왜 그렇게 민감한가?

#### 8. 측정 — "올라갈 때만 다시"가 접히는 자리 (셋)

- 인상 0·50·100 %에서 "올라갈 때만"의 통과 건수는 각각 몇인가?
- "전부 다시"의 통과 건수는 인상 비율을 타는가?
- "그대로"의 통과 건수는 몇인가?
- 인하만 있는 자료로 테스트하면 "올라갈 때만"은 어느 규칙과 같아 보이는가?
- 인상만 있는 자료면 어느 규칙과 같아 보이는가?
- "세 규칙인데 자료가 어느 둘로 접을지를 정한다"를 자기 말로 설명할 수 있는가?
- 어느 쪽으로 접혔는지가 테스트 결과에 안 보이는 이유는 무엇인가?

#### 9. 측정 — 같은 결재함, 통과율 498 vs 1,000 (넷)

- 순서 섞임 30 %, 금액 변경 40 %, 그 중 절반이 인상인 결재함에서 가장 빡빡한 조합은 무엇이고 통과는 몇인가?
- 가장 헐거운 조합은 무엇이고 통과는 몇인가?
- 빡빡한 쪽이 버린 클릭은 몇 개인가?
- 같은 입력인데 통과율이 절반과 전부로 갈린다 — 이 사실이 "요구사항 한 문장"에 대해 무엇을 말해주는가?
- 버린 클릭 1,328개를 누른 사람들은 자기 상태를 어떻게 알고 있는가?

#### 10. 측정 — 정족수가 차면 그 뒤 클릭은 (다섯)

- 병렬 결재, 섞임 30 %에서 전원/과반/한 명 정족수가 버린 클릭은 각각 몇인가?
- 정족수가 전원일 때 버린 클릭이 0인 이유는 무엇인가?
- 한 명 정족수에서 본부장 셋 중 둘의 클릭이 늘 버려지는 이유는 무엇인가?
- 그 세 경우의 통과 건수는 다른가 같은가?
- "버려도 결과는 같다. 그런데 그 두 사람은 결재를 한 것인가 안 한 것인가" — 이 질문이 왜 코드가 아니라 제품의 질문인가?

#### 11. 함정 (원본 README "함정")

- README가 꼽은 함정 넷은 각각 무엇인가?
- "그 단계의 승인자인지 안 보는 것"이 왜 "결재선이 그냥 목록이 된다"로 이어지는가?
- "상신자가 자기 건을 누르게 두는 것"은 왜 결재선을 사람 이름으로 짜면 늘 생기는가?
- "버린 클릭을 안 세는 것"의 피해자는 누구인가?

#### 12. 변종 검증에서 고친 것 (원본 README 이관)

- 약했던 테스트 넷은 각각 어떤 입력을 안 만들고 있었는가?
- 그 넷의 공통점 한 줄은 무엇인가?
- "그 단계 승인자가 아닌 사람의 클릭"은 실제로 어떤 경로로 들어오는가?
- "두 번째 상신"은 실제로 어떤 운영에서 생기는가?
- "금액을 그대로 둔 변경"은 실제로 어떤 조작에서 생기는가?
- "정족수를 채운 단계에 남은 사람"을 안 빼면 무엇이 나가는가?
- 화면이 막아주는 것과 도메인이 막는 것은 어떻게 다른가?

#### 13. 생각해볼 것 (원본 README 이관)

- 버린 클릭을 누른 사람에게 무엇을 보여주나?
- 결재선을 사람이 아니라 직책으로 짜면 자기결재 문제가 없어지나?
- 금액이 오르내린 결재함의 감사 자료에 무엇을 남기나?
- 대결(위임)이 들어오면 정족수는 어떻게 세나?

#### 14. 연결

- 이 트랙이 세는 것("답이 갈리는 케이스 수")은 알고리즘 트랙이 세던 것과 무엇이 다른가?
- 사건 목록을 흘려서 상태를 만드는 이 구조는 실무의 어떤 패턴과 같은가?
- 이 박스의 "버린 클릭"은 앞의 어느 박스의 어느 값과 같은 자리인가?
- PR 리뷰의 "코드 오너 + 승인 N개" 규칙을 이 박스의 세 갈래로 번역하면 무엇이 되는가?
- "올라갈 때만 다시"가 자료에 따라 접히는 현상은 다른 박스의 어떤 현상과 같은 패턴인가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
