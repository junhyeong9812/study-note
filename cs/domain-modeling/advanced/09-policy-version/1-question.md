# domain-modeling-advanced/09-policy-version — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 날짜 셋과 판 둘 (원본 README)

```text
  주문 3/7   신청 3/12   처리 3/15
  정책 v1 (3/1 시행, 환불 100 %)  ->  v2 (3/10 시행, 환불 80 %)

    주문 시점   v1   100,000
    신청 시점   v2    80,000
    처리 시점   v2    80,000
```

- "3월 10일부터 적용"이라는 말이 **안 정해주는** 것은 무엇인가?
- 취소 한 건에 날짜가 셋인 이유는 무엇인가?
- 신청과 처리가 왜 벌어지는가?
- 고객이 바라는 기준과 회사가 하고 싶은 기준은 각각 무엇인가?
- 정책 한 판에도 날짜가 셋 붙는데 각각 무엇인가?
- "3월 10일부터 적용"은 그 셋 중 어느 것인가?
- 기준이 대개 무엇으로 정해지는가?

#### 1. TODO 1 — 생성자 (판 목록 보관)

```java
/**
 * 정책 판들을 받는다. 시행일 순으로 정렬해 둔다.
 *
 * <p>같은 날 시행되는 판이 둘이면 던진다. 그 둘 중 무엇을 쓸지 정할 방법이 없다.
 */
public PolicyResolver(List<PolicyVersion> versions) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- 왜 받은 그대로 두지 않고 시행일 순으로 정렬하는가?
- 운영자가 화면에서 판을 만드는 순서는 어떤가?
- 같은 날 시행되는 판이 둘이면 왜 던지는가?
- 안 던지고 그냥 두면 무엇이 결과를 정하게 되는가?
- 그 순서는 다시 무엇이 정하는가?
- 판이 하나도 없으면 어떻게 하는가?

#### 2. TODO 2 — anchorDate (기준으로 쓸 날짜)

```java
/** 기준으로 쓸 날짜. */
private LocalDate anchorDate(CancelRequest request, Anchor anchor) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 세 기준(`ORDERED`·`REQUESTED`·`PROCESSED`)은 각각 어느 날짜를 쓰는가?
- 처리 시점을 기준으로 쓰면 안 되는 이유는 무엇인가?
- 주문 3/18·신청 3/19·처리 3/22에서 신청 기준과 처리 기준의 환불금은 각각 얼마인가?
- 같은 날 처리했다면 얼마였는가?
- 이 함수가 세 줄짜리 `switch` 인데도 따로 빼낸 이유는 무엇인가?

#### 3. TODO 3 — versionOn (그날 유효한 판)

```java
/**
 * 그날에 유효한 판.
 *
 * <p>시행일이 그날 이하인 것 중 가장 늦은 것이다.
 * 뒤에서부터 찾으면 첫 번째로 걸리는 것이 답이다.
 */
public PolicyVersion versionOn(LocalDate date, MissingPolicy missing) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- "그날 유효한 판"의 정확한 정의는 무엇인가?
- 왜 뒤에서부터 찾는가?
- 앞에서부터 찾으면 3/15에 어느 판이 나오는가?
- 시행일 당일은 유효한가 아직 아닌가?
- 부등호를 하나 틀리면 며칠이 어떻게 되는가?
- 그날 유효한 판이 하나도 없는 날은 언제 생기는가?
- `FAIL` 과 `OLDEST` 는 각각 무엇을 하는가?
- 어느 쪽을 골라야 하는가?

#### 4. TODO 4 — anchorsDisagree (세 기준이 갈리나)

```java
/**
 * 세 기준이 서로 다른 판을 고르나.
 *
 * <p>세 날짜가 같은 판 안에 다 들어가면 안 갈린다.
 * 정책이 자주 안 바뀌면 대부분이 그렇다.
 */
public boolean anchorsDisagree(CancelRequest request, MissingPolicy missing) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 이 함수는 무엇을 비교하는가 — 금액인가 판 번호인가?
- 왜 금액이 아니라 판 번호로 비교하는가?
- 세 날짜가 한 판 안에 있으면 어떻게 되는가?
- 판이 하나뿐이면 이 함수가 무엇을 돌려주는가?
- 계산에 안 쓰이는 이 함수를 왜 만들어 두는가?

#### 5. 금액 계산 — refundOf (계약으로 주어짐)

```java
/** 이 금액에 이 정책을 적용하면 얼마를 돌려받나. */
public long refundOf(long amount) {
    long refunded = amount * refundRatePercent / 100;
    return Math.max(0, refunded - cancellationFee);
}
```

- 수수료는 비율 앞에 빼는가 뒤에 빼는가?
- 100,000원·50%·수수료 5,000원에서 두 순서의 결과는 각각 얼마인가?
- 어느 쪽인지가 약관에 적혀 있는가?
- 환불액이 수수료보다 작으면 어떻게 되는가?
- 음수를 그냥 두면 고객에게 무슨 일이 생기는가?

### B. 개념

#### 6. 함정 (원본 README "함정")

- README가 꼽은 함정 다섯은 각각 무엇인가?
- 그중 코드가 아니라 **사람이 정해야 하는** 것은 어느 것인가?
- "처리 시점만으로 기준이 되면 안 된다"는 말에서 "만으로"는 무슨 뜻인가?

#### 7. 측정 — 답이 갈리는 건수 (원본 README "측정이 알려준 것")

- 판이 2·4·12·52개일 때 기준이 다른 판을 고른 건은 각각 몇 건인가?
- 환불 폭 평균은 각각 얼마인가?
- 1년에 두 번 바꿀 때와 주마다 바꿀 때 갈림 비율은 각각 몇 퍼센트인가?
- "코드는 그대로인데 문제가 갑자기 나타난다"는 말은 무슨 뜻인가?
- 무작위 정책에서 처리 지연 0·3·7·30일의 환불 총액은 각각 얼마인가?
- 7일이 3일보다 적은 이유는 무엇인가?
- 무작위 자료로는 왜 "늦게 처리하면 회사 이득"이 안 보이는가?
- 정책이 나빠지는 방향(100→45%)이면 지연 0일과 30일의 총액은 각각 얼마인가?
- 지연 7일에서 주문 기준과 처리 기준의 차이는 몇 퍼센트인가?
- 시행 전 주문은 몇 건이고 전체의 몇 퍼센트인가?
- 그 건들은 어떻게 되는가?
- 그 자리가 언제 반드시 나오는가?
- 판이 하나면 5,000건 중 몇 건이 갈리는가?
- 이 문제는 정확히 언제 시작되는가?

#### 8. 변종 검증 (원본 README 이관)

- 변종 10개가 전부 잡힌 비결은 무엇인가?
- 어느 날짜 넷을 계약 테스트에 못 박았는가?
- `PolicyVersion` 은 왜 변종을 못 돌리는가?
- 그 대신 무엇을 계약 테스트에서 확인하는가?

#### 9. 생각해볼 것 (원본 README 이관)

- 정책을 소급해서 바꾸면 이미 처리한 취소는 어떻게 하나?
- 고객에게 "주문하실 때 약관"을 보여주려면 그 약관 전문을 어디에 보관하나?
- 시행일이 미래인 판을 미리 만들어두면 그 판이 화면에 보이나?
- 기준을 주문 시점으로 정하면 10년 전 주문의 취소도 그때 약관으로 하나?

#### 10. 연결

- 06-tax의 "발행 후 배분 규칙이 바뀌면"이라는 질문은 이 박스와 어떻게 이어지는가?
- "시행일 당일 포함"은 앞의 어느 박스와 같은 자리인가?
- `anchorsDisagree` 같은 "갈림 관측 도구"는 다른 박스의 무엇에 해당하는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
