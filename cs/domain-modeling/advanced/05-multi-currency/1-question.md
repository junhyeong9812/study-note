# domain-modeling-advanced/05-multi-currency — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 시작 계산 (원본 README)

```text
  JPY 111, 222, 333 을 원화로

    항목별   1,005 + 2,009 + 3,014 = 6,028
    묶어서   666 x 9.05 = 6,027.3  ->  6,027
```

- "합계를 원화로 보여달라"는 한 줄이 **안 말해주는** 결정은 무엇무엇인가?
- 왜 두 값이 다른가?
- 어느 쪽이 "더 정확한" 값인가?
- 그런데 왜 더 정확한 쪽을 그냥 못 쓰는가?
- "정확한 합계"와 "맞는 세로 합"을 둘 다 가질 수 있는가?

#### 1. 금액 표현 — 최소 단위 정수 (`Money`, `Currency` — 주어진 계약)

```java
/**
 * 금액. 최소 단위 정수로 들고 있다.
 *
 *   1,000 원        KRW  minor = 1000    (소수 0 자리)
 *   10 달러 50 센트  USD  minor = 1050    (소수 2 자리)
 *   1.234 디나르     BHD  minor = 1234    (소수 3 자리)
 */
public record Money(long minorUnits, Currency currency) { ... }
```

- 금액을 `double` 로 들면 무엇이 문제인가?
- KRW·JPY·USD·EUR·BHD 의 소수 자릿수는 각각 몇인가?
- "소수 둘째 자리에서 반올림"이라는 규칙은 어느 통화에서 틀리는가?
- `Money.of("10.5", KRW)` 는 왜 던지는가?
- `Money.of("10.500", BHD)` 는 어떻게 되는가?
- `plus` 가 같은 통화끼리만 되는 이유는 무엇인가?
- "원 + 달러"를 타입으로 막으면 무엇을 얻는가?

#### 2. TODO 1 — convert (금액 하나를 환산한다)

```java
/**
 * 금액 하나를 환산한다.
 *
 * <p>사람이 읽는 금액으로 바꿔 곱하고, 대상 통화의 자릿수로 되돌린다.
 * 최소 단위 정수끼리 곱하면 자릿수가 다른 통화에서 100 배씩 틀린다.
 */
public Money convert(Money money, Currency target, Path path) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- 최소 단위 정수 1050(= $10.50)에 환율 1350.25를 그냥 곱하면 얼마가 나오는가?
- 올바른 값은 얼마인가?
- 몇 배 틀리는가 — 그리고 예외는 나는가?
- 같은 통화로 환산할 때 환율표를 안 보는 이유는 무엇인가?
- `KRW → KRW` 환율이 표에 없어도 되는 이유는 무엇인가?
- 자릿수가 줄어드는 환산(BHD 3자리 → USD 2자리)에서는 무엇이 사라지는가?
- 반올림은 계산의 어느 시점에 한 번 하는가?

#### 3. 환율 — 역수가 아니다 (`RateTable` — 주어진 계약)

```text
  USD -> KRW  1350.25      KRW -> USD  0.00073
  (1350.25 의 역수는 0.00074060)
```

- 왜 서로의 역수가 아닌가?
- 그 차이는 무엇이라고 부르는가?
- KRW 10,000원을 달러로 바꿨다가 도로 원화로 바꾸면 얼마가 되는가?
- 얼마가 사라지는가?
- 그 손실이 버그인가 정상인가?
- 역수로 반대 방향을 계산하면 무엇을 지우는 것인가?
- `RateTable` 이 생성 시점에 막는 것 셋은 무엇인가?

#### 4. TODO 2 — viaUsd (USD 를 거치는 환율)

```java
/**
 * USD 를 거치는 환율.
 *
 * <p>중간에 반올림하지 않는다. 두 환율을 곱한 다음 한 번만 반올림한다.
 *
 * <p>출발이나 도착이 USD 인 경우를 따로 안 나눈다.
 * RateTable 이 같은 통화의 환율을 1 로 주기 때문에 USD -> USD 가 곱해져도 값이 안 변한다.
 */
private BigDecimal viaUsd(Currency from, Currency to) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 중간에 USD 금액으로 반올림하면 무엇이 생기는가?
- JPY 1,000을 원화로 바꿀 때 직접과 경유는 각각 얼마인가?
- EUR 100이면 두 값이 얼마이고 차이는 얼마인가?
- 금액이 크면 차이가 어떻게 되는가?
- JPY 100이면 두 길이 같은가 다른가?
- 그 사실이 왜 함정인가?
- 출발이나 도착이 USD 이면 두 길이 같은 이유는 무엇인가?
- 변종 검증에서 무엇을 지웠고 왜 지워도 됐는가?
- 지운 뒤 무엇을 테스트로 남겼는가?
- BHD → KRW 는 직접 길에서 어떻게 되는가?
- 환율이 없을 때 조용히 0을 돌려주면 무슨 사고가 나는가?

#### 5. TODO 3 — total (합계)

```java
/**
 * 여러 금액의 합계를 대상 통화로 낸다.
 *
 * <p>같은 통화끼리 먼저 더하면 반올림 횟수가 준다. 그만큼 오차도 준다.
 * 그런데 항목별 환산 금액을 화면에 보여주고 있으면
 * 그 항목들의 합이 여기서 낸 합계와 안 맞는다.
 */
public Money total(List<Money> monies, Currency target, SumOrder order, Path path) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- `CONVERT_EACH_THEN_SUM` 은 반올림을 몇 번 하는가?
- `SUM_THEN_CONVERT` 는 몇 번 하는가?
- 통화가 셋 섞이면 "묶어서"도 몇 번 반올림하는가?
- 그래서 "한 번만 반올림한다"는 말은 언제만 맞는 말인가?
- 왜 "합한 뒤 환산"이 사실은 "같은 통화끼리 먼저 묶기"를 뜻하는가?
- 빈 목록은 어떻게 되는가?

#### 6. TODO 4 — reconciliationGap (정합)

```java
/**
 * 항목의 합과 합계의 차이.
 *
 * <p>0 이 아니면 화면의 세로 합이 안 맞는다는 뜻이다.
 */
public long reconciliationGap(List<Money> monies, Currency target, SumOrder order, Path path) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 이 값은 무엇에서 무엇을 뺀 것인가?
- `CONVERT_EACH_THEN_SUM` 에서 이 값이 항상 0인 이유는 무엇인가?
- JPY 111/222/333 예제에서 두 순서의 gap은 각각 얼마인가?
- gap이 0이 아닌 영수증을 고객이 보면 무엇을 묻는가?

#### 7. 반올림 방식 (`RoundingMode` 주입)

```text
  JPY 111 x 9.05 = 1,004.55        JPY 10 x 9.05 = 90.50
```

- 세 방식(HALF_UP / HALF_EVEN / DOWN)의 값은 각각 얼마인가?
- 은행가 반올림은 어떤 경우에만 갈리는가?
- 환율에 소수가 많으면 그런 경우가 왜 잘 안 생기는가?
- "쓰기로 해놓고 안 쓴 것을 아무도 모른다"는 무슨 뜻인가?
- 버림은 어느 쪽에 유리한가?
- 왜 반올림 방식을 코드에 박지 않고 **주입**하는가?

### B. 개념

#### 8. 측정 — 어긋남의 크기 (원본 README "측정이 알려준 것")

- 바구니 5,000개 중 세로 합이 안 맞는 바구니는 몇 개인가?
- 차이 합·한 건 최대·총액 차는 각각 얼마인가?
- 총액으로는 없는 셈인데 왜 문제인가?
- 항목이 1개·5개·20개·100개일 때 안 맞는 바구니 수는 각각 몇인가?
- 항목이 하나면 왜 한 번도 안 어긋나는가?
- 그 사실이 예제에 대해 무엇을 말해주는가?
- 경유 여부로 갈린 바구니는 몇 개이고 한 건 최대는 얼마인가?
- 비율로는 0.0028%인데 왜 그냥 넘길 수 없는가?
- 은행가 반올림과 버림은 각각 몇 개의 바구니에서 갈렸는가?
- 두 방식의 총액 차이는 각각 얼마인가?
- 원화만 있는 바구니면 아홉 조합이 어떻게 되는가?
- "개발 자료는 대개 국내 주문"이 왜 경고인가?

#### 9. 함정 (원본 README "함정")

- README가 꼽은 함정 다섯은 각각 무엇인가?
- 그중 **100배 틀리는데 예외가 안 나는** 것은 어느 것인가?
- 그중 **결제가 0원이 되는** 것은 어느 것인가?

#### 10. 생각해볼 것 (원본 README 이관)

- 환율이 언제 것인가 — 주문 시점인가 결제 시점인가 정산 시점인가?
- 세로 합이 안 맞는 영수증을 고객이 물으면 뭐라고 답하나?
- 부분 환불에서 환율이 바뀌었으면 어느 환율로 돌려주나?
- 통화 하나를 더 지원하면 환율표에 몇 줄이 필요한가 — 열 개면?

#### 11. 연결

- "언제 반올림하나"는 03-subscription-change 의 어느 자리와 같은 구조인가?
- "죽은 분기를 지우고 지워도 되는 이유를 테스트로 남긴다"는 03의 어느 이야기와 같은가?
- 최소 단위 정수로 금액을 드는 방식은 실무의 어떤 관행과 같은가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
