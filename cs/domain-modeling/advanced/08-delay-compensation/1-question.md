# domain-modeling-advanced/08-delay-compensation — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 규칙 한 줄 (원본 README)

```text
  "하루 늦을 때마다 주문 금액의 0.5 퍼센트, 최대 10 퍼센트"

    하루를 어떻게 세나     달력일인가 영업일인가
    면책 기간을 빼나       천재지변만인가 택배사 탓도인가 안 빼나
    상한을 어디에 거나     주문 금액 기준인가 배송비 기준인가
```

- 이 한 줄이 **안 말해주는** 결정 셋은 무엇인가?
- 셋을 곱하면 조합이 몇 개인가?
- 같은 배송인데 배상금이 얼마와 얼마 사이 어디든 될 수 있는가?
- 이 셋을 enum 으로 올리지 않고 하나씩 골라 구현하면 무엇을 잃는가?
- "최대 10 퍼센트"라는 말이 무엇의 10 퍼센트인지 안 정해주는 이유는 무엇인가?

#### 1. TODO 1 — delayedDays (지연된 날들)

```java
/**
 * 지연된 날들.
 *
 * <p>약속일 다음 날부터 도착일까지다. 약속일 당일은 안 센다.
 * 그날은 아직 안 늦었기 때문이다.
 *
 * <p>도착일은 센다. 그날 받았어도 늦게 받은 것이다.
 */
private List<LocalDate> delayedDays(Delivery delivery) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- 약속일 당일을 안 세는 이유는 무엇인가?
- 도착일을 세는 이유는 무엇인가?
- 양쪽 끝을 다 세면 무엇이 어떻게 어긋나는가?
- 양쪽 끝을 다 빼면 무엇이 어떻게 어긋나는가?
- 약속보다 일찍 도착하면 이 루프는 몇 바퀴 도는가?
- 날 수(int)가 아니라 날짜 목록(List)을 돌려주는 이유는 무엇인가?

#### 2. TODO 2 — excused (이 날을 면책으로 빼나)

```java
/**
 * 이 날을 면책으로 빼나.
 *
 * <p>면책이 없는 날은 언제나 센다.
 */
private boolean excused(Delivery delivery, LocalDate date, ExcusePolicy policy) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 세 면책 정책은 각각 어떤 사유를 빼는가?
- "남 탓만 뺀다"에서 택배사 탓은 남 탓인가 우리 탓인가?
- 그 판단의 근거는 무엇인가?
- 제일 흔한 정책이 왜 문구로 적기 제일 어려운가?
- 면책 기간을 양끝 포함으로 잡은 이유는 무엇인가?
- 면책이 겹치면 어느 사유를 쓰는가?
- 면책이 하나도 없는 배송으로 시험하면 무엇이 안 보이는가?

#### 3. TODO 3 — cap (상한 금액)

```java
/** 상한 금액. */
private long cap(Delivery delivery, CapBasis basis) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- 두 상한 기준의 계산식은 각각 무엇인가?
- 주문 500,000 배송비 3,000일 때 두 상한은 각각 얼마인가?
- 주문 기준 상한에 닿는 날이 언제나 20일인 이유는 무엇인가?
- 배송비 기준에서 비싼 물건일수록 빨리 닿는 이유는 무엇인가?
- 무료 배송에서 배송비 기준 상한을 쓰면 무슨 일이 생기는가?
- 요즘 배송이 대개 무료라는 사실이 그 규칙을 어떻게 만드는가?

#### 4. TODO 4 — compensate (배상금)

```java
/**
 * 배상금을 낸다.
 *
 * <p>상한은 금액에 건다. 날 수에 걸면 상한에 닿은 뒤로도 날짜가 늘 때
 * 금액이 계속 같은 것이 안 보인다. 금액에 걸어야 {@code capped} 가 사실이 된다.
 */
public Compensation compensate(
        Delivery delivery, DayCount dayCount, ExcusePolicy policy, CapBasis capBasis) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 상한을 날 수에 걸면 무엇이 안 보이게 되는가?
- 금액에 걸어야 `capped` 가 사실이 되는 이유는 무엇인가?
- 주말이면서 면책인 날을 두 번 빼지 않으려면 루프를 어떻게 짜야 하는가?
- 두 번 빼면 표본 배송의 세는 날은 몇이 되어야 할 것을 몇으로 만드는가?
- `Compensation` 이 rawDays·countedDays·chargeableDays 셋을 다 들고 있는 이유는 무엇인가?
- 금액은 세 날 수 중 어느 것에 곱하는가?
- 배상금 계산에서 곱하기와 반올림의 순서는 어떻게 되는가?

### B. 개념

#### 5. 함정 (원본 README "함정")

- README가 꼽은 함정 넷은 각각 무엇인가?
- "면책과 주말을 두 번 빼는 것"이 왜 눈에 잘 안 띄는가?
- 무료 배송 + 배송비 기준 상한에서 `capped` 는 참인가 거짓인가?

#### 6. 측정 — 답이 갈리는 건수 (원본 README "측정이 알려준 것")

- 배송 5,000건 중 금액이 갈린 건은 몇 건인가?
- 전부 같은 건은 몇 건이고 대부분 어떤 경우인가?
- 금액 폭의 평균과 최대는 각각 얼마인가?
- 배송비 기준 상한의 배상 총액은 주문 기준의 몇 퍼센트인가?
- 그 둘이 규칙 문구로는 왜 구별이 안 되는가?
- 주문 금액 10만·50만·100만에서 배송비 기준 상한 도달일은 각각 며칠인가?
- 100만 원짜리를 열흘 늦게 받으면 두 기준에서 배상금이 각각 얼마인가?
- 영업일로 세면 총액이 몇 퍼센트가 되는가?
- 주말 비율로 예상한 71 퍼센트보다 왜 덜 주는가?
- 면책 세 정책의 총액은 각각 얼마이고 폭은 몇 퍼센트인가?
- 짧고 깔끔한 예제(3/10~3/13, 면책 없음, 50만 원)에서 열두 조합은 각각 얼마인가?
- 예제가 대개 그렇게 생겼다는 사실이 왜 문제인가?

#### 7. 변종 검증에서 고친 것 (원본 README 이관)

- `daysToCap` 에서 무엇을 지웠는가?
- 왜 지워도 되는가?
- "가드가 많은 게 아니라 정확한 게 안전"이라는 말은 무슨 뜻인가?

#### 8. 생각해볼 것 (원본 README 이관)

- 면책 기간이 서로 겹치면 어느 사유로 보나?
- 먼저 적힌 것인가 가장 유리한 것인가?
- 배송비 기준 상한과 주문 기준 상한 중 큰 쪽을 쓰면 어떻게 되나?
- 공휴일은 영업일인가?
- 그 목록은 어디에 두나?
- 배상금을 현금으로 주나 포인트로 주나?
- 그 선택이 상한 논의를 바꾸나?

#### 9. 연결

- "구간의 양끝을 세나"는 앞의 어느 박스와 같은 자리인가?
- 06-tax·07-installment와 공통으로 반복되는 "예제가 쉬워서 갈림이 숨는다" 패턴은 여기서 어떤 모습인가?
- 이 박스의 `Compensation` 이 중간 단계를 다 들고 있는 설계는 실무의 어떤 관행과 같은가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
