# domain-modeling-advanced/12-seat-hold — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 그림 한 장 (원본 README)

```text
  3 열 x 5 석에서 세 명씩 세 무리가 앉으면

    A1 A2 A3 | A4 A5
    B1 B2 B3 | B4 B5
    C1 C2 C3 | C4 C5

  남은 자리는 여섯인데 붙은 세 자리가 없다
```

- 좌석이 재고(11번)와 다른 점은 정확히 무엇인가?
- "여섯 자리 남음"이라고 표시해두고 세 자리를 못 파는 일이 왜 생기는가?
- "세 자리 주세요"라는 요청에 안 들어 있는 결정 셋은 각각 무엇인가?
- 빈자리 수(`freeCount`)와 최장 연석(`longestRun`)을 왜 따로 재야 하는가?
- 이 셋을 enum 으로 올리지 않고 하나씩 골라 구현하면 무엇을 못 하게 되는가?

#### 1. TODO 1 — free (그 시각에 비었나)

```java
/**
 * 그 시각에 그 좌석이 비어 있나.
 *
 * <p>배치 방식이면 만료됐어도 아직 잡혀 있는 것으로 본다.
 */
private boolean free(String seat, Instant at, ExpiryCheck check) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- 한 번도 안 잡힌 좌석은 `heldUntil` 에서 무엇으로 나오고, 그때 답은 무엇인가?
- LAZY 와 SWEEP 은 각각 어떤 조건식이 되는가?
- 만료 정각(`at == until`)은 비었는가 잡혔는가?
- 11번의 `Boundary` enum 이 여기에는 왜 없는가?
- SWEEP 에서 이 함수가 "만료"를 아예 안 보는 것은 무슨 뜻인가?

#### 2. TODO 2 — findRun (한 열에서 연석 찾기)

```java
/**
 * 한 열에서 연속으로 빈 자리를 찾는다.
 *
 * <p>{@code Fit} 이 앞에서부터면 처음 맞는 자리를, 딱 맞는 자리면
 * 남는 칸이 제일 적은 자리를 고른다.
 *
 * @return 좌석 이름들. 못 찾으면 빈 목록
 */
private List<String> findRun(int size, Instant at, Fit fit, ExpiryCheck check) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 안쪽 반복이 `n <= seatsPerRow + 1` 로 한 칸 더 도는 이유는 무엇인가?
- 그 한 칸을 안 돌면 어떤 연석이 통째로 안 보이게 되는가?
- 연석의 길이는 어떤 식으로 계산되는가?
- FIRST_FIT 은 왜 찾자마자 바로 반환할 수 있는가?
- BEST_FIT 이 재는 `slack` 은 무엇이고, 왜 그게 제일 작은 자리를 고르는가?
- `slack` 이 같은 후보가 둘이면 어느 쪽이 남는가?
- 연석을 열을 넘겨 이어붙이면 안 되는 이유는 무엇인가?

#### 3. TODO 3 — hold (좌석 잡기)

```java
/**
 * 좌석을 잡는다.
 *
 * <p>붙은 자리를 먼저 찾고, 정책이 허락하면 흩어서 찾는다.
 * 못 잡으면 아무것도 안 잡고 빈 결과를 준다. 반쯤 잡아두면
 * 그 좌석이 아무에게도 안 팔린 채로 남는다.
 */
public Hold hold(
        String groupId, int size, Instant at, Duration ttl,
        Adjacency adjacency, Fit fit, ExpiryCheck check) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- 흩어짐을 허락하는 규칙도 붙은 자리를 먼저 찾는 이유는 무엇인가?
- `together` 값은 어느 시점에 정해지는가?
- ALLOW_SPLIT 과 PREFER_TOGETHER 는 이 코드에서 구별되는가?
- 못 잡았을 때 조기 반환(`if (seats.isEmpty()) return ...`)을 일부러 안 두는 이유는 무엇인가?
- "반쯤 잡아두는 것"이 왜 거절보다 나쁜가?
- 같은 무리가 두 번 잡으려 하면 어떻게 되는가?
- 좌석 15개짜리 극장에 20명이 오면 세 규칙은 각각 어떻게 되는가?

#### 4. TODO 4 — longestRun (최장 연석)

```java
/**
 * 그 시각에 남은 가장 긴 연석.
 *
 * <p>빈 좌석 수가 같아도 이 값이 다르면 팔 수 있는 것이 다르다.
 * 단편화를 재는 값이다.
 */
public int longestRun(Instant at, ExpiryCheck check) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 좌석이 잡혀 있을 때 카운터를 0으로 안 되돌리면 이 함수는 무엇을 재게 되는가?
- A2 와 A4 만 빈 한 열에서 이 함수의 정답은 얼마인가?
- 그 결함이 원래 테스트로 안 잡혔던 이유는 무엇인가?
- 이 값을 재는 것이 왜 "단편화를 재는 것"인가?

#### 5. adjacent (주어진 코드 — 연속 판정)

```java
/** 잡은 좌석들이 한 열에 연속인가. */
public boolean adjacent(List<String> seats) { ... }
```

- 이 함수가 열을 안 보고 번호만 보면 어떤 입력에서 틀리는가?
- `A5` 와 `B1` 을 넣어도 그 결함이 안 잡히는 이유는 무엇인가?
- 번호를 정렬하고 나서 보는 것은 무엇을 허용하기 위함인가?
- 좌석이 0개·1개면 무엇을 돌려주고, 왜 그렇게 정했는가?

### B. 개념

#### 6. 측정 — 연석 요구의 값 (원본 README "측정이 알려준 것" 하나)

10열 x 10석, 무리가 10초 간격, 15~30분씩 잡고, 300무리를 흘려보낸다.

| 무리 크기 | 붙어야 함 | 흩어져도 됨 | 차이 |
|---|---|---|---|
| 1~2 인 | ? | ? | ? |
| 1~4 인 | ? | ? | ? |
| 3~6 인 | ? | ? | ? |
| 5~8 인 | ? | ? | ? |

- 네 줄의 숫자는 각각 얼마였는가?
- 두 명짜리만 오면 연석 요구가 왜 사실상 공짜인가?
- 여덟 명짜리가 섞이면 몇 퍼센트를 못 파는가?
- "연석 보장의 비용은 정책이 아니라 손님 구성이 정한다"는 말은 무슨 뜻인가?
- 영화관과 콘서트장의 답이 달라질 수 있는 이유는 무엇인가?

#### 7. 측정 — PREFER_TOGETHER (원본 README "측정이 알려준 것" 둘)

- 흩어진 무리 수는 무리 크기별로 각각 얼마였는가?
- 좌석 판매량이 ALLOW_SPLIT 과 "정확히 같다"는 것이 왜 중요한가?
- 이 규칙이 "거의 공짜로 둘 다 얻는다"고 말할 수 있는 근거는 무엇인가?

#### 8. 측정 — first fit / best fit (원본 README "측정이 알려준 것" 셋·넷)

| 무리 크기 | 앞에서부터 | 딱 맞는 자리 | 거절 |
|---|---|---|---|
| 1~2 인 | ? | ? | ? |
| 1~4 인 | ? | ? | ? |
| 3~6 인 | ? | ? | ? |
| 5~8 인 | ? | ? | ? |

- 거절 수는 두 방식에서 어떻게 달랐는가?
- 한 번의 배정에서 두 방식이 남기는 최장 연석은 각각 얼마인가?
- 300번 누적하면 251과 252인데, 한 번은 3과 5로 갈리는 이유는 무엇인가?
- "이 경우에 크게 다르다"와 "전체적으로 크게 다르다"는 어떻게 다른 말인가?
- 자료구조 교재의 first fit / best fit 논의를 이 도메인에 그대로 가져오면 무엇이 틀리는가?

#### 9. 측정 — 빈 극장 (원본 README "측정이 알려준 것" 다섯)

- 무리 몇 개까지 세 규칙이 전부 같은 답을 내는가?
- 그 범위에서 거절과 흩어짐은 각각 얼마인가?
- 개발할 때 시험하는 범위가 대개 거기인 것이 왜 문제인가?

#### 10. 함정 (원본 README "함정")

- README 가 꼽은 함정 다섯은 각각 무엇인가?
- 좌석을 개수로만 보면 무엇을 못 팔게 되는가?
- 배치가 만료 안 된 자리까지 비우면 손님에게 무슨 일이 생기는가?

#### 11. 변종 검증 (원본 README "변종 검증에서 고친 것")

- 조기 반환을 지운 이유는 무엇인가 — 같은 답을 내는 갈래가 둘이면 무엇이 나쁜가?
- 배치가 만료 안 된 자리까지 비워도 통과했던 이유는 무엇인가?
- 최장 연석에서 0으로 안 되돌려도 통과했던 이유는 무엇인가?
- 연속 판정에서 열을 안 봐도 통과한 것을 잡아낸 입력은 무엇인가?
- 네 가지 결함의 공통점은 무엇인가?

#### 12. 생각해볼 것 (원본 README 이관)

- 흩어 앉게 될 것을 예매 화면에서 언제 알려주는가 — 결제 직전인가 좌석 고를 때인가?
- 통로 옆자리를 붙은 것으로 보는가, 그 정보를 배치도가 들고 있는가?
- 만료된 자리를 새 무리가 가져가면 앞 무리의 결제 화면은 어떻게 되는가?
- 한 무리가 여러 열에 걸쳐 앉아야 한다면 어느 열끼리 묶어 주는가?

#### 13. 연결

- 11-stock-reservation 과 같은 축은 무엇이고 달라진 축은 무엇인가?
- `longestRun` 이 재는 것은 운영체제의 어떤 문제와 같은 구조인가?
- "부하가 없으면 설계 차이가 안 보인다"는 다른 어느 주제에서도 되풀이되는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
