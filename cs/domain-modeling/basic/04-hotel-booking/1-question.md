# domain-modeling-basic/04-hotel-booking — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     과제(A)는 원문 규칙·javadoc·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문 유형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제

#### 0. 요구사항 — 그림 한 장 (원본 README)

```text
  A  3/1 ~ 3/3        B  3/3 ~ 3/5

    3/1  3/2  3/3  3/4
    A----A----|
              B----B----|

  3/3 에 A 는 나가고 B 는 들어온다. 겹치나 안 겹치나.
```

- 이 질문에 답이 둘인 이유는 각각 무엇인가?
- 이 결정은 코드의 어느 자리로 올라갔는가?
- "3/1 \~ 3/3" 은 며칠이고 몇 박인가?
- 날짜 개수와 숙박 일수가 다른 이유는 무엇인가?
- 체크아웃 날은 방을 쓰는 날인가?
- 요구사항이 안 말해주는 또 다른 결정(0박·뒤집힌 날짜)은 어디서 막는가?

#### 1. Stay 의 계약 (TODO 없음 — 왜 다 주어졌나)

```java
public Stay {
    if (guest == null || guest.isBlank()) { throw ...; }
    if (!checkOut.isAfter(checkIn)) { throw ...; }
}
public long nights() { return checkOut.toEpochDay() - checkIn.toEpochDay(); }
public boolean occupies(LocalDate date) {
    return !date.isBefore(checkIn) && date.isBefore(checkOut);
}
```

- `nights()` 가 "날짜 개수 − 1" 이 아니라 두 epochDay 의 차인 이유는 무엇인가?
- `occupies` 의 두 부등호가 각각 담당하는 경계는 어디인가?
- "반열린 구간 `[checkIn, checkOut)`" 이라는 말은 무슨 뜻인가?
- 0박 예약을 허용하면 무슨 일이 벌어지는가?
- 이 검증을 `Bookings` 가 아니라 `Stay` 에 둔 이유는 무엇인가?

#### 2. TODO 1 — overlaps (겹침의 정의)

```java
/**
 * 두 숙박이 겹치나.
 *
 * <p>sameDayTurnover 가 true 면 같은 날 퇴실과 입실을 허용한다(안 겹친다고 본다).
 * false 면 그날 하루를 비워야 한다.
 */
public static boolean overlaps(Stay a, Stay b, boolean sameDayTurnover) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- `sameDayTurnover=true` 일 때의 겹침 조건식은 무엇인가?
- 그 식이 왜 "표준 구간 겹침" 공식인가?
- `sameDayTurnover=false` 일 때는 부등호를 어떻게 바꾸는가?
- "경계를 하루씩 넓혀서 본다"는 말을 식으로 어떻게 실현하는가?
- 판정이 대칭이어야 한다는 것은 무슨 뜻이고, 두 식은 대칭인가?
- A(3/1\~3/5) 기준으로 안에 들어감/뒤로 걸침/완전히 뒤/완전히 앞은 각각 어떻게 판정되는가?

#### 3. TODO 2 — occupancyByDate (날짜별 세기)

```java
/**
 * 날짜별 사용 객실 수. 체크아웃 날은 안 센다.
 */
public static Map<LocalDate, Integer> occupancyByDate(List<Stay> stays) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 날짜 루프의 종료 조건은 왜 `isBefore(checkOut)` 인가?
- `<=` 로 쓰면 무엇이 하나 더 붙는가?
- 아무도 안 쓰는 날은 지도(Map)에 있어야 하는가?
- A(3/1\~3/3)·B(3/2\~3/4) 일 때 지도의 크기와 각 날의 값은?
- 이 방식의 비용은 무엇에 비례하는가?

#### 4. TODO 3 — peakOccupancy (이벤트로 세기)

```java
/**
 * 이벤트로 세는 판. 날짜 수가 아니라 예약 수에 비례한다.
 *
 * <p>체크인에 +1, 체크아웃에 -1 을 두고 날짜 순으로 훑는다.
 * 같은 날짜에서 체크아웃을 먼저 처리해야 그날 퇴실한 방을 다시 팔 수 있다.
 */
public static int peakOccupancy(List<Stay> stays, boolean sameDayTurnover) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- 이벤트 두 종류는 각각 무엇이고 변화량은 얼마인가?
- 같은 날짜에 퇴실과 입실이 겹치면 어느 쪽을 먼저 처리해야 "같은 날 교체 허용"이 되는가?
- 반대로 입실을 먼저 처리하면 무엇을 세는 셈이 되는가?
- 정렬 비교자에서 이 순서를 어떻게 표현하는가(−1 과 +1 의 대소)?
- 빈 목록이면 무엇을 돌려주는가?
- 이 방식의 비용은 무엇에 비례하는가?
- 1년짜리 장기 숙박 한 건에서 날짜별과 이벤트는 각각 몇 칸을 도는가?

#### 5. TODO 4 — canAccept (받을 수 있나)

```java
/**
 * 객실 수를 넘기지 않고 추가할 수 있나.
 *
 * <p>기존 예약에 새 예약을 더했을 때 어느 날도 객실 수를 안 넘으면 받을 수 있다.
 */
public static boolean canAccept(
        List<Stay> existing, Stay candidate, int rooms, boolean sameDayTurnover) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 판정 식은 무엇인가(`<` 인가 `<=` 인가)?
- 왜 후보를 목록에 **더한 뒤** 최대치를 보는가?
- 객실 수가 0 이하면 어떻게 하는가?
- 원본 `existing` 목록을 건드리지 않으려면 무엇을 해야 하는가?
- 객실 1개, A(3/1\~3/3) 가 있을 때 B(3/3\~3/5) 는 두 규칙에서 각각 받을 수 있는가?

#### 6. overlappingPairs — 대조용 (주어진 코드)

- 이 함수가 세는 것과 `peakOccupancy` 가 세는 것은 무엇이 다른가?
- 서로 겹치는 예약 3건·4건일 때 쌍의 수와 필요한 객실 수는 각각 얼마인가?
- 쌍의 수는 어떤 속도로 늘고 필요한 객실은 어떤 속도로 느는가?

### B. 개념

#### 7. 측정 — 교체 규칙의 값 (원본 README 하나·둘)

- 12건씩 1,000회 · 20일 범위에서 최대 동시 사용이 다른 경우는 몇 회인가?
- 두 규칙의 합계는 각각 얼마이고 몇 퍼센트 차이인가?
- 그 퍼센트가 현실에서 무슨 뜻인가?
- 예약 수락 여부는 1,000회 중 몇 회가 갈리는가?
- "방향이 한쪽뿐이다"는 무슨 뜻이고, 왜 그럴 수밖에 없는가?

#### 8. 측정 — 쌍의 수 vs 객실 수 (셋)

- 1,000회 중 두 값이 같았던 적은 몇 번인가?
- "겹치는 예약이 6건 있습니다"가 왜 쓸모없는 보고인가?
- 그런데도 쌍을 세는 코드를 자주 만드는 이유는 무엇인가?

#### 9. 측정 — 두 세는 방법 (넷)

- 500회 시험에서 두 값이 어긋난 적은 몇 회인가?
- 답이 같은데도 두 방법을 다 만들어 본 이유는 무엇인가?
- 비용은 각각 무엇에 비례하는가?
- 이 구조는 알고리즘 트랙의 어느 주제와 같은가?

#### 10. 변종 검증에서 배운 것 (원본 README 절 이관)

- 변종 12개 중 둘이 `impl/` 이 아니라 어디에 있었는가?
- "주어진 코드라고 검증을 건너뛰면" 어떻게 되는가?

#### 11. 생각해볼 것 (원본 README 이관)

- 방마다 등급이 다르면 등급별로 따로 세야 하는가, 상위 등급으로 대체할 수 있는가?
- 예약을 취소하면 그 자리를 즉시 다시 파는가 — 결제 취소가 아직 안 끝났다면?
- 최소 숙박일(성수기 2박 이상) 규칙은 `Stay` 에 두는가 `Bookings` 에 두는가?
- 체크인 시각과 체크아웃 시각까지 다루면 "하루" 대신 무엇으로 세는가?

#### 12. 연결

- 01번(주차 요금)의 "구간 경계 포함" 문제와 여기의 "체크아웃 날"은 무엇이 같은가?
- 02번(좌석)의 `Preference` 처럼, 여기서 "안 정해도 돌아가는데 뒤에 영향이 큰" 값은 무엇인가?
- "무엇을 세느냐"의 교훈은 02번의 고아 좌석과 어떻게 이어지는가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
