# domain-modeling-basic/08-meeting-slot — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 요구사항·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문형: "이 한 줄에 안 들어 있는 결정은 무엇인가" -->

### A. 과제 (MeetingFinder.java 의 TODO 1~4)

#### 0. 요구사항

```text
"참석자들의 일정을 모아 공통으로 빈 시간을 찾는다."

  A  [10-11]      [14-15]
  B      [10:30-12]
  C            [13-14]

  합치면  [10-12] [13-15]
  빈 곳   09-10, 12-13, 15-18

어려운 것은 "이동 시간" 이다. 회의가 끝나자마자 다음 회의를 넣을 수 있나.
```

- 이 요구사항이 안 정해준 결정 넷을 꼽으면?
- "이동 시간"을 있다고 보느냐 없다고 보느냐로 하루가 어떻게 달라지는가?
- 이동 시간은 누구의 일정에도 안 적히는데 왜 계산에 넣어야 하는가?
- 근무 시간(09-18)은 이 문장에 나오는가, 어디서 오는 값인가?
- "빈 시간을 찾는다"와 "회의를 넣을 수 있다"는 같은 말인가?

#### 1. TODO 1 — mergeBusy (바쁜 구간 합치기)

```java
/**
 * 바쁜 구간들을 합친다. 맞닿거나 겹치는 것은 하나로 만든다.
 *
 * <p>buffer 를 주면 각 구간 앞뒤에 그만큼 붙인 뒤 합친다.
 * 그러면 회의 사이의 짧은 틈이 사라진다.
 */
public static List<TimeSlot> mergeBusy(List<TimeSlot> busy, Duration buffer) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- 맞닿은 구간([12-13]과 [13-14])을 안 합치면 무슨 일이 일어나는가?
- 그 사고가 왜 조용히 넘어가지 않고 예외로 터지는가?
- [10-15] 와 [11-12] 를 합칠 때 끝을 그냥 덮어쓰면 결과가 무엇이 되는가?
- 그래서 끝은 어떤 연산으로 갱신해야 하는가?
- 왜 합치기 전에 정렬부터 해야 하는가?
- 정렬 키를 시작만으로 잡는 것과 시작·끝 둘로 잡는 것의 차이는 무엇인가?
- buffer 를 붙이는 것과 합치는 것 중 무엇을 먼저 해야 하는가?
- [10-11] 과 [11:15-12] 에 15분 여유를 주면 결과는 몇 구간이고 어디부터 어디까지인가?
- 빈 목록이 들어오면 무엇을 반환해야 하는가?

#### 2. TODO 2 — freeSlots (빈 구간)

```java
/**
 * 근무 시간 안에서 비어 있는 구간들.
 *
 * <p>바쁜 구간이 근무 시간 밖으로 넘칠 수 있다. 그때는 잘라서 본다.
 *
 * <p>앞뒤로 완전히 벗어난 구간이 특히 조용하다.
 * 근무 시작 전에 끝난 회의를 안 건너뛰면 커서가 뒤로 가고,
 * 근무 종료 후에 시작한 회의에서 안 멈추면 근무 시간 밖에 빈 구간이 생긴다.
 * 둘 다 "걸쳐 있는" 회의만 넣어 시험하면 안 드러난다.
 */
```

- 커서(cursor) 방식으로 빈 구간을 줍는 절차를 순서대로 말하면?
- 근무 09-18 에 07-08 회의가 있을 때 건너뛰지 않으면 결과가 어떻게 되는가?
- 근무 09-18 에 19-20 회의가 있을 때 멈추지 않으면 결과가 어떻게 되는가?
- 왜 이 두 결함이 "가장 조용한" 결함인가?
- "걸쳐 있는" 회의(08-10, 17-20)만으로 시험하면 왜 둘 다 안 드러나는가?
- 마지막 빈 구간(커서~근무 종료)을 넣을 때 어떤 조건을 확인해야 하는가?
- 바쁜 일정이 하나도 없으면 결과는 무엇인가?
- 근무 시간이 통째로 차 있으면 결과는 무엇인가?

#### 3. TODO 3 — candidateStarts (회의 후보)

```java
/**
 * 길이가 duration 이상인 빈 구간의 시작 시각들.
 *
 * <p>step 만큼 건너뛰며 후보를 만든다. 15 분 단위 회의라면 step 이 15 분이다.
 */
```

- 빈 구간 [9-10] · 회의 30분 · step 15분일 때 후보는 몇 개이고 각각 언제인가?
- 09:45 시작이 후보에서 빠지는 이유는 무엇인가?
- 루프의 종료 조건을 `at.plus(duration).isAfter(free.end())` 로 쓰는 이유는 무엇인가?
- 회의 길이가 0이나 음수면 어떻게 해야 하는가?
- 빈 시간이 45분인데 30분 회의를 못 넣는 상황이 왜 가능한가?
- `step` 을 5분으로 줄이면 "넣을 수 있나"가 바뀌는가?
- 그럼 `step` 이 정하는 것은 무엇인가?

#### 4. TODO 4 — totalFree (빈 시간 총합)

```java
/** 빈 시간의 총합. */
public static Duration totalFree(
        TimeSlot workingHours, List<TimeSlot> busy, Duration buffer) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 이 값이 보고서에 쓰이면 어떤 오해를 만드는가?
- 총합과 후보 개수 중 "회의를 잡을 수 있나"를 말해주는 것은 어느 쪽인가?

### B. 개념

#### 5. 측정이 알려준 것 (원본 README 측정 절)

- 참석자 3명·각 3건·500일 기준으로 여유 15분을 주면 빈 시간과 후보가 각각 얼마에서 얼마가 되는가?
- 500일 중 몇 일에서 값이 갈리는가?
- 회의 3건에 앞뒤 15분씩 붙이면 하루 몇 시간을 더 먹는가?
- 그 시간이 무서운 이유는 무엇인가?
- 참석자가 1 → 12명으로 늘 때 평균 빈 시간과 1시간 회의 후보는 각각 어떻게 변하는가?
- 빈 시간은 선형으로 주는데 후보는 왜 절벽으로 떨어지는가?
- 8명일 때 하루 107분이 남는데도 62퍼센트의 날에 1시간 회의를 못 잡는 이유는?
- 총 빈 시간 45분인데 30분 회의가 0개인 그림을 그려서 설명하면?
- `step` 을 30분·15분·5분으로 바꾸면 빈 구간 1시간에 30분 회의 후보는 각각 몇 개인가?

#### 6. 변종 검증에서 고친 것 (원본 README)

- `freeSlots` 에 있던 겹치는 방어 둘은 각각 무엇이었는가?
- 둘 중 하나만 지워도 아무 테스트가 안 깨진 이유는 무엇인가?
- 어느 쪽을 지우고 어느 쪽을 남겼는가?
- 어떤 테스트를 더해서 남긴 쪽이 죽지 않게 했는가?
- 이 교훈은 06번 정기권의 무엇과 같은가?

#### 7. 생각해볼 것 (원본 README)

- 참석자마다 근무 시간이 다르면(시차·시간제) 공통 근무 시간은 무엇인가?
- 필수 참석자와 선택 참석자를 나누면 무엇이 달라지는가?
- 후보가 없을 때 무엇을 제안하는가?
- 회의실도 자원이면 참석자 하나가 더 있는 것과 같은가?

#### 8. 연결

- `TimeSlot` 의 "시작 포함·끝 미포함"은 04번·06번의 무엇과 같은 규약인가?
- 안에 들어간 구간이 끝을 줄이는 실수는 어느 챕터에서 본 것과 같은가?
- 07번 알림의 "순서가 계약"과 여기 mergeBusy 의 순서 문제는 어떻게 같고 어떻게 다른가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
