# domain-modeling/basic/17-cart-discount — 정리 (힌트)

> 복습 시 이 파일은 **질문에 막혔을 때만** 연다. 먼저 읽고 답하면 인출이 아니라 받아쓰기다.
> 작성 방식: 내가 먼저 기억으로 흐름을 서술하고, Claude는 빠지거나 틀린 곳을 짚는다. 대신 써주지 않는다.
> 이미 따라 치며 만든 정리본이 따로 있으면(organize류) 이 파일은 핵심 문장 압축 + 링크만 담는다.
> 2026-09-14: 쉽게 풀어쓴 서머리(Claude 작성) — 원본 myway 코드·문서 기준.

## 한눈에 — 쉽게 말하면

**장바구니 할인 = 세일 스티커 두 장을 어느 순서로 붙이느냐 문제.**

- 만 원짜리 물건에 "10% 할인" 스티커와 "1,000원 할인" 스티커를 둘 다 붙인다고 하자. **어느 걸 먼저 계산하느냐에 따라 값이 달라진다** — 10%를 먼저 하면 8,000원, 1,000원을 먼저 빼면 8,100원.
- 100원 차이인데, **그 100원이 어느 쪽인지는 어떤 문서에도 안 적혀 있다.** 요구사항은 "10% 쿠폰과 1,000원 쿠폰을 둘 다 쓸 수 있다"까지만 말한다.
- 이 챕터가 보여주는 것: 요구사항이 침묵하는 결정이 **셋**(순서·기준·반올림) 있고, 셋 다 최종 금액을 바꾸며, 코드를 쓰는 순간 **어느 한쪽으로든 정해진다.** 안 정한 게 아니라 모르고 정한 게 된다.

```text
10,000 원 + [10% 쿠폰] + [1,000원 쿠폰]

  정률 먼저   10,000 ─10%→ 9,000 ─1,000→ 8,000
  정액 먼저   10,000 ─1,000→ 9,000 ─10%→ 8,100   ← 100 원 차이
```

  - *정률 할인*: "10%"처럼 비율로 깎는 것. / *정액 할인*: "1,000원"처럼 금액으로 깎는 것.

실무 예: 쇼핑몰 쿠폰 중복 적용, 카드사 청구할인+상품 할인, 게임의 버프 중첩(12번과 같은 구조).

## 문제 — 이 챕터가 시키는 것

원본 README가 시키는 것은 이 말 몇 줄이 전부다.

> 10,000 원에 **"10 퍼센트"** 와 **"1,000 원 할인"** 을 적용하면 얼마인가.
> 정률 먼저 `10,000 → 9,000 → 8,000` / 정액 먼저 `10,000 → 9,000 → 8,100`.
> **100 원 차이다. 그리고 그 100 원이 어느 쪽인지는 아무 문서에도 없다.**
> 정할 것이 셋이다. **순서**, **정률의 기준**, **반올림 시점**. 셋 다 요구사항에 안 적힌다.

과제 목록:

- `src/main/java/com/domain/cart/Cart.java` 의 **TODO 1~3** 을 채운다.
  - TODO 1 `apply(total, discounts, order, base, roundEachStep)` — 할인을 순서대로 적용해 **적용 내역(`List<Applied>`)** 을 낸다. javadoc: "순서 규칙이 AS_GIVEN 이면 준 순서 그대로 쓴다. 정렬할 때는 **안정 정렬**이어야 같은 종류끼리의 순서가 유지된다."
  - TODO 2 `payable(...)` — 적용 후 낼 금액.
  - TODO 3 `totalDeduction(...)` — 총 할인액.
- `Discount`(정률/정액·값·상한 cap)와 `Order`·`Base` enum 은 **계약이라 전부 주어져 있다**. 깎는 금액을 `cap` 과 `remaining` 으로 자르는 `deductionFrom` 도 이미 있다.
- 테스트가 못 박은 계약: 10,000원+10%+1,000원 → **PERCENT_FIRST 8,000 / AMOUNT_FIRST 8,100** · 10,000원+10%×2 → **ORIGINAL 8,000 / RUNNING 8,100** · 60%×2 → **ORIGINAL 0원 / RUNNING 1,600원** · 10,001원+10%×2 → **매 단계 버림 8,101 / 마지막 한 번 8,100** · 8,000+5,000 쿠폰이어도 **낼 금액 0원(음수 금지), 총 할인액도 10,000 초과 금지** · 50,000원에 "20%, 상한 1,000" → 49,000 · 정률A·정액C·정률B를 PERCENT_FIRST 로 정렬하면 내역 순서 **A, B, C**(안정 정렬) · 매 단계 버림이면 **내역의 합 == 총 할인액**.

아래 서머리는 이 문제(README)를 분석·정리한 것이다.

## 전체 흐름

```text
요구사항에 안 적히는 결정 셋 → 전부 enum/boolean 매개변수로 끌어올렸다

  Order  (순서)      AS_GIVEN | PERCENT_FIRST | AMOUNT_FIRST
  Base   (정률 기준)  ORIGINAL(원가) | RUNNING(그때까지 깎인 금액)
  roundEachStep      true(매 단계 버림) | false(마지막에 한 번)

Discount (계약, 전부 주어짐)  name / kind(PERCENT|AMOUNT) / value / cap(정률 상한, 0=없음)
  deductionFrom(base, remaining) = 계산 → cap 으로 자르고 → remaining 으로 자른다(음수 방지)

Cart.apply(total, discounts, order, base, roundEachStep)
  [1] 순서 규칙대로 정렬 (★안정 정렬)
  [2] 할인마다: 기준 금액 정하기(원가? 잔액?) → 깎을 금액 → (옵션) 버림 → 잔액 갱신
  → 적용 내역 List<Applied> (화면에 줄줄이 보여줄 것)
```

**설계의 핵심**: 숨은 결정을 코드 깊숙이 묻지 않고 **호출자가 고르는 값**으로 올렸다. 그래야 "우리 정책이 뭐냐"가 코드 한 줄이 아니라 눈에 보이는 설정이 된다.

## 결정 1 — 적용 순서: 100원이 갈린다

**요구사항 문장**: "쿠폰을 여러 장 쓸 수 있다." (순서는 말 안 해준다)

손계산 — 10,000원, 10% + 1,000원 (누적 기준):

```text
PERCENT_FIRST   10,000 → -1,000(10%) → 9,000 → -1,000 → 8,000   손님에게 유리
AMOUNT_FIRST    10,000 → -1,000      → 9,000 → -900(10%) → 8,100  회사에 유리
```

- 정액을 먼저 빼면 정률이 **작아진 금액에** 붙어서 할인이 준다. 그래서 정률 먼저가 손님 유리.
- `AS_GIVEN`은 "정하지 않은 것"이다 — 목록을 만든 코드(보통 DB 조회 순서)가 순서를 정하게 된다. 가장 위험한 기본값.
- 정렬은 **안정 정렬**이어야 한다. 정률 A, 정액 C, 정률 B를 "정률 먼저"로 정렬하면 A,B,C — A와 B의 순서가 안 뒤집혀야 한다. 정률끼리는 곱셈이라 결과는 같지만(교환법칙) **화면에 찍히는 내역 순서가 바뀐다.**
  - *안정 정렬(stable sort)*: 정렬 기준이 같은 것끼리는 원래 순서를 유지하는 정렬.

측정(원본 README): 1,000건에서 706건이 갈리고, 합계 차이 **717,911원**(37,294,681 vs 38,012,592), 최대 4,300원, 건당 평균 718원.

## 결정 2 — 정률의 기준: 순서보다 두 배 크고, 더 조용하다

**요구사항 문장**: "10% 할인." (무엇의 10%인지 말 안 해준다)

손계산 — 10,000원에 10% 두 장:

```text
ORIGINAL(원가 기준)   1,000 + 1,000 = 2,000 할인  → 8,000   할인끼리 독립 = 더하기
RUNNING (누적 기준)   1,000 +   900 = 1,900 할인  → 8,100   뒤 할인이 작아짐 = 곱하기(0.9×0.9)
```

- 12번 게임 버프의 "가산 vs 승산"이 여기서 다시 나온다 — 같은 수학 구조.
- 원가 기준은 합이 100%를 넘을 수 있다: 60% 두 장 = 120% → 6,000+6,000을 깎으려는데 잔액이 4,000뿐. **잔액을 넘지 않게 자르는 규칙**(`min(remaining)`)이 여기서 일해서 0원에서 멈춘다. 누적 기준은 6,000+2,400=8,400 할인 → 1,600원. 공짜가 되느냐 마느냐가 갈린다.
- 측정: 1,000건 합계 차이 **1,737,494원**(35,921,980 vs 37,659,474) — **순서 결정의 두 배가 넘는다.** 그런데 순서는 "어느 쿠폰 먼저?"라고 묻기라도 하지, 기준은 아무도 안 묻는다. **코드에서 `total`을 쓰느냐 `remaining`을 쓰느냐 한 단어 차이다.**

## 결정 3 — 반올림 시점: 금액은 작고 소리는 크다

**요구사항 문장**: (아예 없음 — 소수점이 생긴다는 것 자체를 요구사항은 모른다)

손계산 — 10,001원에 10% 두 장(누적 기준):

```text
매 단계 버림    1,000.1→1,000 깎고 9,001 / 900.1→900 깎고 → 8,101
마지막에 한 번  1,000.1 깎고 9,000.9 / 900.09 깎고 8,100.81 → 버림 → 8,100
```

- 1원 차이. 측정: 1,000건 중 351건이 갈리는데 총액 차이는 **391원**(건당 1.1원). 앞의 두 결정에 비하면 돈으로는 아무것도 아니다.
- **그런데 회계에서 시끄러운 건 이쪽이다.** 매 단계 버리면 내역의 각 줄이 정수라서 "줄들의 합 = 총 할인액"이 언제나 성립하고 화면에 그대로 보여줄 수 있다. 마지막에 한 번 하면 내역과 합계가 안 맞을 수 있다 — 손님이 계산기를 두드리는 순간이다.
- **금액이 큰 결정(기준·순서)과 시끄러운 결정(반올림)이 다르다** — 우선순위를 금액만으로 매기면 틀린다.

## 경계·모서리 케이스 — 원본 테스트가 잡는 함정

- **함정: 할인 합이 금액을 넘어 음수 결제** → 10,000원에 8,000+5,000 쿠폰: 두 번째는 잔액 2,000까지만 깎고 **0에서 멈춘다.** 음수를 허용하면 손님에게 돈을 주는 것이고, 그 음수가 정산으로 흘러가면 출처를 못 찾는다. `deductionFrom`의 `min(remaining)`이 이 방어선.
- **함정: 상한 없는 정률** → 50,000원에 "20%, 최대 1,000원": 상한 없으면 10,000원이 깎인다. 정률은 장바구니가 비쌀수록 폭발하므로 실제 쿠폰에는 거의 항상 상한(cap)이 붙는다.
- **함정: 할인 하나로만 테스트** → 할인이 하나면 순서(적용할 게 하나)·기준(원가=잔액)·반올림(한 번뿐) **세 결정이 전부 사라져서** 여섯 조합이 같은 답을 낸다(측정: 1,000건 전부). "10% 쿠폰 하나 테스트 통과"로는 이 챕터의 문제가 통째로 안 보인다.
- **함정: "정률끼리는 순서 무관"** → 합계는 같다(0.9×0.8=0.72, 어느 순서든 7,200원). 그런데 내역이 다르다 — 10% 먼저면 1,000원+1,800원, 20% 먼저면 2,000원+800원. **결과가 같다는 말은 반쪽만 맞다. 손님이 보는 화면이 달라진다.**
- **함정: 잘못된 할인 생성** → 0% / 101% / 빈 이름 / 음수 상한은 생성자에서 예외 — 잘못된 값은 만들어지는 순간 막는다.

## 핵심 문장

- 요구사항이 침묵하는 결정이 셋 있다 — 순서·정률의 기준·반올림 시점. 코드를 쓰는 순간 셋 다 정해지므로, **모르고 정하지 말고 매개변수로 끌어올려 보이게 하라.**
- 갈리는 돈의 크기: 기준(174만) > 순서(72만) >> 반올림(391원/1,000건) — 그런데 회계 항의는 제일 작은 반올림에서 온다. **금액이 큰 결정과 시끄러운 결정은 다르다.**
- 원가 기준은 더하기(할인 독립, 100% 초과 가능), 누적 기준은 곱하기(뒤 할인이 작아짐) — 12번 버프의 가산/승산이 도메인만 바꿔 다시 나온 것.
- 어떤 조합에서도 결제 금액은 음수가 되지 않는다 — 잔액으로 자르는 한 줄이 정산 사고를 막는 불변식이다.
- 할인 하나로 시험하면 세 결정이 전부 안 보인다 — 경계는 "둘 이상"에서만 드러난다.

## 관련 자료

- 챕터 안내: `/home/jun/project/myway/domain-modeling-basic/17-cart-discount/README.md`
- 정답 기준 소스: `/home/jun/project/myway/domain-modeling-basic/17-cart-discount/impl/com/domain/cart/Cart.java`
- 내 구현(TODO 껍데기): `/home/jun/project/myway/domain-modeling-basic/17-cart-discount/src/main/java/com/domain/cart/Cart.java`
- 할인 한 건(계약, 전부 주어짐): `.../src/main/java/com/domain/cart/Discount.java`
- 테스트: `.../src/test/java/com/domain/cart/CartTest.java`(순서·기준·반올림·음수 방지·안정 정렬 계약), `MeasurementTest.java`(1,000건 시뮬레이션 측정)
- 이웃 챕터: 12번 게임 버프(가산 vs 승산 — 같은 수학), 15번 쿠폰(조건 하나로 테스트하면 안 보이는 자리), 18번 배송비(이 할인 결과가 무료 배송 기준으로 흘러든다)

## 용어 풀이

- **정률 할인(percent discount)**: 비율로 깎는 할인. 기준 금액이 필요해서 "무엇의 몇 %냐"는 질문이 따라온다.
- **정액 할인(amount discount)**: 고정 금액으로 깎는 할인. 기준이 필요 없다.
- **상한(cap)**: 정률 할인이 깎을 수 있는 최대 금액. 비싼 장바구니에서의 폭발을 막는다.
- **원가 기준(ORIGINAL)**: 정률을 항상 처음 금액에 적용. 할인끼리 독립(더하기 구조).
- **누적 기준(RUNNING)**: 정률을 그때까지 깎인 잔액에 적용. 뒤 할인이 작아짐(곱하기 구조).
- **안정 정렬(stable sort)**: 정렬 기준이 같은 원소끼리 원래 순서를 유지하는 정렬. 적용 내역의 표시 순서를 지킨다.
- **교환법칙**: 곱셈은 순서를 바꿔도 결과가 같다(0.9×0.8=0.8×0.9). 정률끼리 합계가 순서 무관인 이유 — 단, 내역은 다르다.
- **버림(RoundingMode.DOWN)**: 소수점을 잘라내는 것. 매 단계 버리면 손님에게 유리하고 내역이 정수로 맞아떨어진다.
- **BigDecimal**: 돈 계산용 십진 소수 타입. 이진 소수(double)의 오차 없이 반올림 시점을 명시적으로 정하게 강제한다.
- **불변식(invariant)**: 어떤 경우에도 깨지면 안 되는 조건. 예: "낼 금액 ≥ 0", "내역의 합 = 총 할인액(매 단계 버림일 때)".
