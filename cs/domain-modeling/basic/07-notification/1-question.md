# domain-modeling-basic/07-notification — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 요구사항·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문형: "이 한 줄에 안 들어 있는 결정은 무엇인가" -->

### A. 과제 (Scheduler.java 의 TODO 1~4)

#### 0. 요구사항 한 줄

```text
"밤에는 알림을 보내지 않는다."
```

- 이 한 줄에 안 들어 있는 결정 다섯을 꼽으면?
- "밤"이 몇 시부터 몇 시까지인지가 왜 코드의 모양을 바꾸는가?
- 밤에 걸린 알림을 어떻게 할지를 이 문장은 말해주는가?
- 이 규칙에 아무도 반대하지 않는데 왜 재보면 문제가 둘이나 나오는가?
- 재려면 무엇을 세야 하는가?

#### 1. TODO 1 — nextAllowed (조용 시간이 끝나는 시각)

```java
/**
 * 조용 시간이 끝나는 시각. 이미 조용 시간이 아니면 그대로.
 *
 * <p>자정을 걸치는 조용 시간에서 22 시에 온 알림은 다음날 8 시로 간다.
 * 2 시에 온 알림은 같은 날 8 시다. 날짜가 갈린다.
 */
public static LocalDateTime nextAllowed(LocalDateTime at, QuietHours quiet) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- `22:00 ~ 08:00` 을 `시작 <= t < 끝` 으로 쓰면 무슨 일이 일어나는가?
- 그 사고가 특히 위험한 이유는 무엇인가?
- 자정을 걸치는 조용 시간의 올바른 판정식은 무엇인가?
- 자정을 안 걸치는 조용 시간(09:00~18:00)도 같은 코드가 다뤄야 하는 이유는?
- 22:00 은 조용 시간에 드는가, 08:00 은 드는가?
- 3/1 23시 · 3/2 02시 · 3/2 12시 알림의 발송 시각은 각각 언제인가?
- "같은 8시"인데 하루가 갈리는 판정은 코드에서 어떤 비교로 하는가?
- `22:00 ~ 22:00` 같은 조용 시간을 왜 생성자에서 막는가?

#### 2. TODO 2 — plan (발송 계획)

```java
/**
 * 발송 계획을 만든다. 중복을 없애고 조용 시간을 처리한다.
 *
 * <p>순서가 계약이다. 중복을 먼저 없애고 조용 시간을 처리한다.
 * 반대로 하면 미뤄진 알림들이 같은 시각이 되어 중복 판정에 걸린다.
 */
```

- 순서를 반대로 하면 정확히 어떤 알림이 어떤 알림을 지우는가?
- 그 사고를 한 문장으로 요약하면?
- 조용 시간이 아닌 알림은 정책과 무관하게 어떻게 되어야 하는가?
- 그 규칙을 빼먹으면 어제 낮 알림이 언제 도착하는가?
- DROP · DEFER · DEFER_AND_SPREAD 각각이 파는 것과 사는 것은 무엇인가?
- 흩기의 `index` 는 무엇을 세서 만드는가?
- 첫 번째로 미뤄진 알림의 시각이 정확히 08:00:00 이어야 하는 이유는?
- 흩기에서 `spread` 가 null 이거나 0이면 어떻게 동작해야 하는가?

#### 3. TODO 3 — dedupe (중복 제거)

```java
/**
 * 같은 키가 창 안에 여러 번 오면 첫 것만 남긴다.
 *
 * <p>창을 안 두면(0) 중복을 아예 안 없앤다.
 * 창이 무한이면 같은 알림을 평생 한 번만 보낸다.
 */
```

- 중복 판정 키는 무엇으로 만드는가?
- 키를 사람만으로 잡으면 어떤 사고가 나는가?
- 창 판정의 기준점은 "마지막으로 보낸 시각"인가 "마지막으로 요청한 시각"인가?
- 요청 시각 기준이면 30분마다 오는 알림은 어떻게 되는가?
- u1/order 가 09:00·09:30·15:00 에 왔을 때 1시간 창의 결과는 무엇인가?
- 왜 시각 순으로 정렬부터 해야 하는가?
- 정렬을 빼먹으면 "첫 것을 남긴다"가 어떻게 어긋나는가?
- 창이 null 이거나 0이면 무엇을 반환해야 하는가?
- 입력 리스트를 그대로 정렬하지 않고 복사본을 만드는 이유는?

#### 4. TODO 4 — peakBurst (몰림)

```java
/** 어느 순간에 가장 많이 몰리나. 같은 시각에 나가는 건수의 최대치다. */
public static int peakBurst(List<Notification> planned) {
    throw new UnsupportedOperationException("TODO 4");
}
```

- 이 값이 "이 챕터의 자"인 이유는 무엇인가?
- 발송 건수만 세면 DEFER 와 DEFER_AND_SPREAD 를 구별할 수 있는가?
- 같은 시각에 몰린 건수를 세려면 무엇을 키로 묶어야 하는가?

### B. 개념

#### 5. 측정이 알려준 것 (원본 README 측정 절)

- 22:00~08:00 은 하루의 몇 퍼센트이고 5,000건 중 몇 건이 걸리는가?
- "밤에는 안 보낸다"가 어떤 말과 같은 말이 되는가?
- 버리기·미루기·1초 흩기의 최대 몰림은 각각 얼마이고 배율은 몇 배인가?
- 이 스파이크가 랜덤 장애보다 나쁜 점은 무엇인가?
- 흩는 간격을 1초 → 60초로 늘리면 몰림과 마지막 발송 시각이 각각 어떻게 되는가?
- 간격을 늘렸는데 몰림이 7 → 8 로 오히려 느는 이유는 무엇인가?
- 흩었는데도 몰림이 1이 안 되는 이유는 무엇인가?
- 중복 제거 창을 24시간으로 두면 발송량이 몇 건에서 몇 건이 되는가?
- 그 -61퍼센트가 무조건 좋은 일이 아닌 이유는 무엇인가?

#### 6. 변종 검증 (원본 README)

- 변종 14개 중 둘은 왜 손으로 확인해야 했는가?
- 자정 걸침 조건의 `or` 를 `and` 로 바꾸면 테스트 몇 개가 깨지는가?

#### 7. 생각해볼 것 (원본 README)

- 알림 종류마다 조용 시간 정책이 다르면 — 보안 알림도 미루는가?
- 사용자마다 시간대가 다르면 "밤"은 언제인가?
- 흩기가 만드는 지연을 사용자에게 알리는가?
- 중복 제거 창 안에서 내용이 바뀌면 첫 것을 보내는가 마지막 것을 보내는가?

#### 8. 연결

- 06번 정기권의 "요구사항 한 줄이 안 정해준 것"과 여기의 구조는 어떻게 같은가?
- "순서가 계약"이라는 이 챕터의 교훈을 다른 도메인에 옮기면 어떤 형태가 되는가?
- 조용 시간 규칙을 넣은 대가로 무엇을 샀는지 한 문장으로 말하면?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
