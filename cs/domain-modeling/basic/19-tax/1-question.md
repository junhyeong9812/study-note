# domain-modeling-basic/19-tax — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 요구사항/javadoc/TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문형: "이 한 줄 요구사항에 안 들어 있는 결정은 무엇인가" -->

### A. 과제 (ProgressiveTax 의 TODO 1~4)

#### 1. 챕터 지문 — 말로 된 요구사항

```text
  0 ~ 1,400 만       6 %
  1,400 ~ 5,000 만  15 %
  5,000 ~ 8,800 만  24 %

"구간을 넘으면 전체에 높은 세율이 붙는다" 는 흔한 오해다.
실제 세법은 안 그런데, 코드가 그렇게 되면 진짜로 그렇게 된다.
```

- 이 표 세 줄에 안 적힌 결정을 세 개 이상 꼽을 수 있는가?
- "1,400만"이 앞 구간인지 뒤 구간인지 표는 말해주는가?
- 구간 사이가 끊기면(1,400만 다음이 1,500만부터) 무슨 일이 생기는가?
- 소득 0은 어느 구간에 드는가?
- "코드가 그렇게 되면 진짜로 그렇게 된다"는 문장이 뜻하는 것은 무엇인가?

#### 2. TODO 1 — taxBySummingBrackets (구간별 합산)

```java
/**
 * 구간별로 더해서 계산한다.
 *
 * <p>소득이 어느 구간에 걸치든 그 구간에 해당하는 만큼만 그 세율로 매긴다.
 */
public BigDecimal taxBySummingBrackets(long income) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- 이 메서드는 구간을 고르는가 전부 도는가?
- 소득보다 위에 있는 구간을 돌아도 괜찮은 이유는 무엇인가?
- `TaxBracket.taxableIn` 이 구간 아래·구간 안·구간 위에서 각각 무엇을 돌려주는가?
- 소득 2,000만원의 세금을 손으로 계산하면 얼마인가?
- 마지막에 `setScale(0, DOWN)` 을 하는 이유는 무엇인가?

#### 3. TODO 2 — progressiveDeductions (공제액을 표에 안 적고 계산한다)

```java
/**
 * 누진 공제액. 구간마다 하나씩.
 *
 * <p>그 구간의 세율로 전체를 매겼을 때 실제보다 얼마나 더 나오는지다.
 */
public List<BigDecimal> progressiveDeductions() {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 공제액을 한 줄 식으로 쓰면 무엇인가?
- 그 식에서 "전체"는 소득인가 구간의 하한인가?
- 첫 구간의 공제액이 0인 이유는 무엇인가?
- 두 번째 구간(1,400만~5,000만, 15%)의 공제액 1,260,000원을 손으로 계산할 수 있는가?
- 공제액을 표에 미리 적어두면 무엇이 위험해지는가?
- 구간에서 계산하면 "두 방식이 어긋날 수가 없다"는 말은 왜 성립하는가?

#### 4. TODO 3 — taxByProgressiveDeduction (표 한 줄로 끝내기)

```java
/**
 * 누진 공제로 계산한다. 위와 답이 같아야 한다.
 *
 * <p>구간을 찾는 조건이 초과인지 이상인지는 결과를 안 바꾼다.
 * 경계에서 두 구간의 공제 후 세액이 같아지도록 공제액이 정의되어 있기 때문이다.
 */
public BigDecimal taxByProgressiveDeduction(long income) {
    throw new UnsupportedOperationException("TODO 3");
}
```

- 구간을 찾을 때 왜 앞에서부터가 아니라 뒤에서부터 도는가?
- 찾는 조건은 `income > lowerBound` 인가 `income >= lowerBound` 인가?
- 어느 구간에도 안 걸리면 무엇을 돌려주는가, 그게 어떤 소득인가?
- 실무에서 이 방식을 쓰는 이유는 무엇인가?
- 이 방식과 구간별 합산 중 손님에게 설명하기 좋은 쪽은 어디인가?

#### 5. TODO 4 · 대조용 오답 구현

```java
/** 세금을 뺀 실수령. */
public long takeHome(long income, boolean flatRate) {
    throw new UnsupportedOperationException("TODO 4");
}

/** 흔한 오해대로 계산한 판. 대조용이다. (주어짐)
 *  소득이 든 구간의 세율을 전체에 매긴다. 그러면 역전이 생긴다. */
public BigDecimal taxByFlatRateOfTopBracket(long income) { ... }
```

- `takeHome` 의 `flatRate` 플래그는 무엇을 위해 있는가?
- 올바른 구현과 오답 구현의 코드 차이는 정확히 몇 글자인가?
- 소득 14,000,000과 14,000,001의 오답 세금·실수령은 각각 얼마인가?
- 오답 구현을 코드베이스에 **일부러 남겨둔** 설계 의도는 무엇인가?

#### 6. 계약으로 주어진 TaxBracket·생성자 검사

```java
/** 이 구간에 해당하는 금액. 소득이 구간 아래면 0. */
public long taxableIn(long income) {
    if (income <= lowerBound) return 0;
    return Math.min(income, upperBound) - lowerBound;
}
// 생성자: brackets.get(i).lowerBound() != brackets.get(i-1).upperBound() 이면 예외
```

- `Math.min(income, upperBound) - lowerBound` 한 줄이 처리하는 경우 셋은 무엇인가?
- 마지막 구간의 `upperBound` 가 `Long.MAX_VALUE` 인 것은 무슨 뜻인가?
- 구간 연속성을 생성자에서 검사하면 무엇이 보장되는가?
- 표를 손으로 옮기다 한 자리를 틀리면 누가 피해를 보는가?
- `upperBound <= lowerBound` 와 세율 101%를 막는 것은 어디인가?

### B. 개념

#### 7. 측정 하나 — 경계 규칙이 결과를 안 바꾼다

- 1,400만원 정확히를 아래 구간에 넣든 위 구간에 넣든 세금이 같은 이유는 무엇인가?
- 그 값은 얼마인가?
- 04·06·08·15·17·18번과 대조된다는 말은 무슨 뜻인가?
- 이 도메인이 "드문" 이유를 한 문장으로 말하면?

#### 8. 측정 둘 — 두 계산 방식이 10만 번 중 한 번도 안 어긋난다

- 두 방식은 각각 무엇을 계산하는가?
- 같은 값을 두 방법으로 구해 대조하는 비용은 무엇인가?
- 그 비용을 낼 만한 자리는 어디인가?
- 두 방식이 어긋날 수 없게 만든 설계상의 장치는 무엇인가?

#### 9. 측정 셋·넷 — 역전과 손해 구간

- 100만원 간격으로 2억까지 훑을 때 올바른 계산과 오답 계산의 "실수령이 줄어든 횟수"는 각각 몇 번인가?
- 그 횟수가 경계 수와 같은 이유는 무엇인가?
- 최대 감소액은 얼마인가?
- 1,400만 경계에서 손해를 회복하려면 얼마를 벌어야 하는가?
- 손해 구간의 폭은 각 경계에서 얼마인가?
- 그 구간에 든 사람은 연봉 협상에서 왜 못 올려달라고 하는가?

#### 10. 측정 다섯·여섯 — 안 보이는 결함, 걷히는 돈

- 0~1,400만원 구간에서 10,000번 시험하면 몇 건이 갈리는가?
- 경계를 하나만 넘기면 몇 건이 갈리는가?
- "월급 300만원으로 시험하면 이 결함이 절대 안 보인다"는 무엇을 경고하는가?
- 1,000만~2억 사이 10,000건에서 두 방식의 세금 합계는 각각 얼마이고 몇 % 차이인가?

#### 11. 변종 검증에서 남은 것

- 누진 공제 방식에서 `초과` 를 `이상` 으로 바꿔도 테스트가 전부 통과하는 이유는 무엇인가?
- 그것이 결함이 아닌 이유를 한 문장으로 말하면?
- 같은 변경을 오답 구현에 하면 왜 테스트가 잡는가?
- "같은 코드 모양이 한쪽에서는 무해하고 다른 쪽에서는 결함이다"가 뮤테이션 테스트에 주는 교훈은?

#### 12. 생각해볼 것 (원본 README 이관)

- 세액 공제와 소득 공제는 이 계산의 어디에 들어가는가?
- 지방소득세(소득세의 10%)를 붙이면 반올림을 언제 하는가?
- 구간 표가 해마다 바뀌는데 작년 소득을 올해 표로 계산하는 것을 어떻게 막는가?
- 손님에게 설명할 때 구간별 합산과 누진 공제 중 무엇을 보여주는가?

#### 13. 연결

- 17번의 "반올림 시점"이 여기 지방소득세 질문에서 어떻게 다시 나오는가?
- 18번의 "기준은 이상이다"와 여기 "경계는 결과를 안 바꾼다"는 왜 다른 결론이 났는가?
- 두 방식 대조(cross-check)는 어떤 종류의 시스템에서 쓸 만한가?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
