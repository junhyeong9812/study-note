# domain-modeling-basic/06-subscription — 질문

> 복습은 항상 이 파일에서 시작한다. **맨기억으로 답을 시도**하고,
> 막히면 [2-summary.md](2-summary.md)를 힌트로, 최후에만 [3-answer.md](3-answer.md)를 연다.
> 정답까지 봤던 질문은 아래 복습 기록에 "틀림"으로 표시한다.

⚠️ 질문 목록은 Claude 초안(2026-09-15) — 원본 요구사항·생각해볼 것 이관 + 파생 질문. 본인 검토 후 이 줄 삭제

## 질문

<!-- 형식: 질문 하나 = "?" 하나 = 한 줄. 옆으로 묶지 않고 아래로 늘어놓는다.
     문제(A)는 원문 요구사항·TODO 를 코드블록으로 먼저 두고 그 아래에 질문을 단다.
     도메인 모델링의 핵심 질문형: "이 한 줄에 안 들어 있는 결정은 무엇인가" -->

### A. 과제 (Subscription.java 의 TODO 1~5)

#### 0. 요구사항 한 줄

```text
"한 달 30,000 원, 중도 해지하면 남은 날만큼 돌려드립니다."
```

- 이 한 줄이 안 말해주는 것 넷은 각각 무엇인가?
- 그 넷은 왜 "말투 다듬기"가 아니라 전부 **금액을 바꾸는** 결정인가?
- 이 넷을 코드의 어디에 올려야 요구사항이 바뀔 때 계산 코드를 안 고치는가?
- "남은 날만큼"이라는 표현은 청구를 기준으로 쓴 말인가 환불을 기준으로 쓴 말인가?

#### 1. TODO 1 — usedDays (쓴 날 수)

```java
/**
 * 쓴 날 수.
 *
 * <p>시작일과 해지일을 각각 셀지가 정책이다.
 * 둘 다 안 세면 하루 쓰고 해지한 사람이 0 일이 된다. 그래서 정책 생성 시 막는다.
 *
 * <p>그 막음 덕에 여기서 음수가 안 나온다. 최소값은 0 이다.
 * 여기서 다시 0 으로 막으면 같은 것을 두 곳에서 지키게 되고,
 * 정책 쪽 검사를 지워도 이쪽이 덮어버려서 안 드러난다.
 */
public static long usedDays(LocalDate start, LocalDate end, ProrationPolicy policy) {
    throw new UnsupportedOperationException("TODO 1");
}
```

- 3/1 시작 3/10 해지일 때 standard 정책의 답은 며칠인가?
- 같은 날짜쌍인데 "해지일 포함" 정책이면 며칠이 되는가?
- 두 날짜의 차이를 먼저 구한 다음 정책 boolean 둘을 어떻게 더하고 빼는가?
- 시작일도 해지일도 안 세는 정책을 왜 생성자에서 아예 막는가?
- `Math.max(0, days)` 방어를 여기에 넣으면 무엇이 안 드러나게 되는가?
- 해지일이 시작일보다 빠르면 어떻게 해야 하는가?

#### 2. TODO 2 — daysInPeriod (분모)

```java
/** 일할 계산의 분모. */
public static int daysInPeriod(YearMonth month, ProrationPolicy policy) {
    throw new UnsupportedOperationException("TODO 2");
}
```

- 분모의 두 선택지는 무엇인가?
- 월 30,000원·1일~15일일 때 2월과 1월의 금액이 각각 어떻게 갈리는가?
- 유불리 방향이 달마다 반대라는 사실이 왜 "손님에게 유리한 쪽으로"라는 지침을 무력화하는가?
- 이 결정은 개발자가 골라도 되는 결정인가?

#### 3. TODO 3 — chargeFor (그 달에 받을 금액)

```java
/**
 * 그 달에 실제로 받을 금액.
 *
 * <p>쓴 날이 분모를 넘으면 월 요금 전액이다. 2 월에 31 일을 쓸 수는 없지만
 * 분모를 30 으로 고정하면 31 일 달에서 그런 일이 생긴다.
 *
 * <p>경계(쓴 날 == 분모) 에서는 끊으나 안 끊으나 결과가 같다. 나눗셈이 정확히 1 이다.
 * 초과일 때만 실제로 필요하다. 변종 검증에서 그것을 확인했다.
 */
```

- 전액 상한이 없으면 30일 고정 분모 × 31일 달에서 몇 퍼센트가 나오는가?
- 상한의 비교를 `>` 로 쓰나 `>=` 로 쓰나 결과가 같은 이유는 무엇인가?
- 그런데도 `>=` 로 쓰는 것이 왜 손해가 아닌가?
- 1월 1일 시작·1월 31일 해지(standard)가 전액이 아닌 이유는 무엇인가?
- 그럼 한 달 전액이 되려면 해지일이 언제여야 하는가?
- 이 "끝 날을 미포함으로 받는다"는 성질은 04번 숙박의 무엇과 같은가?
- 돈 계산에 `double` 을 쓰면 무엇이 깨지는가?

#### 4. TODO 4 — refundFor (환불)

```java
/**
 * 환불액. 월 요금에서 쓴 만큼을 뺀다.
 *
 * <p>"쓴 만큼 받는다" 와 "안 쓴 만큼 돌려준다" 가 반올림 때문에 안 맞을 수 있다.
 * 둘을 따로 계산하면 합이 월 요금이 아니다. 그래서 뺄셈으로 만든다.
 */
```

- 이 함수가 지켜야 하는 불변식을 한 줄로 쓰면?
- 1월 1일~15일·버림 정책에서 청구와 환불을 각각 따로 계산하면 합이 얼마인가?
- 그 1원은 어디로 갔는가 — 왜 남는 경우는 없고 항상 모자라기만 하는가?
- "반올림이 나쁜 것이 아니라 두 번 반올림한 것이 나쁘다"는 말을 자기 말로 바꾸면?
- 뺄셈으로 만들면 반올림 오차는 청구와 환불 중 어느 쪽에 몰리는가?
- 대조용 `refundByUnusedDays` 를 굳이 남겨둔 이유는 무엇인가?

#### 5. TODO 5 — monthlyCharges (여러 달)

```java
/**
 * 여러 달에 걸친 이용료. 달마다 따로 일할한다.
 *
 * <p>기간을 통째로 일수로 나누면 달마다 일수가 다른 것이 반영되지 않는다.
 */
```

- 기간 전체를 한 번에 일수로 나누면 무엇이 반영되지 않는가?
- 1/20 ~ 3/10 · 월 30,000원 · standard 정책의 달별 금액과 합계는 얼마인가?
- 각 달에서 잘라낼 시작·끝 날짜는 어떤 식으로 고르는가?
- 3/1 ~ 3/10 처럼 한 달 안에서 끝나면 목록의 크기는 몇이어야 하고, 두 칸이 되면 무슨 사고가 나는가?

### B. 개념

#### 6. 측정이 알려준 것 (원본 README 측정 절)

- 분모 규칙으로 1,000건 중 몇 건이 갈리고 최대 차이는 얼마인가?
- 해지일을 세느냐로 갈리는 비율이 1,000/1,000 인 이유는 무엇인가?
- 반올림 방식 차이의 합계 금액(308원)이 갈린 건수와 정확히 같은 이유는 무엇인가?
- 환불을 따로 계산했을 때 1원이 사라지는 건수는 몇이고 방향은 왜 한쪽인가?

#### 7. 변종 검증에서 고친 것 (원본 README)

- 무엇을 지웠고 왜 지웠는가?
- 같은 것을 두 곳에서 지키면 어떤 결함이 숨는가?
- 지우고 나니 어떤 변종이 몇 개의 테스트에 잡혔는가?
- 전액 상한의 경계는 왜 변종 검증에서 "끊으나 안 끊으나 같다"로 판정됐는가?

#### 8. 생각해볼 것 (원본 README)

- 이용 중에 요금이 바뀌면 바뀐 날 기준으로 쪼개는가 계약 시점 요금을 쓰는가?
- 일할 계산 없이 "월 단위로만" 하면 무엇이 단순해지고 무엇을 잃는가?
- 반올림 오차를 청구 쪽에 몰면 무엇이 달라지는가?
- 연 단위 결제에 중도 해지가 들어오면 분모는 무엇인가?

#### 9. 연결

- 01번 주차 요금과 "같은 모양"인 지점은 정확히 무엇인가?
- 04번 숙박의 체크아웃 날과 여기의 해지일은 어떻게 같은 구조인가?
- 이 챕터가 말하는 "도메인 모델링"을 한 문장으로 정의하면?

## 복습 기록

| 날짜 | 결과 | 틀린 질문 | 다음 복습 |
|------|------|-----------|-----------|
| | | | |
