# 요구사항 명세서 (requirement-spec)

> 작성일: 2026-09-11 · 작업 폴더: `docs/plans/2026-09-11/call-stack-readme/`
> 자명한 작업 — 칸당 1행 축약.

---

## 0. 요구사항 원문 (인터뷰 기록)

- 원문: "call-stack으로 폴더 만들어줘 그리고 그 안에 리드미 하나만 부탁해 내가 작성하고 거기에 추가 내용 요청할게. 정리를 우선할게."
- Q/A: 위치·이름은 대화에서 합의됨 — `cs/systems/call-stack/`(단일 주제, assembly 상위 폴더 안 만듦). 원고는 사용자가 직접 작성, Claude 추가분은 요청 시 분리.

---

## 1. 목표·대상 (필수)

`cs/systems/call-stack/README.md` 1파일 생성(최소 골격 — 제목 + 주제 한 줄) — 생성되면 끝.

## 2. 경계·불변식 (필수)

새 파일 1개만 생성, 기존 파일 무수정. 본문 내용은 채우지 않는다(사용자 원고 영역).

## 3. 기준소스 (필수)

이 대화의 합의(폴더명 `call-stack`, 주제 = ESP 기준 스택 메모리 동작) + 기존 `cs/systems/` 컨벤션.

## 4. 금지영역 (필수)

`cs/systems/` 기존 폴더들 및 그 외 모든 기존 파일. 커밋·푸시는 별도 지시 전 금지.

## 5. 검증 방법 (필수)

파일 존재 확인(ls) + 내용 확인 — 골격 그대로인지.

## 6. stakes (필수)

- 판정: 낮음 — 근거 1줄: 신규 문서 1파일, blast radius 없음, 삭제로 즉시 복구 가능.

---

## 7. 자율성 (모드 — stakes와 별개 축)

- [x] auto (기본 권장 — 합의 후 Claude 자율 실행)
- [ ] lazy

## 8. load-bearing 가정 (1~2개)

- `cs/systems/` 평면 kebab-case 컨벤션이 이 주제에 적용된다(대화에서 사용자 확인됨).

---

## 승인 상태

- [ ] 필수 6칸 전부 기입 (빈 칸 없음)
- [ ] 사용자 합의 → SPEC=1
- [ ] 자율성 선택 → MODE 기록
