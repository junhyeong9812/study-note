# log — 언어별 문법·API 학습 자료

명세: `requirement-spec.md` (2026-09-20 합의, MODE=auto)
1단계 = 리스트업. 2단계(3파일 작성)는 목록 확정 후 재합의.

## 원장

| 시각 | 사건 | 결과 |
|---|---|---|
| 2026-09-20 | 인터뷰 3문 → 언어 4개·기능 단위·위치 확정 | 이후 사용자가 Go·Rust·JS·TS 추가, 다시 C·C++·C# 추가 → **11개** |
| 2026-09-20 | 구조 재확정 — 언어 우선, 축은 폴더로 분리 | `<언어>/언어-특성/` ↔ `<언어>/syntax/` |
| 2026-09-20 | 실행 환경 조사 | **가능**: Java 17·21·25(sdkman, javac) · Python 3.12.3 · Rust 1.92.0 · Node 18·20 / **불가**: Kotlin · Go · TS(`npx tsc` 여지) · SQL |
| 2026-09-20 | 기존 노트 4개를 언어 폴더로 이동 | git rename 4건. 상대 링크 **39개 전수 검사 → 깨짐 0** |
| 2026-09-20 | `languages/README.md` 두 축 안내로 갱신 | — |

## 사용자 제안 하나를 채택하지 않았다 — 공식 문서 전문 이식·번역

사용자 제안: 「각 언어 docs 페이지 내용을 그대로 전부 가져와 더 작게 나누고 번역」.
**채택하지 않고 기준 소스 방식으로 진행하기로 했다**(사용자 동의 2026-09-20). 이유 둘:

1. **라이선스가 갈린다.** Python·Go·Rust·Kotlin·TS·MDN·PostgreSQL 문서는 재배포를 허용하는 편이나
   **Oracle 의 Java 언어 명세·API 문서와 MySQL 문서는 그렇지 않다.** 이 repo 는 공개 저장소다.
2. ★ **방식이 목적과 맞지 않는다.** 이 repo 의 값은 **골라내고 압축해 인출 가능한 크기로 자른 것**이다.
   전문 번역본은 「읽는 것」이지 「인출하는 것」이 아니고, Java API 문서만 해도 수만 쪽이다.

→ 대신 공식 문서를 **기준 소스로 열어 대조**하고 설명·예시는 직접 쓴다.
   규칙 문구가 중요한 자리만 짧게 인용하고 링크를 건다. history 8주제에서 이 방식으로 인용 층 오류 0건을 냈다.
→ 여기에 **실행 검증**이 더해진다 — Java·Python·Rust·JS 는 예시를 실제로 돌려 출력을 확인한 채로 싣는다.
   번역본으로는 못 하는 검증이고, 이것이 이 자료가 공식 문서보다 나은 유일한 지점이다.
| 2026-09-20 | 리스트업 워커 5개 착수 (Java·Kotlin / SQL / Python·JS·TS / Go·Rust / C·C++·C#) | 공통 형식·중복 검사 목록·40~60 기준선을 브리핑에 고정 |
| 2026-09-21 | Java·Kotlin 회수 | **Java 60 · Kotlin 58**. 링크 92개 깨짐 0 |
| 2026-09-21 | Go·Rust 회수 | **Go 52 · Rust 58** |

## 가정 1 실증 (진행 중) — 「기능 단위면 언어당 40~60주제」

| 언어 | 주제 | 문법 | API | 관용구 | A | B | C |
|---|---:|---:|---:|---:|---:|---:|---:|
| Java | 60 | 34 | 24 | 2 | 42 | 14 | 4 |
| Kotlin | 58 | 39 | 17 | 2 | 40 | 15 | 3 |
| Go | 52 | 27 | 16 | 9 | 34 | 13 | 5 |
| Rust | 58 | 34 | 16 | 8 | 38 | 15 | 5 |

→ 4개 언어 모두 기준선 안. **다만 Java 가 상한(60)에 붙었다** — 워커 보고: 초안 61 에서
빌더·정적 팩토리를 `cs/engineering/design-patterns-gof/` 로 넘겨 1개 줄였고,
스트림을 6갈래·컬렉션을 5갈래로 이미 쪼갠 상태다. **Java 는 상한이 실질 한계**라는 신호.

## 중복 처리 실측

| 언어 | 「기존 주제」 링크를 단 행 |
|---|---|
| Java | 11묶음(표 행 기준 33건 링크) |
| Kotlin | 13묶음 |
| Go | 24행 (14묶음) |
| Rust | 29행 (16묶음) |

좁힌 방식이 네 언어에서 같은 모양으로 나왔다 —
**자료구조 원리는 `data-structure/`, 언어 쪽은 「어느 구현체를 왜 고르나」**,
**연혁은 `history/`, 언어 쪽은 「어떻게 쓰고 무엇을 못 하나」**,
**모델의 논증은 `언어-특성/`, 언어 쪽은 「그래서 코드에서 어떻게 쓰나」**.

★ `history/` 에 **Go·Kotlin 편이 없다**는 것이 드러났다(java·js·python·rust·spring 뿐).
두 워커가 각각 그 사실을 README 에 적고 버전 표기로 대신했다.

## 실행 검증 가능 여부 (워커 실측)

| 언어 | 가능? | 비고 |
|---|---|---|
| Java | **가능** | sdkman 17.0.13·21.0.5·25.0.1 + javac. **시스템 기본 `java` 는 1.8 이라 JDK 경로 명시 필요** |
| Rust | **가능** | rustc·cargo 1.92.0 |
| Kotlin | 불가 | `kotlinc` 없음. `sdk install kotlin` 제안만 |
| Go | 불가 | `go`·`gofmt` 없음 |
| 2026-09-21 | Python·JS·TS 회수 | **Python 53 · JS 52 · TS 48**. 순환 0·링크 깨짐 0 |

### 누적 (7/11 언어)
Java 60 · Kotlin 58 · Go 52 · Rust 58 · Python 53 · JS 52 · TS 48 = **381주제**
전부 40~60 안. **중앙값 55 근처로 수렴** — 가정 1 이 7개 언어에서 성립한다.

### ★ 워커가 잡아낸 기준 변화 2건 (메인 재확인)

1. **TypeScript 최신 안정이 7.0.2 다**(메인이 `registry.npmjs.org/typescript/latest` 로 직접 확인).
   Go 네이티브 포트이고 **기본값이 크게 바뀌었다** — `strict` 기본 `true` · `module` 기본 `esnext` ·
   `types` 기본 `[]` · `rootDir` 기본 `./` · `target: es5`·`downlevelIteration`·`baseUrl`·
   `moduleResolution: node10/classic`·AMD/UMD 제거.
   → **tsconfig 주제 6개(#34·35·38·39·43·48)가 이 변화에 직접 걸린다.** 2단계에서 옛 기본값으로 쓰면 전부 틀린다.
   (이 세션의 `history/js/99-그-뒤.md` 가 이미 「TypeScript 7(Go)」을 다뤘다 — 일관된다.)
2. **`tc39.es/ecma262` 는 이미 ES2027 초안**이다. 확정 기능은 ES2026 까지.
   Temporal 과 `using`(명시적 자원 관리)은 **ES2027 초안 단계**라 그렇게 표기했다.
   → 2단계에서 판이 바뀌면 JS #49·#51 표기를 갱신해야 한다.

### JS ↔ TS 분리 실측
**TS 목록에 JS 런타임 문법 행 0개.** 겹칠 뻔한 10갈래를 TS 에서 빼고 `선행` 칸에 `JS n` 으로만 걸었다
(값·강제 변환 · 클로저 · `this` · 구조분해 · class 문법 · 템플릿 리터럴 · 모듈 구문 · `try`/`catch` ·
이터러블/제너레이터/`async` · 내장 객체 메서드). 반대로 JS 목록에 타입 표기는 한 줄도 없다.

### `python-basics` 중복 10건 — 좁힌 축
기존 노트가 「이렇게 쓴다」까지이므로 새 주제는 **「무엇을 못 하고 어디서 틀리나」**로만 간다
(슬라이스 대입의 길이 변화 · 정렬 `key` 와 안정성 · 불변 tuple 안의 가변 원소 ·
키 해시 요건 · 컴프리헨션 대 제너레이터 표현식의 평가 시점 · 순회 중 변경 · 음수 바닥 나눗셈 · `and`/`or` 반환값).
| 2026-09-21 | SQL 회수 | **60주제**(상한). 기존 주제와 **직접 중복 0건**, 인접해 좁힌 것 13건 |

### SQL — 이 repo 에 언어로서 처음 들어온 것
분류: 질의 29 · 함수 18 · DDL·제약 6 · 트랜잭션·동시성 3 · 실행·최적화 4
방언: 표준 20 · **차이 39** · PG 1 · MySQL 0
- 초안 70 → 9건 통합해 60. 뺀 것은 사유와 함께 「뺀 것과 이유」 절에 남겼다.
- **01 논리적 질의 처리 순서**를 맨 앞 A 로 두고 **13개 주제가 직접 선행으로** 걸게 했다.
- 조인을 형태별 **9주제**로 쪼갰다(CROSS·INNER·LEFT/RIGHT·FULL·SELF·USING/NATURAL·SEMI/ANTI·LATERAL + `ON`/`WHERE` 차이 + 팬아웃).
- MySQL 전용 주제 0은 **의도한 결과** — `REPLACE`·`INSERT IGNORE`·`STRAIGHT_JOIN` 은 독립 주제가 될 만큼 크지 않아 해당 주제의 방언 메모로 돌렸고 그 이유를 README 에 적었다.

### ★ 워커가 스스로 건 경고 (좋은 처리)
- 「방언 차이가 큰 자리」 **24행 표의 항목은 전부 공식 문서에서 문장을 확인**했으나,
  **표의 나머지 `차이` 표시는 확신도가 낮다**고 README 에 명시하고 **2단계 재확인 대상으로 걸었다.**
- 두 건(MySQL 의 CUBE/GROUPING SETS·구체화 뷰)은 「미지원」 문장을 못 찾아
  **「문서 부재로 판단」이라고 표시**했다 — 없는 것을 「없다」고 단정하지 않았다.
- **표준 조항 번호는 확인 불가라 어디에도 적지 않았다.**
→ 이 세션 내내 문제였던 **「근거 없는 부정 주장」**과 **「확인 못 한 값 적기」**를 워커가 스스로 피했다.

### 실행 검증
SQL 은 **한 줄도 돌려보지 못했다**(psql·mysql·sqlite3·duckdb 전부 없음). 머리말에 명시.
2단계 제안: **PostgreSQL·MySQL 둘 다 필요**(도커면 충분). `sqlite3` 는 설치가 쉬우나
**세 번째 방언이라 이 목록의 차이 확인에는 부적합**하다.

## 2단계 — 첫 배치 (2026-09-21)

| 시각 | 사건 | 결과 |
|---|---|---|
| 2026-09-21 | web-api 회수 — **60주제** | js·html·css 와 **겹침 0**, 교차 선행 30행. 공백 6건 전부 편입 |
| 2026-09-21 | **1단계 완결 — 14갈래 783주제** | 커밋 `99dfcfc`, 푸시 완료 |
| 2026-09-21 | 첫 배치 착수 — Java 5 · CSS 4 · Python 4 | 서로 다른 모양 셋을 **동시에** 돌려 형식 위험을 한 번에 건다 |
| 2026-09-21 | SQL 용 DB 기동 (사용자 승인) | `postgres:18` 이미지 수신 중 · `mysql:8.4` 는 로컬에 있음 |

### 왜 전면 확대 전에 소수 배치를 먼저 도는가
형식이 **아직 한 번도 검증된 적 없다.** 특히 `demo` 블록은 이 저장소에서 **처음 쓰는 메커니즘**이다.
형식을 고칠 일이 생기면 **13주제만 손보면 되지 783주제를 손보지 않아도 된다.**
→ 다만 **Java 배치는 CSS·SQL 의 위험을 전혀 줄이지 못한다**(demo 블록·방언 칸을 시험하지 않는다).
   그래서 하나를 기다리지 않고 **서로 다른 모양 셋을 동시에** 돌린다.

### 첫 배치가 각각 시험하는 것
| 언어 | 주제 | 시험 대상 |
|---|---|---|
| Java | 01·06·27·44·45 | 문법 중심 · 컴파일 검증 · 선행 체인(44→45) · hashmap 중복 좁히기 |
| CSS | 01·02 + Flexbox·transition | ★ **`demo` 블록** · 도식 대 demo 의 비중 · `render-rules.md` 실사용 |
| Python | is/== · 가변 기본 인자 · 제너레이터 · 컴프리헨션 | 동적 타입 · **구현 세부 대 언어 보장** 구분 · `python-basics` 중복 |
| SQL | (DB 기동 후) | 방언 칸(60 중 39 이 「차이」) · 실행 대조 |

### 분량 기준선 (실측)
`cs/` 185주제 — **중앙값 773줄 · 평균 934줄 · 상위 10% 1,761줄 · 최대 2,754줄.**
본보기로 준 `data-structure/01-dynamic-array` 는 2,240줄.
→ Java 01(1,030줄)은 **과하지 않다.** 세 워커 모두에게 이 기준선을 줬다.

### DB 기동 (SQL 배치용)
```
study-pg18    postgres:18  → 5432
study-mysql84 mysql:8.4    → 3306
```
둘 다 필요하다 — 리스트업의 「방언 차이가 큰 자리」 24행이 **PG 와 MySQL 을 나란히 대조**한 것이고,
`sqlite3` 는 세 번째 방언이라 어느 쪽도 대신하지 못한다.
